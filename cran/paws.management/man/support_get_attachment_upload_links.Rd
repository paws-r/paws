% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support_operations.R
\name{support_get_attachment_upload_links}
\alias{support_get_attachment_upload_links}
\title{Returns one or more presigned upload URLs for uploading a large file
attachment to a support case by using a multipart upload workflow}
\usage{
support_get_attachment_upload_links(
  fileName,
  fileSizeBytes = NULL,
  uploadId = NULL,
  uploadRange = NULL,
  dryRun = NULL
)
}
\arguments{
\item{fileName}{[required] The name of the file to upload, including the file extension. This value is required when you initiate a new upload.}

\item{fileSizeBytes}{The total size of the file in bytes. The service uses this value to calculate the total number of parts and the size of each part. Required when you initiate a new upload (when \code{uploadId} isn't provided). Valid range: 1 to 157,286,400 bytes (approximately 150 MB).}

\item{uploadId}{The unique identifier of an in-progress multipart upload, returned by a previous call to \code{\link[=support_get_attachment_upload_links]{get_attachment_upload_links}}. Specify \code{uploadId} to retrieve additional presigned upload URLs for an upload that has already been initiated. Required when \code{fileSizeBytes} isn't provided. Length: 1 to 2,048 characters.}

\item{uploadRange}{The range of part indexes for which to return presigned upload URLs. Use this parameter to page through the upload URLs for a large file across multiple calls. If you omit this parameter, the service determines the range to return.}

\item{dryRun}{Specifies whether to validate the request without actually generating upload URLs. When set to \code{true}, the request is validated but no URLs are returned, and the operation returns a \code{DryRunOperationException}. When omitted or set to \code{false}, the request runs normally.}
}
\description{
Returns one or more presigned upload URLs for uploading a large file attachment to a support case by using a multipart upload workflow. The maximum file size that you can upload with this workflow is 150 MB, and parts can be up to 100 MB each. Initiate a new upload by providing \code{fileName} and \code{fileSizeBytes}; the response returns a unique \code{uploadId}, the part size, the total number of parts, and a list of presigned upload URLs for the requested range of parts. A maximum of 10 upload URLs are returned per call. To retrieve more upload URLs for an upload that's already in progress, call \code{\link[=support_get_attachment_upload_links]{get_attachment_upload_links}} again with the existing \code{uploadId} and a new \code{uploadRange}.

See \url{https://www.paws-r-sdk.com/docs/support_get_attachment_upload_links/} for full documentation.
}
\keyword{internal}
