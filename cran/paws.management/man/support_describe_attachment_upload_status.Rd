% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support_operations.R
\name{support_describe_attachment_upload_status}
\alias{support_describe_attachment_upload_status}
\title{Returns the current status, file name, and progress of a multipart
attachment upload that was started with GetAttachmentUploadLinks}
\usage{
support_describe_attachment_upload_status(uploadId, dryRun = NULL)
}
\arguments{
\item{uploadId}{[required] The unique identifier for the upload. The \code{uploadId} is returned by \code{\link[=support_get_attachment_upload_links]{get_attachment_upload_links}} when you initiate the upload.}

\item{dryRun}{Specifies whether to validate the request without actually returning upload status. When set to \code{true}, the request is validated but no status is returned, and the operation returns a \code{DryRunOperationException}. When omitted or set to \code{false}, the request runs normally.}
}
\description{
Returns the current status, file name, and progress of a multipart attachment upload that was started with \code{\link[=support_get_attachment_upload_links]{get_attachment_upload_links}}. Use this operation to track where an upload is in the workflow. While parts are still being uploaded and reported through \code{\link[=support_complete_attachment_upload]{complete_attachment_upload}}, the \code{uploadStatus} is \code{attachment-not-ready} and \code{uploadProgress} reports the total number of parts and how many have been completed so far. After every part has been reported and the service finishes processing the upload asynchronously, the \code{uploadStatus} becomes \code{attachment-ready} and the \code{uploadId} can be attached to a case through \code{\link[=support_create_case]{create_case}} or \code{\link[=support_add_communication_to_case]{add_communication_to_case}}.

See \url{https://www.paws-r-sdk.com/docs/support_describe_attachment_upload_status/} for full documentation.
}
\keyword{internal}
