% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloudwatchapplicationsignals_operations.R
\name{cloudwatchapplicationsignals_create_instrumentation_configuration}
\alias{cloudwatchapplicationsignals_create_instrumentation_configuration}
\title{Creates a dynamic instrumentation configuration for a specific code or
endpoint location within a service and environment}
\usage{
cloudwatchapplicationsignals_create_instrumentation_configuration(
  InstrumentationType,
  Service,
  Environment,
  SignalType,
  Location,
  Description = NULL,
  ExpiresAt = NULL,
  AttributeFilters = NULL,
  CaptureConfiguration,
  Tags = NULL
)
}
\arguments{
\item{InstrumentationType}{[required] Type of instrumentation: BREAKPOINT (temporary) or PROBE (permanent)}

\item{Service}{[required] The name of the service to instrument. This should match the \code{service.name} resource attribute reported by the application.}

\item{Environment}{[required] The environment that the service is running in, such as \code{eks:cluster-prod/namespace} or \code{ec2:production}.}

\item{SignalType}{[required] The telemetry signal type to emit for this instrumentation. The supported value is \code{SNAPSHOT}.}

\item{Location}{[required] The location where instrumentation should be applied. Specify a \code{CodeLocation} for code-level instrumentation.}

\item{Description}{An optional short description (up to 50 characters) that explains the purpose of this instrumentation.}

\item{ExpiresAt}{For BREAKPOINT: optional, defaults to 24 hours, must be between 5 min and 24 hours. For PROBE: not supported. PROBE configurations are permanent and persist until explicitly deleted.}

\item{AttributeFilters}{Client-side filters that target specific instances. Each object in the array is AND-matched on its keys, and multiple objects are OR-matched to decide where to apply the instrumentation.}

\item{CaptureConfiguration}{[required] Specifies what to capture when the instrumentation point is hit. Specify \code{CodeCapture} for code-level capture settings.}

\item{Tags}{An optional list of key-value pairs to associate with the instrumentation configuration. Tags can help you organize and categorize your resources.}
}
\description{
Creates a dynamic instrumentation configuration for a specific code or endpoint location within a service and environment. Configurations are immutable after creation.

See \url{https://www.paws-r-sdk.com/docs/cloudwatchapplicationsignals_create_instrumentation_configuration/} for full documentation.
}
\keyword{internal}
