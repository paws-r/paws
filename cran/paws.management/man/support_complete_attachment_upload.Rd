% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/support_operations.R
\name{support_complete_attachment_upload}
\alias{support_complete_attachment_upload}
\title{Completes an attachment upload that was started with
GetAttachmentUploadLinks}
\usage{
support_complete_attachment_upload(uploadId, completedUploads, dryRun = NULL)
}
\arguments{
\item{uploadId}{[required] The identifier associated with the upload to complete.}

\item{completedUploads}{[required] The list of parts being reported as completed in this call. Each entry must contain the \code{partIndex} of an uploaded part and the \code{ETag} returned by Amazon S3 when that part was uploaded.}

\item{dryRun}{Specifies whether to validate the request without actually completing the upload. When set to \code{true}, the request is validated but the upload isn't finalized, and the operation returns a \code{DryRunOperationException}. When omitted or set to \code{false}, the request runs normally.}
}
\description{
Completes an attachment upload that was started with \code{\link[=support_get_attachment_upload_links]{get_attachment_upload_links}}. After you upload a part of the file to its presigned Amazon S3 URL, call \code{\link[=support_complete_attachment_upload]{complete_attachment_upload}} with the \code{partIndex} and \code{eTag} of that part. You can include one part per call, or multiple parts in a single call. After \code{\link[=support_complete_attachment_upload]{complete_attachment_upload}} has been called for every part of the file, the service processes the upload asynchronously. The \code{attachment-ready} status might not be reflected immediately. Use \code{\link[=support_describe_attachment_upload_status]{describe_attachment_upload_status}} to poll for the \code{uploadStatus} to become \code{attachment-ready} before passing the \code{uploadId} to \code{\link[=support_create_case]{create_case}} or \code{\link[=support_add_communication_to_case]{add_communication_to_case}}.

See \url{https://www.paws-r-sdk.com/docs/support_complete_attachment_upload/} for full documentation.
}
\keyword{internal}
