% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ec2_operations.R
\name{ec2_create_ipam_routing_policy_registration}
\alias{ec2_create_ipam_routing_policy_registration}
\title{Creates a routing policy registration and publishes Route Origin
Authorizations (ROAs) to the RPKI for the specified CIDR prefix and ASNs}
\usage{
ec2_create_ipam_routing_policy_registration(
  DryRun = NULL,
  IpamInternetRegistryAssociationId,
  Cidr,
  Asns,
  PermitMoreSpecificAnnouncements = NULL,
  MaxLength = NULL,
  Description = NULL,
  Force = NULL,
  ClientToken = NULL
)
}
\arguments{
\item{DryRun}{Checks whether you have the required permissions for the operation, without actually making the request, and provides an error response. If you have the required permissions, the error response is \code{DryRunOperation}. Otherwise, it is \code{UnauthorizedOperation}.}

\item{IpamInternetRegistryAssociationId}{[required] The ID of the IPAM internet registry association.}

\item{Cidr}{[required] The IP address prefix in CIDR notation to authorize in the ROA.}

\item{Asns}{[required] The Autonomous System Numbers (ASNs) authorized to originate the prefix.}

\item{PermitMoreSpecificAnnouncements}{Specifies whether to permit more specific route announcements than the CIDR prefix. When enabled, ASNs can announce sub-prefixes of the authorized CIDR up to the specified maximum length. Default: \code{false}.}

\item{MaxLength}{The maximum prefix length that the ASNs are authorized to announce. Must be greater than or equal to the prefix length of the CIDR. If not specified, defaults to the prefix length of the CIDR (exact match only).}

\item{Description}{A description for the routing policy registration.}

\item{Force}{Forces the creation of the routing policy registration even if it conflicts with an announced route. Default: \code{false}.}

\item{ClientToken}{A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, the operation ignores the request, but does not return an error.}
}
\description{
Creates a routing policy registration and publishes Route Origin Authorizations (ROAs) to the RPKI for the specified CIDR prefix and ASNs.

See \url{https://www.paws-r-sdk.com/docs/ec2_create_ipam_routing_policy_registration/} for full documentation.
}
\keyword{internal}
