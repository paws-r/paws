% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codecommit_operations.R
\name{codecommit_get_blob_differences}
\alias{codecommit_get_blob_differences}
\title{Returns a structured, line-level diff between two blob versions in a
repository}
\usage{
codecommit_get_blob_differences(
  repositoryName,
  afterBlobId,
  beforeBlobId = NULL,
  contextLines = NULL,
  ignoreWhitespace = NULL,
  MaxResults = NULL,
  NextToken = NULL
)
}
\arguments{
\item{repositoryName}{[required] The name of the repository that contains the blobs to compare.}

\item{afterBlobId}{[required] The ID of the "after" (destination) blob in the diff. Typically the value of \code{afterBlob.blobId} from a \code{Difference} object returned by \code{\link[=codecommit_get_differences]{get_differences}}.}

\item{beforeBlobId}{The ID of the "before" (source) blob in the diff. Typically the value of \code{beforeBlob.blobId} from a \code{Difference} object returned by \code{\link[=codecommit_get_differences]{get_differences}}.

If you do not specify a value, the operation returns a diff against an empty before-state. This is equivalent to treating the file as newly added.}

\item{contextLines}{The number of unchanged lines of context to include before and after each block of changes in a hunk. Valid values are 0 through 20. Defaults to \code{3}.}

\item{ignoreWhitespace}{Specifies whether to ignore whitespace-only changes when computing the diff. When \code{true}, the operation treats lines that differ only in whitespace as unchanged. Defaults to \code{false}.}

\item{MaxResults}{The maximum number of \code{DiffHunk} entries to return in a single response page. Defaults to \code{100}.}

\item{NextToken}{An enumeration token that returns the next batch of results when present in a request.}
}
\description{
Returns a structured, line-level diff between two blob versions in a repository. The diff is returned as an ordered list of hunks, where each hunk represents a contiguous run of changed lines together with any surrounding unchanged context lines.

See \url{https://www.paws-r-sdk.com/docs/codecommit_get_blob_differences/} for full documentation.
}
\keyword{internal}
