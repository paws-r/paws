# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common get_config new_operation new_request send_request
#' @include mobile_service.R
NULL

#' Creates an AWS Mobile Hub project
#'
#' @description
#' Creates an AWS Mobile Hub project.
#'
#' @usage
#' mobile_create_project(name, region, contents, snapshotId)
#'
#' @param name Name of the project.
#' @param region Default region where project resources should be created.
#' @param contents ZIP or YAML file which contains configuration settings to be used when
#' creating the project. This may be the contents of the file downloaded
#' from the URL provided in an export project operation.
#' @param snapshotId Unique identifier for an exported snapshot of project configuration.
#' This snapshot identifier is included in the share URL when a project is
#' exported.
#'
#' @section Request syntax:
#' ```
#' svc$create_project(
#'   name = "string",
#'   region = "string",
#'   contents = raw,
#'   snapshotId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_create_project
mobile_create_project <- function(name = NULL, region = NULL, contents = NULL, snapshotId = NULL) {
  op <- new_operation(
    name = "CreateProject",
    http_method = "POST",
    http_path = "/projects",
    paginator = list()
  )
  input <- .mobile$create_project_input(name = name, region = region, contents = contents, snapshotId = snapshotId)
  output <- .mobile$create_project_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$create_project <- mobile_create_project

#' Delets a project in AWS Mobile Hub
#'
#' @description
#' Delets a project in AWS Mobile Hub.
#'
#' @usage
#' mobile_delete_project(projectId)
#'
#' @param projectId &#91;required&#93; Unique project identifier.
#'
#' @section Request syntax:
#' ```
#' svc$delete_project(
#'   projectId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_delete_project
mobile_delete_project <- function(projectId) {
  op <- new_operation(
    name = "DeleteProject",
    http_method = "DELETE",
    http_path = "/projects/{projectId}",
    paginator = list()
  )
  input <- .mobile$delete_project_input(projectId = projectId)
  output <- .mobile$delete_project_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$delete_project <- mobile_delete_project

#' Get the bundle details for the requested bundle id
#'
#' @description
#' Get the bundle details for the requested bundle id.
#'
#' @usage
#' mobile_describe_bundle(bundleId)
#'
#' @param bundleId &#91;required&#93; Unique bundle identifier.
#'
#' @section Request syntax:
#' ```
#' svc$describe_bundle(
#'   bundleId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_describe_bundle
mobile_describe_bundle <- function(bundleId) {
  op <- new_operation(
    name = "DescribeBundle",
    http_method = "GET",
    http_path = "/bundles/{bundleId}",
    paginator = list()
  )
  input <- .mobile$describe_bundle_input(bundleId = bundleId)
  output <- .mobile$describe_bundle_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$describe_bundle <- mobile_describe_bundle

#' Gets details about a project in AWS Mobile Hub
#'
#' @description
#' Gets details about a project in AWS Mobile Hub.
#'
#' @usage
#' mobile_describe_project(projectId, syncFromResources)
#'
#' @param projectId &#91;required&#93; Unique project identifier.
#' @param syncFromResources If set to true, causes AWS Mobile Hub to synchronize information from
#' other services, e.g., update state of AWS CloudFormation stacks in the
#' AWS Mobile Hub project.
#'
#' @section Request syntax:
#' ```
#' svc$describe_project(
#'   projectId = "string",
#'   syncFromResources = TRUE|FALSE
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_describe_project
mobile_describe_project <- function(projectId, syncFromResources = NULL) {
  op <- new_operation(
    name = "DescribeProject",
    http_method = "GET",
    http_path = "/project",
    paginator = list()
  )
  input <- .mobile$describe_project_input(projectId = projectId, syncFromResources = syncFromResources)
  output <- .mobile$describe_project_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$describe_project <- mobile_describe_project

#' Generates customized software development kit (SDK) and or tool packages
#' used to integrate mobile web or mobile app clients with backend AWS
#' resources
#'
#' @description
#' Generates customized software development kit (SDK) and or tool packages
#' used to integrate mobile web or mobile app clients with backend AWS
#' resources.
#'
#' @usage
#' mobile_export_bundle(bundleId, projectId, platform)
#'
#' @param bundleId &#91;required&#93; Unique bundle identifier.
#' @param projectId Unique project identifier.
#' @param platform Developer desktop or target application platform.
#'
#' @section Request syntax:
#' ```
#' svc$export_bundle(
#'   bundleId = "string",
#'   projectId = "string",
#'   platform = "OSX"|"WINDOWS"|"LINUX"|"OBJC"|"SWIFT"|"ANDROID"|"JAVASCRIPT"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_export_bundle
mobile_export_bundle <- function(bundleId, projectId = NULL, platform = NULL) {
  op <- new_operation(
    name = "ExportBundle",
    http_method = "POST",
    http_path = "/bundles/{bundleId}",
    paginator = list()
  )
  input <- .mobile$export_bundle_input(bundleId = bundleId, projectId = projectId, platform = platform)
  output <- .mobile$export_bundle_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$export_bundle <- mobile_export_bundle

#' Exports project configuration to a snapshot which can be downloaded and
#' shared
#'
#' @description
#' Exports project configuration to a snapshot which can be downloaded and
#' shared. Note that mobile app push credentials are encrypted in exported
#' projects, so they can only be shared successfully within the same AWS
#' account.
#'
#' @usage
#' mobile_export_project(projectId)
#'
#' @param projectId &#91;required&#93; Unique project identifier.
#'
#' @section Request syntax:
#' ```
#' svc$export_project(
#'   projectId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_export_project
mobile_export_project <- function(projectId) {
  op <- new_operation(
    name = "ExportProject",
    http_method = "POST",
    http_path = "/exports/{projectId}",
    paginator = list()
  )
  input <- .mobile$export_project_input(projectId = projectId)
  output <- .mobile$export_project_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$export_project <- mobile_export_project

#' List all available bundles
#'
#' @description
#' List all available bundles.
#'
#' @usage
#' mobile_list_bundles(maxResults, nextToken)
#'
#' @param maxResults Maximum number of records to list in a single response.
#' @param nextToken Pagination token. Set to null to start listing bundles from start. If
#' non-null pagination token is returned in a result, then pass its value
#' in here in another request to list more bundles.
#'
#' @section Request syntax:
#' ```
#' svc$list_bundles(
#'   maxResults = 123,
#'   nextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_list_bundles
mobile_list_bundles <- function(maxResults = NULL, nextToken = NULL) {
  op <- new_operation(
    name = "ListBundles",
    http_method = "GET",
    http_path = "/bundles",
    paginator = list()
  )
  input <- .mobile$list_bundles_input(maxResults = maxResults, nextToken = nextToken)
  output <- .mobile$list_bundles_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$list_bundles <- mobile_list_bundles

#' Lists projects in AWS Mobile Hub
#'
#' @description
#' Lists projects in AWS Mobile Hub.
#'
#' @usage
#' mobile_list_projects(maxResults, nextToken)
#'
#' @param maxResults Maximum number of records to list in a single response.
#' @param nextToken Pagination token. Set to null to start listing projects from start. If
#' non-null pagination token is returned in a result, then pass its value
#' in here in another request to list more projects.
#'
#' @section Request syntax:
#' ```
#' svc$list_projects(
#'   maxResults = 123,
#'   nextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_list_projects
mobile_list_projects <- function(maxResults = NULL, nextToken = NULL) {
  op <- new_operation(
    name = "ListProjects",
    http_method = "GET",
    http_path = "/projects",
    paginator = list()
  )
  input <- .mobile$list_projects_input(maxResults = maxResults, nextToken = nextToken)
  output <- .mobile$list_projects_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$list_projects <- mobile_list_projects

#' Update an existing project
#'
#' @description
#' Update an existing project.
#'
#' @usage
#' mobile_update_project(contents, projectId)
#'
#' @param contents ZIP or YAML file which contains project configuration to be updated.
#' This should be the contents of the file downloaded from the URL provided
#' in an export project operation.
#' @param projectId &#91;required&#93; Unique project identifier.
#'
#' @section Request syntax:
#' ```
#' svc$update_project(
#'   contents = raw,
#'   projectId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname mobile_update_project
mobile_update_project <- function(contents = NULL, projectId) {
  op <- new_operation(
    name = "UpdateProject",
    http_method = "POST",
    http_path = "/update",
    paginator = list()
  )
  input <- .mobile$update_project_input(contents = contents, projectId = projectId)
  output <- .mobile$update_project_output()
  config <- get_config()
  svc <- .mobile$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.mobile$operations$update_project <- mobile_update_project
