% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamodb_operations.R
\name{dynamodb_search_vectors}
\alias{dynamodb_search_vectors}
\title{Performs a vector similarity search on a vector index associated with an
Amazon DynamoDB table, and returns the most similar items sorted by
similarity score based on the distance function configured for the index}
\usage{
dynamodb_search_vectors(
  TableName,
  IndexName,
  ReturnConsumedCapacity = NULL,
  ExpressionAttributeNames = NULL,
  ExpressionAttributeValues = NULL,
  ProjectionExpression = NULL,
  SearchVector,
  SearchConditionExpression = NULL,
  TopK
)
}
\arguments{
\item{TableName}{[required] The name or Amazon Resource Name (ARN) of the table containing the vector index.}

\item{IndexName}{[required] The name of the vector index to search. The index must be in the \code{ACTIVE} state.}

\item{ReturnConsumedCapacity}{Determines the level of detail about either provisioned or on-demand throughput consumption that is returned in the response:
\itemize{
\item \code{INDEXES} - The response includes the aggregate \code{ConsumedCapacity} for the operation, together with \code{ConsumedCapacity} for each table and secondary index that was accessed.

Note that some operations, such as \code{\link[=dynamodb_get_item]{get_item}} and \code{\link[=dynamodb_batch_get_item]{batch_get_item}}, do not access any indexes at all. In these cases, specifying \code{INDEXES} will only return \code{ConsumedCapacity} information for table(s).
\item \code{TOTAL} - The response includes only the aggregate \code{ConsumedCapacity} for the operation.
\item \code{NONE} - No \code{ConsumedCapacity} details are included in the response.
}}

\item{ExpressionAttributeNames}{One or more substitution tokens for attribute names in an expression. Use the \verb{#} character in an expression to dereference an attribute name.}

\item{ExpressionAttributeValues}{One or more values that can be substituted in an expression. Use the \code{:} character in an expression to dereference an attribute value.}

\item{ProjectionExpression}{A string that identifies one or more attributes to retrieve from the index. Separate attribute names with commas. If not specified, the operation returns all attributes projected into the vector index.

Only attributes projected into the vector index can be retrieved.}

\item{SearchVector}{[required] The search vector to compare against the indexed vectors. Each element is a 32-bit IEEE-754 floating point number, provided in DynamoDB list format.

The number of dimensions must match the number of dimensions configured for the vector index.}

\item{SearchConditionExpression}{A condition expression used to filter the vector search results. The expression can reference attributes defined in the vector index search schema, including \code{HASH} and \code{INLINE_FILTER} key elements.

Only the equality operator (\code{=}) is supported for \code{HASH} attributes. Comparison and range operators are supported for \code{INLINE_FILTER} attributes. Only top-level attributes from the search schema can be referenced.}

\item{TopK}{[required] The number of most similar results to return.}
}
\description{
Performs a vector similarity search on a vector index associated with an Amazon DynamoDB table, and returns the most similar items sorted by similarity score based on the distance function configured for the index.

See \url{https://www.paws-r-sdk.com/docs/dynamodb_search_vectors/} for full documentation.
}
\keyword{internal}
