# This file is generated by make.paws. Please do not edit here.

#' Adds outputs to an existing flow
#'
#' Adds outputs to an existing flow. You can create up to 20 outputs per flow.
#'
#' @usage
#' add_flow_outputs(
#'   FlowArn = "string",
#'   Outputs = list(
#'     list(
#'       Description = "string",
#'       Destination = "string",
#'       Encryption = list(
#'         Algorithm = "aes128"|"aes192"|"aes256",
#'         KeyType = "static-key",
#'         RoleArn = "string",
#'         SecretArn = "string"
#'       ),
#'       MaxLatency = 123,
#'       Name = "string",
#'       Port = 123,
#'       Protocol = "zixi-push"|"rtp-fec"|"rtp",
#'       SmoothingLatency = 123,
#'       StreamId = "string"
#'     )
#'   )
#' )
#'
#' @param FlowArn &#91;required&#93; The flow that you want to add outputs to.
#' @param Outputs &#91;required&#93; A list of outputs that you want to add.
#'
#' @examples
#'
#' @export
add_flow_outputs <- function (FlowArn, Outputs) 
{
    op <- Operation(name = "AddFlowOutputs", http_method = "POST", 
        http_path = "/v1/flows/{flowArn}/outputs", paginator = list())
    input <- add_flow_outputs_input(FlowArn = FlowArn, Outputs = Outputs)
    output <- add_flow_outputs_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new flow
#'
#' Creates a new flow. The request must include one source. The request optionally can include outputs (up to 20) and entitlements (up to 50).
#'
#' @usage
#' create_flow(
#'   AvailabilityZone = "string",
#'   Entitlements = list(
#'     list(
#'       Description = "string",
#'       Encryption = list(
#'         Algorithm = "aes128"|"aes192"|"aes256",
#'         KeyType = "static-key",
#'         RoleArn = "string",
#'         SecretArn = "string"
#'       ),
#'       Name = "string",
#'       Subscribers = list(
#'         "string"
#'       )
#'     )
#'   ),
#'   Name = "string",
#'   Outputs = list(
#'     list(
#'       Description = "string",
#'       Destination = "string",
#'       Encryption = list(
#'         Algorithm = "aes128"|"aes192"|"aes256",
#'         KeyType = "static-key",
#'         RoleArn = "string",
#'         SecretArn = "string"
#'       ),
#'       MaxLatency = 123,
#'       Name = "string",
#'       Port = 123,
#'       Protocol = "zixi-push"|"rtp-fec"|"rtp",
#'       SmoothingLatency = 123,
#'       StreamId = "string"
#'     )
#'   ),
#'   Source = list(
#'     Decryption = list(
#'       Algorithm = "aes128"|"aes192"|"aes256",
#'       KeyType = "static-key",
#'       RoleArn = "string",
#'       SecretArn = "string"
#'     ),
#'     Description = "string",
#'     EntitlementArn = "string",
#'     IngestPort = 123,
#'     MaxBitrate = 123,
#'     MaxLatency = 123,
#'     Name = "string",
#'     Protocol = "zixi-push"|"rtp-fec"|"rtp",
#'     StreamId = "string",
#'     WhitelistCidr = "string"
#'   )
#' )
#'
#' @param AvailabilityZone The Availability Zone that you want to create the flow in. These options are limited to the Availability Zones within the current AWS Region.
#' @param Entitlements The entitlements that you want to grant on a flow.
#' @param Name &#91;required&#93; The name of the flow.
#' @param Outputs The outputs that you want to add to this flow.
#' @param Source &#91;required&#93; 
#'
#' @examples
#'
#' @export
create_flow <- function (AvailabilityZone = NULL, Entitlements = NULL, 
    Name, Outputs = NULL, Source) 
{
    op <- Operation(name = "CreateFlow", http_method = "POST", 
        http_path = "/v1/flows", paginator = list())
    input <- create_flow_input(AvailabilityZone = AvailabilityZone, 
        Entitlements = Entitlements, Name = Name, Outputs = Outputs, 
        Source = Source)
    output <- create_flow_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a flow
#'
#' Deletes a flow. Before you can delete a flow, you must stop the flow.
#'
#' @usage
#' delete_flow(
#'   FlowArn = "string"
#' )
#'
#' @param FlowArn &#91;required&#93; The ARN of the flow that you want to delete.
#'
#' @examples
#'
#' @export
delete_flow <- function (FlowArn) 
{
    op <- Operation(name = "DeleteFlow", http_method = "DELETE", 
        http_path = "/v1/flows/{flowArn}", paginator = list())
    input <- delete_flow_input(FlowArn = FlowArn)
    output <- delete_flow_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Displays the details of a flow
#'
#' Displays the details of a flow. The response includes the flow ARN, name, and Availability Zone, as well as details about the source, outputs, and entitlements.
#'
#' @usage
#' describe_flow(
#'   FlowArn = "string"
#' )
#'
#' @param FlowArn &#91;required&#93; The ARN of the flow that you want to describe.
#'
#' @examples
#'
#' @export
describe_flow <- function (FlowArn) 
{
    op <- Operation(name = "DescribeFlow", http_method = "GET", 
        http_path = "/v1/flows/{flowArn}", paginator = list())
    input <- describe_flow_input(FlowArn = FlowArn)
    output <- describe_flow_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Grants entitlements to an existing flow
#'
#' Grants entitlements to an existing flow.
#'
#' @usage
#' grant_flow_entitlements(
#'   Entitlements = list(
#'     list(
#'       Description = "string",
#'       Encryption = list(
#'         Algorithm = "aes128"|"aes192"|"aes256",
#'         KeyType = "static-key",
#'         RoleArn = "string",
#'         SecretArn = "string"
#'       ),
#'       Name = "string",
#'       Subscribers = list(
#'         "string"
#'       )
#'     )
#'   ),
#'   FlowArn = "string"
#' )
#'
#' @param Entitlements &#91;required&#93; The list of entitlements that you want to grant.
#' @param FlowArn &#91;required&#93; The flow that you want to grant entitlements on.
#'
#' @examples
#'
#' @export
grant_flow_entitlements <- function (Entitlements, FlowArn) 
{
    op <- Operation(name = "GrantFlowEntitlements", http_method = "POST", 
        http_path = "/v1/flows/{flowArn}/entitlements", paginator = list())
    input <- grant_flow_entitlements_input(Entitlements = Entitlements, 
        FlowArn = FlowArn)
    output <- grant_flow_entitlements_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Displays a list of all entitlements that have been granted to this account
#'
#' Displays a list of all entitlements that have been granted to this account. This request returns 20 results per page.
#'
#' @usage
#' list_entitlements(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param MaxResults The maximum number of results to return per API request. For example, you submit a ListEntitlements request with MaxResults set at 5. Although 20 items match your request, the service returns no more than the first 5 items. (The service also returns a NextToken value that you can use to fetch the next batch of results.) The service might return fewer results than the MaxResults value. If MaxResults is not included in the request, the service defaults to pagination with a maximum of 20 results per page.
#' @param NextToken The token that identifies which batch of results that you want to see. For example, you submit a ListEntitlements request with MaxResults set at 5. The service returns the first batch of results (up to 5) and a NextToken value. To see the next batch of results, you can submit the ListEntitlements request a second time and specify the NextToken value.
#'
#' @examples
#'
#' @export
list_entitlements <- function (MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListEntitlements", http_method = "GET", 
        http_path = "/v1/entitlements", paginator = list())
    input <- list_entitlements_input(MaxResults = MaxResults, 
        NextToken = NextToken)
    output <- list_entitlements_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Displays a list of flows that are associated with this account
#'
#' Displays a list of flows that are associated with this account. This request returns a paginated result.
#'
#' @usage
#' list_flows(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param MaxResults The maximum number of results to return per API request. For example, you submit a ListFlows request with MaxResults set at 5. Although 20 items match your request, the service returns no more than the first 5 items. (The service also returns a NextToken value that you can use to fetch the next batch of results.) The service might return fewer results than the MaxResults value. If MaxResults is not included in the request, the service defaults to pagination with a maximum of 10 results per page.
#' @param NextToken The token that identifies which batch of results that you want to see. For example, you submit a ListFlows request with MaxResults set at 5. The service returns the first batch of results (up to 5) and a NextToken value. To see the next batch of results, you can submit the ListFlows request a second time and specify the NextToken value.
#'
#' @examples
#'
#' @export
list_flows <- function (MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListFlows", http_method = "GET", 
        http_path = "/v1/flows", paginator = list())
    input <- list_flows_input(MaxResults = MaxResults, NextToken = NextToken)
    output <- list_flows_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes an output from an existing flow
#'
#' Removes an output from an existing flow. This request can be made only on an output that does not have an entitlement associated with it. If the output has an entitlement, you must revoke the entitlement instead. When an entitlement is revoked from a flow, the service automatically removes the associated output.
#'
#' @usage
#' remove_flow_output(
#'   FlowArn = "string",
#'   OutputArn = "string"
#' )
#'
#' @param FlowArn &#91;required&#93; The flow that you want to remove an output from.
#' @param OutputArn &#91;required&#93; The ARN of the output that you want to remove.
#'
#' @examples
#'
#' @export
remove_flow_output <- function (FlowArn, OutputArn) 
{
    op <- Operation(name = "RemoveFlowOutput", http_method = "DELETE", 
        http_path = "/v1/flows/{flowArn}/outputs/{outputArn}", 
        paginator = list())
    input <- remove_flow_output_input(FlowArn = FlowArn, OutputArn = OutputArn)
    output <- remove_flow_output_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Revokes an entitlement from a flow
#'
#' Revokes an entitlement from a flow. Once an entitlement is revoked, the content becomes unavailable to the subscriber and the associated output is removed.
#'
#' @usage
#' revoke_flow_entitlement(
#'   EntitlementArn = "string",
#'   FlowArn = "string"
#' )
#'
#' @param EntitlementArn &#91;required&#93; The ARN of the entitlement that you want to revoke.
#' @param FlowArn &#91;required&#93; The flow that you want to revoke an entitlement from.
#'
#' @examples
#'
#' @export
revoke_flow_entitlement <- function (EntitlementArn, FlowArn) 
{
    op <- Operation(name = "RevokeFlowEntitlement", http_method = "DELETE", 
        http_path = "/v1/flows/{flowArn}/entitlements/{entitlementArn}", 
        paginator = list())
    input <- revoke_flow_entitlement_input(EntitlementArn = EntitlementArn, 
        FlowArn = FlowArn)
    output <- revoke_flow_entitlement_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Starts a flow
#'
#' Starts a flow.
#'
#' @usage
#' start_flow(
#'   FlowArn = "string"
#' )
#'
#' @param FlowArn &#91;required&#93; The ARN of the flow that you want to start.
#'
#' @examples
#'
#' @export
start_flow <- function (FlowArn) 
{
    op <- Operation(name = "StartFlow", http_method = "POST", 
        http_path = "/v1/flows/start/{flowArn}", paginator = list())
    input <- start_flow_input(FlowArn = FlowArn)
    output <- start_flow_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Stops a flow
#'
#' Stops a flow.
#'
#' @usage
#' stop_flow(
#'   FlowArn = "string"
#' )
#'
#' @param FlowArn &#91;required&#93; The ARN of the flow that you want to stop.
#'
#' @examples
#'
#' @export
stop_flow <- function (FlowArn) 
{
    op <- Operation(name = "StopFlow", http_method = "POST", 
        http_path = "/v1/flows/stop/{flowArn}", paginator = list())
    input <- stop_flow_input(FlowArn = FlowArn)
    output <- stop_flow_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' You can change an entitlement's description, subscribers, and encryption
#'
#' You can change an entitlement's description, subscribers, and encryption. If you change the subscribers, the service will remove the outputs that are are used by the subscribers that are removed.
#'
#' @usage
#' update_flow_entitlement(
#'   Description = "string",
#'   Encryption = list(
#'     Algorithm = "aes128"|"aes192"|"aes256",
#'     KeyType = "static-key",
#'     RoleArn = "string",
#'     SecretArn = "string"
#'   ),
#'   EntitlementArn = "string",
#'   FlowArn = "string",
#'   Subscribers = list(
#'     "string"
#'   )
#' )
#'
#' @param Description A description of the entitlement. This description appears only on the AWS Elemental MediaConnect console and will not be seen by the subscriber or end user.
#' @param Encryption The type of encryption that will be used on the output associated with this entitlement.
#' @param EntitlementArn &#91;required&#93; The ARN of the entitlement that you want to update.
#' @param FlowArn &#91;required&#93; The flow that is associated with the entitlement that you want to update.
#' @param Subscribers The AWS account IDs that you want to share your content with. The receiving accounts (subscribers) will be allowed to create their own flow using your content as the source.
#'
#' @examples
#'
#' @export
update_flow_entitlement <- function (Description = NULL, Encryption = NULL, 
    EntitlementArn, FlowArn, Subscribers = NULL) 
{
    op <- Operation(name = "UpdateFlowEntitlement", http_method = "PUT", 
        http_path = "/v1/flows/{flowArn}/entitlements/{entitlementArn}", 
        paginator = list())
    input <- update_flow_entitlement_input(Description = Description, 
        Encryption = Encryption, EntitlementArn = EntitlementArn, 
        FlowArn = FlowArn, Subscribers = Subscribers)
    output <- update_flow_entitlement_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates an existing flow output
#'
#' Updates an existing flow output.
#'
#' @usage
#' update_flow_output(
#'   Description = "string",
#'   Destination = "string",
#'   Encryption = list(
#'     Algorithm = "aes128"|"aes192"|"aes256",
#'     KeyType = "static-key",
#'     RoleArn = "string",
#'     SecretArn = "string"
#'   ),
#'   FlowArn = "string",
#'   MaxLatency = 123,
#'   OutputArn = "string",
#'   Port = 123,
#'   Protocol = "zixi-push"|"rtp-fec"|"rtp",
#'   SmoothingLatency = 123,
#'   StreamId = "string"
#' )
#'
#' @param Description A description of the output. This description appears only on the AWS Elemental MediaConnect console and will not be seen by the end user.
#' @param Destination The IP address where you want to send the output.
#' @param Encryption The type of key used for the encryption. If no keyType is provided, the service will use the default setting (static-key).
#' @param FlowArn &#91;required&#93; The flow that is associated with the output that you want to update.
#' @param MaxLatency The maximum latency in milliseconds for Zixi-based streams.
#' @param OutputArn &#91;required&#93; The ARN of the output that you want to update.
#' @param Port The port to use when content is distributed to this output.
#' @param Protocol The protocol to use for the output.
#' @param SmoothingLatency The smoothing latency in milliseconds for RTP and RTP-FEC streams.
#' @param StreamId The stream ID that you want to use for this transport. This parameter applies only to Zixi-based streams.
#'
#' @examples
#'
#' @export
update_flow_output <- function (Description = NULL, Destination = NULL, 
    Encryption = NULL, FlowArn, MaxLatency = NULL, OutputArn, 
    Port = NULL, Protocol = NULL, SmoothingLatency = NULL, StreamId = NULL) 
{
    op <- Operation(name = "UpdateFlowOutput", http_method = "PUT", 
        http_path = "/v1/flows/{flowArn}/outputs/{outputArn}", 
        paginator = list())
    input <- update_flow_output_input(Description = Description, 
        Destination = Destination, Encryption = Encryption, FlowArn = FlowArn, 
        MaxLatency = MaxLatency, OutputArn = OutputArn, Port = Port, 
        Protocol = Protocol, SmoothingLatency = SmoothingLatency, 
        StreamId = StreamId)
    output <- update_flow_output_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the source of a flow
#'
#' Updates the source of a flow.
#'
#' @usage
#' update_flow_source(
#'   Decryption = list(
#'     Algorithm = "aes128"|"aes192"|"aes256",
#'     KeyType = "static-key",
#'     RoleArn = "string",
#'     SecretArn = "string"
#'   ),
#'   Description = "string",
#'   EntitlementArn = "string",
#'   FlowArn = "string",
#'   IngestPort = 123,
#'   MaxBitrate = 123,
#'   MaxLatency = 123,
#'   Protocol = "zixi-push"|"rtp-fec"|"rtp",
#'   SourceArn = "string",
#'   StreamId = "string",
#'   WhitelistCidr = "string"
#' )
#'
#' @param Decryption The type of encryption used on the content ingested from this source.
#' @param Description A description for the source. This value is not used or seen outside of the current AWS Elemental MediaConnect account.
#' @param EntitlementArn The ARN of the entitlement that allows you to subscribe to this flow. The entitlement is set by the flow originator, and the ARN is generated as part of the originator's flow.
#' @param FlowArn &#91;required&#93; The flow that is associated with the source that you want to update.
#' @param IngestPort The port that the flow will be listening on for incoming content.
#' @param MaxBitrate The smoothing max bitrate for RTP and RTP-FEC streams.
#' @param MaxLatency The maximum latency in milliseconds for Zixi-based streams.
#' @param Protocol The protocol that is used by the source.
#' @param SourceArn &#91;required&#93; The ARN of the source that you want to update.
#' @param StreamId The stream ID that you want to use for this transport. This parameter applies only to Zixi-based streams.
#' @param WhitelistCidr The range of IP addresses that should be allowed to contribute content to your source. These IP addresses should in the form of a Classless Inter-Domain Routing (CIDR) block; for example, 10.0.0.0/16.
#'
#' @examples
#'
#' @export
update_flow_source <- function (Decryption = NULL, Description = NULL, 
    EntitlementArn = NULL, FlowArn, IngestPort = NULL, MaxBitrate = NULL, 
    MaxLatency = NULL, Protocol = NULL, SourceArn, StreamId = NULL, 
    WhitelistCidr = NULL) 
{
    op <- Operation(name = "UpdateFlowSource", http_method = "PUT", 
        http_path = "/v1/flows/{flowArn}/source/{sourceArn}", 
        paginator = list())
    input <- update_flow_source_input(Decryption = Decryption, 
        Description = Description, EntitlementArn = EntitlementArn, 
        FlowArn = FlowArn, IngestPort = IngestPort, MaxBitrate = MaxBitrate, 
        MaxLatency = MaxLatency, Protocol = Protocol, SourceArn = SourceArn, 
        StreamId = StreamId, WhitelistCidr = WhitelistCidr)
    output <- update_flow_source_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
