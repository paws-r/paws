# This file is generated by make.paws. Please do not edit here.

#' Instantiates an autoscaling virtual server based on Secure File Transfer Protocol (SFTP) in AWS
#'
#' Instantiates an autoscaling virtual server based on Secure File Transfer Protocol (SFTP) in AWS. The call returns the `ServerId` property assigned by the service to the newly created server. Reference this `ServerId` property when you make updates to your server, or work with users.
#' 
#' The response returns the `ServerId` value for the newly created server.
#'
#' @usage
#' create_server(
#'   IdentityProviderDetails = list(
#'     Url = "string",
#'     InvocationRole = "string"
#'   ),
#'   IdentityProviderType = "SERVICE_MANAGED"|"API_GATEWAY",
#'   LoggingRole = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#'
#' @param IdentityProviderDetails An array containing all of the information required to call a customer-supplied authentication API. This parameter is not required when the `IdentityProviderType` value of server that is created uses the `SERVICE_MANAGED` authentication method.
#' @param IdentityProviderType The mode of authentication enabled for this service. The default value is `SERVICE_MANAGED`, which allows you to store and access SFTP user credentials within the service. An `IdentityProviderType` value of `API_GATEWAY` indicates that user authentication requires a call to an API Gateway endpoint URL provided by you to integrate an identity provider of your choice.
#' @param LoggingRole A value that allows the service to write your SFTP users' activity to your Amazon CloudWatch logs for monitoring and auditing purposes.
#' @param Tags Key-value pairs that can be used to group and search for servers.
#'
#' @examples
#'
#' @export
create_server <- function (IdentityProviderDetails = NULL, IdentityProviderType = NULL, 
    LoggingRole = NULL, Tags = NULL) 
{
    op <- Operation(name = "CreateServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_server_input(IdentityProviderDetails = IdentityProviderDetails, 
        IdentityProviderType = IdentityProviderType, LoggingRole = LoggingRole, 
        Tags = Tags)
    output <- create_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds a user and associate them with an existing Secure File Transfer Protocol (SFTP) server
#'
#' Adds a user and associate them with an existing Secure File Transfer Protocol (SFTP) server. Using parameters for `CreateUser`, you can specify the user name, set the home directory, store the user\'s public key, and assign the user\'s AWS Identity and Access Management (IAM) role. You can also optionally add a scope-down policy, and assign metadata with tags that can be used to group and search for users.
#' 
#' The response returns the `UserName` and `ServerId` values of the new user for that server.
#'
#' @usage
#' create_user(
#'   HomeDirectory = "string",
#'   Policy = "string",
#'   Role = "string",
#'   ServerId = "string",
#'   SshPublicKeyBody = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   ),
#'   UserName = "string"
#' )
#'
#' @param HomeDirectory The landing directory (folder) for a user when they log in to the server using their SFTP client. An example is `/home/username username `.
#' @param Policy A scope-down policy for your user so you can use the same IAM role across multiple users. This policy scopes down user access to portions of their Amazon S3 bucket. Variables you can use inside this policy include `${Transfer:UserName}`, `${Transfer:HomeDirectory}`, and `${Transfer:HomeBucket}`.
#' @param Role &#91;required&#93; The IAM role that controls your user's access to your Amazon S3 bucket. The policies attached to this role will determine the level of access you want to provide your users when transferring files into and out of your Amazon S3 bucket or buckets. The IAM role should also contain a trust relationship that allows the SFTP server to access your resources when servicing your SFTP user's transfer requests.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance. This is the specific SFTP server that you added your user to.
#' @param SshPublicKeyBody The public portion of the Secure Shall (SSH) key used to authenticate the user to the SFTP server.
#' @param Tags Key-value pairs that can be used to group and search for users. Tags are metadata attached to users for any purpose.
#' @param UserName &#91;required&#93; A unique string that identifies a user and is associated with a server as specified by the `ServerId`.
#'
#' @examples
#'
#' @export
create_user <- function (HomeDirectory = NULL, Policy = NULL, 
    Role, ServerId, SshPublicKeyBody = NULL, Tags = NULL, UserName) 
{
    op <- Operation(name = "CreateUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_user_input(HomeDirectory = HomeDirectory, 
        Policy = Policy, Role = Role, ServerId = ServerId, SshPublicKeyBody = SshPublicKeyBody, 
        Tags = Tags, UserName = UserName)
    output <- create_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the Secure File Transfer Protocol (SFTP) server that you specify
#'
#' Deletes the Secure File Transfer Protocol (SFTP) server that you specify. If you used `SERVICE_MANAGED` as your `IdentityProviderType`, you need to delete all users associated with this server before deleting the server itself
#' 
#' No response returns from this call.
#'
#' @usage
#' delete_server(
#'   ServerId = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A unique system-assigned identifier for an SFTP server instance.
#'
#' @examples
#'
#' @export
delete_server <- function (ServerId) 
{
    op <- Operation(name = "DeleteServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_server_input(ServerId = ServerId)
    output <- delete_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a user\'s Secure Shell (SSH) public key
#'
#' Deletes a user\'s Secure Shell (SSH) public key.
#' 
#' No response is returned from this call.
#'
#' @usage
#' delete_ssh_public_key(
#'   ServerId = "string",
#'   SshPublicKeyId = "string",
#'   UserName = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for a Secure File Transfer Protocol (SFTP) server instance that has the user assigned to it.
#' @param SshPublicKeyId &#91;required&#93; A unique identifier used to reference your user's specific SSH key.
#' @param UserName &#91;required&#93; A unique string that identifies a user whose public key is being deleted.
#'
#' @examples
#'
#' @export
delete_ssh_public_key <- function (ServerId, SshPublicKeyId, 
    UserName) 
{
    op <- Operation(name = "DeleteSshPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_ssh_public_key_input(ServerId = ServerId, 
        SshPublicKeyId = SshPublicKeyId, UserName = UserName)
    output <- delete_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the user belonging to the server you specify
#'
#' Deletes the user belonging to the server you specify.
#' 
#' No response returns from this call.
#' 
#' When you delete a user from a server, the user\'s information is lost.
#'
#' @usage
#' delete_user(
#'   ServerId = "string",
#'   UserName = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that has the user assigned to it.
#' @param UserName &#91;required&#93; A unique string that identifies a user that is being deleted from the server.
#'
#' @examples
#'
#' @export
delete_user <- function (ServerId, UserName) 
{
    op <- Operation(name = "DeleteUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_user_input(ServerId = ServerId, UserName = UserName)
    output <- delete_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Describes the server that you specify by passing the `ServerId` parameter
#'
#' Describes the server that you specify by passing the `ServerId` parameter.
#' 
#' The response contains a description of the server\'s properties.
#'
#' @usage
#' describe_server(
#'   ServerId = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server.
#'
#' @examples
#'
#' @export
describe_server <- function (ServerId) 
{
    op <- Operation(name = "DescribeServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_server_input(ServerId = ServerId)
    output <- describe_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Describes the user assigned to a specific server, as identified by its `ServerId` property
#'
#' Describes the user assigned to a specific server, as identified by its `ServerId` property.
#' 
#' The response from this call returns the properties of the user associated with the `ServerId` value that was specified.
#'
#' @usage
#' describe_user(
#'   ServerId = "string",
#'   UserName = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that has this user assigned.
#' @param UserName &#91;required&#93; The name of the user assigned to one or more servers. User names are part of the sign-in credentials to use the AWS Transfer service and perform file transfer tasks.
#'
#' @examples
#'
#' @export
describe_user <- function (ServerId, UserName) 
{
    op <- Operation(name = "DescribeUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_user_input(ServerId = ServerId, UserName = UserName)
    output <- describe_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds a Secure Shell (SSH) public key to a user account identified by a `UserName` value assigned to a specific server, identified by `ServerId`
#'
#' Adds a Secure Shell (SSH) public key to a user account identified by a `UserName` value assigned to a specific server, identified by `ServerId`.
#' 
#' The response returns the `UserName` value, the `ServerId` value, and the name of the `SshPublicKeyId`.
#'
#' @usage
#' import_ssh_public_key(
#'   ServerId = "string",
#'   SshPublicKeyBody = "string",
#'   UserName = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server.
#' @param SshPublicKeyBody &#91;required&#93; The public key portion of an SSH key pair.
#' @param UserName &#91;required&#93; The name of the user account that is assigned to one or more servers.
#'
#' @examples
#'
#' @export
import_ssh_public_key <- function (ServerId, SshPublicKeyBody, 
    UserName) 
{
    op <- Operation(name = "ImportSshPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- import_ssh_public_key_input(ServerId = ServerId, 
        SshPublicKeyBody = SshPublicKeyBody, UserName = UserName)
    output <- import_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the Secure File Transfer Protocol (SFTP) servers that are associated with your AWS account
#'
#' Lists the Secure File Transfer Protocol (SFTP) servers that are associated with your AWS account.
#'
#' @usage
#' list_servers(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param MaxResults Specifies the number of servers to return as a response to the `ListServers` query.
#' @param NextToken When additional results are obtained from the ListServers command, a `NextToken` parameter is returned in the output. You can then pass the `NextToken` parameter in a subsequent command to continue listing additional servers.
#'
#' @examples
#'
#' @export
list_servers <- function (MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListServers", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_servers_input(MaxResults = MaxResults, NextToken = NextToken)
    output <- list_servers_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all of the tags associated with the Amazon Resource Number (ARN) you specify
#'
#' Lists all of the tags associated with the Amazon Resource Number (ARN) you specify. The resource can be a user, server, or role.
#'
#' @usage
#' list_tags_for_resource(
#'   Arn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param Arn &#91;required&#93; Requests the tags associated with a particular Amazon Resource Name (ARN). An ARN is an identifier for a specific AWS resource, such as a server, user, or role.
#' @param MaxResults 
#' @param NextToken 
#'
#' @examples
#'
#' @export
list_tags_for_resource <- function (Arn, MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListTagsForResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_tags_for_resource_input(Arn = Arn, MaxResults = MaxResults, 
        NextToken = NextToken)
    output <- list_tags_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the users for the server that you specify by passing the `ServerId` parameter
#'
#' Lists the users for the server that you specify by passing the `ServerId` parameter.
#'
#' @usage
#' list_users(
#'   MaxResults = 123,
#'   NextToken = "string",
#'   ServerId = "string"
#' )
#'
#' @param MaxResults Specifies the number of users to return as a response to the `ListUsers` request.
#' @param NextToken When you can get additional results from the `ListUsers`ListUsers call, a `NextToken` parameter is returned in the output. You can then pass in a subsequent command the `NextToken` parameter to continue listing additional users.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for a Secure File Transfer Protocol (SFTP) server that has users are assigned to it.
#'
#' @examples
#'
#' @export
list_users <- function (MaxResults = NULL, NextToken = NULL, 
    ServerId) 
{
    op <- Operation(name = "ListUsers", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_users_input(MaxResults = MaxResults, NextToken = NextToken, 
        ServerId = ServerId)
    output <- list_users_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the state of a Secure File Transfer Protocol (SFTP) server from `OFFLINE` to `ONLINE`
#'
#' Changes the state of a Secure File Transfer Protocol (SFTP) server from `OFFLINE` to `ONLINE`. It has no impact on an SFTP server that is already `ONLINE`. An `ONLINE` server can accept and process file transfer jobs.
#' 
#' The state of `STARTING` indicates that the server is in an intermediate state, either not fully able to respond, or not fully online. The values of `START_FAILED` can indicate an error condition.
#' 
#' No response is returned from this call.
#'
#' @usage
#' start_server(
#'   ServerId = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that you start.
#'
#' @examples
#'
#' @export
start_server <- function (ServerId) 
{
    op <- Operation(name = "StartServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- start_server_input(ServerId = ServerId)
    output <- start_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the state of an SFTP server from `ONLINE` to `OFFLINE`
#'
#' Changes the state of an SFTP server from `ONLINE` to `OFFLINE`. An `OFFLINE` server cannot accept and process file transfer jobs. Information tied to your server such as server and user properties are not affected by stopping your server. Stopping a server will not reduce or impact your Secure File Transfer Protocol (SFTP) endpoint billing.
#' 
#' The states of `STOPPING` indicates that the server is in an intermediate state, either not fully able to respond, or not fully offline. The values of `STOP_FAILED` can indicate an error condition.
#' 
#' No response is returned from this call.
#'
#' @usage
#' stop_server(
#'   ServerId = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that you stopped.
#'
#' @examples
#'
#' @export
stop_server <- function (ServerId) 
{
    op <- Operation(name = "StopServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- stop_server_input(ServerId = ServerId)
    output <- stop_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches a key-value pair to a resource, as identified by its Amazon Resource Name (ARN)
#'
#' Attaches a key-value pair to a resource, as identified by its Amazon Resource Name (ARN). Resources are users, servers, roles, and other entities.
#' 
#' There is no response returned from this call.
#'
#' @usage
#' tag_resource(
#'   Arn = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#'
#' @param Arn &#91;required&#93; An Amazon Resource Name (ARN) for a specific AWS resource, such as a server, user, or role.
#' @param Tags &#91;required&#93; Key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to user accounts for any purpose.
#'
#' @examples
#'
#' @export
tag_resource <- function (Arn, Tags) 
{
    op <- Operation(name = "TagResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- tag_resource_input(Arn = Arn, Tags = Tags)
    output <- tag_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' If the `IdentityProviderType` of the server is `API_Gateway`, tests whether your API Gateway is set up successfully
#'
#' If the `IdentityProviderType` of the server is `API_Gateway`, tests whether your API Gateway is set up successfully. We highly recommend that you call this method to test your authentication method as soon as you create your server. By doing so, you can troubleshoot issues with the API Gateway integration to ensure that your users can successfully use the service.
#'
#' @usage
#' test_identity_provider(
#'   ServerId = "string",
#'   UserName = "string",
#'   UserPassword = "string"
#' )
#'
#' @param ServerId &#91;required&#93; A system assigned identifier for a specific server. That server\'s user authentication method is tested with a user name and password.
#' @param UserName &#91;required&#93; This request parameter is name of the user account to be tested.
#' @param UserPassword The password of the user account to be tested.
#'
#' @examples
#'
#' @export
test_identity_provider <- function (ServerId, UserName, UserPassword = NULL) 
{
    op <- Operation(name = "TestIdentityProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- test_identity_provider_input(ServerId = ServerId, 
        UserName = UserName, UserPassword = UserPassword)
    output <- test_identity_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Detaches a key-value pair from a resource, as identified by its Amazon Resource Name (ARN)
#'
#' Detaches a key-value pair from a resource, as identified by its Amazon Resource Name (ARN). Resources are users, servers, roles, and other entities.
#' 
#' No response is returned from this call.
#'
#' @usage
#' untag_resource(
#'   Arn = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#'
#' @param Arn &#91;required&#93; This is the value of the resource that will have the tag removed. An Amazon Resource Name (ARN) is an identifier for a specific AWS resource, such as a server, user, or role.
#' @param TagKeys &#91;required&#93; TagKeys are key-value pairs assigned to ARNs that can be used to group and search for resources by type. This metadata can be attached to resources for any purpose.
#'
#' @examples
#'
#' @export
untag_resource <- function (Arn, TagKeys) 
{
    op <- Operation(name = "UntagResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- untag_resource_input(Arn = Arn, TagKeys = TagKeys)
    output <- untag_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the server properties after that server has been created
#'
#' Updates the server properties after that server has been created.
#' 
#' The `UpdateServer` call returns the `ServerId` of the Secure File Transfer Protocol (SFTP) server you updated.
#'
#' @usage
#' update_server(
#'   IdentityProviderDetails = list(
#'     Url = "string",
#'     InvocationRole = "string"
#'   ),
#'   LoggingRole = "string",
#'   ServerId = "string"
#' )
#'
#' @param IdentityProviderDetails This response parameter is an array containing all of the information required to call a customer\'s authentication API method.
#' @param LoggingRole Changes the AWS Identity and Access Management (IAM) role that allows Amazon S3 events to be logged in Amazon CloudWatch, turning logging on or off.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that the user account is assigned to.
#'
#' @examples
#'
#' @export
update_server <- function (IdentityProviderDetails = NULL, LoggingRole = NULL, 
    ServerId) 
{
    op <- Operation(name = "UpdateServer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_server_input(IdentityProviderDetails = IdentityProviderDetails, 
        LoggingRole = LoggingRole, ServerId = ServerId)
    output <- update_server_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Assigns new properties to a user
#'
#' Assigns new properties to a user. Parameters you pass modify any or all of the following: the home directory, role, and policy for the `UserName` and `ServerId` you specify.
#' 
#' The response returns the `ServerId` and the `UserName` for the updated user.
#'
#' @usage
#' update_user(
#'   HomeDirectory = "string",
#'   Policy = "string",
#'   Role = "string",
#'   ServerId = "string",
#'   UserName = "string"
#' )
#'
#' @param HomeDirectory The HomeDirectory parameter specifies the landing directory (folder) for a user when they log in to the server using their client. An example would be: `/home/username username `.
#' @param Policy Allows you to supply a scope-down policy for your user so you can use the same AWS Identity and Access Management (IAM) role across multiple users. The policy scopes down users access to portions of your Amazon S3 bucket. Variables you can use inside this policy include `${Transfer:UserName}`, `${Transfer:HomeDirectory}`, and `${Transfer:HomeBucket}`.
#' @param Role The IAM role that controls your user's access to your Amazon S3 bucket. The policies attached to this role will determine the level of access you want to provide your users when transferring files into and out of your Amazon S3 bucket or buckets. The IAM role should also contain a trust relationship that allows the Secure File Transfer Protocol (SFTP) server to access your resources when servicing your SFTP user's transfer requests.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that the user account is assigned to.
#' @param UserName &#91;required&#93; A unique string that identifies a user and is associated with a server as specified by the ServerId. This is the string that will be used by your user when they log in to your SFTP server.
#'
#' @examples
#'
#' @export
update_user <- function (HomeDirectory = NULL, Policy = NULL, 
    Role = NULL, ServerId, UserName) 
{
    op <- Operation(name = "UpdateUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_user_input(HomeDirectory = HomeDirectory, 
        Policy = Policy, Role = Role, ServerId = ServerId, UserName = UserName)
    output <- update_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
