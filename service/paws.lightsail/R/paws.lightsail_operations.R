# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Allocates a static IP address
#'
#' Allocates a static IP address.
#'
#' @section Accepted Parameters:
#' ```
#' allocate_static_ip(
#'   staticIpName = "string"
#' )
#' ```
#'
#' @param staticIpName &#91;required&#93; The name of the static IP address.
#'
#' @export
allocate_static_ip <- function (staticIpName) 
{
    op <- new_operation(name = "AllocateStaticIp", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- allocate_static_ip_input(staticIpName = staticIpName)
    output <- allocate_static_ip_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches a block storage disk to a running or stopped Lightsail instance and exposes it to the instance with the specified disk name
#'
#' Attaches a block storage disk to a running or stopped Lightsail instance and exposes it to the instance with the specified disk name.
#'
#' @section Accepted Parameters:
#' ```
#' attach_disk(
#'   diskName = "string",
#'   instanceName = "string",
#'   diskPath = "string"
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique Lightsail disk name (e.g., `my-disk`).
#' @param instanceName &#91;required&#93; The name of the Lightsail instance where you want to utilize the storage disk.
#' @param diskPath &#91;required&#93; The disk path to expose to the instance (e.g., `/dev/xvdf`).
#'
#' @export
attach_disk <- function (diskName, instanceName, diskPath) 
{
    op <- new_operation(name = "AttachDisk", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_disk_input(diskName = diskName, instanceName = instanceName, 
        diskPath = diskPath)
    output <- attach_disk_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches one or more Lightsail instances to a load balancer
#'
#' Attaches one or more Lightsail instances to a load balancer.
#' 
#' After some time, the instances are attached to the load balancer and the health check status is available.
#'
#' @section Accepted Parameters:
#' ```
#' attach_instances_to_load_balancer(
#'   loadBalancerName = "string",
#'   instanceNames = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer.
#' @param instanceNames &#91;required&#93; An array of strings representing the instance name(s) you want to attach to your load balancer.
#' 
#' An instance must be `running` before you can attach it to your load balancer.
#' 
#' There are no additional limits on the number of instances you can attach to your load balancer, aside from the limit of Lightsail instances you can create in your account (20).
#'
#' @export
attach_instances_to_load_balancer <- function (loadBalancerName, 
    instanceNames) 
{
    op <- new_operation(name = "AttachInstancesToLoadBalancer", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- attach_instances_to_load_balancer_input(loadBalancerName = loadBalancerName, 
        instanceNames = instanceNames)
    output <- attach_instances_to_load_balancer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches a Transport Layer Security (TLS) certificate to your load balancer
#'
#' Attaches a Transport Layer Security (TLS) certificate to your load balancer. TLS is just an updated, more secure version of Secure Socket Layer (SSL).
#' 
#' Once you create and validate your certificate, you can attach it to your load balancer. You can also use this API to rotate the certificates on your account. Use the `AttachLoadBalancerTlsCertificate` operation with the non-attached certificate, and it will replace the existing one and become the attached certificate.
#'
#' @section Accepted Parameters:
#' ```
#' attach_load_balancer_tls_certificate(
#'   loadBalancerName = "string",
#'   certificateName = "string"
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer to which you want to associate the SSL/TLS certificate.
#' @param certificateName &#91;required&#93; The name of your SSL/TLS certificate.
#'
#' @export
attach_load_balancer_tls_certificate <- function (loadBalancerName, 
    certificateName) 
{
    op <- new_operation(name = "AttachLoadBalancerTlsCertificate", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- attach_load_balancer_tls_certificate_input(loadBalancerName = loadBalancerName, 
        certificateName = certificateName)
    output <- attach_load_balancer_tls_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches a static IP address to a specific Amazon Lightsail instance
#'
#' Attaches a static IP address to a specific Amazon Lightsail instance.
#'
#' @section Accepted Parameters:
#' ```
#' attach_static_ip(
#'   staticIpName = "string",
#'   instanceName = "string"
#' )
#' ```
#'
#' @param staticIpName &#91;required&#93; The name of the static IP.
#' @param instanceName &#91;required&#93; The instance name to which you want to attach the static IP address.
#'
#' @export
attach_static_ip <- function (staticIpName, instanceName) 
{
    op <- new_operation(name = "AttachStaticIp", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_static_ip_input(staticIpName = staticIpName, 
        instanceName = instanceName)
    output <- attach_static_ip_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Closes the public ports on a specific Amazon Lightsail instance
#'
#' Closes the public ports on a specific Amazon Lightsail instance.
#'
#' @section Accepted Parameters:
#' ```
#' close_instance_public_ports(
#'   portInfo = list(
#'     fromPort = 123,
#'     toPort = 123,
#'     protocol = "tcp"|"all"|"udp"
#'   ),
#'   instanceName = "string"
#' )
#' ```
#'
#' @param portInfo &#91;required&#93; Information about the public port you are trying to close.
#' @param instanceName &#91;required&#93; The name of the instance on which you\'re attempting to close the public ports.
#'
#' @export
close_instance_public_ports <- function (portInfo, instanceName) 
{
    op <- new_operation(name = "CloseInstancePublicPorts", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- close_instance_public_ports_input(portInfo = portInfo, 
        instanceName = instanceName)
    output <- close_instance_public_ports_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a block storage disk that can be attached to a Lightsail instance in the same Availability Zone (e
#'
#' Creates a block storage disk that can be attached to a Lightsail instance in the same Availability Zone (e.g., `us-east-2a`). The disk is created in the regional endpoint that you send the HTTP request to. For more information, see [Regions and Availability Zones in Lightsail](https://lightsail.aws.amazon.com/ls/docs/overview/article/understanding-regions-and-availability-zones-in-amazon-lightsail).
#'
#' @section Accepted Parameters:
#' ```
#' create_disk(
#'   diskName = "string",
#'   availabilityZone = "string",
#'   sizeInGb = 123
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique Lightsail disk name (e.g., `my-disk`).
#' @param availabilityZone &#91;required&#93; The Availability Zone where you want to create the disk (e.g., `us-east-2a`). Choose the same Availability Zone as the Lightsail instance where you want to create the disk.
#' 
#' Use the GetRegions operation to list the Availability Zones where Lightsail is currently available.
#' @param sizeInGb &#91;required&#93; The size of the disk in GB (e.g., `32`).
#'
#' @export
create_disk <- function (diskName, availabilityZone, sizeInGb) 
{
    op <- new_operation(name = "CreateDisk", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_disk_input(diskName = diskName, availabilityZone = availabilityZone, 
        sizeInGb = sizeInGb)
    output <- create_disk_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a block storage disk from a disk snapshot that can be attached to a Lightsail instance in the same Availability Zone (e
#'
#' Creates a block storage disk from a disk snapshot that can be attached to a Lightsail instance in the same Availability Zone (e.g., `us-east-2a`). The disk is created in the regional endpoint that you send the HTTP request to. For more information, see [Regions and Availability Zones in Lightsail](https://lightsail.aws.amazon.com/ls/docs/overview/article/understanding-regions-and-availability-zones-in-amazon-lightsail).
#'
#' @section Accepted Parameters:
#' ```
#' create_disk_from_snapshot(
#'   diskName = "string",
#'   diskSnapshotName = "string",
#'   availabilityZone = "string",
#'   sizeInGb = 123
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique Lightsail disk name (e.g., `my-disk`).
#' @param diskSnapshotName &#91;required&#93; The name of the disk snapshot (e.g., `my-snapshot`) from which to create the new storage disk.
#' @param availabilityZone &#91;required&#93; The Availability Zone where you want to create the disk (e.g., `us-east-2a`). Choose the same Availability Zone as the Lightsail instance where you want to create the disk.
#' 
#' Use the GetRegions operation to list the Availability Zones where Lightsail is currently available.
#' @param sizeInGb &#91;required&#93; The size of the disk in GB (e.g., `32`).
#'
#' @export
create_disk_from_snapshot <- function (diskName, diskSnapshotName, 
    availabilityZone, sizeInGb) 
{
    op <- new_operation(name = "CreateDiskFromSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_disk_from_snapshot_input(diskName = diskName, 
        diskSnapshotName = diskSnapshotName, availabilityZone = availabilityZone, 
        sizeInGb = sizeInGb)
    output <- create_disk_from_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a snapshot of a block storage disk
#'
#' Creates a snapshot of a block storage disk. You can use snapshots for backups, to make copies of disks, and to save data before shutting down a Lightsail instance.
#' 
#' You can take a snapshot of an attached disk that is in use; however, snapshots only capture data that has been written to your disk at the time the snapshot command is issued. This may exclude any data that has been cached by any applications or the operating system. If you can pause any file systems on the disk long enough to take a snapshot, your snapshot should be complete. Nevertheless, if you cannot pause all file writes to the disk, you should unmount the disk from within the Lightsail instance, issue the create disk snapshot command, and then remount the disk to ensure a consistent and complete snapshot. You may remount and use your disk while the snapshot status is pending.
#'
#' @section Accepted Parameters:
#' ```
#' create_disk_snapshot(
#'   diskName = "string",
#'   diskSnapshotName = "string"
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique name of the source disk (e.g., `my-source-disk`).
#' @param diskSnapshotName &#91;required&#93; The name of the destination disk snapshot (e.g., `my-disk-snapshot`) based on the source disk.
#'
#' @export
create_disk_snapshot <- function (diskName, diskSnapshotName) 
{
    op <- new_operation(name = "CreateDiskSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_disk_snapshot_input(diskName = diskName, 
        diskSnapshotName = diskSnapshotName)
    output <- create_disk_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a domain resource for the specified domain (e
#'
#' Creates a domain resource for the specified domain (e.g., example.com).
#'
#' @section Accepted Parameters:
#' ```
#' create_domain(
#'   domainName = "string"
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The domain name to manage (e.g., `example.com`).
#' 
#' You cannot register a new domain name using Lightsail. You must register a domain name using Amazon Route 53 or another domain name registrar. If you have already registered your domain, you can enter its name in this parameter to manage the DNS records for that domain.
#'
#' @export
create_domain <- function (domainName) 
{
    op <- new_operation(name = "CreateDomain", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_domain_input(domainName = domainName)
    output <- create_domain_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates one of the following entry records associated with the domain: A record, CNAME record, TXT record, or MX record
#'
#' Creates one of the following entry records associated with the domain: A record, CNAME record, TXT record, or MX record.
#'
#' @section Accepted Parameters:
#' ```
#' create_domain_entry(
#'   domainName = "string",
#'   domainEntry = list(
#'     id = "string",
#'     name = "string",
#'     target = "string",
#'     isAlias = TRUE|FALSE,
#'     type = "string",
#'     options = list(
#'       "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The domain name (e.g., `example.com`) for which you want to create the domain entry.
#' @param domainEntry &#91;required&#93; An array of key-value pairs containing information about the domain entry request.
#'
#' @export
create_domain_entry <- function (domainName, domainEntry) 
{
    op <- new_operation(name = "CreateDomainEntry", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_domain_entry_input(domainName = domainName, 
        domainEntry = domainEntry)
    output <- create_domain_entry_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a snapshot of a specific virtual private server, or _instance_
#'
#' Creates a snapshot of a specific virtual private server, or *instance*. You can use a snapshot to create a new instance that is based on that snapshot.
#'
#' @section Accepted Parameters:
#' ```
#' create_instance_snapshot(
#'   instanceSnapshotName = "string",
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceSnapshotName &#91;required&#93; The name for your new snapshot.
#' @param instanceName &#91;required&#93; The Lightsail instance on which to base your snapshot.
#'
#' @export
create_instance_snapshot <- function (instanceSnapshotName, instanceName) 
{
    op <- new_operation(name = "CreateInstanceSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_instance_snapshot_input(instanceSnapshotName = instanceSnapshotName, 
        instanceName = instanceName)
    output <- create_instance_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates one or more Amazon Lightsail virtual private servers, or _instances_
#'
#' Creates one or more Amazon Lightsail virtual private servers, or *instances*. Create instances using active blueprints. Inactive blueprints are listed to support customers with existing instances but are not necessarily available for launch of new instances. Blueprints are marked inactive when they become outdated due to operating system updates or new application releases. Use the get blueprints operation to return a list of available blueprints.
#'
#' @section Accepted Parameters:
#' ```
#' create_instances(
#'   instanceNames = list(
#'     "string"
#'   ),
#'   availabilityZone = "string",
#'   customImageName = "string",
#'   blueprintId = "string",
#'   bundleId = "string",
#'   userData = "string",
#'   keyPairName = "string"
#' )
#' ```
#'
#' @param instanceNames &#91;required&#93; The names to use for your new Lightsail instances. Separate multiple values using quotation marks and commas, for example: `["MyFirstInstance","MySecondInstance"]`
#' @param availabilityZone &#91;required&#93; The Availability Zone in which to create your instance. Use the following format: `us-east-2a` (case sensitive). You can get a list of Availability Zones by using the [get regions](http://docs.aws.amazon.com/lightsail/2016-11-28/api-reference/API_GetRegions.html) operation. Be sure to add the `include Availability Zones` parameter to your request.
#' @param customImageName (Deprecated) The name for your custom image.
#' 
#' In releases prior to June 12, 2017, this parameter was ignored by the API. It is now deprecated.
#' @param blueprintId &#91;required&#93; The ID for a virtual private server image (e.g., `app_wordpress_4_4` or `app_lamp_7_0`). Use the get blueprints operation to return a list of available images (or *blueprints*).
#' @param bundleId &#91;required&#93; The bundle of specification information for your virtual private server (or *instance*), including the pricing plan (e.g., `micro_1_0`).
#' @param userData A launch script you can create that configures a server with additional user data. For example, you might want to run `apt-get -y update`.
#' 
#' Depending on the machine image you choose, the command to get software on your instance varies. Amazon Linux and CentOS use `yum`, Debian and Ubuntu use `apt-get`, and FreeBSD uses `pkg`. For a complete list, see the [Dev Guide](https://lightsail.aws.amazon.com/ls/docs/getting-started/article/compare-options-choose-lightsail-instance-image).
#' @param keyPairName The name of your key pair.
#'
#' @export
create_instances <- function (instanceNames, availabilityZone, 
    customImageName = NULL, blueprintId, bundleId, userData = NULL, 
    keyPairName = NULL) 
{
    op <- new_operation(name = "CreateInstances", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_instances_input(instanceNames = instanceNames, 
        availabilityZone = availabilityZone, customImageName = customImageName, 
        blueprintId = blueprintId, bundleId = bundleId, userData = userData, 
        keyPairName = keyPairName)
    output <- create_instances_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Uses a specific snapshot as a blueprint for creating one or more new instances that are based on that identical configuration
#'
#' Uses a specific snapshot as a blueprint for creating one or more new instances that are based on that identical configuration.
#'
#' @section Accepted Parameters:
#' ```
#' create_instances_from_snapshot(
#'   instanceNames = list(
#'     "string"
#'   ),
#'   attachedDiskMapping = list(
#'     list(
#'       list(
#'         originalDiskPath = "string",
#'         newDiskName = "string"
#'       )
#'     )
#'   ),
#'   availabilityZone = "string",
#'   instanceSnapshotName = "string",
#'   bundleId = "string",
#'   userData = "string",
#'   keyPairName = "string"
#' )
#' ```
#'
#' @param instanceNames &#91;required&#93; The names for your new instances.
#' @param attachedDiskMapping An object containing information about one or more disk mappings.
#' @param availabilityZone &#91;required&#93; The Availability Zone where you want to create your instances. Use the following formatting: `us-east-2a` (case sensitive). You can get a list of Availability Zones by using the [get regions](http://docs.aws.amazon.com/lightsail/2016-11-28/api-reference/API_GetRegions.html) operation. Be sure to add the `include Availability Zones` parameter to your request.
#' @param instanceSnapshotName &#91;required&#93; The name of the instance snapshot on which you are basing your new instances. Use the get instance snapshots operation to return information about your existing snapshots.
#' @param bundleId &#91;required&#93; The bundle of specification information for your virtual private server (or *instance*), including the pricing plan (e.g., `micro_1_0`).
#' @param userData You can create a launch script that configures a server with additional user data. For example, `apt-get -y update`.
#' 
#' Depending on the machine image you choose, the command to get software on your instance varies. Amazon Linux and CentOS use `yum`, Debian and Ubuntu use `apt-get`, and FreeBSD uses `pkg`. For a complete list, see the [Dev Guide](https://lightsail.aws.amazon.com/ls/docs/getting-started/article/compare-options-choose-lightsail-instance-image).
#' @param keyPairName The name for your key pair.
#'
#' @export
create_instances_from_snapshot <- function (instanceNames, attachedDiskMapping = NULL, 
    availabilityZone, instanceSnapshotName, bundleId, userData = NULL, 
    keyPairName = NULL) 
{
    op <- new_operation(name = "CreateInstancesFromSnapshot", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_instances_from_snapshot_input(instanceNames = instanceNames, 
        attachedDiskMapping = attachedDiskMapping, availabilityZone = availabilityZone, 
        instanceSnapshotName = instanceSnapshotName, bundleId = bundleId, 
        userData = userData, keyPairName = keyPairName)
    output <- create_instances_from_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an SSH key pair
#'
#' Creates an SSH key pair.
#'
#' @section Accepted Parameters:
#' ```
#' create_key_pair(
#'   keyPairName = "string"
#' )
#' ```
#'
#' @param keyPairName &#91;required&#93; The name for your new key pair.
#'
#' @export
create_key_pair <- function (keyPairName) 
{
    op <- new_operation(name = "CreateKeyPair", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_key_pair_input(keyPairName = keyPairName)
    output <- create_key_pair_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a Lightsail load balancer
#'
#' Creates a Lightsail load balancer. To learn more about deciding whether to load balance your application, see [Configure your Lightsail instances for load balancing](https://lightsail.aws.amazon.com/ls/docs/how-to/article/configure-lightsail-instances-for-load-balancing). You can create up to 5 load balancers per AWS Region in your account.
#' 
#' When you create a load balancer, you can specify a unique name and port settings. To change additional load balancer settings, use the `UpdateLoadBalancerAttribute` operation.
#'
#' @section Accepted Parameters:
#' ```
#' create_load_balancer(
#'   loadBalancerName = "string",
#'   instancePort = 123,
#'   healthCheckPath = "string",
#'   certificateName = "string",
#'   certificateDomainName = "string",
#'   certificateAlternativeNames = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of your load balancer.
#' @param instancePort &#91;required&#93; The instance port where you\'re creating your load balancer.
#' @param healthCheckPath The path you provided to perform the load balancer health check. If you didn\'t specify a health check path, Lightsail uses the root path of your website (e.g., `"/"`).
#' 
#' You may want to specify a custom health check path other than the root of your application if your home page loads slowly or has a lot of media or scripting on it.
#' @param certificateName The name of the SSL/TLS certificate.
#' 
#' If you specify `certificateName`, then `certificateDomainName` is required (and vice-versa).
#' @param certificateDomainName The domain name with which your certificate is associated (e.g., `example.com`).
#' 
#' If you specify `certificateDomainName`, then `certificateName` is required (and vice-versa).
#' @param certificateAlternativeNames The optional alternative domains and subdomains to use with your SSL/TLS certificate (e.g., `www.example.com`, `example.com`, `m.example.com`, `blog.example.com`).
#'
#' @export
create_load_balancer <- function (loadBalancerName, instancePort, 
    healthCheckPath = NULL, certificateName = NULL, certificateDomainName = NULL, 
    certificateAlternativeNames = NULL) 
{
    op <- new_operation(name = "CreateLoadBalancer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_load_balancer_input(loadBalancerName = loadBalancerName, 
        instancePort = instancePort, healthCheckPath = healthCheckPath, 
        certificateName = certificateName, certificateDomainName = certificateDomainName, 
        certificateAlternativeNames = certificateAlternativeNames)
    output <- create_load_balancer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a Lightsail load balancer TLS certificate
#'
#' Creates a Lightsail load balancer TLS certificate.
#' 
#' TLS is just an updated, more secure version of Secure Socket Layer (SSL).
#'
#' @section Accepted Parameters:
#' ```
#' create_load_balancer_tls_certificate(
#'   loadBalancerName = "string",
#'   certificateName = "string",
#'   certificateDomainName = "string",
#'   certificateAlternativeNames = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The load balancer name where you want to create the SSL/TLS certificate.
#' @param certificateName &#91;required&#93; The SSL/TLS certificate name.
#' 
#' You can have up to 10 certificates in your account at one time. Each Lightsail load balancer can have up to 2 certificates associated with it at one time. There is also an overall limit to the number of certificates that can be issue in a 365-day period. For more information, see [Limits](http://docs.aws.amazon.com/acm/latest/userguide/acm-limits.html).
#' @param certificateDomainName &#91;required&#93; The domain name (e.g., `example.com`) for your SSL/TLS certificate.
#' @param certificateAlternativeNames An array of strings listing alternative domains and subdomains for your SSL/TLS certificate. Lightsail will de-dupe the names for you. You can have a maximum of 9 alternative names (in addition to the 1 primary domain). We do not support wildcards (e.g., `*.example.com`).
#'
#' @export
create_load_balancer_tls_certificate <- function (loadBalancerName, 
    certificateName, certificateDomainName, certificateAlternativeNames = NULL) 
{
    op <- new_operation(name = "CreateLoadBalancerTlsCertificate", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_load_balancer_tls_certificate_input(loadBalancerName = loadBalancerName, 
        certificateName = certificateName, certificateDomainName = certificateDomainName, 
        certificateAlternativeNames = certificateAlternativeNames)
    output <- create_load_balancer_tls_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new database in Amazon Lightsail
#'
#' Creates a new database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' create_relational_database(
#'   relationalDatabaseName = "string",
#'   availabilityZone = "string",
#'   relationalDatabaseBlueprintId = "string",
#'   relationalDatabaseBundleId = "string",
#'   masterDatabaseName = "string",
#'   masterUsername = "string",
#'   masterUserPassword = "string",
#'   preferredBackupWindow = "string",
#'   preferredMaintenanceWindow = "string",
#'   publiclyAccessible = TRUE|FALSE
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name to use for your new database.
#' 
#' Constraints:
#' 
#' -   Must contain from 2 to 255 alphanumeric characters, or hyphens.
#' 
#' -   The first and last character must be a letter or number.
#' @param availabilityZone The Availability Zone in which to create your new database. Use the `us-east-2a` case-sensitive format.
#' 
#' You can get a list of Availability Zones by using the `get regions` operation. Be sure to add the `include relational database Availability Zones` parameter to your request.
#' @param relationalDatabaseBlueprintId &#91;required&#93; The blueprint ID for your new database. A blueprint describes the major engine version of a database.
#' 
#' You can get a list of database blueprints IDs by using the `get relational database blueprints` operation.
#' @param relationalDatabaseBundleId &#91;required&#93; The bundle ID for your new database. A bundle describes the performance specifications for your database.
#' 
#' You can get a list of database bundle IDs by using the `get relational database bundles` operation.
#' @param masterDatabaseName &#91;required&#93; The name of the master database created when the Lightsail database resource is created.
#' 
#' Constraints:
#' 
#' -   Must contain from 1 to 64 alphanumeric characters.
#' 
#' -   Cannot be a word reserved by the specified database engine
#' @param masterUsername &#91;required&#93; The master user name for your new database.
#' 
#' Constraints:
#' 
#' -   Master user name is required.
#' 
#' -   Must contain from 1 to 16 alphanumeric characters.
#' 
#' -   The first character must be a letter.
#' 
#' -   Cannot be a reserved word for the database engine you choose.
#' 
#'     For more information about reserved words in MySQL 5.6 or 5.7, see the Keywords and Reserved Words articles for [MySQL 5.6](https://dev.mysql.com/doc/refman/5.6/en/keywords.html) or [MySQL 5.7](https://dev.mysql.com/doc/refman/5.7/en/keywords.html) respectively.
#' @param masterUserPassword The password for the master user of your new database. The password can include any printable ASCII character except \"/\", \"\"\", or \"@\".
#' 
#' Constraints: Must contain 8 to 41 characters.
#' @param preferredBackupWindow The daily time range during which automated backups are created for your new database if automated backups are enabled.
#' 
#' The default is a 30-minute window selected at random from an 8-hour block of time for each AWS Region. For more information about the preferred backup window time blocks for each region, see the [Working With Backups](https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/USER_WorkingWithAutomatedBackups.html#USER_WorkingWithAutomatedBackups.BackupWindow) guide in the Amazon Relational Database Service (Amazon RDS) documentation.
#' 
#' Constraints:
#' 
#' -   Must be in the `hh24:mi-hh24:mi` format.
#' 
#'     Example: `16:00-16:30`
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Must not conflict with the preferred maintenance window.
#' 
#' -   Must be at least 30 minutes.
#' @param preferredMaintenanceWindow The weekly time range during which system maintenance can occur on your new database.
#' 
#' The default is a 30-minute window selected at random from an 8-hour block of time for each AWS Region, occurring on a random day of the week.
#' 
#' Constraints:
#' 
#' -   Must be in the `ddd:hh24:mi-ddd:hh24:mi` format.
#' 
#' -   Valid days: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
#' 
#' -   Must be at least 30 minutes.
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Example: `Tue:17:00-Tue:17:30`
#' @param publiclyAccessible Specifies the accessibility options for your new database. A value of `true` specifies a database that is available to resources outside of your Lightsail account. A value of `false` specifies a database that is available only to your Lightsail resources in the same region as your database.
#'
#' @export
create_relational_database <- function (relationalDatabaseName, 
    availabilityZone = NULL, relationalDatabaseBlueprintId, relationalDatabaseBundleId, 
    masterDatabaseName, masterUsername, masterUserPassword = NULL, 
    preferredBackupWindow = NULL, preferredMaintenanceWindow = NULL, 
    publiclyAccessible = NULL) 
{
    op <- new_operation(name = "CreateRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_relational_database_input(relationalDatabaseName = relationalDatabaseName, 
        availabilityZone = availabilityZone, relationalDatabaseBlueprintId = relationalDatabaseBlueprintId, 
        relationalDatabaseBundleId = relationalDatabaseBundleId, 
        masterDatabaseName = masterDatabaseName, masterUsername = masterUsername, 
        masterUserPassword = masterUserPassword, preferredBackupWindow = preferredBackupWindow, 
        preferredMaintenanceWindow = preferredMaintenanceWindow, 
        publiclyAccessible = publiclyAccessible)
    output <- create_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new database from an existing database snapshot in Amazon Lightsail
#'
#' Creates a new database from an existing database snapshot in Amazon Lightsail.
#' 
#' You can create a new database from a snapshot in if something goes wrong with your original database, or to change it to a different plan, such as a high availability or standard plan.
#'
#' @section Accepted Parameters:
#' ```
#' create_relational_database_from_snapshot(
#'   relationalDatabaseName = "string",
#'   availabilityZone = "string",
#'   publiclyAccessible = TRUE|FALSE,
#'   relationalDatabaseSnapshotName = "string",
#'   relationalDatabaseBundleId = "string",
#'   sourceRelationalDatabaseName = "string",
#'   restoreTime = as.POSIXct("2015-01-01"),
#'   useLatestRestorableTime = TRUE|FALSE
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name to use for your new database.
#' 
#' Constraints:
#' 
#' -   Must contain from 2 to 255 alphanumeric characters, or hyphens.
#' 
#' -   The first and last character must be a letter or number.
#' @param availabilityZone The Availability Zone in which to create your new database. Use the `us-east-2a` case-sensitive format.
#' 
#' You can get a list of Availability Zones by using the `get regions` operation. Be sure to add the `include relational database Availability Zones` parameter to your request.
#' @param publiclyAccessible Specifies the accessibility options for your new database. A value of `true` specifies a database that is available to resources outside of your Lightsail account. A value of `false` specifies a database that is available only to your Lightsail resources in the same region as your database.
#' @param relationalDatabaseSnapshotName The name of the database snapshot from which to create your new database.
#' @param relationalDatabaseBundleId The bundle ID for your new database. A bundle describes the performance specifications for your database.
#' 
#' You can get a list of database bundle IDs by using the `get relational database bundles` operation.
#' 
#' When creating a new database from a snapshot, you cannot choose a bundle that is smaller than the bundle of the source database.
#' @param sourceRelationalDatabaseName The name of the source database.
#' @param restoreTime The date and time to restore your database from.
#' 
#' Constraints:
#' 
#' -   Must be before the latest restorable time for the database.
#' 
#' -   Cannot be specified if the `use latest restorable time` parameter is `true`.
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Specified in the Unix time format.
#' 
#'     For example, if you wish to use a restore time of October 1, 2018, at 8 PM UTC, then you input `1538424000` as the restore time.
#' @param useLatestRestorableTime Specifies whether your database is restored from the latest backup time. A value of `true` restores from the latest backup time.
#' 
#' Default: `false`
#' 
#' Constraints: Cannot be specified if the `restore time` parameter is provided.
#'
#' @export
create_relational_database_from_snapshot <- function (relationalDatabaseName, 
    availabilityZone = NULL, publiclyAccessible = NULL, relationalDatabaseSnapshotName = NULL, 
    relationalDatabaseBundleId = NULL, sourceRelationalDatabaseName = NULL, 
    restoreTime = NULL, useLatestRestorableTime = NULL) 
{
    op <- new_operation(name = "CreateRelationalDatabaseFromSnapshot", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_relational_database_from_snapshot_input(relationalDatabaseName = relationalDatabaseName, 
        availabilityZone = availabilityZone, publiclyAccessible = publiclyAccessible, 
        relationalDatabaseSnapshotName = relationalDatabaseSnapshotName, 
        relationalDatabaseBundleId = relationalDatabaseBundleId, 
        sourceRelationalDatabaseName = sourceRelationalDatabaseName, 
        restoreTime = restoreTime, useLatestRestorableTime = useLatestRestorableTime)
    output <- create_relational_database_from_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a snapshot of your database in Amazon Lightsail
#'
#' Creates a snapshot of your database in Amazon Lightsail. You can use snapshots for backups, to make copies of a database, and to save data before deleting a database.
#'
#' @section Accepted Parameters:
#' ```
#' create_relational_database_snapshot(
#'   relationalDatabaseName = "string",
#'   relationalDatabaseSnapshotName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of the database on which to base your new snapshot.
#' @param relationalDatabaseSnapshotName &#91;required&#93; The name for your new database snapshot.
#' 
#' Constraints:
#' 
#' -   Must contain from 2 to 255 alphanumeric characters, or hyphens.
#' 
#' -   The first and last character must be a letter or number.
#'
#' @export
create_relational_database_snapshot <- function (relationalDatabaseName, 
    relationalDatabaseSnapshotName) 
{
    op <- new_operation(name = "CreateRelationalDatabaseSnapshot", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_relational_database_snapshot_input(relationalDatabaseName = relationalDatabaseName, 
        relationalDatabaseSnapshotName = relationalDatabaseSnapshotName)
    output <- create_relational_database_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified block storage disk
#'
#' Deletes the specified block storage disk. The disk must be in the `available` state (not attached to a Lightsail instance).
#' 
#' The disk may remain in the `deleting` state for several minutes.
#'
#' @section Accepted Parameters:
#' ```
#' delete_disk(
#'   diskName = "string"
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique name of the disk you want to delete (e.g., `my-disk`).
#'
#' @export
delete_disk <- function (diskName) 
{
    op <- new_operation(name = "DeleteDisk", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_disk_input(diskName = diskName)
    output <- delete_disk_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified disk snapshot
#'
#' Deletes the specified disk snapshot.
#' 
#' When you make periodic snapshots of a disk, the snapshots are incremental, and only the blocks on the device that have changed since your last snapshot are saved in the new snapshot. When you delete a snapshot, only the data not needed for any other snapshot is removed. So regardless of which prior snapshots have been deleted, all active snapshots will have access to all the information needed to restore the disk.
#'
#' @section Accepted Parameters:
#' ```
#' delete_disk_snapshot(
#'   diskSnapshotName = "string"
#' )
#' ```
#'
#' @param diskSnapshotName &#91;required&#93; The name of the disk snapshot you want to delete (e.g., `my-disk-snapshot`).
#'
#' @export
delete_disk_snapshot <- function (diskSnapshotName) 
{
    op <- new_operation(name = "DeleteDiskSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_disk_snapshot_input(diskSnapshotName = diskSnapshotName)
    output <- delete_disk_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified domain recordset and all of its domain records
#'
#' Deletes the specified domain recordset and all of its domain records.
#'
#' @section Accepted Parameters:
#' ```
#' delete_domain(
#'   domainName = "string"
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The specific domain name to delete.
#'
#' @export
delete_domain <- function (domainName) 
{
    op <- new_operation(name = "DeleteDomain", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_domain_input(domainName = domainName)
    output <- delete_domain_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a specific domain entry
#'
#' Deletes a specific domain entry.
#'
#' @section Accepted Parameters:
#' ```
#' delete_domain_entry(
#'   domainName = "string",
#'   domainEntry = list(
#'     id = "string",
#'     name = "string",
#'     target = "string",
#'     isAlias = TRUE|FALSE,
#'     type = "string",
#'     options = list(
#'       "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The name of the domain entry to delete.
#' @param domainEntry &#91;required&#93; An array of key-value pairs containing information about your domain entries.
#'
#' @export
delete_domain_entry <- function (domainName, domainEntry) 
{
    op <- new_operation(name = "DeleteDomainEntry", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_domain_entry_input(domainName = domainName, 
        domainEntry = domainEntry)
    output <- delete_domain_entry_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a specific Amazon Lightsail virtual private server, or _instance_
#'
#' Deletes a specific Amazon Lightsail virtual private server, or *instance*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_instance(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance to delete.
#'
#' @export
delete_instance <- function (instanceName) 
{
    op <- new_operation(name = "DeleteInstance", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_instance_input(instanceName = instanceName)
    output <- delete_instance_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a specific snapshot of a virtual private server (or _instance_)
#'
#' Deletes a specific snapshot of a virtual private server (or *instance*).
#'
#' @section Accepted Parameters:
#' ```
#' delete_instance_snapshot(
#'   instanceSnapshotName = "string"
#' )
#' ```
#'
#' @param instanceSnapshotName &#91;required&#93; The name of the snapshot to delete.
#'
#' @export
delete_instance_snapshot <- function (instanceSnapshotName) 
{
    op <- new_operation(name = "DeleteInstanceSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_instance_snapshot_input(instanceSnapshotName = instanceSnapshotName)
    output <- delete_instance_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a specific SSH key pair
#'
#' Deletes a specific SSH key pair.
#'
#' @section Accepted Parameters:
#' ```
#' delete_key_pair(
#'   keyPairName = "string"
#' )
#' ```
#'
#' @param keyPairName &#91;required&#93; The name of the key pair to delete.
#'
#' @export
delete_key_pair <- function (keyPairName) 
{
    op <- new_operation(name = "DeleteKeyPair", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_key_pair_input(keyPairName = keyPairName)
    output <- delete_key_pair_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a Lightsail load balancer and all its associated SSL/TLS certificates
#'
#' Deletes a Lightsail load balancer and all its associated SSL/TLS certificates. Once the load balancer is deleted, you will need to create a new load balancer, create a new certificate, and verify domain ownership again.
#'
#' @section Accepted Parameters:
#' ```
#' delete_load_balancer(
#'   loadBalancerName = "string"
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer you want to delete.
#'
#' @export
delete_load_balancer <- function (loadBalancerName) 
{
    op <- new_operation(name = "DeleteLoadBalancer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_load_balancer_input(loadBalancerName = loadBalancerName)
    output <- delete_load_balancer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes an SSL/TLS certificate associated with a Lightsail load balancer
#'
#' Deletes an SSL/TLS certificate associated with a Lightsail load balancer.
#'
#' @section Accepted Parameters:
#' ```
#' delete_load_balancer_tls_certificate(
#'   loadBalancerName = "string",
#'   certificateName = "string",
#'   force = TRUE|FALSE
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The load balancer name.
#' @param certificateName &#91;required&#93; The SSL/TLS certificate name.
#' @param force When `true`, forces the deletion of an SSL/TLS certificate.
#' 
#' There can be two certificates associated with a Lightsail load balancer: the primary and the backup. The `force` parameter is required when the primary SSL/TLS certificate is in use by an instance attached to the load balancer.
#'
#' @export
delete_load_balancer_tls_certificate <- function (loadBalancerName, 
    certificateName, force = NULL) 
{
    op <- new_operation(name = "DeleteLoadBalancerTlsCertificate", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_load_balancer_tls_certificate_input(loadBalancerName = loadBalancerName, 
        certificateName = certificateName, force = force)
    output <- delete_load_balancer_tls_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a database in Amazon Lightsail
#'
#' Deletes a database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' delete_relational_database(
#'   relationalDatabaseName = "string",
#'   skipFinalSnapshot = TRUE|FALSE,
#'   finalRelationalDatabaseSnapshotName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of the database that you are deleting.
#' @param skipFinalSnapshot Determines whether a final database snapshot is created before your database is deleted. If `true` is specified, no database snapshot is created. If `false` is specified, a database snapshot is created before your database is deleted.
#' 
#' You must specify the `final relational database snapshot name` parameter if the `skip final snapshot` parameter is `false`.
#' 
#' Default: `false`
#' @param finalRelationalDatabaseSnapshotName The name of the database snapshot created if `skip final snapshot` is `false`, which is the default value for that parameter.
#' 
#' Specifying this parameter and also specifying the `skip final snapshot` parameter to `true` results in an error.
#' 
#' Constraints:
#' 
#' -   Must contain from 2 to 255 alphanumeric characters, or hyphens.
#' 
#' -   The first and last character must be a letter or number.
#'
#' @export
delete_relational_database <- function (relationalDatabaseName, 
    skipFinalSnapshot = NULL, finalRelationalDatabaseSnapshotName = NULL) 
{
    op <- new_operation(name = "DeleteRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_relational_database_input(relationalDatabaseName = relationalDatabaseName, 
        skipFinalSnapshot = skipFinalSnapshot, finalRelationalDatabaseSnapshotName = finalRelationalDatabaseSnapshotName)
    output <- delete_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a database snapshot in Amazon Lightsail
#'
#' Deletes a database snapshot in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' delete_relational_database_snapshot(
#'   relationalDatabaseSnapshotName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseSnapshotName &#91;required&#93; The name of the database snapshot that you are deleting.
#'
#' @export
delete_relational_database_snapshot <- function (relationalDatabaseSnapshotName) 
{
    op <- new_operation(name = "DeleteRelationalDatabaseSnapshot", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_relational_database_snapshot_input(relationalDatabaseSnapshotName = relationalDatabaseSnapshotName)
    output <- delete_relational_database_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Detaches a stopped block storage disk from a Lightsail instance
#'
#' Detaches a stopped block storage disk from a Lightsail instance. Make sure to unmount any file systems on the device within your operating system before stopping the instance and detaching the disk.
#'
#' @section Accepted Parameters:
#' ```
#' detach_disk(
#'   diskName = "string"
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The unique name of the disk you want to detach from your instance (e.g., `my-disk`).
#'
#' @export
detach_disk <- function (diskName) 
{
    op <- new_operation(name = "DetachDisk", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_disk_input(diskName = diskName)
    output <- detach_disk_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Detaches the specified instances from a Lightsail load balancer
#'
#' Detaches the specified instances from a Lightsail load balancer.
#' 
#' This operation waits until the instances are no longer needed before they are detached from the load balancer.
#'
#' @section Accepted Parameters:
#' ```
#' detach_instances_from_load_balancer(
#'   loadBalancerName = "string",
#'   instanceNames = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the Lightsail load balancer.
#' @param instanceNames &#91;required&#93; An array of strings containing the names of the instances you want to detach from the load balancer.
#'
#' @export
detach_instances_from_load_balancer <- function (loadBalancerName, 
    instanceNames) 
{
    op <- new_operation(name = "DetachInstancesFromLoadBalancer", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- detach_instances_from_load_balancer_input(loadBalancerName = loadBalancerName, 
        instanceNames = instanceNames)
    output <- detach_instances_from_load_balancer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Detaches a static IP from the Amazon Lightsail instance to which it is attached
#'
#' Detaches a static IP from the Amazon Lightsail instance to which it is attached.
#'
#' @section Accepted Parameters:
#' ```
#' detach_static_ip(
#'   staticIpName = "string"
#' )
#' ```
#'
#' @param staticIpName &#91;required&#93; The name of the static IP to detach from the instance.
#'
#' @export
detach_static_ip <- function (staticIpName) 
{
    op <- new_operation(name = "DetachStaticIp", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_static_ip_input(staticIpName = staticIpName)
    output <- detach_static_ip_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Downloads the default SSH key pair from the user's account
#'
#' Downloads the default SSH key pair from the user\'s account.
#'
#' @section Accepted Parameters:
#' ```
#' download_default_key_pair()
#' ```
#'
#' @export
download_default_key_pair <- function () 
{
    op <- new_operation(name = "DownloadDefaultKeyPair", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- download_default_key_pair_input()
    output <- download_default_key_pair_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the names of all active (not deleted) resources
#'
#' Returns the names of all active (not deleted) resources.
#'
#' @section Accepted Parameters:
#' ```
#' get_active_names(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for paginating results from your get active names request.
#'
#' @export
get_active_names <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetActiveNames", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_active_names_input(pageToken = pageToken)
    output <- get_active_names_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the list of available instance images, or _blueprints_
#'
#' Returns the list of available instance images, or *blueprints*. You can use a blueprint to create a new virtual private server already running a specific operating system, as well as a preinstalled app or development stack. The software each instance is running depends on the blueprint image you choose.
#'
#' @section Accepted Parameters:
#' ```
#' get_blueprints(
#'   includeInactive = TRUE|FALSE,
#'   pageToken = "string"
#' )
#' ```
#'
#' @param includeInactive A Boolean value indicating whether to include inactive results in your request.
#' @param pageToken A token used for advancing to the next page of results from your get blueprints request.
#'
#' @export
get_blueprints <- function (includeInactive = NULL, pageToken = NULL) 
{
    op <- new_operation(name = "GetBlueprints", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_blueprints_input(includeInactive = includeInactive, 
        pageToken = pageToken)
    output <- get_blueprints_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the list of bundles that are available for purchase
#'
#' Returns the list of bundles that are available for purchase. A bundle describes the specs for your virtual private server (or *instance*).
#'
#' @section Accepted Parameters:
#' ```
#' get_bundles(
#'   includeInactive = TRUE|FALSE,
#'   pageToken = "string"
#' )
#' ```
#'
#' @param includeInactive A Boolean value that indicates whether to include inactive bundle results in your request.
#' @param pageToken A token used for advancing to the next page of results from your get bundles request.
#'
#' @export
get_bundles <- function (includeInactive = NULL, pageToken = NULL) 
{
    op <- new_operation(name = "GetBundles", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_bundles_input(includeInactive = includeInactive, 
        pageToken = pageToken)
    output <- get_bundles_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific block storage disk
#'
#' Returns information about a specific block storage disk.
#'
#' @section Accepted Parameters:
#' ```
#' get_disk(
#'   diskName = "string"
#' )
#' ```
#'
#' @param diskName &#91;required&#93; The name of the disk (e.g., `my-disk`).
#'
#' @export
get_disk <- function (diskName) 
{
    op <- new_operation(name = "GetDisk", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_disk_input(diskName = diskName)
    output <- get_disk_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific block storage disk snapshot
#'
#' Returns information about a specific block storage disk snapshot.
#'
#' @section Accepted Parameters:
#' ```
#' get_disk_snapshot(
#'   diskSnapshotName = "string"
#' )
#' ```
#'
#' @param diskSnapshotName &#91;required&#93; The name of the disk snapshot (e.g., `my-disk-snapshot`).
#'
#' @export
get_disk_snapshot <- function (diskSnapshotName) 
{
    op <- new_operation(name = "GetDiskSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_disk_snapshot_input(diskSnapshotName = diskSnapshotName)
    output <- get_disk_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all block storage disk snapshots in your AWS account and region
#'
#' Returns information about all block storage disk snapshots in your AWS account and region.
#' 
#' If you are describing a long list of disk snapshots, you can paginate the output to make the list more manageable. You can use the pageToken and nextPageToken values to retrieve the next items in the list.
#'
#' @section Accepted Parameters:
#' ```
#' get_disk_snapshots(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your GetDiskSnapshots request.
#'
#' @export
get_disk_snapshots <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetDiskSnapshots", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_disk_snapshots_input(pageToken = pageToken)
    output <- get_disk_snapshots_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all block storage disks in your AWS account and region
#'
#' Returns information about all block storage disks in your AWS account and region.
#' 
#' If you are describing a long list of disks, you can paginate the output to make the list more manageable. You can use the pageToken and nextPageToken values to retrieve the next items in the list.
#'
#' @section Accepted Parameters:
#' ```
#' get_disks(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your GetDisks request.
#'
#' @export
get_disks <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetDisks", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_disks_input(pageToken = pageToken)
    output <- get_disks_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific domain recordset
#'
#' Returns information about a specific domain recordset.
#'
#' @section Accepted Parameters:
#' ```
#' get_domain(
#'   domainName = "string"
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The domain name for which your want to return information about.
#'
#' @export
get_domain <- function (domainName) 
{
    op <- new_operation(name = "GetDomain", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_domain_input(domainName = domainName)
    output <- get_domain_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of all domains in the user's account
#'
#' Returns a list of all domains in the user\'s account.
#'
#' @section Accepted Parameters:
#' ```
#' get_domains(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get domains request.
#'
#' @export
get_domains <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetDomains", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_domains_input(pageToken = pageToken)
    output <- get_domains_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific Amazon Lightsail instance, which is a virtual private server
#'
#' Returns information about a specific Amazon Lightsail instance, which is a virtual private server.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance.
#'
#' @export
get_instance <- function (instanceName) 
{
    op <- new_operation(name = "GetInstance", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_input(instanceName = instanceName)
    output <- get_instance_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns temporary SSH keys you can use to connect to a specific virtual private server, or _instance_
#'
#' Returns temporary SSH keys you can use to connect to a specific virtual private server, or *instance*.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_access_details(
#'   instanceName = "string",
#'   protocol = "ssh"|"rdp"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance to access.
#' @param protocol The protocol to use to connect to your instance. Defaults to `ssh`.
#'
#' @export
get_instance_access_details <- function (instanceName, protocol = NULL) 
{
    op <- new_operation(name = "GetInstanceAccessDetails", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_access_details_input(instanceName = instanceName, 
        protocol = protocol)
    output <- get_instance_access_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the data points for the specified Amazon Lightsail instance metric, given an instance name
#'
#' Returns the data points for the specified Amazon Lightsail instance metric, given an instance name.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_metric_data(
#'   instanceName = "string",
#'   metricName = "CPUUtilization"|"NetworkIn"|"NetworkOut"|"StatusCheckFailed"|"StatusCheckFailed_Instance"|"StatusCheckFailed_System",
#'   period = 123,
#'   startTime = as.POSIXct("2015-01-01"),
#'   endTime = as.POSIXct("2015-01-01"),
#'   unit = "Seconds"|"Microseconds"|"Milliseconds"|"Bytes"|"Kilobytes"|"Megabytes"|"Gigabytes"|"Terabytes"|"Bits"|"Kilobits"|"Megabits"|"Gigabits"|"Terabits"|"Percent"|"Count"|"Bytes/Second"|"Kilobytes/Second"|"Megabytes/Second"|"Gigabytes/Second"|"Terabytes/Second"|"Bits/Second"|"Kilobits/Second"|"Megabits/Second"|"Gigabits/Second"|"Terabits/Second"|"Count/Second"|"None",
#'   statistics = list(
#'     "Minimum"|"Maximum"|"Sum"|"Average"|"SampleCount"
#'   )
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance for which you want to get metrics data.
#' @param metricName &#91;required&#93; The metric name to get data about.
#' @param period &#91;required&#93; The granularity, in seconds, of the returned data points.
#' @param startTime &#91;required&#93; The start time of the time period.
#' @param endTime &#91;required&#93; The end time of the time period.
#' @param unit &#91;required&#93; The unit. The list of valid values is below.
#' @param statistics &#91;required&#93; The instance statistics.
#'
#' @export
get_instance_metric_data <- function (instanceName, metricName, 
    period, startTime, endTime, unit, statistics) 
{
    op <- new_operation(name = "GetInstanceMetricData", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_metric_data_input(instanceName = instanceName, 
        metricName = metricName, period = period, startTime = startTime, 
        endTime = endTime, unit = unit, statistics = statistics)
    output <- get_instance_metric_data_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the port states for a specific virtual private server, or _instance_
#'
#' Returns the port states for a specific virtual private server, or *instance*.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_port_states(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance.
#'
#' @export
get_instance_port_states <- function (instanceName) 
{
    op <- new_operation(name = "GetInstancePortStates", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_port_states_input(instanceName = instanceName)
    output <- get_instance_port_states_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific instance snapshot
#'
#' Returns information about a specific instance snapshot.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_snapshot(
#'   instanceSnapshotName = "string"
#' )
#' ```
#'
#' @param instanceSnapshotName &#91;required&#93; The name of the snapshot for which you are requesting information.
#'
#' @export
get_instance_snapshot <- function (instanceSnapshotName) 
{
    op <- new_operation(name = "GetInstanceSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_snapshot_input(instanceSnapshotName = instanceSnapshotName)
    output <- get_instance_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns all instance snapshots for the user's account
#'
#' Returns all instance snapshots for the user\'s account.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_snapshots(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get instance snapshots request.
#'
#' @export
get_instance_snapshots <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetInstanceSnapshots", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_snapshots_input(pageToken = pageToken)
    output <- get_instance_snapshots_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the state of a specific instance
#'
#' Returns the state of a specific instance. Works on one instance at a time.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_state(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance to get state information about.
#'
#' @export
get_instance_state <- function (instanceName) 
{
    op <- new_operation(name = "GetInstanceState", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_state_input(instanceName = instanceName)
    output <- get_instance_state_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all Amazon Lightsail virtual private servers, or _instances_
#'
#' Returns information about all Amazon Lightsail virtual private servers, or *instances*.
#'
#' @section Accepted Parameters:
#' ```
#' get_instances(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get instances request.
#'
#' @export
get_instances <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetInstances", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instances_input(pageToken = pageToken)
    output <- get_instances_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific key pair
#'
#' Returns information about a specific key pair.
#'
#' @section Accepted Parameters:
#' ```
#' get_key_pair(
#'   keyPairName = "string"
#' )
#' ```
#'
#' @param keyPairName &#91;required&#93; The name of the key pair for which you are requesting information.
#'
#' @export
get_key_pair <- function (keyPairName) 
{
    op <- new_operation(name = "GetKeyPair", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_key_pair_input(keyPairName = keyPairName)
    output <- get_key_pair_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all key pairs in the user's account
#'
#' Returns information about all key pairs in the user\'s account.
#'
#' @section Accepted Parameters:
#' ```
#' get_key_pairs(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get key pairs request.
#'
#' @export
get_key_pairs <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetKeyPairs", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_key_pairs_input(pageToken = pageToken)
    output <- get_key_pairs_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the specified Lightsail load balancer
#'
#' Returns information about the specified Lightsail load balancer.
#'
#' @section Accepted Parameters:
#' ```
#' get_load_balancer(
#'   loadBalancerName = "string"
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer.
#'
#' @export
get_load_balancer <- function (loadBalancerName) 
{
    op <- new_operation(name = "GetLoadBalancer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_load_balancer_input(loadBalancerName = loadBalancerName)
    output <- get_load_balancer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about health metrics for your Lightsail load balancer
#'
#' Returns information about health metrics for your Lightsail load balancer.
#'
#' @section Accepted Parameters:
#' ```
#' get_load_balancer_metric_data(
#'   loadBalancerName = "string",
#'   metricName = "ClientTLSNegotiationErrorCount"|"HealthyHostCount"|"UnhealthyHostCount"|"HTTPCode_LB_4XX_Count"|"HTTPCode_LB_5XX_Count"|"HTTPCode_Instance_2XX_Count"|"HTTPCode_Instance_3XX_Count"|"HTTPCode_Instance_4XX_Count"|"HTTPCode_Instance_5XX_Count"|"InstanceResponseTime"|"RejectedConnectionCount"|"RequestCount",
#'   period = 123,
#'   startTime = as.POSIXct("2015-01-01"),
#'   endTime = as.POSIXct("2015-01-01"),
#'   unit = "Seconds"|"Microseconds"|"Milliseconds"|"Bytes"|"Kilobytes"|"Megabytes"|"Gigabytes"|"Terabytes"|"Bits"|"Kilobits"|"Megabits"|"Gigabits"|"Terabits"|"Percent"|"Count"|"Bytes/Second"|"Kilobytes/Second"|"Megabytes/Second"|"Gigabytes/Second"|"Terabytes/Second"|"Bits/Second"|"Kilobits/Second"|"Megabits/Second"|"Gigabits/Second"|"Terabits/Second"|"Count/Second"|"None",
#'   statistics = list(
#'     "Minimum"|"Maximum"|"Sum"|"Average"|"SampleCount"
#'   )
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer.
#' @param metricName &#91;required&#93; The metric about which you want to return information. Valid values are listed below, along with the most useful `statistics` to include in your request.
#' 
#' -   **`ClientTLSNegotiationErrorCount`** - The number of TLS connections initiated by the client that did not establish a session with the load balancer. Possible causes include a mismatch of ciphers or protocols.
#' 
#'     `Statistics`: The most useful statistic is `Sum`.
#' 
#' -   **`HealthyHostCount`** - The number of target instances that are considered healthy.
#' 
#'     `Statistics`: The most useful statistic are `Average`, `Minimum`, and `Maximum`.
#' 
#' -   **`UnhealthyHostCount`** - The number of target instances that are considered unhealthy.
#' 
#'     `Statistics`: The most useful statistic are `Average`, `Minimum`, and `Maximum`.
#' 
#' -   **`HTTPCode_LB_4XX_Count`** - The number of HTTP 4XX client error codes that originate from the load balancer. Client errors are generated when requests are malformed or incomplete. These requests have not been received by the target instance. This count does not include any response codes generated by the target instances.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`HTTPCode_LB_5XX_Count`** - The number of HTTP 5XX server error codes that originate from the load balancer. This count does not include any response codes generated by the target instances.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`HTTPCode_Instance_2XX_Count`** - The number of HTTP response codes generated by the target instances. This does not include any response codes generated by the load balancer.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`HTTPCode_Instance_3XX_Count`** - The number of HTTP response codes generated by the target instances. This does not include any response codes generated by the load balancer.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`HTTPCode_Instance_4XX_Count`** - The number of HTTP response codes generated by the target instances. This does not include any response codes generated by the load balancer.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`HTTPCode_Instance_5XX_Count`** - The number of HTTP response codes generated by the target instances. This does not include any response codes generated by the load balancer.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' 
#' -   **`InstanceResponseTime`** - The time elapsed, in seconds, after the request leaves the load balancer until a response from the target instance is received.
#' 
#'     `Statistics`: The most useful statistic is `Average`.
#' 
#' -   **`RejectedConnectionCount`** - The number of connections that were rejected because the load balancer had reached its maximum number of connections.
#' 
#'     `Statistics`: The most useful statistic is `Sum`.
#' 
#' -   **`RequestCount`** - The number of requests processed over IPv4. This count includes only the requests with a response generated by a target instance of the load balancer.
#' 
#'     `Statistics`: The most useful statistic is `Sum`. Note that `Minimum`, `Maximum`, and `Average` all return `1`.
#' @param period &#91;required&#93; The granularity, in seconds, of the returned data points.
#' @param startTime &#91;required&#93; The start time of the period.
#' @param endTime &#91;required&#93; The end time of the period.
#' @param unit &#91;required&#93; The unit for the time period request. Valid values are listed below.
#' @param statistics &#91;required&#93; An array of statistics that you want to request metrics for. Valid values are listed below.
#' 
#' -   **`SampleCount`** - The count (number) of data points used for the statistical calculation.
#' 
#' -   **`Average`** - The value of Sum / SampleCount during the specified period. By comparing this statistic with the Minimum and Maximum, you can determine the full scope of a metric and how close the average use is to the Minimum and Maximum. This comparison helps you to know when to increase or decrease your resources as needed.
#' 
#' -   **`Sum`** - All values submitted for the matching metric added together. This statistic can be useful for determining the total volume of a metric.
#' 
#' -   **`Minimum`** - The lowest value observed during the specified period. You can use this value to determine low volumes of activity for your application.
#' 
#' -   **`Maximum`** - The highest value observed during the specified period. You can use this value to determine high volumes of activity for your application.
#'
#' @export
get_load_balancer_metric_data <- function (loadBalancerName, 
    metricName, period, startTime, endTime, unit, statistics) 
{
    op <- new_operation(name = "GetLoadBalancerMetricData", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_load_balancer_metric_data_input(loadBalancerName = loadBalancerName, 
        metricName = metricName, period = period, startTime = startTime, 
        endTime = endTime, unit = unit, statistics = statistics)
    output <- get_load_balancer_metric_data_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the TLS certificates that are associated with the specified Lightsail load balancer
#'
#' Returns information about the TLS certificates that are associated with the specified Lightsail load balancer.
#' 
#' TLS is just an updated, more secure version of Secure Socket Layer (SSL).
#' 
#' You can have a maximum of 2 certificates associated with a Lightsail load balancer. One is active and the other is inactive.
#'
#' @section Accepted Parameters:
#' ```
#' get_load_balancer_tls_certificates(
#'   loadBalancerName = "string"
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer you associated with your SSL/TLS certificate.
#'
#' @export
get_load_balancer_tls_certificates <- function (loadBalancerName) 
{
    op <- new_operation(name = "GetLoadBalancerTlsCertificates", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_load_balancer_tls_certificates_input(loadBalancerName = loadBalancerName)
    output <- get_load_balancer_tls_certificates_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all load balancers in an account
#'
#' Returns information about all load balancers in an account.
#' 
#' If you are describing a long list of load balancers, you can paginate the output to make the list more manageable. You can use the pageToken and nextPageToken values to retrieve the next items in the list.
#'
#' @section Accepted Parameters:
#' ```
#' get_load_balancers(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for paginating the results from your GetLoadBalancers request.
#'
#' @export
get_load_balancers <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetLoadBalancers", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_load_balancers_input(pageToken = pageToken)
    output <- get_load_balancers_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific operation
#'
#' Returns information about a specific operation. Operations include events such as when you create an instance, allocate a static IP, attach a static IP, and so on.
#'
#' @section Accepted Parameters:
#' ```
#' get_operation(
#'   operationId = "string"
#' )
#' ```
#'
#' @param operationId &#91;required&#93; A GUID used to identify the operation.
#'
#' @export
get_operation <- function (operationId) 
{
    op <- new_operation(name = "GetOperation", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_operation_input(operationId = operationId)
    output <- get_operation_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all operations
#'
#' Returns information about all operations.
#' 
#' Results are returned from oldest to newest, up to a maximum of 200. Results can be paged by making each subsequent call to `GetOperations` use the maximum (last) `statusChangedAt` value from the previous request.
#'
#' @section Accepted Parameters:
#' ```
#' get_operations(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get operations request.
#'
#' @export
get_operations <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetOperations", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_operations_input(pageToken = pageToken)
    output <- get_operations_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets operations for a specific resource (e
#'
#' Gets operations for a specific resource (e.g., an instance or a static IP).
#'
#' @section Accepted Parameters:
#' ```
#' get_operations_for_resource(
#'   resourceName = "string",
#'   pageToken = "string"
#' )
#' ```
#'
#' @param resourceName &#91;required&#93; The name of the resource for which you are requesting information.
#' @param pageToken A token used for advancing to the next page of results from your get operations for resource request.
#'
#' @export
get_operations_for_resource <- function (resourceName, pageToken = NULL) 
{
    op <- new_operation(name = "GetOperationsForResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_operations_for_resource_input(resourceName = resourceName, 
        pageToken = pageToken)
    output <- get_operations_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of all valid regions for Amazon Lightsail
#'
#' Returns a list of all valid regions for Amazon Lightsail. Use the `include availability zones` parameter to also return the Availability Zones in a region.
#'
#' @section Accepted Parameters:
#' ```
#' get_regions(
#'   includeAvailabilityZones = TRUE|FALSE,
#'   includeRelationalDatabaseAvailabilityZones = TRUE|FALSE
#' )
#' ```
#'
#' @param includeAvailabilityZones A Boolean value indicating whether to also include Availability Zones in your get regions request. Availability Zones are indicated with a letter: e.g., `us-east-2a`.
#' @param includeRelationalDatabaseAvailabilityZones \>A Boolean value indicating whether to also include Availability Zones for databases in your get regions request. Availability Zones are indicated with a letter (e.g., `us-east-2a`).
#'
#' @export
get_regions <- function (includeAvailabilityZones = NULL, includeRelationalDatabaseAvailabilityZones = NULL) 
{
    op <- new_operation(name = "GetRegions", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_regions_input(includeAvailabilityZones = includeAvailabilityZones, 
        includeRelationalDatabaseAvailabilityZones = includeRelationalDatabaseAvailabilityZones)
    output <- get_regions_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific database in Amazon Lightsail
#'
#' Returns information about a specific database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database(
#'   relationalDatabaseName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of the database that you are looking up.
#'
#' @export
get_relational_database <- function (relationalDatabaseName) 
{
    op <- new_operation(name = "GetRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_relational_database_input(relationalDatabaseName = relationalDatabaseName)
    output <- get_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of available database blueprints in Amazon Lightsail
#'
#' Returns a list of available database blueprints in Amazon Lightsail. A blueprint describes the major engine version of a database.
#' 
#' You can use a blueprint ID to create a new database that runs a specific database engine.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_blueprints(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to a specific page of results for your get relational database blueprints request.
#'
#' @export
get_relational_database_blueprints <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseBlueprints", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_blueprints_input(pageToken = pageToken)
    output <- get_relational_database_blueprints_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the list of bundles that are available in Amazon Lightsail
#'
#' Returns the list of bundles that are available in Amazon Lightsail. A bundle describes the performance specifications for a database.
#' 
#' You can use a bundle ID to create a new database with explicit performance specifications.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_bundles(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to a specific page of results for your get relational database bundles request.
#'
#' @export
get_relational_database_bundles <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseBundles", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_bundles_input(pageToken = pageToken)
    output <- get_relational_database_bundles_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of events for a specific database in Amazon Lightsail
#'
#' Returns a list of events for a specific database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_events(
#'   relationalDatabaseName = "string",
#'   durationInMinutes = 123,
#'   pageToken = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of the database from which to get events.
#' @param durationInMinutes The number of minutes in the past from which to retrieve events. For example, to get all events from the past 2 hours, enter 120.
#' 
#' Default: `60`
#' 
#' The minimum is 1 and the maximum is 14 days (20160 minutes).
#' @param pageToken A token used for advancing to a specific page of results from for get relational database events request.
#'
#' @export
get_relational_database_events <- function (relationalDatabaseName, 
    durationInMinutes = NULL, pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseEvents", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_events_input(relationalDatabaseName = relationalDatabaseName, 
        durationInMinutes = durationInMinutes, pageToken = pageToken)
    output <- get_relational_database_events_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of log events for a database in Amazon Lightsail
#'
#' Returns a list of log events for a database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_log_events(
#'   relationalDatabaseName = "string",
#'   logStreamName = "string",
#'   startTime = as.POSIXct("2015-01-01"),
#'   endTime = as.POSIXct("2015-01-01"),
#'   startFromHead = TRUE|FALSE,
#'   pageToken = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database for which to get log events.
#' @param logStreamName &#91;required&#93; The name of the log stream.
#' 
#' Use the `get relational database log streams` operation to get a list of available log streams.
#' @param startTime The start of the time interval from which to get log events.
#' 
#' Constraints:
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Specified in the Unix time format.
#' 
#'     For example, if you wish to use a start time of October 1, 2018, at 8 PM UTC, then you input `1538424000` as the start time.
#' @param endTime The end of the time interval from which to get log events.
#' 
#' Constraints:
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Specified in the Unix time format.
#' 
#'     For example, if you wish to use an end time of October 1, 2018, at 8 PM UTC, then you input `1538424000` as the end time.
#' @param startFromHead Parameter to specify if the log should start from head or tail. If `true` is specified, the log event starts from the head of the log. If `false` is specified, the log event starts from the tail of the log.
#' 
#' Default: `false`
#' @param pageToken A token used for advancing to a specific page of results for your get relational database log events request.
#'
#' @export
get_relational_database_log_events <- function (relationalDatabaseName, 
    logStreamName, startTime = NULL, endTime = NULL, startFromHead = NULL, 
    pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseLogEvents", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_log_events_input(relationalDatabaseName = relationalDatabaseName, 
        logStreamName = logStreamName, startTime = startTime, 
        endTime = endTime, startFromHead = startFromHead, pageToken = pageToken)
    output <- get_relational_database_log_events_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of available log streams for a specific database in Amazon Lightsail
#'
#' Returns a list of available log streams for a specific database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_log_streams(
#'   relationalDatabaseName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database for which to get log streams.
#'
#' @export
get_relational_database_log_streams <- function (relationalDatabaseName) 
{
    op <- new_operation(name = "GetRelationalDatabaseLogStreams", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_log_streams_input(relationalDatabaseName = relationalDatabaseName)
    output <- get_relational_database_log_streams_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the current, previous, or pending versions of the master user password for a Lightsail database
#'
#' Returns the current, previous, or pending versions of the master user password for a Lightsail database.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_master_user_password(
#'   relationalDatabaseName = "string",
#'   passwordVersion = "CURRENT"|"PREVIOUS"|"PENDING"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database for which to get the master user password.
#' @param passwordVersion The password version to return.
#' 
#' Specifying `CURRENT` or `PREVIOUS` returns the current or previous passwords respectively. Specifying `PENDING` returns the newest version of the password that will rotate to `CURRENT`. After the `PENDING` password rotates to `CURRENT`, the `PENDING` password is no longer available.
#' 
#' Default: `CURRENT`
#'
#' @export
get_relational_database_master_user_password <- function (relationalDatabaseName, 
    passwordVersion = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseMasterUserPassword", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_master_user_password_input(relationalDatabaseName = relationalDatabaseName, 
        passwordVersion = passwordVersion)
    output <- get_relational_database_master_user_password_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the data points of the specified metric for a database in Amazon Lightsail
#'
#' Returns the data points of the specified metric for a database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_metric_data(
#'   relationalDatabaseName = "string",
#'   metricName = "CPUUtilization"|"DatabaseConnections"|"DiskQueueDepth"|"FreeStorageSpace"|"NetworkReceiveThroughput"|"NetworkTransmitThroughput",
#'   period = 123,
#'   startTime = as.POSIXct("2015-01-01"),
#'   endTime = as.POSIXct("2015-01-01"),
#'   unit = "Seconds"|"Microseconds"|"Milliseconds"|"Bytes"|"Kilobytes"|"Megabytes"|"Gigabytes"|"Terabytes"|"Bits"|"Kilobits"|"Megabits"|"Gigabits"|"Terabits"|"Percent"|"Count"|"Bytes/Second"|"Kilobytes/Second"|"Megabytes/Second"|"Gigabytes/Second"|"Terabytes/Second"|"Bits/Second"|"Kilobits/Second"|"Megabits/Second"|"Gigabits/Second"|"Terabits/Second"|"Count/Second"|"None",
#'   statistics = list(
#'     "Minimum"|"Maximum"|"Sum"|"Average"|"SampleCount"
#'   )
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database from which to get metric data.
#' @param metricName &#91;required&#93; The name of the metric data to return.
#' @param period &#91;required&#93; The granularity, in seconds, of the returned data points.
#' @param startTime &#91;required&#93; The start of the time interval from which to get metric data.
#' 
#' Constraints:
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Specified in the Unix time format.
#' 
#'     For example, if you wish to use a start time of October 1, 2018, at 8 PM UTC, then you input `1538424000` as the start time.
#' @param endTime &#91;required&#93; The end of the time interval from which to get metric data.
#' 
#' Constraints:
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Specified in the Unix time format.
#' 
#'     For example, if you wish to use an end time of October 1, 2018, at 8 PM UTC, then you input `1538424000` as the end time.
#' @param unit &#91;required&#93; The unit for the metric data request.
#' @param statistics &#91;required&#93; The array of statistics for your metric data request.
#'
#' @export
get_relational_database_metric_data <- function (relationalDatabaseName, 
    metricName, period, startTime, endTime, unit, statistics) 
{
    op <- new_operation(name = "GetRelationalDatabaseMetricData", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_metric_data_input(relationalDatabaseName = relationalDatabaseName, 
        metricName = metricName, period = period, startTime = startTime, 
        endTime = endTime, unit = unit, statistics = statistics)
    output <- get_relational_database_metric_data_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns all of the runtime parameters offered by the underlying database software, or engine, for a specific database in Amazon Lightsail
#'
#' Returns all of the runtime parameters offered by the underlying database software, or engine, for a specific database in Amazon Lightsail.
#' 
#' In addition to the parameter names and values, this operation returns other information about each parameter. This information includes whether changes require a reboot, whether the parameter is modifiable, the allowed values, and the data types.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_parameters(
#'   relationalDatabaseName = "string",
#'   pageToken = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database for which to get parameters.
#' @param pageToken A token used for advancing to a specific page of results for your get relational database parameters request.
#'
#' @export
get_relational_database_parameters <- function (relationalDatabaseName, 
    pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseParameters", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_parameters_input(relationalDatabaseName = relationalDatabaseName, 
        pageToken = pageToken)
    output <- get_relational_database_parameters_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific database snapshot in Amazon Lightsail
#'
#' Returns information about a specific database snapshot in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_snapshot(
#'   relationalDatabaseSnapshotName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseSnapshotName &#91;required&#93; The name of the database snapshot for which to get information.
#'
#' @export
get_relational_database_snapshot <- function (relationalDatabaseSnapshotName) 
{
    op <- new_operation(name = "GetRelationalDatabaseSnapshot", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_snapshot_input(relationalDatabaseSnapshotName = relationalDatabaseSnapshotName)
    output <- get_relational_database_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all of your database snapshots in Amazon Lightsail
#'
#' Returns information about all of your database snapshots in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_database_snapshots(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to a specific page of results for your get relational database snapshots request.
#'
#' @export
get_relational_database_snapshots <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabaseSnapshots", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_relational_database_snapshots_input(pageToken = pageToken)
    output <- get_relational_database_snapshots_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all of your databases in Amazon Lightsail
#'
#' Returns information about all of your databases in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' get_relational_databases(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to a specific page of results for your get relational database request.
#'
#' @export
get_relational_databases <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetRelationalDatabases", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_relational_databases_input(pageToken = pageToken)
    output <- get_relational_databases_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a specific static IP
#'
#' Returns information about a specific static IP.
#'
#' @section Accepted Parameters:
#' ```
#' get_static_ip(
#'   staticIpName = "string"
#' )
#' ```
#'
#' @param staticIpName &#91;required&#93; The name of the static IP in Lightsail.
#'
#' @export
get_static_ip <- function (staticIpName) 
{
    op <- new_operation(name = "GetStaticIp", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_static_ip_input(staticIpName = staticIpName)
    output <- get_static_ip_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about all static IPs in the user's account
#'
#' Returns information about all static IPs in the user\'s account.
#'
#' @section Accepted Parameters:
#' ```
#' get_static_ips(
#'   pageToken = "string"
#' )
#' ```
#'
#' @param pageToken A token used for advancing to the next page of results from your get static IPs request.
#'
#' @export
get_static_ips <- function (pageToken = NULL) 
{
    op <- new_operation(name = "GetStaticIps", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_static_ips_input(pageToken = pageToken)
    output <- get_static_ips_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Imports a public SSH key from a specific key pair
#'
#' Imports a public SSH key from a specific key pair.
#'
#' @section Accepted Parameters:
#' ```
#' import_key_pair(
#'   keyPairName = "string",
#'   publicKeyBase64 = "string"
#' )
#' ```
#'
#' @param keyPairName &#91;required&#93; The name of the key pair for which you want to import the public key.
#' @param publicKeyBase64 &#91;required&#93; A base64-encoded public key of the `ssh-rsa` type.
#'
#' @export
import_key_pair <- function (keyPairName, publicKeyBase64) 
{
    op <- new_operation(name = "ImportKeyPair", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- import_key_pair_input(keyPairName = keyPairName, 
        publicKeyBase64 = publicKeyBase64)
    output <- import_key_pair_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a Boolean value indicating whether your Lightsail VPC is peered
#'
#' Returns a Boolean value indicating whether your Lightsail VPC is peered.
#'
#' @section Accepted Parameters:
#' ```
#' is_vpc_peered()
#' ```
#'
#' @export
is_vpc_peered <- function () 
{
    op <- new_operation(name = "IsVpcPeered", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- is_vpc_peered_input()
    output <- is_vpc_peered_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds public ports to an Amazon Lightsail instance
#'
#' Adds public ports to an Amazon Lightsail instance.
#'
#' @section Accepted Parameters:
#' ```
#' open_instance_public_ports(
#'   portInfo = list(
#'     fromPort = 123,
#'     toPort = 123,
#'     protocol = "tcp"|"all"|"udp"
#'   ),
#'   instanceName = "string"
#' )
#' ```
#'
#' @param portInfo &#91;required&#93; An array of key-value pairs containing information about the port mappings.
#' @param instanceName &#91;required&#93; The name of the instance for which you want to open the public ports.
#'
#' @export
open_instance_public_ports <- function (portInfo, instanceName) 
{
    op <- new_operation(name = "OpenInstancePublicPorts", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- open_instance_public_ports_input(portInfo = portInfo, 
        instanceName = instanceName)
    output <- open_instance_public_ports_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Tries to peer the Lightsail VPC with the user's default VPC
#'
#' Tries to peer the Lightsail VPC with the user\'s default VPC.
#'
#' @section Accepted Parameters:
#' ```
#' peer_vpc()
#' ```
#'
#' @export
peer_vpc <- function () 
{
    op <- new_operation(name = "PeerVpc", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- peer_vpc_input()
    output <- peer_vpc_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the specified open ports for an Amazon Lightsail instance, and closes all ports for every protocol not included in the current request
#'
#' Sets the specified open ports for an Amazon Lightsail instance, and closes all ports for every protocol not included in the current request.
#'
#' @section Accepted Parameters:
#' ```
#' put_instance_public_ports(
#'   portInfos = list(
#'     list(
#'       fromPort = 123,
#'       toPort = 123,
#'       protocol = "tcp"|"all"|"udp"
#'     )
#'   ),
#'   instanceName = "string"
#' )
#' ```
#'
#' @param portInfos &#91;required&#93; Specifies information about the public port(s).
#' @param instanceName &#91;required&#93; The Lightsail instance name of the public port(s) you are setting.
#'
#' @export
put_instance_public_ports <- function (portInfos, instanceName) 
{
    op <- new_operation(name = "PutInstancePublicPorts", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_instance_public_ports_input(portInfos = portInfos, 
        instanceName = instanceName)
    output <- put_instance_public_ports_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Restarts a specific instance
#'
#' Restarts a specific instance. When your Amazon Lightsail instance is finished rebooting, Lightsail assigns a new public IP address. To use the same IP address after restarting, create a static IP address and attach it to the instance.
#'
#' @section Accepted Parameters:
#' ```
#' reboot_instance(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance to reboot.
#'
#' @export
reboot_instance <- function (instanceName) 
{
    op <- new_operation(name = "RebootInstance", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- reboot_instance_input(instanceName = instanceName)
    output <- reboot_instance_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Restarts a specific database in Amazon Lightsail
#'
#' Restarts a specific database in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' reboot_relational_database(
#'   relationalDatabaseName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database to reboot.
#'
#' @export
reboot_relational_database <- function (relationalDatabaseName) 
{
    op <- new_operation(name = "RebootRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- reboot_relational_database_input(relationalDatabaseName = relationalDatabaseName)
    output <- reboot_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a specific static IP from your account
#'
#' Deletes a specific static IP from your account.
#'
#' @section Accepted Parameters:
#' ```
#' release_static_ip(
#'   staticIpName = "string"
#' )
#' ```
#'
#' @param staticIpName &#91;required&#93; The name of the static IP to delete.
#'
#' @export
release_static_ip <- function (staticIpName) 
{
    op <- new_operation(name = "ReleaseStaticIp", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- release_static_ip_input(staticIpName = staticIpName)
    output <- release_static_ip_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Starts a specific Amazon Lightsail instance from a stopped state
#'
#' Starts a specific Amazon Lightsail instance from a stopped state. To restart an instance, use the reboot instance operation.
#'
#' @section Accepted Parameters:
#' ```
#' start_instance(
#'   instanceName = "string"
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance (a virtual private server) to start.
#'
#' @export
start_instance <- function (instanceName) 
{
    op <- new_operation(name = "StartInstance", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- start_instance_input(instanceName = instanceName)
    output <- start_instance_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Starts a specific database from a stopped state in Amazon Lightsail
#'
#' Starts a specific database from a stopped state in Amazon Lightsail. To restart a database, use the `reboot relational database` operation.
#'
#' @section Accepted Parameters:
#' ```
#' start_relational_database(
#'   relationalDatabaseName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database to start.
#'
#' @export
start_relational_database <- function (relationalDatabaseName) 
{
    op <- new_operation(name = "StartRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- start_relational_database_input(relationalDatabaseName = relationalDatabaseName)
    output <- start_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Stops a specific Amazon Lightsail instance that is currently running
#'
#' Stops a specific Amazon Lightsail instance that is currently running.
#'
#' @section Accepted Parameters:
#' ```
#' stop_instance(
#'   instanceName = "string",
#'   force = TRUE|FALSE
#' )
#' ```
#'
#' @param instanceName &#91;required&#93; The name of the instance (a virtual private server) to stop.
#' @param force When set to `True`, forces a Lightsail instance that is stuck in a `stopping` state to stop.
#' 
#' Only use the `force` parameter if your instance is stuck in the `stopping` state. In any other state, your instance should stop normally without adding this parameter to your API request.
#'
#' @export
stop_instance <- function (instanceName, force = NULL) 
{
    op <- new_operation(name = "StopInstance", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- stop_instance_input(instanceName = instanceName, 
        force = force)
    output <- stop_instance_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Stops a specific database that is currently running in Amazon Lightsail
#'
#' Stops a specific database that is currently running in Amazon Lightsail.
#'
#' @section Accepted Parameters:
#' ```
#' stop_relational_database(
#'   relationalDatabaseName = "string",
#'   relationalDatabaseSnapshotName = "string"
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database to stop.
#' @param relationalDatabaseSnapshotName The name of your new database snapshot to be created before stopping your database.
#'
#' @export
stop_relational_database <- function (relationalDatabaseName, 
    relationalDatabaseSnapshotName = NULL) 
{
    op <- new_operation(name = "StopRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- stop_relational_database_input(relationalDatabaseName = relationalDatabaseName, 
        relationalDatabaseSnapshotName = relationalDatabaseSnapshotName)
    output <- stop_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attempts to unpeer the Lightsail VPC from the user's default VPC
#'
#' Attempts to unpeer the Lightsail VPC from the user\'s default VPC.
#'
#' @section Accepted Parameters:
#' ```
#' unpeer_vpc()
#' ```
#'
#' @export
unpeer_vpc <- function () 
{
    op <- new_operation(name = "UnpeerVpc", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- unpeer_vpc_input()
    output <- unpeer_vpc_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a domain recordset after it is created
#'
#' Updates a domain recordset after it is created.
#'
#' @section Accepted Parameters:
#' ```
#' update_domain_entry(
#'   domainName = "string",
#'   domainEntry = list(
#'     id = "string",
#'     name = "string",
#'     target = "string",
#'     isAlias = TRUE|FALSE,
#'     type = "string",
#'     options = list(
#'       "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param domainName &#91;required&#93; The name of the domain recordset to update.
#' @param domainEntry &#91;required&#93; An array of key-value pairs containing information about the domain entry.
#'
#' @export
update_domain_entry <- function (domainName, domainEntry) 
{
    op <- new_operation(name = "UpdateDomainEntry", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_domain_entry_input(domainName = domainName, 
        domainEntry = domainEntry)
    output <- update_domain_entry_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the specified attribute for a load balancer
#'
#' Updates the specified attribute for a load balancer. You can only update one attribute at a time.
#'
#' @section Accepted Parameters:
#' ```
#' update_load_balancer_attribute(
#'   loadBalancerName = "string",
#'   attributeName = "HealthCheckPath"|"SessionStickinessEnabled"|"SessionStickiness_LB_CookieDurationSeconds",
#'   attributeValue = "string"
#' )
#' ```
#'
#' @param loadBalancerName &#91;required&#93; The name of the load balancer that you want to modify (e.g., `my-load-balancer`.
#' @param attributeName &#91;required&#93; The name of the attribute you want to update. Valid values are below.
#' @param attributeValue &#91;required&#93; The value that you want to specify for the attribute name.
#'
#' @export
update_load_balancer_attribute <- function (loadBalancerName, 
    attributeName, attributeValue) 
{
    op <- new_operation(name = "UpdateLoadBalancerAttribute", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_load_balancer_attribute_input(loadBalancerName = loadBalancerName, 
        attributeName = attributeName, attributeValue = attributeValue)
    output <- update_load_balancer_attribute_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Allows the update of one or more attributes of a database in Amazon Lightsail
#'
#' Allows the update of one or more attributes of a database in Amazon Lightsail.
#' 
#' Updates are applied immediately, or in cases where the updates could result in an outage, are applied during the database\'s predefined maintenance window.
#'
#' @section Accepted Parameters:
#' ```
#' update_relational_database(
#'   relationalDatabaseName = "string",
#'   masterUserPassword = "string",
#'   rotateMasterUserPassword = TRUE|FALSE,
#'   preferredBackupWindow = "string",
#'   preferredMaintenanceWindow = "string",
#'   enableBackupRetention = TRUE|FALSE,
#'   disableBackupRetention = TRUE|FALSE,
#'   publiclyAccessible = TRUE|FALSE,
#'   applyImmediately = TRUE|FALSE
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database to update.
#' @param masterUserPassword The password for the master user of your database. The password can include any printable ASCII character except \"/\", \"\"\", or \"@\".
#' 
#' Constraints: Must contain 8 to 41 characters.
#' @param rotateMasterUserPassword When `true`, the master user password is changed to a new strong password generated by Lightsail.
#' 
#' Use the `get relational database master user password` operation to get the new password.
#' @param preferredBackupWindow The daily time range during which automated backups are created for your database if automated backups are enabled.
#' 
#' Constraints:
#' 
#' -   Must be in the `hh24:mi-hh24:mi` format.
#' 
#'     Example: `16:00-16:30`
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Must not conflict with the preferred maintenance window.
#' 
#' -   Must be at least 30 minutes.
#' @param preferredMaintenanceWindow The weekly time range during which system maintenance can occur on your database.
#' 
#' The default is a 30-minute window selected at random from an 8-hour block of time for each AWS Region, occurring on a random day of the week.
#' 
#' Constraints:
#' 
#' -   Must be in the `ddd:hh24:mi-ddd:hh24:mi` format.
#' 
#' -   Valid days: Mon, Tue, Wed, Thu, Fri, Sat, Sun.
#' 
#' -   Must be at least 30 minutes.
#' 
#' -   Specified in Universal Coordinated Time (UTC).
#' 
#' -   Example: `Tue:17:00-Tue:17:30`
#' @param enableBackupRetention When `true`, enables automated backup retention for your database.
#' 
#' Updates are applied during the next maintenance window because this can result in an outage.
#' @param disableBackupRetention When `true`, disables automated backup retention for your database.
#' 
#' Disabling backup retention deletes all automated database backups. Before disabling this, you may want to create a snapshot of your database using the `create relational database snapshot` operation.
#' 
#' Updates are applied during the next maintenance window because this can result in an outage.
#' @param publiclyAccessible Specifies the accessibility options for your database. A value of `true` specifies a database that is available to resources outside of your Lightsail account. A value of `false` specifies a database that is available only to your Lightsail resources in the same region as your database.
#' @param applyImmediately When `true`, applies changes immediately. When `false`, applies changes during the preferred maintenance window. Some changes may cause an outage.
#' 
#' Default: `false`
#'
#' @export
update_relational_database <- function (relationalDatabaseName, 
    masterUserPassword = NULL, rotateMasterUserPassword = NULL, 
    preferredBackupWindow = NULL, preferredMaintenanceWindow = NULL, 
    enableBackupRetention = NULL, disableBackupRetention = NULL, 
    publiclyAccessible = NULL, applyImmediately = NULL) 
{
    op <- new_operation(name = "UpdateRelationalDatabase", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_relational_database_input(relationalDatabaseName = relationalDatabaseName, 
        masterUserPassword = masterUserPassword, rotateMasterUserPassword = rotateMasterUserPassword, 
        preferredBackupWindow = preferredBackupWindow, preferredMaintenanceWindow = preferredMaintenanceWindow, 
        enableBackupRetention = enableBackupRetention, disableBackupRetention = disableBackupRetention, 
        publiclyAccessible = publiclyAccessible, applyImmediately = applyImmediately)
    output <- update_relational_database_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Allows the update of one or more parameters of a database in Amazon Lightsail
#'
#' Allows the update of one or more parameters of a database in Amazon Lightsail.
#' 
#' Parameter updates don\'t cause outages; therefore, their application is not subject to the preferred maintenance window. However, there are two ways in which paramater updates are applied: `dynamic` or `pending-reboot`. Parameters marked with a `dynamic` apply type are applied immediately. Parameters marked with a `pending-reboot` apply type are applied only after the database is rebooted using the `reboot relational database` operation.
#'
#' @section Accepted Parameters:
#' ```
#' update_relational_database_parameters(
#'   relationalDatabaseName = "string",
#'   parameters = list(
#'     list(
#'       allowedValues = "string",
#'       applyMethod = "string",
#'       applyType = "string",
#'       dataType = "string",
#'       description = "string",
#'       isModifiable = TRUE|FALSE,
#'       parameterName = "string",
#'       parameterValue = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param relationalDatabaseName &#91;required&#93; The name of your database for which to update parameters.
#' @param parameters &#91;required&#93; The database parameters to update.
#'
#' @export
update_relational_database_parameters <- function (relationalDatabaseName, 
    parameters) 
{
    op <- new_operation(name = "UpdateRelationalDatabaseParameters", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_relational_database_parameters_input(relationalDatabaseName = relationalDatabaseName, 
        parameters = parameters)
    output <- update_relational_database_parameters_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
