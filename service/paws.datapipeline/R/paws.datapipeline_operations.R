# This file is generated by make.paws. Please do not edit here.

#' Validates the specified pipeline and starts processing pipeline tasks
#'
#' Validates the specified pipeline and starts processing pipeline tasks. If the pipeline does not pass validation, activation fails.
#' 
#' If you need to pause the pipeline to investigate an issue with a component, such as a data source or script, call DeactivatePipeline.
#' 
#' To activate a finished pipeline, modify the end date for the pipeline and then activate it.
#'
#' @usage
#' activate_pipeline(
#'   pipelineId = "string",
#'   parameterValues = list(
#'     list(
#'       id = "string",
#'       stringValue = "string"
#'     )
#'   ),
#'   startTimestamp = as.POSIXct("2015-01-01")
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param parameterValues A list of parameter values to pass to the pipeline at activation.
#' @param startTimestamp The date and time to resume the pipeline. By default, the pipeline resumes from the last completed execution.
#'
#' @examples
#'
#' @export
activate_pipeline <- function (pipelineId, parameterValues = NULL, 
    startTimestamp = NULL) 
{
    op <- Operation(name = "ActivatePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- activate_pipeline_input(pipelineId = pipelineId, 
        parameterValues = parameterValues, startTimestamp = startTimestamp)
    output <- activate_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or modifies tags for the specified pipeline
#'
#' Adds or modifies tags for the specified pipeline.
#'
#' @usage
#' add_tags(
#'   pipelineId = "string",
#'   tags = list(
#'     list(
#'       key = "string",
#'       value = "string"
#'     )
#'   )
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param tags &#91;required&#93; The tags to add, as key/value pairs.
#'
#' @examples
#'
#' @export
add_tags <- function (pipelineId, tags) 
{
    op <- Operation(name = "AddTags", http_method = "POST", http_path = "/", 
        paginator = list())
    input <- add_tags_input(pipelineId = pipelineId, tags = tags)
    output <- add_tags_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new, empty pipeline
#'
#' Creates a new, empty pipeline. Use PutPipelineDefinition to populate the pipeline.
#'
#' @usage
#' create_pipeline(
#'   name = "string",
#'   uniqueId = "string",
#'   description = "string",
#'   tags = list(
#'     list(
#'       key = "string",
#'       value = "string"
#'     )
#'   )
#' )
#'
#' @param name &#91;required&#93; The name for the pipeline. You can use the same name for multiple pipelines associated with your AWS account, because AWS Data Pipeline assigns each pipeline a unique pipeline identifier.
#' @param uniqueId &#91;required&#93; A unique identifier. This identifier is not the same as the pipeline identifier assigned by AWS Data Pipeline. You are responsible for defining the format and ensuring the uniqueness of this identifier. You use this parameter to ensure idempotency during repeated calls to `CreatePipeline`. For example, if the first call to `CreatePipeline` does not succeed, you can pass in the same unique identifier and pipeline name combination on a subsequent call to `CreatePipeline`. `CreatePipeline` ensures that if a pipeline already exists with the same name and unique identifier, a new pipeline is not created. Instead, you\'ll receive the pipeline identifier from the previous attempt. The uniqueness of the name and unique identifier combination is scoped to the AWS account or IAM user credentials.
#' @param description The description for the pipeline.
#' @param tags A list of tags to associate with the pipeline at creation. Tags let you control access to pipelines. For more information, see [Controlling User Access to Pipelines](http://docs.aws.amazon.com/datapipeline/latest/DeveloperGuide/dp-control-access.html) in the *AWS Data Pipeline Developer Guide*.
#'
#' @examples
#'
#' @export
create_pipeline <- function (name, uniqueId, description = NULL, 
    tags = NULL) 
{
    op <- Operation(name = "CreatePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_pipeline_input(name = name, uniqueId = uniqueId, 
        description = description, tags = tags)
    output <- create_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deactivates the specified running pipeline
#'
#' Deactivates the specified running pipeline. The pipeline is set to the `DEACTIVATING` state until the deactivation process completes.
#' 
#' To resume a deactivated pipeline, use ActivatePipeline. By default, the pipeline resumes from the last completed execution. Optionally, you can specify the date and time to resume the pipeline.
#'
#' @usage
#' deactivate_pipeline(
#'   pipelineId = "string",
#'   cancelActive = TRUE|FALSE
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param cancelActive Indicates whether to cancel any running objects. The default is true, which sets the state of any running objects to `CANCELED`. If this value is false, the pipeline is deactivated after all running objects finish.
#'
#' @examples
#'
#' @export
deactivate_pipeline <- function (pipelineId, cancelActive = NULL) 
{
    op <- Operation(name = "DeactivatePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- deactivate_pipeline_input(pipelineId = pipelineId, 
        cancelActive = cancelActive)
    output <- deactivate_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a pipeline, its pipeline definition, and its run history
#'
#' Deletes a pipeline, its pipeline definition, and its run history. AWS Data Pipeline attempts to cancel instances associated with the pipeline that are currently being processed by task runners.
#' 
#' Deleting a pipeline cannot be undone. You cannot query or restore a deleted pipeline. To temporarily pause a pipeline instead of deleting it, call SetStatus with the status set to `PAUSE` on individual components. Components that are paused by SetStatus can be resumed.
#'
#' @usage
#' delete_pipeline(
#'   pipelineId = "string"
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#'
#' @examples
#'
#' @export
delete_pipeline <- function (pipelineId) 
{
    op <- Operation(name = "DeletePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_pipeline_input(pipelineId = pipelineId)
    output <- delete_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets the object definitions for a set of objects associated with the pipeline
#'
#' Gets the object definitions for a set of objects associated with the pipeline. Object definitions are composed of a set of fields that define the properties of the object.
#'
#' @usage
#' describe_objects(
#'   pipelineId = "string",
#'   objectIds = list(
#'     "string"
#'   ),
#'   evaluateExpressions = TRUE|FALSE,
#'   marker = "string"
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline that contains the object definitions.
#' @param objectIds &#91;required&#93; The IDs of the pipeline objects that contain the definitions to be described. You can pass as many as 25 identifiers in a single call to `DescribeObjects`.
#' @param evaluateExpressions Indicates whether any expressions in the object should be evaluated when the object descriptions are returned.
#' @param marker The starting point for the results to be returned. For the first call, this value should be empty. As long as there are more results, continue to call `DescribeObjects` with the marker value from the previous call to retrieve the next set of results.
#'
#' @examples
#'
#' @export
describe_objects <- function (pipelineId, objectIds, evaluateExpressions = NULL, 
    marker = NULL) 
{
    op <- Operation(name = "DescribeObjects", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_objects_input(pipelineId = pipelineId, 
        objectIds = objectIds, evaluateExpressions = evaluateExpressions, 
        marker = marker)
    output <- describe_objects_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves metadata about one or more pipelines
#'
#' Retrieves metadata about one or more pipelines. The information retrieved includes the name of the pipeline, the pipeline identifier, its current state, and the user account that owns the pipeline. Using account credentials, you can retrieve metadata about pipelines that you or your IAM users have created. If you are using an IAM user account, you can retrieve metadata about only those pipelines for which you have read permissions.
#' 
#' To retrieve the full pipeline definition instead of metadata about the pipeline, call GetPipelineDefinition.
#'
#' @usage
#' describe_pipelines(
#'   pipelineIds = list(
#'     "string"
#'   )
#' )
#'
#' @param pipelineIds &#91;required&#93; The IDs of the pipelines to describe. You can pass as many as 25 identifiers in a single call. To obtain pipeline IDs, call ListPipelines.
#'
#' @examples
#'
#' @export
describe_pipelines <- function (pipelineIds) 
{
    op <- Operation(name = "DescribePipelines", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_pipelines_input(pipelineIds = pipelineIds)
    output <- describe_pipelines_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Task runners call `EvaluateExpression` to evaluate a string in the context of the specified object
#'
#' Task runners call `EvaluateExpression` to evaluate a string in the context of the specified object. For example, a task runner can evaluate SQL queries stored in Amazon S3.
#'
#' @usage
#' evaluate_expression(
#'   pipelineId = "string",
#'   objectId = "string",
#'   expression = "string"
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param objectId &#91;required&#93; The ID of the object.
#' @param expression &#91;required&#93; The expression to evaluate.
#'
#' @examples
#'
#' @export
evaluate_expression <- function (pipelineId, objectId, expression) 
{
    op <- Operation(name = "EvaluateExpression", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- evaluate_expression_input(pipelineId = pipelineId, 
        objectId = objectId, expression = expression)
    output <- evaluate_expression_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets the definition of the specified pipeline
#'
#' Gets the definition of the specified pipeline. You can call `GetPipelineDefinition` to retrieve the pipeline definition that you provided using PutPipelineDefinition.
#'
#' @usage
#' get_pipeline_definition(
#'   pipelineId = "string",
#'   version = "string"
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param version The version of the pipeline definition to retrieve. Set this parameter to `latest` (default) to use the last definition saved to the pipeline or `active` to use the last definition that was activated.
#'
#' @examples
#'
#' @export
get_pipeline_definition <- function (pipelineId, version = NULL) 
{
    op <- Operation(name = "GetPipelineDefinition", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_pipeline_definition_input(pipelineId = pipelineId, 
        version = version)
    output <- get_pipeline_definition_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the pipeline identifiers for all active pipelines that you have permission to access
#'
#' Lists the pipeline identifiers for all active pipelines that you have permission to access.
#'
#' @usage
#' list_pipelines(
#'   marker = "string"
#' )
#'
#' @param marker The starting point for the results to be returned. For the first call, this value should be empty. As long as there are more results, continue to call `ListPipelines` with the marker value from the previous call to retrieve the next set of results.
#'
#' @examples
#'
#' @export
list_pipelines <- function (marker = NULL) 
{
    op <- Operation(name = "ListPipelines", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_pipelines_input(marker = marker)
    output <- list_pipelines_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Task runners call `PollForTask` to receive a task to perform from AWS Data Pipeline
#'
#' Task runners call `PollForTask` to receive a task to perform from AWS Data Pipeline. The task runner specifies which tasks it can perform by setting a value for the `workerGroup` parameter. The task returned can come from any of the pipelines that match the `workerGroup` value passed in by the task runner and that was launched using the IAM user credentials specified by the task runner.
#' 
#' If tasks are ready in the work queue, `PollForTask` returns a response immediately. If no tasks are available in the queue, `PollForTask` uses long-polling and holds on to a poll connection for up to a 90 seconds, during which time the first newly scheduled task is handed to the task runner. To accomodate this, set the socket timeout in your task runner to 90 seconds. The task runner should not call `PollForTask` again on the same `workerGroup` until it receives a response, and this can take up to 90 seconds.
#'
#' @usage
#' poll_for_task(
#'   workerGroup = "string",
#'   hostname = "string",
#'   instanceIdentity = list(
#'     document = "string",
#'     signature = "string"
#'   )
#' )
#'
#' @param workerGroup &#91;required&#93; The type of task the task runner is configured to accept and process. The worker group is set as a field on objects in the pipeline when they are created. You can only specify a single value for `workerGroup` in the call to `PollForTask`. There are no wildcard values permitted in `workerGroup`; the string must be an exact, case-sensitive, match.
#' @param hostname The public DNS name of the calling task runner.
#' @param instanceIdentity Identity information for the EC2 instance that is hosting the task runner. You can get this value from the instance using `http://169.254.169.254/latest/meta-data/instance-id`. For more information, see [Instance Metadata](http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/AESDG-chapter-instancedata.html) in the *Amazon Elastic Compute Cloud User Guide.* Passing in this value proves that your task runner is running on an EC2 instance, and ensures the proper AWS Data Pipeline service charges are applied to your pipeline.
#'
#' @examples
#'
#' @export
poll_for_task <- function (workerGroup, hostname = NULL, instanceIdentity = NULL) 
{
    op <- Operation(name = "PollForTask", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- poll_for_task_input(workerGroup = workerGroup, hostname = hostname, 
        instanceIdentity = instanceIdentity)
    output <- poll_for_task_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds tasks, schedules, and preconditions to the specified pipeline
#'
#' Adds tasks, schedules, and preconditions to the specified pipeline. You can use `PutPipelineDefinition` to populate a new pipeline.
#' 
#' `PutPipelineDefinition` also validates the configuration as it adds it to the pipeline. Changes to the pipeline are saved unless one of the following three validation errors exists in the pipeline.
#' 
#' 1.  An object is missing a name or identifier field.
#' 2.  A string or reference field is empty.
#' 3.  The number of objects in the pipeline exceeds the maximum allowed objects.
#' 4.  The pipeline is in a FINISHED state.
#' 
#' Pipeline object definitions are passed to the `PutPipelineDefinition` action and returned by the GetPipelineDefinition action.
#'
#' @usage
#' put_pipeline_definition(
#'   pipelineId = "string",
#'   pipelineObjects = list(
#'     list(
#'       id = "string",
#'       name = "string",
#'       fields = list(
#'         list(
#'           key = "string",
#'           stringValue = "string",
#'           refValue = "string"
#'         )
#'       )
#'     )
#'   ),
#'   parameterObjects = list(
#'     list(
#'       id = "string",
#'       attributes = list(
#'         list(
#'           key = "string",
#'           stringValue = "string"
#'         )
#'       )
#'     )
#'   ),
#'   parameterValues = list(
#'     list(
#'       id = "string",
#'       stringValue = "string"
#'     )
#'   )
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param pipelineObjects &#91;required&#93; The objects that define the pipeline. These objects overwrite the existing pipeline definition.
#' @param parameterObjects The parameter objects used with the pipeline.
#' @param parameterValues The parameter values used with the pipeline.
#'
#' @examples
#'
#' @export
put_pipeline_definition <- function (pipelineId, pipelineObjects, 
    parameterObjects = NULL, parameterValues = NULL) 
{
    op <- Operation(name = "PutPipelineDefinition", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_pipeline_definition_input(pipelineId = pipelineId, 
        pipelineObjects = pipelineObjects, parameterObjects = parameterObjects, 
        parameterValues = parameterValues)
    output <- put_pipeline_definition_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Queries the specified pipeline for the names of objects that match the specified set of conditions
#'
#' Queries the specified pipeline for the names of objects that match the specified set of conditions.
#'
#' @usage
#' query_objects(
#'   pipelineId = "string",
#'   query = list(
#'     selectors = list(
#'       list(
#'         fieldName = "string",
#'         operator = list(
#'           type = "EQ"|"REF_EQ"|"LE"|"GE"|"BETWEEN",
#'           values = list(
#'             "string"
#'           )
#'         )
#'       )
#'     )
#'   ),
#'   sphere = "string",
#'   marker = "string",
#'   limit = 123
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param query The query that defines the objects to be returned. The `Query` object can contain a maximum of ten selectors. The conditions in the query are limited to top-level String fields in the object. These filters can be applied to components, instances, and attempts.
#' @param sphere &#91;required&#93; Indicates whether the query applies to components or instances. The possible values are: `COMPONENT`, `INSTANCE`, and `ATTEMPT`.
#' @param marker The starting point for the results to be returned. For the first call, this value should be empty. As long as there are more results, continue to call `QueryObjects` with the marker value from the previous call to retrieve the next set of results.
#' @param limit The maximum number of object names that `QueryObjects` will return in a single call. The default value is 100.
#'
#' @examples
#'
#' @export
query_objects <- function (pipelineId, query = NULL, sphere, 
    marker = NULL, limit = NULL) 
{
    op <- Operation(name = "QueryObjects", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- query_objects_input(pipelineId = pipelineId, query = query, 
        sphere = sphere, marker = marker, limit = limit)
    output <- query_objects_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes existing tags from the specified pipeline
#'
#' Removes existing tags from the specified pipeline.
#'
#' @usage
#' remove_tags(
#'   pipelineId = "string",
#'   tagKeys = list(
#'     "string"
#'   )
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param tagKeys &#91;required&#93; The keys of the tags to remove.
#'
#' @examples
#'
#' @export
remove_tags <- function (pipelineId, tagKeys) 
{
    op <- Operation(name = "RemoveTags", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- remove_tags_input(pipelineId = pipelineId, tagKeys = tagKeys)
    output <- remove_tags_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Task runners call `ReportTaskProgress` when assigned a task to acknowledge that it has the task
#'
#' Task runners call `ReportTaskProgress` when assigned a task to acknowledge that it has the task. If the web service does not receive this acknowledgement within 2 minutes, it assigns the task in a subsequent PollForTask call. After this initial acknowledgement, the task runner only needs to report progress every 15 minutes to maintain its ownership of the task. You can change this reporting time from 15 minutes by specifying a `reportProgressTimeout` field in your pipeline.
#' 
#' If a task runner does not report its status after 5 minutes, AWS Data Pipeline assumes that the task runner is unable to process the task and reassigns the task in a subsequent response to PollForTask. Task runners should call `ReportTaskProgress` every 60 seconds.
#'
#' @usage
#' report_task_progress(
#'   taskId = "string",
#'   fields = list(
#'     list(
#'       key = "string",
#'       stringValue = "string",
#'       refValue = "string"
#'     )
#'   )
#' )
#'
#' @param taskId &#91;required&#93; The ID of the task assigned to the task runner. This value is provided in the response for PollForTask.
#' @param fields Key-value pairs that define the properties of the ReportTaskProgressInput object.
#'
#' @examples
#'
#' @export
report_task_progress <- function (taskId, fields = NULL) 
{
    op <- Operation(name = "ReportTaskProgress", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- report_task_progress_input(taskId = taskId, fields = fields)
    output <- report_task_progress_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Task runners call `ReportTaskRunnerHeartbeat` every 15 minutes to indicate that they are operational
#'
#' Task runners call `ReportTaskRunnerHeartbeat` every 15 minutes to indicate that they are operational. If the AWS Data Pipeline Task Runner is launched on a resource managed by AWS Data Pipeline, the web service can use this call to detect when the task runner application has failed and restart a new instance.
#'
#' @usage
#' report_task_runner_heartbeat(
#'   taskrunnerId = "string",
#'   workerGroup = "string",
#'   hostname = "string"
#' )
#'
#' @param taskrunnerId &#91;required&#93; The ID of the task runner. This value should be unique across your AWS account. In the case of AWS Data Pipeline Task Runner launched on a resource managed by AWS Data Pipeline, the web service provides a unique identifier when it launches the application. If you have written a custom task runner, you should assign a unique identifier for the task runner.
#' @param workerGroup The type of task the task runner is configured to accept and process. The worker group is set as a field on objects in the pipeline when they are created. You can only specify a single value for `workerGroup`. There are no wildcard values permitted in `workerGroup`; the string must be an exact, case-sensitive, match.
#' @param hostname The public DNS name of the task runner.
#'
#' @examples
#'
#' @export
report_task_runner_heartbeat <- function (taskrunnerId, workerGroup = NULL, 
    hostname = NULL) 
{
    op <- Operation(name = "ReportTaskRunnerHeartbeat", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- report_task_runner_heartbeat_input(taskrunnerId = taskrunnerId, 
        workerGroup = workerGroup, hostname = hostname)
    output <- report_task_runner_heartbeat_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Requests that the status of the specified physical or logical pipeline objects be updated in the specified pipeline
#'
#' Requests that the status of the specified physical or logical pipeline objects be updated in the specified pipeline. This update might not occur immediately, but is eventually consistent. The status that can be set depends on the type of object (for example, DataNode or Activity). You cannot perform this operation on `FINISHED` pipelines and attempting to do so returns `InvalidRequestException`.
#'
#' @usage
#' set_status(
#'   pipelineId = "string",
#'   objectIds = list(
#'     "string"
#'   ),
#'   status = "string"
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline that contains the objects.
#' @param objectIds &#91;required&#93; The IDs of the objects. The corresponding objects can be either physical or components, but not a mix of both types.
#' @param status &#91;required&#93; The status to be set on all the objects specified in `objectIds`. For components, use `PAUSE` or `RESUME`. For instances, use `TRY_CANCEL`, `RERUN`, or `MARK_FINISHED`.
#'
#' @examples
#'
#' @export
set_status <- function (pipelineId, objectIds, status) 
{
    op <- Operation(name = "SetStatus", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- set_status_input(pipelineId = pipelineId, objectIds = objectIds, 
        status = status)
    output <- set_status_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Task runners call `SetTaskStatus` to notify AWS Data Pipeline that a task is completed and provide information about the final status
#'
#' Task runners call `SetTaskStatus` to notify AWS Data Pipeline that a task is completed and provide information about the final status. A task runner makes this call regardless of whether the task was sucessful. A task runner does not need to call `SetTaskStatus` for tasks that are canceled by the web service during a call to ReportTaskProgress.
#'
#' @usage
#' set_task_status(
#'   taskId = "string",
#'   taskStatus = "FINISHED"|"FAILED"|"FALSE",
#'   errorId = "string",
#'   errorMessage = "string",
#'   errorStackTrace = "string"
#' )
#'
#' @param taskId &#91;required&#93; The ID of the task assigned to the task runner. This value is provided in the response for PollForTask.
#' @param taskStatus &#91;required&#93; If `FINISHED`, the task successfully completed. If `FAILED`, the task ended unsuccessfully. Preconditions use false.
#' @param errorId If an error occurred during the task, this value specifies the error code. This value is set on the physical attempt object. It is used to display error information to the user. It should not start with string \"Service\_\" which is reserved by the system.
#' @param errorMessage If an error occurred during the task, this value specifies a text description of the error. This value is set on the physical attempt object. It is used to display error information to the user. The web service does not parse this value.
#' @param errorStackTrace If an error occurred during the task, this value specifies the stack trace associated with the error. This value is set on the physical attempt object. It is used to display error information to the user. The web service does not parse this value.
#'
#' @examples
#'
#' @export
set_task_status <- function (taskId, taskStatus, errorId = NULL, 
    errorMessage = NULL, errorStackTrace = NULL) 
{
    op <- Operation(name = "SetTaskStatus", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- set_task_status_input(taskId = taskId, taskStatus = taskStatus, 
        errorId = errorId, errorMessage = errorMessage, errorStackTrace = errorStackTrace)
    output <- set_task_status_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Validates the specified pipeline definition to ensure that it is well formed and can be run without error
#'
#' Validates the specified pipeline definition to ensure that it is well formed and can be run without error.
#'
#' @usage
#' validate_pipeline_definition(
#'   pipelineId = "string",
#'   pipelineObjects = list(
#'     list(
#'       id = "string",
#'       name = "string",
#'       fields = list(
#'         list(
#'           key = "string",
#'           stringValue = "string",
#'           refValue = "string"
#'         )
#'       )
#'     )
#'   ),
#'   parameterObjects = list(
#'     list(
#'       id = "string",
#'       attributes = list(
#'         list(
#'           key = "string",
#'           stringValue = "string"
#'         )
#'       )
#'     )
#'   ),
#'   parameterValues = list(
#'     list(
#'       id = "string",
#'       stringValue = "string"
#'     )
#'   )
#' )
#'
#' @param pipelineId &#91;required&#93; The ID of the pipeline.
#' @param pipelineObjects &#91;required&#93; The objects that define the pipeline changes to validate against the pipeline.
#' @param parameterObjects The parameter objects used with the pipeline.
#' @param parameterValues The parameter values used with the pipeline.
#'
#' @examples
#'
#' @export
validate_pipeline_definition <- function (pipelineId, pipelineObjects, 
    parameterObjects = NULL, parameterValues = NULL) 
{
    op <- Operation(name = "ValidatePipelineDefinition", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- validate_pipeline_definition_input(pipelineId = pipelineId, 
        pipelineObjects = pipelineObjects, parameterObjects = parameterObjects, 
        parameterValues = parameterValues)
    output <- validate_pipeline_definition_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
