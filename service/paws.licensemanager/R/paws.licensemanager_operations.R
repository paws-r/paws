# This file is generated by make.paws. Please do not edit here.

#' Creates a new license configuration object
#'
#' Creates a new license configuration object. A license configuration is an abstraction of a customer license agreement that can be consumed and enforced by License Manager. Components include specifications for the license type (licensing by instance, socket, CPU, or VCPU), tenancy (shared tenancy, Amazon EC2 Dedicated Instance, Amazon EC2 Dedicated Host, or any of these), host affinity (how long a VM must be associated with a host), the number of licenses purchased and used.
#'
#' @usage
#' create_license_configuration(
#'   Name = "string",
#'   Description = "string",
#'   LicenseCountingType = "vCPU"|"Instance"|"Core"|"Socket",
#'   LicenseCount = 123,
#'   LicenseCountHardLimit = TRUE|FALSE,
#'   LicenseRules = list(
#'     "string"
#'   ),
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#'
#' @param Name &#91;required&#93; Name of the license configuration.
#' @param Description Human-friendly description of the license configuration.
#' @param LicenseCountingType &#91;required&#93; Dimension to use to track the license inventory.
#' @param LicenseCount Number of licenses managed by the license configuration.
#' @param LicenseCountHardLimit Flag indicating whether hard or soft license enforcement is used. Exceeding a hard limit results in the blocked deployment of new instances.
#' @param LicenseRules Array of configured License Manager rules.
#' @param Tags The tags to apply to the resources during launch. You can only tag instances and volumes on launch. The specified tags are applied to all instances or volumes that are created during launch. To tag a resource after it has been created, see CreateTags .
#'
#' @examples
#'
#' @export
create_license_configuration <- function (Name, Description = NULL, 
    LicenseCountingType, LicenseCount = NULL, LicenseCountHardLimit = NULL, 
    LicenseRules = NULL, Tags = NULL) 
{
    op <- Operation(name = "CreateLicenseConfiguration", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_license_configuration_input(Name = Name, 
        Description = Description, LicenseCountingType = LicenseCountingType, 
        LicenseCount = LicenseCount, LicenseCountHardLimit = LicenseCountHardLimit, 
        LicenseRules = LicenseRules, Tags = Tags)
    output <- create_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes an existing license configuration
#'
#' Deletes an existing license configuration. This action fails if the configuration is in use.
#'
#' @usage
#' delete_license_configuration(
#'   LicenseConfigurationArn = "string"
#' )
#'
#' @param LicenseConfigurationArn &#91;required&#93; Unique ID of the configuration object to delete.
#'
#' @examples
#'
#' @export
delete_license_configuration <- function (LicenseConfigurationArn) 
{
    op <- Operation(name = "DeleteLicenseConfiguration", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_license_configuration_input(LicenseConfigurationArn = LicenseConfigurationArn)
    output <- delete_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a detailed description of a license configuration
#'
#' Returns a detailed description of a license configuration.
#'
#' @usage
#' get_license_configuration(
#'   LicenseConfigurationArn = "string"
#' )
#'
#' @param LicenseConfigurationArn &#91;required&#93; ARN of the license configuration being requested.
#'
#' @examples
#'
#' @export
get_license_configuration <- function (LicenseConfigurationArn) 
{
    op <- Operation(name = "GetLicenseConfiguration", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_license_configuration_input(LicenseConfigurationArn = LicenseConfigurationArn)
    output <- get_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets License Manager settings for a region
#'
#' Gets License Manager settings for a region. Exposes the configured S3 bucket, SNS topic, etc., for inspection.
#'
#' @usage
#' get_service_settings()
#'
#' @examples
#'
#' @export
get_service_settings <- function () 
{
    op <- Operation(name = "GetServiceSettings", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_service_settings_input()
    output <- get_service_settings_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the resource associations for a license configuration
#'
#' Lists the resource associations for a license configuration. Resource associations need not consume licenses from a license configuration. For example, an AMI or a stopped instance may not consume a license (depending on the license rules). Use this operation to find all resources associated with a license configuration.
#'
#' @usage
#' list_associations_for_license_configuration(
#'   LicenseConfigurationArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param LicenseConfigurationArn &#91;required&#93; ARN of a `LicenseConfiguration` object.
#' @param MaxResults Maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned `NextToken` value.
#' @param NextToken Token for the next set of results.
#'
#' @examples
#'
#' @export
list_associations_for_license_configuration <- function (LicenseConfigurationArn, 
    MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListAssociationsForLicenseConfiguration", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_associations_for_license_configuration_input(LicenseConfigurationArn = LicenseConfigurationArn, 
        MaxResults = MaxResults, NextToken = NextToken)
    output <- list_associations_for_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists license configuration objects for an account, each containing the name, description, license type, and other license terms modeled from a license agreement
#'
#' Lists license configuration objects for an account, each containing the name, description, license type, and other license terms modeled from a license agreement.
#'
#' @usage
#' list_license_configurations(
#'   LicenseConfigurationArns = list(
#'     "string"
#'   ),
#'   MaxResults = 123,
#'   NextToken = "string",
#'   Filters = list(
#'     list(
#'       Name = "string",
#'       Values = list(
#'         "string"
#'       )
#'     )
#'   )
#' )
#'
#' @param LicenseConfigurationArns An array of ARNs for the calling account's license configurations.
#' @param MaxResults Maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned `NextToken` value.
#' @param NextToken Token for the next set of results.
#' @param Filters One or more filters.
#'
#' @examples
#'
#' @export
list_license_configurations <- function (LicenseConfigurationArns = NULL, 
    MaxResults = NULL, NextToken = NULL, Filters = NULL) 
{
    op <- Operation(name = "ListLicenseConfigurations", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_license_configurations_input(LicenseConfigurationArns = LicenseConfigurationArns, 
        MaxResults = MaxResults, NextToken = NextToken, Filters = Filters)
    output <- list_license_configurations_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the license configuration for a resource
#'
#' Returns the license configuration for a resource.
#'
#' @usage
#' list_license_specifications_for_resource(
#'   ResourceArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#'
#' @param ResourceArn &#91;required&#93; ARN of an AMI or Amazon EC2 instance that has an associated license configuration.
#' @param MaxResults Maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned `NextToken` value.
#' @param NextToken Token for the next set of results.
#'
#' @examples
#'
#' @export
list_license_specifications_for_resource <- function (ResourceArn, 
    MaxResults = NULL, NextToken = NULL) 
{
    op <- Operation(name = "ListLicenseSpecificationsForResource", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_license_specifications_for_resource_input(ResourceArn = ResourceArn, 
        MaxResults = MaxResults, NextToken = NextToken)
    output <- list_license_specifications_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a detailed list of resources
#'
#' Returns a detailed list of resources.
#'
#' @usage
#' list_resource_inventory(
#'   MaxResults = 123,
#'   NextToken = "string",
#'   Filters = list(
#'     list(
#'       Name = "string",
#'       Condition = "EQUALS"|"NOT_EQUALS"|"BEGINS_WITH"|"CONTAINS",
#'       Value = "string"
#'     )
#'   )
#' )
#'
#' @param MaxResults Maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned `NextToken` value.
#' @param NextToken Token for the next set of results.
#' @param Filters One or more filters.
#'
#' @examples
#'
#' @export
list_resource_inventory <- function (MaxResults = NULL, NextToken = NULL, 
    Filters = NULL) 
{
    op <- Operation(name = "ListResourceInventory", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_resource_inventory_input(MaxResults = MaxResults, 
        NextToken = NextToken, Filters = Filters)
    output <- list_resource_inventory_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists tags attached to a resource
#'
#' Lists tags attached to a resource.
#'
#' @usage
#' list_tags_for_resource(
#'   ResourceArn = "string"
#' )
#'
#' @param ResourceArn &#91;required&#93; ARN for the resource.
#'
#' @examples
#'
#' @export
list_tags_for_resource <- function (ResourceArn) 
{
    op <- Operation(name = "ListTagsForResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_tags_for_resource_input(ResourceArn = ResourceArn)
    output <- list_tags_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all license usage records for a license configuration, displaying license consumption details by resource at a selected point in time
#'
#' Lists all license usage records for a license configuration, displaying license consumption details by resource at a selected point in time. Use this action to audit the current license consumption for any license inventory and configuration.
#'
#' @usage
#' list_usage_for_license_configuration(
#'   LicenseConfigurationArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string",
#'   Filters = list(
#'     list(
#'       Name = "string",
#'       Values = list(
#'         "string"
#'       )
#'     )
#'   )
#' )
#'
#' @param LicenseConfigurationArn &#91;required&#93; ARN of the targeted `LicenseConfiguration` object.
#' @param MaxResults Maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned `NextToken` value.
#' @param NextToken Token for the next set of results.
#' @param Filters List of filters to apply.
#'
#' @examples
#'
#' @export
list_usage_for_license_configuration <- function (LicenseConfigurationArn, 
    MaxResults = NULL, NextToken = NULL, Filters = NULL) 
{
    op <- Operation(name = "ListUsageForLicenseConfiguration", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_usage_for_license_configuration_input(LicenseConfigurationArn = LicenseConfigurationArn, 
        MaxResults = MaxResults, NextToken = NextToken, Filters = Filters)
    output <- list_usage_for_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attach one of more tags to any resource
#'
#' Attach one of more tags to any resource.
#'
#' @usage
#' tag_resource(
#'   ResourceArn = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#'
#' @param ResourceArn &#91;required&#93; Resource of the ARN to be tagged.
#' @param Tags &#91;required&#93; Names of the tags to attach to the resource.
#'
#' @examples
#'
#' @export
tag_resource <- function (ResourceArn, Tags) 
{
    op <- Operation(name = "TagResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- tag_resource_input(ResourceArn = ResourceArn, Tags = Tags)
    output <- tag_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Remove tags from a resource
#'
#' Remove tags from a resource.
#'
#' @usage
#' untag_resource(
#'   ResourceArn = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#'
#' @param ResourceArn &#91;required&#93; ARN of the resource.
#' @param TagKeys &#91;required&#93; List keys identifying tags to remove.
#'
#' @examples
#'
#' @export
untag_resource <- function (ResourceArn, TagKeys) 
{
    op <- Operation(name = "UntagResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- untag_resource_input(ResourceArn = ResourceArn, 
        TagKeys = TagKeys)
    output <- untag_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Modifies the attributes of an existing license configuration object
#'
#' Modifies the attributes of an existing license configuration object. A license configuration is an abstraction of a customer license agreement that can be consumed and enforced by License Manager. Components include specifications for the license type (Instances, cores, sockets, VCPUs), tenancy (shared or Dedicated Host), host affinity (how long a VM is associated with a host), the number of licenses purchased and used.
#'
#' @usage
#' update_license_configuration(
#'   LicenseConfigurationArn = "string",
#'   LicenseConfigurationStatus = "AVAILABLE"|"DISABLED",
#'   LicenseRules = list(
#'     "string"
#'   ),
#'   LicenseCount = 123,
#'   LicenseCountHardLimit = TRUE|FALSE,
#'   Name = "string",
#'   Description = "string"
#' )
#'
#' @param LicenseConfigurationArn &#91;required&#93; ARN for a license configuration.
#' @param LicenseConfigurationStatus New status of the license configuration (`ACTIVE` or `INACTIVE`).
#' @param LicenseRules List of flexible text strings designating license rules.
#' @param LicenseCount New number of licenses managed by the license configuration.
#' @param LicenseCountHardLimit Sets the number of available licenses as a hard limit.
#' @param Name New name of the license configuration.
#' @param Description New human-friendly description of the license configuration.
#'
#' @examples
#'
#' @export
update_license_configuration <- function (LicenseConfigurationArn, 
    LicenseConfigurationStatus = NULL, LicenseRules = NULL, LicenseCount = NULL, 
    LicenseCountHardLimit = NULL, Name = NULL, Description = NULL) 
{
    op <- Operation(name = "UpdateLicenseConfiguration", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_license_configuration_input(LicenseConfigurationArn = LicenseConfigurationArn, 
        LicenseConfigurationStatus = LicenseConfigurationStatus, 
        LicenseRules = LicenseRules, LicenseCount = LicenseCount, 
        LicenseCountHardLimit = LicenseCountHardLimit, Name = Name, 
        Description = Description)
    output <- update_license_configuration_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or removes license configurations for a specified AWS resource
#'
#' Adds or removes license configurations for a specified AWS resource. This operation currently supports updating the license specifications of AMIs, instances, and hosts. Launch templates and AWS CloudFormation templates are not managed from this operation as those resources send the license configurations directly to a resource creation operation, such as `RunInstances`.
#'
#' @usage
#' update_license_specifications_for_resource(
#'   ResourceArn = "string",
#'   AddLicenseSpecifications = list(
#'     list(
#'       LicenseConfigurationArn = "string"
#'     )
#'   ),
#'   RemoveLicenseSpecifications = list(
#'     list(
#'       LicenseConfigurationArn = "string"
#'     )
#'   )
#' )
#'
#' @param ResourceArn &#91;required&#93; ARN for an AWS server resource.
#' @param AddLicenseSpecifications License configuration ARNs to be added to a resource.
#' @param RemoveLicenseSpecifications License configuration ARNs to be removed from a resource.
#'
#' @examples
#'
#' @export
update_license_specifications_for_resource <- function (ResourceArn, 
    AddLicenseSpecifications = NULL, RemoveLicenseSpecifications = NULL) 
{
    op <- Operation(name = "UpdateLicenseSpecificationsForResource", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_license_specifications_for_resource_input(ResourceArn = ResourceArn, 
        AddLicenseSpecifications = AddLicenseSpecifications, 
        RemoveLicenseSpecifications = RemoveLicenseSpecifications)
    output <- update_license_specifications_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates License Manager service settings
#'
#' Updates License Manager service settings.
#'
#' @usage
#' update_service_settings(
#'   S3BucketArn = "string",
#'   SnsTopicArn = "string",
#'   OrganizationConfiguration = list(
#'     EnableIntegration = TRUE|FALSE
#'   ),
#'   EnableCrossAccountsDiscovery = TRUE|FALSE
#' )
#'
#' @param S3BucketArn ARN of the Amazon S3 bucket where License Manager information is stored.
#' @param SnsTopicArn ARN of the Amazon SNS topic used for License Manager alerts.
#' @param OrganizationConfiguration Integrates AWS Organizations with License Manager for cross-account discovery.
#' @param EnableCrossAccountsDiscovery Activates cross-account discovery.
#'
#' @examples
#'
#' @export
update_service_settings <- function (S3BucketArn = NULL, SnsTopicArn = NULL, 
    OrganizationConfiguration = NULL, EnableCrossAccountsDiscovery = NULL) 
{
    op <- Operation(name = "UpdateServiceSettings", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_service_settings_input(S3BucketArn = S3BucketArn, 
        SnsTopicArn = SnsTopicArn, OrganizationConfiguration = OrganizationConfiguration, 
        EnableCrossAccountsDiscovery = EnableCrossAccountsDiscovery)
    output <- update_service_settings_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
