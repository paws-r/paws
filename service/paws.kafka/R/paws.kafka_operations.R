# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Creates a new MSK cluster
#'
#' Creates a new MSK cluster.
#'
#' @section Accepted Parameters:
#' ```
#' create_cluster(
#'   BrokerNodeGroupInfo = list(
#'     BrokerAZDistribution = "DEFAULT",
#'     ClientSubnets = list(
#'       "string"
#'     ),
#'     InstanceType = "string",
#'     SecurityGroups = list(
#'       "string"
#'     ),
#'     StorageInfo = list(
#'       EbsStorageInfo = list(
#'         VolumeSize = 123
#'       )
#'     )
#'   ),
#'   ClusterName = "string",
#'   EncryptionInfo = list(
#'     EncryptionAtRest = list(
#'       DataVolumeKMSKeyId = "string"
#'     )
#'   ),
#'   EnhancedMonitoring = "DEFAULT"|"PER_BROKER"|"PER_TOPIC_PER_BROKER",
#'   KafkaVersion = "string",
#'   NumberOfBrokerNodes = 123
#' )
#' ```
#'
#' @param BrokerNodeGroupInfo &#91;required&#93; Information about the broker nodes in the cluster.
#' @param ClusterName &#91;required&#93; The name of the cluster.
#' @param EncryptionInfo Includes all encryption-related information.
#' @param EnhancedMonitoring Specifies the level of monitoring for the MSK cluster. The possible values are DEFAULT, PER\_BROKER, and PER\_TOPIC\_PER\_BROKER.
#' @param KafkaVersion &#91;required&#93; The version of Apache Kafka.
#' @param NumberOfBrokerNodes &#91;required&#93; The number of Kafka broker nodes in the Amazon MSK cluster.
#'
#' @export
create_cluster <- function (BrokerNodeGroupInfo, ClusterName, 
    EncryptionInfo = NULL, EnhancedMonitoring = NULL, KafkaVersion, 
    NumberOfBrokerNodes) 
{
    op <- new_operation(name = "CreateCluster", http_method = "POST", 
        http_path = "/v1/clusters", paginator = list())
    input <- create_cluster_input(BrokerNodeGroupInfo = BrokerNodeGroupInfo, 
        ClusterName = ClusterName, EncryptionInfo = EncryptionInfo, 
        EnhancedMonitoring = EnhancedMonitoring, KafkaVersion = KafkaVersion, 
        NumberOfBrokerNodes = NumberOfBrokerNodes)
    output <- create_cluster_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the MSK cluster specified by the Amazon Resource Name (ARN) in the request
#'
#' Deletes the MSK cluster specified by the Amazon Resource Name (ARN) in the request.
#'
#' @section Accepted Parameters:
#' ```
#' delete_cluster(
#'   ClusterArn = "string",
#'   CurrentVersion = "string"
#' )
#' ```
#'
#' @param ClusterArn &#91;required&#93; The Amazon Resource Name (ARN) that uniquely identifies the cluster.
#' @param CurrentVersion The current version of the MSK cluster.
#'
#' @export
delete_cluster <- function (ClusterArn, CurrentVersion = NULL) 
{
    op <- new_operation(name = "DeleteCluster", http_method = "DELETE", 
        http_path = "/v1/clusters/{clusterArn}", paginator = list())
    input <- delete_cluster_input(ClusterArn = ClusterArn, CurrentVersion = CurrentVersion)
    output <- delete_cluster_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a description of the MSK cluster whose Amazon Resource Name (ARN) is specified in the request
#'
#' Returns a description of the MSK cluster whose Amazon Resource Name (ARN) is specified in the request.
#'
#' @section Accepted Parameters:
#' ```
#' describe_cluster(
#'   ClusterArn = "string"
#' )
#' ```
#'
#' @param ClusterArn &#91;required&#93; The Amazon Resource Name (ARN) that uniquely identifies the cluster.
#'
#' @export
describe_cluster <- function (ClusterArn) 
{
    op <- new_operation(name = "DescribeCluster", http_method = "GET", 
        http_path = "/v1/clusters/{clusterArn}", paginator = list())
    input <- describe_cluster_input(ClusterArn = ClusterArn)
    output <- describe_cluster_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' A list of brokers that a client application can use to bootstrap
#'
#' A list of brokers that a client application can use to bootstrap.
#'
#' @section Accepted Parameters:
#' ```
#' get_bootstrap_brokers(
#'   ClusterArn = "string"
#' )
#' ```
#'
#' @param ClusterArn &#91;required&#93; The Amazon Resource Name (ARN) that uniquely identifies the cluster.
#'
#' @export
get_bootstrap_brokers <- function (ClusterArn) 
{
    op <- new_operation(name = "GetBootstrapBrokers", http_method = "GET", 
        http_path = "/v1/clusters/{clusterArn}/bootstrap-brokers", 
        paginator = list())
    input <- get_bootstrap_brokers_input(ClusterArn = ClusterArn)
    output <- get_bootstrap_brokers_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of clusters in an account
#'
#' Returns a list of clusters in an account.
#'
#' @section Accepted Parameters:
#' ```
#' list_clusters(
#'   ClusterNameFilter = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @param ClusterNameFilter Specify a prefix of the name of the clusters that you want to list. The service lists all the clusters whose names start with this prefix.
#' @param MaxResults The maximum number of clusters to return in the response. If there are more clusters, the response includes a NextToken parameter.
#' @param NextToken The paginated results marker. When the result of a ListClusters operation is truncated, the call returns NextToken in the response. To get another batch of clusters, provide this token in your next request.
#'
#' @export
list_clusters <- function (ClusterNameFilter = NULL, MaxResults = NULL, 
    NextToken = NULL) 
{
    op <- new_operation(name = "ListClusters", http_method = "GET", 
        http_path = "/v1/clusters", paginator = list())
    input <- list_clusters_input(ClusterNameFilter = ClusterNameFilter, 
        MaxResults = MaxResults, NextToken = NextToken)
    output <- list_clusters_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of the broker nodes in the cluster
#'
#' Returns a list of the broker nodes in the cluster.
#'
#' @section Accepted Parameters:
#' ```
#' list_nodes(
#'   ClusterArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @param ClusterArn &#91;required&#93; The Amazon Resource Name (ARN) that uniquely identifies the cluster.
#' @param MaxResults The maximum number of clusters to return in the response. If there are more clusters, the response includes a NextToken parameter.
#' @param NextToken The paginated results marker. When the result of a ListClusters operation is truncated, the call returns NextToken in the response. To get another batch of clusters, provide this token in your next request.
#'
#' @export
list_nodes <- function (ClusterArn, MaxResults = NULL, NextToken = NULL) 
{
    op <- new_operation(name = "ListNodes", http_method = "GET", 
        http_path = "/v1/clusters/{clusterArn}/nodes", paginator = list())
    input <- list_nodes_input(ClusterArn = ClusterArn, MaxResults = MaxResults, 
        NextToken = NextToken)
    output <- list_nodes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
