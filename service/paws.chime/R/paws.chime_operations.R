# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Suspends up to 50 users from a Team or EnterpriseLWA Amazon Chime account
#'
#' Suspends up to 50 users from a `Team` or `EnterpriseLWA` Amazon Chime account. For more information about different account types, see [Managing Your Amazon Chime Accounts](http://docs.aws.amazon.com/chime/latest/ag/manage-chime-account.html) in the *Amazon Chime Administration Guide*.
#' 
#' Users suspended from a `Team` account are dissociated from the account, but they can continue to use Amazon Chime as free users. To remove the suspension from suspended `Team` account users, invite them to the `Team` account again. You can use the InviteUsers action to do so.
#' 
#' Users suspended from an `EnterpriseLWA` account are immediately signed out of Amazon Chime and are no longer able to sign in. To remove the suspension from suspended `EnterpriseLWA` account users, use the BatchUnsuspendUser action.
#' 
#' To sign out users without suspending them, use the LogoutUser action.
#'
#' @section Accepted Parameters:
#' ```
#' batch_suspend_user(
#'   AccountId = "string",
#'   UserIdList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserIdList &#91;required&#93; The request containing the user IDs to suspend.
#'
#' @export
batch_suspend_user <- function (AccountId, UserIdList) 
{
    op <- new_operation(name = "BatchSuspendUser", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users?operation=suspend", 
        paginator = list())
    input <- batch_suspend_user_input(AccountId = AccountId, 
        UserIdList = UserIdList)
    output <- batch_suspend_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the suspension from up to 50 previously suspended users for the specified Amazon Chime EnterpriseLWA account
#'
#' Removes the suspension from up to 50 previously suspended users for the specified Amazon Chime `EnterpriseLWA` account. Only users on `EnterpriseLWA` accounts can be unsuspended using this action. For more information about different account types, see [Managing Your Amazon Chime Accounts](http://docs.aws.amazon.com/chime/latest/ag/manage-chime-account.html) in the *Amazon Chime Administration Guide*.
#' 
#' Previously suspended users who are unsuspended using this action are returned to `Registered` status. Users who are not previously suspended are ignored.
#'
#' @section Accepted Parameters:
#' ```
#' batch_unsuspend_user(
#'   AccountId = "string",
#'   UserIdList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserIdList &#91;required&#93; The request containing the user IDs to unsuspend.
#'
#' @export
batch_unsuspend_user <- function (AccountId, UserIdList) 
{
    op <- new_operation(name = "BatchUnsuspendUser", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users?operation=unsuspend", 
        paginator = list())
    input <- batch_unsuspend_user_input(AccountId = AccountId, 
        UserIdList = UserIdList)
    output <- batch_unsuspend_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates user details within the UpdateUserRequestItem object for up to 20 users for the specified Amazon Chime account
#'
#' Updates user details within the UpdateUserRequestItem object for up to 20 users for the specified Amazon Chime account. Currently, only `LicenseType` updates are supported for this action.
#'
#' @section Accepted Parameters:
#' ```
#' batch_update_user(
#'   AccountId = "string",
#'   UpdateUserRequestItems = list(
#'     list(
#'       UserId = "string",
#'       LicenseType = "Basic"|"Plus"|"Pro"|"ProTrial"
#'     )
#'   )
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UpdateUserRequestItems &#91;required&#93; The request containing the user IDs and details to update.
#'
#' @export
batch_update_user <- function (AccountId, UpdateUserRequestItems) 
{
    op <- new_operation(name = "BatchUpdateUser", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users", paginator = list())
    input <- batch_update_user_input(AccountId = AccountId, UpdateUserRequestItems = UpdateUserRequestItems)
    output <- batch_update_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an Amazon Chime account under the administrator's AWS account
#'
#' Creates an Amazon Chime account under the administrator\'s AWS account. Only `Team` account types are currently supported for this action. For more information about different account types, see [Managing Your Amazon Chime Accounts](http://docs.aws.amazon.com/chime/latest/ag/manage-chime-account.html) in the *Amazon Chime Administration Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_account(
#'   Name = "string"
#' )
#' ```
#'
#' @param Name &#91;required&#93; The name of the Amazon Chime account.
#'
#' @export
create_account <- function (Name) 
{
    op <- new_operation(name = "CreateAccount", http_method = "POST", 
        http_path = "/console/accounts", paginator = list())
    input <- create_account_input(Name = Name)
    output <- create_account_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified Amazon Chime account
#'
#' Deletes the specified Amazon Chime account. You must suspend all users before deleting a `Team` account. You can use the BatchSuspendUser action to do so.
#' 
#' For `EnterpriseLWA` and `EnterpriseAD` accounts, you must release the claimed domains for your Amazon Chime account before deletion. As soon as you release the domain, all users under that account are suspended.
#' 
#' Deleted accounts appear in your `Disabled` accounts list for 90 days. To restore a deleted account from your `Disabled` accounts list, you must contact AWS Support.
#' 
#' After 90 days, deleted accounts are permanently removed from your `Disabled` accounts list.
#'
#' @section Accepted Parameters:
#' ```
#' delete_account(
#'   AccountId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#'
#' @export
delete_account <- function (AccountId) 
{
    op <- new_operation(name = "DeleteAccount", http_method = "DELETE", 
        http_path = "/console/accounts/{accountId}", paginator = list())
    input <- delete_account_input(AccountId = AccountId)
    output <- delete_account_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves details for the specified Amazon Chime account, such as account type and supported licenses
#'
#' Retrieves details for the specified Amazon Chime account, such as account type and supported licenses.
#'
#' @section Accepted Parameters:
#' ```
#' get_account(
#'   AccountId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#'
#' @export
get_account <- function (AccountId) 
{
    op <- new_operation(name = "GetAccount", http_method = "GET", 
        http_path = "/console/accounts/{accountId}", paginator = list())
    input <- get_account_input(AccountId = AccountId)
    output <- get_account_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves account settings for the specified Amazon Chime account ID, such as remote control and dial out settings
#'
#' Retrieves account settings for the specified Amazon Chime account ID, such as remote control and dial out settings. For more information about these settings, see [Use the Policies Page](http://docs.aws.amazon.com/chime/latest/ag/policies.html) in the *Amazon Chime Administration Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_account_settings(
#'   AccountId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#'
#' @export
get_account_settings <- function (AccountId) 
{
    op <- new_operation(name = "GetAccountSettings", http_method = "GET", 
        http_path = "/console/accounts/{accountId}/settings", 
        paginator = list())
    input <- get_account_settings_input(AccountId = AccountId)
    output <- get_account_settings_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves details for the specified user ID, such as primary email address, license type, and personal meeting PIN
#'
#' Retrieves details for the specified user ID, such as primary email address, license type, and personal meeting PIN.
#'
#' @section Accepted Parameters:
#' ```
#' get_user(
#'   AccountId = "string",
#'   UserId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserId &#91;required&#93; The user ID.
#'
#' @export
get_user <- function (AccountId, UserId) 
{
    op <- new_operation(name = "GetUser", http_method = "GET", 
        http_path = "/console/accounts/{accountId}/users/{userId}", 
        paginator = list())
    input <- get_user_input(AccountId = AccountId, UserId = UserId)
    output <- get_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sends email invites to as many as 50 users, inviting them to the specified Amazon Chime Team account
#'
#' Sends email invites to as many as 50 users, inviting them to the specified Amazon Chime `Team` account. Only `Team` account types are currently supported for this action.
#'
#' @section Accepted Parameters:
#' ```
#' invite_users(
#'   AccountId = "string",
#'   UserEmailList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserEmailList &#91;required&#93; The user email addresses to which to send the invite.
#'
#' @export
invite_users <- function (AccountId, UserEmailList) 
{
    op <- new_operation(name = "InviteUsers", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users?operation=add", 
        paginator = list())
    input <- invite_users_input(AccountId = AccountId, UserEmailList = UserEmailList)
    output <- invite_users_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the Amazon Chime accounts under the administrator's AWS account
#'
#' Lists the Amazon Chime accounts under the administrator\'s AWS account. You can filter accounts by account name prefix. To find out which Amazon Chime account a user belongs to, you can filter by the user\'s email address, which returns one account result.
#'
#' @section Accepted Parameters:
#' ```
#' list_accounts(
#'   Name = "string",
#'   UserEmail = "string",
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @param Name Amazon Chime account name prefix with which to filter results.
#' @param UserEmail User email address with which to filter results.
#' @param NextToken The token to use to retrieve the next page of results.
#' @param MaxResults The maximum number of results to return in a single call. Defaults to 100.
#'
#' @export
list_accounts <- function (Name = NULL, UserEmail = NULL, NextToken = NULL, 
    MaxResults = NULL) 
{
    op <- new_operation(name = "ListAccounts", http_method = "GET", 
        http_path = "/console/accounts", paginator = list())
    input <- list_accounts_input(Name = Name, UserEmail = UserEmail, 
        NextToken = NextToken, MaxResults = MaxResults)
    output <- list_accounts_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the users that belong to the specified Amazon Chime account
#'
#' Lists the users that belong to the specified Amazon Chime account.
#'
#' @section Accepted Parameters:
#' ```
#' list_users(
#'   AccountId = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param MaxResults The maximum number of results to return in a single call. Defaults to 100.
#' @param NextToken The token to use to retrieve the next page of results.
#'
#' @export
list_users <- function (AccountId, MaxResults = NULL, NextToken = NULL) 
{
    op <- new_operation(name = "ListUsers", http_method = "GET", 
        http_path = "/console/accounts/{accountId}/users", paginator = list())
    input <- list_users_input(AccountId = AccountId, MaxResults = MaxResults, 
        NextToken = NextToken)
    output <- list_users_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Logs out the specified user from all of the devices they are currently logged into
#'
#' Logs out the specified user from all of the devices they are currently logged into.
#'
#' @section Accepted Parameters:
#' ```
#' logout_user(
#'   AccountId = "string",
#'   UserId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserId &#91;required&#93; The user ID.
#'
#' @export
logout_user <- function (AccountId, UserId) 
{
    op <- new_operation(name = "LogoutUser", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users/{userId}?operation=logout", 
        paginator = list())
    input <- logout_user_input(AccountId = AccountId, UserId = UserId)
    output <- logout_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Resets the personal meeting PIN for the specified user on an Amazon Chime account
#'
#' Resets the personal meeting PIN for the specified user on an Amazon Chime account. Returns the User object with the updated personal meeting PIN.
#'
#' @section Accepted Parameters:
#' ```
#' reset_personal_pin(
#'   AccountId = "string",
#'   UserId = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserId &#91;required&#93; The user ID.
#'
#' @export
reset_personal_pin <- function (AccountId, UserId) 
{
    op <- new_operation(name = "ResetPersonalPIN", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users/{userId}?operation=reset-personal-pin", 
        paginator = list())
    input <- reset_personal_pin_input(AccountId = AccountId, 
        UserId = UserId)
    output <- reset_personal_pin_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates account details for the specified Amazon Chime account
#'
#' Updates account details for the specified Amazon Chime account. Currently, only account name updates are supported for this action.
#'
#' @section Accepted Parameters:
#' ```
#' update_account(
#'   AccountId = "string",
#'   Name = "string"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param Name The new name for the specified Amazon Chime account.
#'
#' @export
update_account <- function (AccountId, Name = NULL) 
{
    op <- new_operation(name = "UpdateAccount", http_method = "POST", 
        http_path = "/console/accounts/{accountId}", paginator = list())
    input <- update_account_input(AccountId = AccountId, Name = Name)
    output <- update_account_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the settings for the specified Amazon Chime account
#'
#' Updates the settings for the specified Amazon Chime account. You can update settings for remote control of shared screens, or for the dial-out option. For more information about these settings, see [Use the Policies Page](http://docs.aws.amazon.com/chime/latest/ag/policies.html) in the *Amazon Chime Administration Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_account_settings(
#'   AccountId = "string",
#'   AccountSettings = list(
#'     DisableRemoteControl = TRUE|FALSE,
#'     EnableDialOut = TRUE|FALSE
#'   )
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param AccountSettings &#91;required&#93; The Amazon Chime account settings to update.
#'
#' @export
update_account_settings <- function (AccountId, AccountSettings) 
{
    op <- new_operation(name = "UpdateAccountSettings", http_method = "PUT", 
        http_path = "/console/accounts/{accountId}/settings", 
        paginator = list())
    input <- update_account_settings_input(AccountId = AccountId, 
        AccountSettings = AccountSettings)
    output <- update_account_settings_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates user details for a specified user ID
#'
#' Updates user details for a specified user ID. Currently, only `LicenseType` updates are supported for this action.
#'
#' @section Accepted Parameters:
#' ```
#' update_user(
#'   AccountId = "string",
#'   UserId = "string",
#'   LicenseType = "Basic"|"Plus"|"Pro"|"ProTrial"
#' )
#' ```
#'
#' @param AccountId &#91;required&#93; The Amazon Chime account ID.
#' @param UserId &#91;required&#93; The user ID.
#' @param LicenseType The user license type to update. This must be a supported license type for the Amazon Chime account that the user belongs to.
#'
#' @export
update_user <- function (AccountId, UserId, LicenseType = NULL) 
{
    op <- new_operation(name = "UpdateUser", http_method = "POST", 
        http_path = "/console/accounts/{accountId}/users/{userId}", 
        paginator = list())
    input <- update_user_input(AccountId = AccountId, UserId = UserId, 
        LicenseType = LicenseType)
    output <- update_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
