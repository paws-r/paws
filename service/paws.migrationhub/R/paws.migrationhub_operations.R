# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Associates a created artifact of an AWS cloud resource, the target receiving the migration, with the migration task performed by a migration tool
#'
#' Associates a created artifact of an AWS cloud resource, the target receiving the migration, with the migration task performed by a migration tool. This API has the following traits:
#' 
#' -   Migration tools can call the `AssociateCreatedArtifact` operation to indicate which AWS artifact is associated with a migration task.
#' 
#' -   The created artifact name must be provided in ARN (Amazon Resource Name) format which will contain information about type and region; for example: `arn:aws:ec2:us-east-1:488216288981:image/ami-6d0ba87b`.
#' 
#' -   Examples of the AWS resource behind the created artifact are, AMI\'s, EC2 instance, or DMS endpoint, etc.
#'
#' @section Accepted Parameters:
#' ```
#' associate_created_artifact(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   CreatedArtifact = list(
#'     Name = "string",
#'     Description = "string"
#'   ),
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task.
#' @param CreatedArtifact &#91;required&#93; An ARN of the AWS resource related to the migration (e.g., AMI, EC2 instance, RDS instance, etc.)
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
associate_created_artifact <- function (ProgressUpdateStream, 
    MigrationTaskName, CreatedArtifact, DryRun = NULL) 
{
    op <- new_operation(name = "AssociateCreatedArtifact", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- associate_created_artifact_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, CreatedArtifact = CreatedArtifact, 
        DryRun = DryRun)
    output <- associate_created_artifact_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Associates a discovered resource ID from Application Discovery Service (ADS) with a migration task
#'
#' Associates a discovered resource ID from Application Discovery Service (ADS) with a migration task.
#'
#' @section Accepted Parameters:
#' ```
#' associate_discovered_resource(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   DiscoveredResource = list(
#'     ConfigurationId = "string",
#'     Description = "string"
#'   ),
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; The identifier given to the MigrationTask.
#' @param DiscoveredResource &#91;required&#93; Object representing a Resource.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
associate_discovered_resource <- function (ProgressUpdateStream, 
    MigrationTaskName, DiscoveredResource, DryRun = NULL) 
{
    op <- new_operation(name = "AssociateDiscoveredResource", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- associate_discovered_resource_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, DiscoveredResource = DiscoveredResource, 
        DryRun = DryRun)
    output <- associate_discovered_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a progress update stream which is an AWS resource used for access control as well as a namespace for migration task names that is implicitly linked to your AWS account
#'
#' Creates a progress update stream which is an AWS resource used for access control as well as a namespace for migration task names that is implicitly linked to your AWS account. It must uniquely identify the migration tool as it is used for all updates made by the tool; however, it does not need to be unique for each AWS account because it is scoped to the AWS account.
#'
#' @section Accepted Parameters:
#' ```
#' create_progress_update_stream(
#'   ProgressUpdateStreamName = "string",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStreamName &#91;required&#93; The name of the ProgressUpdateStream.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
create_progress_update_stream <- function (ProgressUpdateStreamName, 
    DryRun = NULL) 
{
    op <- new_operation(name = "CreateProgressUpdateStream", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_progress_update_stream_input(ProgressUpdateStreamName = ProgressUpdateStreamName, 
        DryRun = DryRun)
    output <- create_progress_update_stream_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a progress update stream, including all of its tasks, which was previously created as an AWS resource used for access control
#'
#' Deletes a progress update stream, including all of its tasks, which was previously created as an AWS resource used for access control. This API has the following traits:
#' 
#' -   The only parameter needed for `DeleteProgressUpdateStream` is the stream name (same as a `CreateProgressUpdateStream` call).
#' 
#' -   The call will return, and a background process will asynchronously delete the stream and all of its resources (tasks, associated resources, resource attributes, created artifacts).
#' 
#' -   If the stream takes time to be deleted, it might still show up on a `ListProgressUpdateStreams` call.
#' 
#' -   `CreateProgressUpdateStream`, `ImportMigrationTask`, `NotifyMigrationTaskState`, and all Associate&#91;\*&#93; APIs realted to the tasks belonging to the stream will throw \"InvalidInputException\" if the stream of the same name is in the process of being deleted.
#' 
#' -   Once the stream and all of its resources are deleted, `CreateProgressUpdateStream` for a stream of the same name will succeed, and that stream will be an entirely new logical resource (without any resources associated with the old stream).
#'
#' @section Accepted Parameters:
#' ```
#' delete_progress_update_stream(
#'   ProgressUpdateStreamName = "string",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStreamName &#91;required&#93; The name of the ProgressUpdateStream.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
delete_progress_update_stream <- function (ProgressUpdateStreamName, 
    DryRun = NULL) 
{
    op <- new_operation(name = "DeleteProgressUpdateStream", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_progress_update_stream_input(ProgressUpdateStreamName = ProgressUpdateStreamName, 
        DryRun = DryRun)
    output <- delete_progress_update_stream_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets the migration status of an application
#'
#' Gets the migration status of an application.
#'
#' @section Accepted Parameters:
#' ```
#' describe_application_state(
#'   ApplicationId = "string"
#' )
#' ```
#'
#' @param ApplicationId &#91;required&#93; The configurationId in ADS that uniquely identifies the grouped application.
#'
#' @export
describe_application_state <- function (ApplicationId) 
{
    op <- new_operation(name = "DescribeApplicationState", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_application_state_input(ApplicationId = ApplicationId)
    output <- describe_application_state_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves a list of all attributes associated with a specific migration task
#'
#' Retrieves a list of all attributes associated with a specific migration task.
#'
#' @section Accepted Parameters:
#' ```
#' describe_migration_task(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string"
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; The identifier given to the MigrationTask.
#'
#' @export
describe_migration_task <- function (ProgressUpdateStream, MigrationTaskName) 
{
    op <- new_operation(name = "DescribeMigrationTask", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_migration_task_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName)
    output <- describe_migration_task_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Disassociates a created artifact of an AWS resource with a migration task performed by a migration tool that was previously associated
#'
#' Disassociates a created artifact of an AWS resource with a migration task performed by a migration tool that was previously associated. This API has the following traits:
#' 
#' -   A migration user can call the `DisassociateCreatedArtifacts` operation to disassociate a created AWS Artifact from a migration task.
#' 
#' -   The created artifact name must be provided in ARN (Amazon Resource Name) format which will contain information about type and region; for example: `arn:aws:ec2:us-east-1:488216288981:image/ami-6d0ba87b`.
#' 
#' -   Examples of the AWS resource behind the created artifact are, AMI\'s, EC2 instance, or RDS instance, etc.
#'
#' @section Accepted Parameters:
#' ```
#' disassociate_created_artifact(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   CreatedArtifactName = "string",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task to be disassociated with the artifact.
#' @param CreatedArtifactName &#91;required&#93; An ARN of the AWS resource related to the migration (e.g., AMI, EC2 instance, RDS instance, etc.)
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
disassociate_created_artifact <- function (ProgressUpdateStream, 
    MigrationTaskName, CreatedArtifactName, DryRun = NULL) 
{
    op <- new_operation(name = "DisassociateCreatedArtifact", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- disassociate_created_artifact_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, CreatedArtifactName = CreatedArtifactName, 
        DryRun = DryRun)
    output <- disassociate_created_artifact_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Disassociate an Application Discovery Service (ADS) discovered resource from a migration task
#'
#' Disassociate an Application Discovery Service (ADS) discovered resource from a migration task.
#'
#' @section Accepted Parameters:
#' ```
#' disassociate_discovered_resource(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   ConfigurationId = "string",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; The identifier given to the MigrationTask.
#' @param ConfigurationId &#91;required&#93; ConfigurationId of the ADS resource to be disassociated.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
disassociate_discovered_resource <- function (ProgressUpdateStream, 
    MigrationTaskName, ConfigurationId, DryRun = NULL) 
{
    op <- new_operation(name = "DisassociateDiscoveredResource", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- disassociate_discovered_resource_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, ConfigurationId = ConfigurationId, 
        DryRun = DryRun)
    output <- disassociate_discovered_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Registers a new migration task which represents a server, database, etc
#'
#' Registers a new migration task which represents a server, database, etc., being migrated to AWS by a migration tool.
#' 
#' This API is a prerequisite to calling the `NotifyMigrationTaskState` API as the migration tool must first register the migration task with Migration Hub.
#'
#' @section Accepted Parameters:
#' ```
#' import_migration_task(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
import_migration_task <- function (ProgressUpdateStream, MigrationTaskName, 
    DryRun = NULL) 
{
    op <- new_operation(name = "ImportMigrationTask", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- import_migration_task_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, DryRun = DryRun)
    output <- import_migration_task_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the created artifacts attached to a given migration task in an update stream
#'
#' Lists the created artifacts attached to a given migration task in an update stream. This API has the following traits:
#' 
#' -   Gets the list of the created artifacts while migration is taking place.
#' 
#' -   Shows the artifacts created by the migration tool that was associated by the `AssociateCreatedArtifact` API.
#' 
#' -   Lists created artifacts in a paginated interface.
#'
#' @section Accepted Parameters:
#' ```
#' list_created_artifacts(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task.
#' @param NextToken If a `NextToken` was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in `NextToken`.
#' @param MaxResults Maximum number of results to be returned per page.
#'
#' @export
list_created_artifacts <- function (ProgressUpdateStream, MigrationTaskName, 
    NextToken = NULL, MaxResults = NULL) 
{
    op <- new_operation(name = "ListCreatedArtifacts", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_created_artifacts_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, NextToken = NextToken, 
        MaxResults = MaxResults)
    output <- list_created_artifacts_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists discovered resources associated with the given MigrationTask
#'
#' Lists discovered resources associated with the given `MigrationTask`.
#'
#' @section Accepted Parameters:
#' ```
#' list_discovered_resources(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; The name of the MigrationTask.
#' @param NextToken If a `NextToken` was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in `NextToken`.
#' @param MaxResults The maximum number of results returned per page.
#'
#' @export
list_discovered_resources <- function (ProgressUpdateStream, 
    MigrationTaskName, NextToken = NULL, MaxResults = NULL) 
{
    op <- new_operation(name = "ListDiscoveredResources", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_discovered_resources_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, NextToken = NextToken, 
        MaxResults = MaxResults)
    output <- list_discovered_resources_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all, or filtered by resource name, migration tasks associated with the user account making this call
#'
#' Lists all, or filtered by resource name, migration tasks associated with the user account making this call. This API has the following traits:
#' 
#' -   Can show a summary list of the most recent migration tasks.
#' 
#' -   Can show a summary list of migration tasks associated with a given discovered resource.
#' 
#' -   Lists migration tasks in a paginated interface.
#'
#' @section Accepted Parameters:
#' ```
#' list_migration_tasks(
#'   NextToken = "string",
#'   MaxResults = 123,
#'   ResourceName = "string"
#' )
#' ```
#'
#' @param NextToken If a `NextToken` was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in `NextToken`.
#' @param MaxResults Value to specify how many results are returned per page.
#' @param ResourceName Filter migration tasks by discovered resource name.
#'
#' @export
list_migration_tasks <- function (NextToken = NULL, MaxResults = NULL, 
    ResourceName = NULL) 
{
    op <- new_operation(name = "ListMigrationTasks", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_migration_tasks_input(NextToken = NextToken, 
        MaxResults = MaxResults, ResourceName = ResourceName)
    output <- list_migration_tasks_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists progress update streams associated with the user account making this call
#'
#' Lists progress update streams associated with the user account making this call.
#'
#' @section Accepted Parameters:
#' ```
#' list_progress_update_streams(
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @param NextToken If a `NextToken` was returned by a previous call, there are more results available. To retrieve the next page of results, make the call again using the returned token in `NextToken`.
#' @param MaxResults Filter to limit the maximum number of results to list per page.
#'
#' @export
list_progress_update_streams <- function (NextToken = NULL, MaxResults = NULL) 
{
    op <- new_operation(name = "ListProgressUpdateStreams", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_progress_update_streams_input(NextToken = NextToken, 
        MaxResults = MaxResults)
    output <- list_progress_update_streams_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the migration state of an application
#'
#' Sets the migration state of an application. For a given application identified by the value passed to `ApplicationId`, its status is set or updated by passing one of three values to `Status`: `NOT_STARTED | IN_PROGRESS | COMPLETED`.
#'
#' @section Accepted Parameters:
#' ```
#' notify_application_state(
#'   ApplicationId = "string",
#'   Status = "NOT_STARTED"|"IN_PROGRESS"|"COMPLETED",
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ApplicationId &#91;required&#93; The configurationId in ADS that uniquely identifies the grouped application.
#' @param Status &#91;required&#93; Status of the application - Not Started, In-Progress, Complete.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
notify_application_state <- function (ApplicationId, Status, 
    DryRun = NULL) 
{
    op <- new_operation(name = "NotifyApplicationState", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- notify_application_state_input(ApplicationId = ApplicationId, 
        Status = Status, DryRun = DryRun)
    output <- notify_application_state_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Notifies Migration Hub of the current status, progress, or other detail regarding a migration task
#'
#' Notifies Migration Hub of the current status, progress, or other detail regarding a migration task. This API has the following traits:
#' 
#' -   Migration tools will call the `NotifyMigrationTaskState` API to share the latest progress and status.
#' 
#' -   `MigrationTaskName` is used for addressing updates to the correct target.
#' 
#' -   `ProgressUpdateStream` is used for access control and to provide a namespace for each migration tool.
#'
#' @section Accepted Parameters:
#' ```
#' notify_migration_task_state(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   Task = list(
#'     Status = "NOT_STARTED"|"IN_PROGRESS"|"FAILED"|"COMPLETED",
#'     StatusDetail = "string",
#'     ProgressPercent = 123
#'   ),
#'   UpdateDateTime = as.POSIXct("2015-01-01"),
#'   NextUpdateSeconds = 123,
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task.
#' @param Task &#91;required&#93; Information about the task\'s progress and status.
#' @param UpdateDateTime &#91;required&#93; The timestamp when the task was gathered.
#' @param NextUpdateSeconds &#91;required&#93; Number of seconds after the UpdateDateTime within which the Migration Hub can expect an update. If Migration Hub does not receive an update within the specified interval, then the migration task will be considered stale.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
notify_migration_task_state <- function (ProgressUpdateStream, 
    MigrationTaskName, Task, UpdateDateTime, NextUpdateSeconds, 
    DryRun = NULL) 
{
    op <- new_operation(name = "NotifyMigrationTaskState", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- notify_migration_task_state_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, Task = Task, UpdateDateTime = UpdateDateTime, 
        NextUpdateSeconds = NextUpdateSeconds, DryRun = DryRun)
    output <- notify_migration_task_state_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Provides identifying details of the resource being migrated so that it can be associated in the Application Discovery Service (ADS)'s repository
#'
#' Provides identifying details of the resource being migrated so that it can be associated in the Application Discovery Service (ADS)\'s repository. This association occurs asynchronously after `PutResourceAttributes` returns.
#' 
#' -   Keep in mind that subsequent calls to PutResourceAttributes will override previously stored attributes. For example, if it is first called with a MAC address, but later, it is desired to *add* an IP address, it will then be required to call it with *both* the IP and MAC addresses to prevent overiding the MAC address.
#' 
#' -   Note the instructions regarding the special use case of the [`ResourceAttributeList`](https://docs.aws.amazon.com/migrationhub/latest/ug/API_PutResourceAttributes.html#migrationhub-PutResourceAttributes-request-ResourceAttributeList) parameter when specifying any \"VM\" related value.
#' 
#' Because this is an asynchronous call, it will always return 200, whether an association occurs or not. To confirm if an association was found based on the provided details, call `ListDiscoveredResources`.
#'
#' @section Accepted Parameters:
#' ```
#' put_resource_attributes(
#'   ProgressUpdateStream = "string",
#'   MigrationTaskName = "string",
#'   ResourceAttributeList = list(
#'     list(
#'       Type = "IPV4_ADDRESS"|"IPV6_ADDRESS"|"MAC_ADDRESS"|"FQDN"|"VM_MANAGER_ID"|"VM_MANAGED_OBJECT_REFERENCE"|"VM_NAME"|"VM_PATH"|"BIOS_ID"|"MOTHERBOARD_SERIAL_NUMBER",
#'       Value = "string"
#'     )
#'   ),
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @param ProgressUpdateStream &#91;required&#93; The name of the ProgressUpdateStream.
#' @param MigrationTaskName &#91;required&#93; Unique identifier that references the migration task.
#' @param ResourceAttributeList &#91;required&#93; Information about the resource that is being migrated. This data will be used to map the task to a resource in the Application Discovery Service (ADS)\'s repository.
#' 
#' Takes the object array of `ResourceAttribute` where the `Type` field is reserved for the following values: `IPV4_ADDRESS | IPV6_ADDRESS | MAC_ADDRESS | FQDN | VM_MANAGER_ID | VM_MANAGED_OBJECT_REFERENCE | VM_NAME | VM_PATH | BIOS_ID | MOTHERBOARD_SERIAL_NUMBER` where the identifying value can be a string up to 256 characters.
#' 
#' -   If any \"VM\" related value is set for a `ResourceAttribute` object, it is required that `VM_MANAGER_ID`, as a minimum, is always set. If `VM_MANAGER_ID` is not set, then all \"VM\" fields will be discarded and \"VM\" fields will not be used for matching the migration task to a server in Application Discovery Service (ADS)\'s repository. See the [Example](https://docs.aws.amazon.com/migrationhub/latest/ug/API_PutResourceAttributes.html#API_PutResourceAttributes_Examples) section below for a use case of specifying \"VM\" related values.
#' 
#' -   If a server you are trying to match has multiple IP or MAC addresses, you should provide as many as you know in separate type/value pairs passed to the `ResourceAttributeList` parameter to maximize the chances of matching.
#' @param DryRun Optional boolean flag to indicate whether any effect should take place. Used to test if the caller has permission to make the call.
#'
#' @export
put_resource_attributes <- function (ProgressUpdateStream, MigrationTaskName, 
    ResourceAttributeList, DryRun = NULL) 
{
    op <- new_operation(name = "PutResourceAttributes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_resource_attributes_input(ProgressUpdateStream = ProgressUpdateStream, 
        MigrationTaskName = MigrationTaskName, ResourceAttributeList = ResourceAttributeList, 
        DryRun = DryRun)
    output <- put_resource_attributes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
