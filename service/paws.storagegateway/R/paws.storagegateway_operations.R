# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Activates the gateway you previously deployed on your host
#'
#' Activates the gateway you previously deployed on your host. In the activation process, you specify information such as the region you want to use for storing snapshots or tapes, the time zone for scheduled snapshots the gateway snapshot schedule window, an activation key, and a name for your gateway. The activation process also associates your gateway with your account; for more information, see UpdateGatewayInformation.
#' 
#' You must turn on the gateway VM before you can activate your gateway.
#'
#' @section Accepted Parameters:
#' ```
#' activate_gateway(
#'   ActivationKey = "string",
#'   GatewayName = "string",
#'   GatewayTimezone = "string",
#'   GatewayRegion = "string",
#'   GatewayType = "string",
#'   TapeDriveType = "string",
#'   MediumChangerType = "string"
#' )
#' ```
#'
#' @param ActivationKey &#91;required&#93; Your gateway activation key. You can obtain the activation key by sending an HTTP GET request with redirects enabled to the gateway IP address (port 80). The redirect URL returned in the response provides you the activation key for your gateway in the query string parameter `activationKey`. It may also include other activation-related parameters, however, these are merely defaults \-- the arguments you pass to the `ActivateGateway` API call determine the actual configuration of your gateway.
#' 
#' For more information, see https://docs.aws.amazon.com/storagegateway/latest/userguide/get-activation-key.html in the Storage Gateway User Guide.
#' @param GatewayName &#91;required&#93; The name you configured for your gateway.
#' @param GatewayTimezone &#91;required&#93; A value that indicates the time zone you want to set for the gateway. The time zone is of the format \"GMT-hr:mm\" or \"GMT+hr:mm\". For example, GMT-4:00 indicates the time is 4 hours behind GMT. GMT+2:00 indicates the time is 2 hours ahead of GMT. The time zone is used, for example, for scheduling snapshots and your gateway\'s maintenance schedule.
#' @param GatewayRegion &#91;required&#93; A value that indicates the region where you want to store your data. The gateway region specified must be the same region as the region in your `Host` header in the request. For more information about available regions and endpoints for AWS Storage Gateway, see [Regions and Endpoints](http://docs.aws.amazon.com/general/latest/gr/rande.html#sg_region) in the *Amazon Web Services Glossary*.
#' 
#' Valid Values: \"us-east-1\", \"us-east-2\", \"us-west-1\", \"us-west-2\", \"ca-central-1\", \"eu-west-1\", \"eu-central-1\", \"eu-west-2\", \"eu-west-3\", \"ap-northeast-1\", \"ap-northeast-2\", \"ap-southeast-1\", \"ap-southeast-2\", \"ap-south-1\", \"sa-east-1\"
#' @param GatewayType A value that defines the type of gateway to activate. The type specified is critical to all later functions of the gateway and cannot be changed after activation. The default value is `CACHED`.
#' 
#' Valid Values: \"STORED\", \"CACHED\", \"VTL\", \"FILE\_S3\"
#' @param TapeDriveType The value that indicates the type of tape drive to use for tape gateway. This field is optional.
#' 
#' Valid Values: \"IBM-ULT3580-TD5\"
#' @param MediumChangerType The value that indicates the type of medium changer to use for tape gateway. This field is optional.
#' 
#' Valid Values: \"STK-L700\", \"AWS-Gateway-VTL\"
#'
#' @examples
#' # Activates the gateway you previously deployed on your host.
#' \donttest{activate_gateway(
#'   ActivationKey = "29AV1-3OFV9-VVIUB-NKT0I-LRO6V",
#'   GatewayName = "My_Gateway",
#'   GatewayRegion = "us-east-1",
#'   GatewayTimezone = "GMT-12:00",
#'   GatewayType = "STORED",
#'   MediumChangerType = "AWS-Gateway-VTL",
#'   TapeDriveType = "IBM-ULT3580-TD5"
#' )}
#'
#' @export
activate_gateway <- function (ActivationKey, GatewayName, GatewayTimezone, 
    GatewayRegion, GatewayType = NULL, TapeDriveType = NULL, 
    MediumChangerType = NULL) 
{
    op <- new_operation(name = "ActivateGateway", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- activate_gateway_input(ActivationKey = ActivationKey, 
        GatewayName = GatewayName, GatewayTimezone = GatewayTimezone, 
        GatewayRegion = GatewayRegion, GatewayType = GatewayType, 
        TapeDriveType = TapeDriveType, MediumChangerType = MediumChangerType)
    output <- activate_gateway_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Configures one or more gateway local disks as cache for a gateway
#'
#' Configures one or more gateway local disks as cache for a gateway. This operation is only supported in the cached volume, tape and file gateway type (see [Storage Gateway Concepts](http://docs.aws.amazon.com/storagegateway/latest/userguide/StorageGatewayConcepts.html)).
#' 
#' In the request, you specify the gateway Amazon Resource Name (ARN) to which you want to add cache, and one or more disk IDs that you want to configure as cache.
#'
#' @section Accepted Parameters:
#' ```
#' add_cache(
#'   GatewayARN = "string",
#'   DiskIds = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param DiskIds &#91;required&#93; 
#'
#' @examples
#' # The following example shows a request that activates a gateway-stored
#' # volume.
#' \donttest{add_cache(
#'   DiskIds = list(
#'     "pci-0000:03:00.0-scsi-0:0:0:0",
#'     "pci-0000:03:00.0-scsi-0:0:1:0"
#'   ),
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
add_cache <- function (GatewayARN, DiskIds) 
{
    op <- new_operation(name = "AddCache", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_cache_input(GatewayARN = GatewayARN, DiskIds = DiskIds)
    output <- add_cache_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds one or more tags to the specified resource
#'
#' Adds one or more tags to the specified resource. You use tags to add metadata to resources, which you can use to categorize these resources. For example, you can categorize resources by purpose, owner, environment, or team. Each tag consists of a key and a value, which you define. You can add tags to the following AWS Storage Gateway resources:
#' 
#' -   Storage gateways of all types
#' 
#' 
#' -   Storage Volumes
#' 
#' 
#' -   Virtual Tapes
#' 
#' You can create a maximum of 10 tags for each resource. Virtual tapes and storage volumes that are recovered to a new gateway maintain their tags.
#'
#' @section Accepted Parameters:
#' ```
#' add_tags_to_resource(
#'   ResourceARN = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param ResourceARN &#91;required&#93; The Amazon Resource Name (ARN) of the resource you want to add tags to.
#' @param Tags &#91;required&#93; The key-value pair that represents the tag you want to add to the resource. The value can be an empty string.
#' 
#' Valid characters for key and value are letters, spaces, and numbers representable in UTF-8 format, and the following special characters: + - = . \_ : / @.
#'
#' @examples
#' # Adds one or more tags to the specified resource.
#' \donttest{add_tags_to_resource(
#'   ResourceARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-11A2222B",
#'   Tags = list(
#'     list(
#'       Key = "Dev Gatgeway Region",
#'       Value = "East Coast"
#'     )
#'   )
#' )}
#'
#' @export
add_tags_to_resource <- function (ResourceARN, Tags) 
{
    op <- new_operation(name = "AddTagsToResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_tags_to_resource_input(ResourceARN = ResourceARN, 
        Tags = Tags)
    output <- add_tags_to_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Configures one or more gateway local disks as upload buffer for a specified gateway
#'
#' Configures one or more gateway local disks as upload buffer for a specified gateway. This operation is supported for the stored volume, cached volume and tape gateway types.
#' 
#' In the request, you specify the gateway Amazon Resource Name (ARN) to which you want to add upload buffer, and one or more disk IDs that you want to configure as upload buffer.
#'
#' @section Accepted Parameters:
#' ```
#' add_upload_buffer(
#'   GatewayARN = "string",
#'   DiskIds = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param DiskIds &#91;required&#93; 
#'
#' @examples
#' # Configures one or more gateway local disks as upload buffer for a
#' # specified gateway.
#' \donttest{add_upload_buffer(
#'   DiskIds = list(
#'     "pci-0000:03:00.0-scsi-0:0:0:0",
#'     "pci-0000:03:00.0-scsi-0:0:1:0"
#'   ),
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
add_upload_buffer <- function (GatewayARN, DiskIds) 
{
    op <- new_operation(name = "AddUploadBuffer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_upload_buffer_input(GatewayARN = GatewayARN, 
        DiskIds = DiskIds)
    output <- add_upload_buffer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Configures one or more gateway local disks as working storage for a gateway
#'
#' Configures one or more gateway local disks as working storage for a gateway. This operation is only supported in the stored volume gateway type. This operation is deprecated in cached volume API version 20120630. Use AddUploadBuffer instead.
#' 
#' Working storage is also referred to as upload buffer. You can also use the AddUploadBuffer operation to add upload buffer to a stored volume gateway.
#' 
#' In the request, you specify the gateway Amazon Resource Name (ARN) to which you want to add working storage, and one or more disk IDs that you want to configure as working storage.
#'
#' @section Accepted Parameters:
#' ```
#' add_working_storage(
#'   GatewayARN = "string",
#'   DiskIds = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param DiskIds &#91;required&#93; An array of strings that identify disks that are to be configured as working storage. Each string have a minimum length of 1 and maximum length of 300. You can get the disk IDs from the ListLocalDisks API.
#'
#' @examples
#' # Configures one or more gateway local disks as working storage for a
#' # gateway. (Working storage is also referred to as upload buffer.)
#' \donttest{add_working_storage(
#'   DiskIds = list(
#'     "pci-0000:03:00.0-scsi-0:0:0:0",
#'     "pci-0000:03:00.0-scsi-0:0:1:0"
#'   ),
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
add_working_storage <- function (GatewayARN, DiskIds) 
{
    op <- new_operation(name = "AddWorkingStorage", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_working_storage_input(GatewayARN = GatewayARN, 
        DiskIds = DiskIds)
    output <- add_working_storage_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Connects a volume to an iSCSI connection and then attaches the volume to the specified gateway
#'
#' Connects a volume to an iSCSI connection and then attaches the volume to the specified gateway. Detaching and attaching a volume enables you to recover your data from one gateway to a different gateway without creating a snapshot. It also makes it easier to move your volumes from an on-premises gateway to a gateway hosted on an Amazon EC2 instance.
#'
#' @section Accepted Parameters:
#' ```
#' attach_volume(
#'   GatewayARN = "string",
#'   TargetName = "string",
#'   VolumeARN = "string",
#'   NetworkInterfaceId = "string",
#'   DiskId = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the gateway that you want to attach the volume to.
#' @param TargetName The name of the iSCSI target used by an initiator to connect to a volume and used as a suffix for the target ARN. For example, specifying `TargetName` as *myvolume* results in the target ARN of `arn:aws:storagegateway:us-east-2:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume`. The target name must be unique across all volumes on a gateway.
#' 
#' If you don\'t specify a value, Storage Gateway uses the value that was previously used for this volume as the new target name.
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume to attach to the specified gateway.
#' @param NetworkInterfaceId &#91;required&#93; The network interface of the gateway on which to expose the iSCSI target. Only IPv4 addresses are accepted. Use DescribeGatewayInformation to get a list of the network interfaces available on a gateway.
#' 
#' Valid Values: A valid IP address.
#' @param DiskId The unique device ID or other distinguishing data that identifies the local disk used to create the volume. This value is only required when you are attaching a stored volume.
#'
#' @export
attach_volume <- function (GatewayARN, TargetName = NULL, VolumeARN, 
    NetworkInterfaceId, DiskId = NULL) 
{
    op <- new_operation(name = "AttachVolume", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_volume_input(GatewayARN = GatewayARN, TargetName = TargetName, 
        VolumeARN = VolumeARN, NetworkInterfaceId = NetworkInterfaceId, 
        DiskId = DiskId)
    output <- attach_volume_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Cancels archiving of a virtual tape to the virtual tape shelf (VTS) after the archiving process is initiated
#'
#' Cancels archiving of a virtual tape to the virtual tape shelf (VTS) after the archiving process is initiated. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' cancel_archival(
#'   GatewayARN = "string",
#'   TapeARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape you want to cancel archiving for.
#'
#' @examples
#' # Cancels archiving of a virtual tape to the virtual tape shelf (VTS)
#' # after the archiving process is initiated.
#' \donttest{cancel_archival(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   TapeARN = "arn:aws:storagegateway:us-east-1:999999999999:tape/AMZN01A2A4"
#' )}
#'
#' @export
cancel_archival <- function (GatewayARN, TapeARN) 
{
    op <- new_operation(name = "CancelArchival", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- cancel_archival_input(GatewayARN = GatewayARN, TapeARN = TapeARN)
    output <- cancel_archival_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Cancels retrieval of a virtual tape from the virtual tape shelf (VTS) to a gateway after the retrieval process is initiated
#'
#' Cancels retrieval of a virtual tape from the virtual tape shelf (VTS) to a gateway after the retrieval process is initiated. The virtual tape is returned to the VTS. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' cancel_retrieval(
#'   GatewayARN = "string",
#'   TapeARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape you want to cancel retrieval for.
#'
#' @examples
#' # Cancels retrieval of a virtual tape from the virtual tape shelf (VTS) to
#' # a gateway after the retrieval process is initiated.
#' \donttest{cancel_retrieval(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   TapeARN = "arn:aws:storagegateway:us-east-1:999999999999:tape/AMZN01A2A4"
#' )}
#'
#' @export
cancel_retrieval <- function (GatewayARN, TapeARN) 
{
    op <- new_operation(name = "CancelRetrieval", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- cancel_retrieval_input(GatewayARN = GatewayARN, 
        TapeARN = TapeARN)
    output <- cancel_retrieval_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a cached volume on a specified cached volume gateway
#'
#' Creates a cached volume on a specified cached volume gateway. This operation is only supported in the cached volume gateway type.
#' 
#' Cache storage must be allocated to the gateway before you can create a cached volume. Use the AddCache operation to add cache storage to a gateway.
#' 
#' In the request, you must specify the gateway, size of the volume in bytes, the iSCSI target name, an IP address on which to expose the target, and a unique client token. In response, the gateway creates the volume and returns information about it. This information includes the volume Amazon Resource Name (ARN), its size, and the iSCSI target ARN that initiators can use to connect to the volume target.
#' 
#' Optionally, you can provide the ARN for an existing volume as the `SourceVolumeARN` for this cached volume, which creates an exact copy of the existing volume's latest recovery point. The `VolumeSizeInBytes` value must be equal to or larger than the size of the copied volume, in bytes.
#'
#' @section Accepted Parameters:
#' ```
#' create_cachedi_scsi_volume(
#'   GatewayARN = "string",
#'   VolumeSizeInBytes = 123,
#'   SnapshotId = "string",
#'   TargetName = "string",
#'   SourceVolumeARN = "string",
#'   NetworkInterfaceId = "string",
#'   ClientToken = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param VolumeSizeInBytes &#91;required&#93; The size of the volume in bytes.
#' @param SnapshotId The snapshot ID (e.g. \"snap-1122aabb\") of the snapshot to restore as the new cached volume. Specify this field if you want to create the iSCSI storage volume from a snapshot otherwise do not include this field. To list snapshots for your account use [DescribeSnapshots](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/ApiReference-query-DescribeSnapshots.html) in the *Amazon Elastic Compute Cloud API Reference*.
#' @param TargetName &#91;required&#93; The name of the iSCSI target used by initiators to connect to the target and as a suffix for the target ARN. For example, specifying `TargetName` as *myvolume* results in the target ARN of arn:aws:storagegateway:us-east-2:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume. The target name must be unique across all volumes of a gateway.
#' @param SourceVolumeARN The ARN for an existing volume. Specifying this ARN makes the new volume into an exact copy of the specified existing volume\'s latest recovery point. The `VolumeSizeInBytes` value for this new volume must be equal to or larger than the size of the existing volume, in bytes.
#' @param NetworkInterfaceId &#91;required&#93; The network interface of the gateway on which to expose the iSCSI target. Only IPv4 addresses are accepted. Use DescribeGatewayInformation to get a list of the network interfaces available on a gateway.
#' 
#' Valid Values: A valid IP address.
#' @param ClientToken &#91;required&#93; A unique identifier that you use to retry a request. If you retry a request, use the same `ClientToken` you specified in the initial request.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#'
#' @examples
#' # Creates a cached volume on a specified cached gateway.
#' \donttest{create_cachedi_scsi_volume(
#'   ClientToken = "cachedvol112233",
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   NetworkInterfaceId = "10.1.1.1",
#'   SnapshotId = "snap-f47b7b94",
#'   TargetName = "my-volume",
#'   VolumeSizeInBytes = 536870912000
#' )}
#'
#' @export
create_cachedi_scsi_volume <- function (GatewayARN, VolumeSizeInBytes, 
    SnapshotId = NULL, TargetName, SourceVolumeARN = NULL, NetworkInterfaceId, 
    ClientToken, KMSEncrypted = NULL, KMSKey = NULL) 
{
    op <- new_operation(name = "CreateCachediSCSIVolume", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_cachedi_scsi_volume_input(GatewayARN = GatewayARN, 
        VolumeSizeInBytes = VolumeSizeInBytes, SnapshotId = SnapshotId, 
        TargetName = TargetName, SourceVolumeARN = SourceVolumeARN, 
        NetworkInterfaceId = NetworkInterfaceId, ClientToken = ClientToken, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey)
    output <- create_cachedi_scsi_volume_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a Network File System (NFS) file share on an existing file gateway
#'
#' Creates a Network File System (NFS) file share on an existing file gateway. In Storage Gateway, a file share is a file system mount point backed by Amazon S3 cloud storage. Storage Gateway exposes file shares using a NFS interface. This operation is only supported for file gateways.
#' 
#' File gateway requires AWS Security Token Service (AWS STS) to be activated to enable you create a file share. Make sure AWS STS is activated in the region you are creating your file gateway in. If AWS STS is not activated in the region, activate it. For information about how to activate AWS STS, see Activating and Deactivating AWS STS in an AWS Region in the AWS Identity and Access Management User Guide.
#' 
#' File gateway does not support creating hard or symbolic links on a file share.
#'
#' @section Accepted Parameters:
#' ```
#' create_nfs_file_share(
#'   ClientToken = "string",
#'   NFSFileShareDefaults = list(
#'     FileMode = "string",
#'     DirectoryMode = "string",
#'     GroupId = 123,
#'     OwnerId = 123
#'   ),
#'   GatewayARN = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string",
#'   Role = "string",
#'   LocationARN = "string",
#'   DefaultStorageClass = "string",
#'   ObjectACL = "private"|"public-read"|"public-read-write"|"authenticated-read"|"bucket-owner-read"|"bucket-owner-full-control"|"aws-exec-read",
#'   ClientList = list(
#'     "string"
#'   ),
#'   Squash = "string",
#'   ReadOnly = TRUE|FALSE,
#'   GuessMIMETypeEnabled = TRUE|FALSE,
#'   RequesterPays = TRUE|FALSE
#' )
#' ```
#'
#' @param ClientToken &#91;required&#93; A unique string value that you supply that is used by file gateway to ensure idempotent file share creation.
#' @param NFSFileShareDefaults File share default values. Optional.
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the file gateway on which you want to create a file share.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#' @param Role &#91;required&#93; The ARN of the AWS Identity and Access Management (IAM) role that a file gateway assumes when it accesses the underlying storage.
#' @param LocationARN &#91;required&#93; The ARN of the backed storage used for storing file data.
#' @param DefaultStorageClass The default storage class for objects put into an Amazon S3 bucket by the file gateway. Possible values are `S3_STANDARD`, `S3_STANDARD_IA`, or `S3_ONEZONE_IA`. If this field is not populated, the default value `S3_STANDARD` is used. Optional.
#' @param ObjectACL A value that sets the access control list permission for objects in the S3 bucket that a file gateway puts objects into. The default value is \"private\".
#' @param ClientList The list of clients that are allowed to access the file gateway. The list must contain either valid IP addresses or valid CIDR blocks.
#' @param Squash Maps a user to anonymous user. Valid options are the following:
#' 
#' -   `RootSquash` - Only root is mapped to anonymous user.
#' 
#' -   `NoSquash` - No one is mapped to anonymous user
#' 
#' -   `AllSquash` - Everyone is mapped to anonymous user.
#' @param ReadOnly A value that sets the write status of a file share. This value is true if the write status is read-only, and otherwise false.
#' @param GuessMIMETypeEnabled A value that enables guessing of the MIME type for uploaded objects based on file extensions. Set this value to true to enable MIME type guessing, and otherwise to false. The default value is true.
#' @param RequesterPays A value that sets the access control list permission for objects in the Amazon S3 bucket that a file gateway puts objects into. The default value is `private`.
#'
#' @export
create_nfs_file_share <- function (ClientToken, NFSFileShareDefaults = NULL, 
    GatewayARN, KMSEncrypted = NULL, KMSKey = NULL, Role, LocationARN, 
    DefaultStorageClass = NULL, ObjectACL = NULL, ClientList = NULL, 
    Squash = NULL, ReadOnly = NULL, GuessMIMETypeEnabled = NULL, 
    RequesterPays = NULL) 
{
    op <- new_operation(name = "CreateNFSFileShare", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_nfs_file_share_input(ClientToken = ClientToken, 
        NFSFileShareDefaults = NFSFileShareDefaults, GatewayARN = GatewayARN, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey, Role = Role, 
        LocationARN = LocationARN, DefaultStorageClass = DefaultStorageClass, 
        ObjectACL = ObjectACL, ClientList = ClientList, Squash = Squash, 
        ReadOnly = ReadOnly, GuessMIMETypeEnabled = GuessMIMETypeEnabled, 
        RequesterPays = RequesterPays)
    output <- create_nfs_file_share_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a Server Message Block (SMB) file share on an existing file gateway
#'
#' Creates a Server Message Block (SMB) file share on an existing file gateway. In Storage Gateway, a file share is a file system mount point backed by Amazon S3 cloud storage. Storage Gateway expose file shares using a SMB interface. This operation is only supported for file gateways.
#' 
#' File gateways require AWS Security Token Service (AWS STS) to be activated to enable you to create a file share. Make sure that AWS STS is activated in the AWS Region you are creating your file gateway in. If AWS STS is not activated in this AWS Region, activate it. For information about how to activate AWS STS, see [Activating and Deactivating AWS STS in an AWS Region](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_enable-regions.html) in the *AWS Identity and Access Management User Guide.*
#' 
#' File gateways don\'t support creating hard or symbolic links on a file share.
#'
#' @section Accepted Parameters:
#' ```
#' create_smb_file_share(
#'   ClientToken = "string",
#'   GatewayARN = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string",
#'   Role = "string",
#'   LocationARN = "string",
#'   DefaultStorageClass = "string",
#'   ObjectACL = "private"|"public-read"|"public-read-write"|"authenticated-read"|"bucket-owner-read"|"bucket-owner-full-control"|"aws-exec-read",
#'   ReadOnly = TRUE|FALSE,
#'   GuessMIMETypeEnabled = TRUE|FALSE,
#'   RequesterPays = TRUE|FALSE,
#'   ValidUserList = list(
#'     "string"
#'   ),
#'   InvalidUserList = list(
#'     "string"
#'   ),
#'   Authentication = "string"
#' )
#' ```
#'
#' @param ClientToken &#91;required&#93; A unique string value that you supply that is used by file gateway to ensure idempotent file share creation.
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the file gateway on which you want to create a file share.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#' @param Role &#91;required&#93; The ARN of the AWS Identity and Access Management (IAM) role that a file gateway assumes when it accesses the underlying storage.
#' @param LocationARN &#91;required&#93; The ARN of the backed storage used for storing file data.
#' @param DefaultStorageClass The default storage class for objects put into an Amazon S3 bucket by the file gateway. Possible values are `S3_STANDARD`, `S3_STANDARD_IA`, or `S3_ONEZONE_IA`. If this field is not populated, the default value `S3_STANDARD` is used. Optional.
#' @param ObjectACL A value that sets the access control list permission for objects in the S3 bucket that a file gateway puts objects into. The default value is \"private\".
#' @param ReadOnly A value that sets the write status of a file share. This value is true if the write status is read-only, and otherwise false.
#' @param GuessMIMETypeEnabled A value that enables guessing of the MIME type for uploaded objects based on file extensions. Set this value to true to enable MIME type guessing, and otherwise to false. The default value is true.
#' @param RequesterPays A value that sets the access control list permission for objects in the Amazon S3 bucket that a file gateway puts objects into. The default value is `private`.
#' @param ValidUserList A list of users or groups in the Active Directory that are allowed to access the file share. A group must be prefixed with the @ character. For example `@group1`. Can only be set if Authentication is set to `ActiveDirectory`.
#' @param InvalidUserList A list of users or groups in the Active Directory that are not allowed to access the file share. A group must be prefixed with the @ character. For example `@group1`. Can only be set if Authentication is set to `ActiveDirectory`.
#' @param Authentication The authentication method that users use to access the file share.
#' 
#' Valid values are `ActiveDirectory` or `GuestAccess`. The default is `ActiveDirectory`.
#'
#' @export
create_smb_file_share <- function (ClientToken, GatewayARN, KMSEncrypted = NULL, 
    KMSKey = NULL, Role, LocationARN, DefaultStorageClass = NULL, 
    ObjectACL = NULL, ReadOnly = NULL, GuessMIMETypeEnabled = NULL, 
    RequesterPays = NULL, ValidUserList = NULL, InvalidUserList = NULL, 
    Authentication = NULL) 
{
    op <- new_operation(name = "CreateSMBFileShare", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_smb_file_share_input(ClientToken = ClientToken, 
        GatewayARN = GatewayARN, KMSEncrypted = KMSEncrypted, 
        KMSKey = KMSKey, Role = Role, LocationARN = LocationARN, 
        DefaultStorageClass = DefaultStorageClass, ObjectACL = ObjectACL, 
        ReadOnly = ReadOnly, GuessMIMETypeEnabled = GuessMIMETypeEnabled, 
        RequesterPays = RequesterPays, ValidUserList = ValidUserList, 
        InvalidUserList = InvalidUserList, Authentication = Authentication)
    output <- create_smb_file_share_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Initiates a snapshot of a volume
#'
#' Initiates a snapshot of a volume.
#' 
#' AWS Storage Gateway provides the ability to back up point-in-time snapshots of your data to Amazon Simple Storage (S3) for durable off-site recovery, as well as import the data to an Amazon Elastic Block Store (EBS) volume in Amazon Elastic Compute Cloud (EC2). You can take snapshots of your gateway volume on a scheduled or ad-hoc basis. This API enables you to take ad-hoc snapshot. For more information, see [Editing a Snapshot Schedule](http://docs.aws.amazon.com/storagegateway/latest/userguide/managing-volumes.html#SchedulingSnapshot).
#' 
#' In the CreateSnapshot request you identify the volume by providing its Amazon Resource Name (ARN). You must also provide description for the snapshot. When AWS Storage Gateway takes the snapshot of specified volume, the snapshot and description appears in the AWS Storage Gateway Console. In response, AWS Storage Gateway returns you a snapshot ID. You can use this snapshot ID to check the snapshot progress or later use it when you want to create a volume from a snapshot. This operation is only supported in stored and cached volume gateway type.
#' 
#' To list or delete a snapshot, you must use the Amazon EC2 API. For more information, see DescribeSnapshots or DeleteSnapshot in the [EC2 API reference](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_Operations.html).
#' 
#' Volume and snapshot IDs are changing to a longer length ID format. For more information, see the important note on the [Welcome](http://docs.aws.amazon.com/storagegateway/latest/APIReference/Welcome.html) page.
#'
#' @section Accepted Parameters:
#' ```
#' create_snapshot(
#'   VolumeARN = "string",
#'   SnapshotDescription = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume. Use the ListVolumes operation to return a list of gateway volumes.
#' @param SnapshotDescription &#91;required&#93; Textual description of the snapshot that appears in the Amazon EC2 console, Elastic Block Store snapshots panel in the **Description** field, and in the AWS Storage Gateway snapshot **Details** pane, **Description** field
#'
#' @examples
#' # Initiates an ad-hoc snapshot of a gateway volume.
#' \donttest{create_snapshot(
#'   SnapshotDescription = "My root volume snapshot as of 10/03/2017",
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
create_snapshot <- function (VolumeARN, SnapshotDescription) 
{
    op <- new_operation(name = "CreateSnapshot", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_snapshot_input(VolumeARN = VolumeARN, SnapshotDescription = SnapshotDescription)
    output <- create_snapshot_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Initiates a snapshot of a gateway from a volume recovery point
#'
#' Initiates a snapshot of a gateway from a volume recovery point. This operation is only supported in the cached volume gateway type.
#' 
#' A volume recovery point is a point in time at which all data of the volume is consistent and from which you can create a snapshot. To get a list of volume recovery point for cached volume gateway, use ListVolumeRecoveryPoints.
#' 
#' In the `CreateSnapshotFromVolumeRecoveryPoint` request, you identify the volume by providing its Amazon Resource Name (ARN). You must also provide a description for the snapshot. When the gateway takes a snapshot of the specified volume, the snapshot and its description appear in the AWS Storage Gateway console. In response, the gateway returns you a snapshot ID. You can use this snapshot ID to check the snapshot progress or later use it when you want to create a volume from a snapshot.
#' 
#' To list or delete a snapshot, you must use the Amazon EC2 API. For more information, in *Amazon Elastic Compute Cloud API Reference*.
#'
#' @section Accepted Parameters:
#' ```
#' create_snapshot_from_volume_recovery_point(
#'   VolumeARN = "string",
#'   SnapshotDescription = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; 
#' @param SnapshotDescription &#91;required&#93; 
#'
#' @examples
#' # Initiates a snapshot of a gateway from a volume recovery point.
#' \donttest{create_snapshot_from_volume_recovery_point(
#'   SnapshotDescription = "My root volume snapshot as of 2017-06-30T10:10:10.000Z",
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
create_snapshot_from_volume_recovery_point <- function (VolumeARN, 
    SnapshotDescription) 
{
    op <- new_operation(name = "CreateSnapshotFromVolumeRecoveryPoint", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_snapshot_from_volume_recovery_point_input(VolumeARN = VolumeARN, 
        SnapshotDescription = SnapshotDescription)
    output <- create_snapshot_from_volume_recovery_point_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a volume on a specified gateway
#'
#' Creates a volume on a specified gateway. This operation is only supported in the stored volume gateway type.
#' 
#' The size of the volume to create is inferred from the disk size. You can choose to preserve existing data on the disk, create volume from an existing snapshot, or create an empty volume. If you choose to create an empty gateway volume, then any existing data on the disk is erased.
#' 
#' In the request you must specify the gateway and the disk information on which you are creating the volume. In response, the gateway creates the volume and returns volume information such as the volume Amazon Resource Name (ARN), its size, and the iSCSI target ARN that initiators can use to connect to the volume target.
#'
#' @section Accepted Parameters:
#' ```
#' create_storedi_scsi_volume(
#'   GatewayARN = "string",
#'   DiskId = "string",
#'   SnapshotId = "string",
#'   PreserveExistingData = TRUE|FALSE,
#'   TargetName = "string",
#'   NetworkInterfaceId = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param DiskId &#91;required&#93; The unique identifier for the gateway local disk that is configured as a stored volume. Use [ListLocalDisks](http://docs.aws.amazon.com/storagegateway/latest/userguide/API_ListLocalDisks.html) to list disk IDs for a gateway.
#' @param SnapshotId The snapshot ID (e.g. \"snap-1122aabb\") of the snapshot to restore as the new stored volume. Specify this field if you want to create the iSCSI storage volume from a snapshot otherwise do not include this field. To list snapshots for your account use [DescribeSnapshots](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/ApiReference-query-DescribeSnapshots.html) in the *Amazon Elastic Compute Cloud API Reference*.
#' @param PreserveExistingData &#91;required&#93; Specify this field as true if you want to preserve the data on the local disk. Otherwise, specifying this field as false creates an empty volume.
#' 
#' Valid Values: true, false
#' @param TargetName &#91;required&#93; The name of the iSCSI target used by initiators to connect to the target and as a suffix for the target ARN. For example, specifying `TargetName` as *myvolume* results in the target ARN of arn:aws:storagegateway:us-east-2:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume. The target name must be unique across all volumes of a gateway.
#' @param NetworkInterfaceId &#91;required&#93; The network interface of the gateway on which to expose the iSCSI target. Only IPv4 addresses are accepted. Use DescribeGatewayInformation to get a list of the network interfaces available on a gateway.
#' 
#' Valid Values: A valid IP address.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#'
#' @examples
#' # Creates a stored volume on a specified stored gateway.
#' \donttest{create_storedi_scsi_volume(
#'   DiskId = "pci-0000:03:00.0-scsi-0:0:0:0",
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   NetworkInterfaceId = "10.1.1.1",
#'   PreserveExistingData = TRUE,
#'   SnapshotId = "snap-f47b7b94",
#'   TargetName = "my-volume"
#' )}
#'
#' @export
create_storedi_scsi_volume <- function (GatewayARN, DiskId, SnapshotId = NULL, 
    PreserveExistingData, TargetName, NetworkInterfaceId, KMSEncrypted = NULL, 
    KMSKey = NULL) 
{
    op <- new_operation(name = "CreateStorediSCSIVolume", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_storedi_scsi_volume_input(GatewayARN = GatewayARN, 
        DiskId = DiskId, SnapshotId = SnapshotId, PreserveExistingData = PreserveExistingData, 
        TargetName = TargetName, NetworkInterfaceId = NetworkInterfaceId, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey)
    output <- create_storedi_scsi_volume_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a virtual tape by using your own barcode
#'
#' Creates a virtual tape by using your own barcode. You write data to the virtual tape and then archive the tape. A barcode is unique and can not be reused if it has already been used on a tape . This applies to barcodes used on deleted tapes. This operation is only supported in the tape gateway type.
#' 
#' Cache storage must be allocated to the gateway before you can create a virtual tape. Use the AddCache operation to add cache storage to a gateway.
#'
#' @section Accepted Parameters:
#' ```
#' create_tape_with_barcode(
#'   GatewayARN = "string",
#'   TapeSizeInBytes = 123,
#'   TapeBarcode = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The unique Amazon Resource Name (ARN) that represents the gateway to associate the virtual tape with. Use the ListGateways operation to return a list of gateways for your account and region.
#' @param TapeSizeInBytes &#91;required&#93; The size, in bytes, of the virtual tape that you want to create.
#' 
#' The size must be aligned by gigabyte (1024\*1024\*1024 byte).
#' @param TapeBarcode &#91;required&#93; The barcode that you want to assign to the tape.
#' 
#' Barcodes cannot be reused. This includes barcodes used for tapes that have been deleted.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS Key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#'
#' @examples
#' # Creates a virtual tape by using your own barcode.
#' \donttest{create_tape_with_barcode(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   TapeBarcode = "TEST12345",
#'   TapeSizeInBytes = 107374182400
#' )}
#'
#' @export
create_tape_with_barcode <- function (GatewayARN, TapeSizeInBytes, 
    TapeBarcode, KMSEncrypted = NULL, KMSKey = NULL) 
{
    op <- new_operation(name = "CreateTapeWithBarcode", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_tape_with_barcode_input(GatewayARN = GatewayARN, 
        TapeSizeInBytes = TapeSizeInBytes, TapeBarcode = TapeBarcode, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey)
    output <- create_tape_with_barcode_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates one or more virtual tapes
#'
#' Creates one or more virtual tapes. You write data to the virtual tapes and then archive the tapes. This operation is only supported in the tape gateway type.
#' 
#' Cache storage must be allocated to the gateway before you can create virtual tapes. Use the AddCache operation to add cache storage to a gateway.
#'
#' @section Accepted Parameters:
#' ```
#' create_tapes(
#'   GatewayARN = "string",
#'   TapeSizeInBytes = 123,
#'   ClientToken = "string",
#'   NumTapesToCreate = 123,
#'   TapeBarcodePrefix = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The unique Amazon Resource Name (ARN) that represents the gateway to associate the virtual tapes with. Use the ListGateways operation to return a list of gateways for your account and region.
#' @param TapeSizeInBytes &#91;required&#93; The size, in bytes, of the virtual tapes that you want to create.
#' 
#' The size must be aligned by gigabyte (1024\*1024\*1024 byte).
#' @param ClientToken &#91;required&#93; A unique identifier that you use to retry a request. If you retry a request, use the same `ClientToken` you specified in the initial request.
#' 
#' Using the same `ClientToken` prevents creating the tape multiple times.
#' @param NumTapesToCreate &#91;required&#93; The number of virtual tapes that you want to create.
#' @param TapeBarcodePrefix &#91;required&#93; A prefix that you append to the barcode of the virtual tape you are creating. This prefix makes the barcode unique.
#' 
#' The prefix must be 1 to 4 characters in length and must be one of the uppercase letters from A to Z.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#'
#' @examples
#' # Creates one or more virtual tapes.
#' \donttest{create_tapes(
#'   ClientToken = "77777",
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   NumTapesToCreate = 3L,
#'   TapeBarcodePrefix = "TEST",
#'   TapeSizeInBytes = 107374182400
#' )}
#'
#' @export
create_tapes <- function (GatewayARN, TapeSizeInBytes, ClientToken, 
    NumTapesToCreate, TapeBarcodePrefix, KMSEncrypted = NULL, 
    KMSKey = NULL) 
{
    op <- new_operation(name = "CreateTapes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_tapes_input(GatewayARN = GatewayARN, TapeSizeInBytes = TapeSizeInBytes, 
        ClientToken = ClientToken, NumTapesToCreate = NumTapesToCreate, 
        TapeBarcodePrefix = TapeBarcodePrefix, KMSEncrypted = KMSEncrypted, 
        KMSKey = KMSKey)
    output <- create_tapes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the bandwidth rate limits of a gateway
#'
#' Deletes the bandwidth rate limits of a gateway. You can delete either the upload and download bandwidth rate limit, or you can delete both. If you delete only one of the limits, the other limit remains unchanged. To specify which gateway to work with, use the Amazon Resource Name (ARN) of the gateway in your request.
#'
#' @section Accepted Parameters:
#' ```
#' delete_bandwidth_rate_limit(
#'   GatewayARN = "string",
#'   BandwidthType = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param BandwidthType &#91;required&#93; One of the BandwidthType values that indicates the gateway bandwidth rate limit to delete.
#' 
#' Valid Values: `Upload`, `Download`, `All`.
#'
#' @examples
#' # Deletes the bandwidth rate limits of a gateway; either the upload or
#' # download limit, or both.
#' \donttest{delete_bandwidth_rate_limit(
#'   BandwidthType = "All",
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
delete_bandwidth_rate_limit <- function (GatewayARN, BandwidthType) 
{
    op <- new_operation(name = "DeleteBandwidthRateLimit", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_bandwidth_rate_limit_input(GatewayARN = GatewayARN, 
        BandwidthType = BandwidthType)
    output <- delete_bandwidth_rate_limit_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes Challenge-Handshake Authentication Protocol (CHAP) credentials for a specified iSCSI target and initiator pair
#'
#' Deletes Challenge-Handshake Authentication Protocol (CHAP) credentials for a specified iSCSI target and initiator pair.
#'
#' @section Accepted Parameters:
#' ```
#' delete_chap_credentials(
#'   TargetARN = "string",
#'   InitiatorName = "string"
#' )
#' ```
#'
#' @param TargetARN &#91;required&#93; The Amazon Resource Name (ARN) of the iSCSI volume target. Use the DescribeStorediSCSIVolumes operation to return to retrieve the TargetARN for specified VolumeARN.
#' @param InitiatorName &#91;required&#93; The iSCSI initiator that connects to the target.
#'
#' @examples
#' # Deletes Challenge-Handshake Authentication Protocol (CHAP) credentials
#' # for a specified iSCSI target and initiator pair.
#' \donttest{delete_chap_credentials(
#'   InitiatorName = "iqn.1991-05.com.microsoft:computername.domain.example.com",
#'   TargetARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume"
#' )}
#'
#' @export
delete_chap_credentials <- function (TargetARN, InitiatorName) 
{
    op <- new_operation(name = "DeleteChapCredentials", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_chap_credentials_input(TargetARN = TargetARN, 
        InitiatorName = InitiatorName)
    output <- delete_chap_credentials_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a file share from a file gateway
#'
#' Deletes a file share from a file gateway. This operation is only supported for file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' delete_file_share(
#'   FileShareARN = "string",
#'   ForceDelete = TRUE|FALSE
#' )
#' ```
#'
#' @param FileShareARN &#91;required&#93; The Amazon Resource Name (ARN) of the file share to be deleted.
#' @param ForceDelete If this value is set to true, the operation deletes a file share immediately and aborts all data uploads to AWS. Otherwise, the file share is not deleted until all data is uploaded to AWS. This process aborts the data upload process, and the file share enters the FORCE\_DELETING status.
#'
#' @export
delete_file_share <- function (FileShareARN, ForceDelete = NULL) 
{
    op <- new_operation(name = "DeleteFileShare", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_file_share_input(FileShareARN = FileShareARN, 
        ForceDelete = ForceDelete)
    output <- delete_file_share_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a gateway
#'
#' Deletes a gateway. To specify which gateway to delete, use the Amazon Resource Name (ARN) of the gateway in your request. The operation deletes the gateway; however, it does not delete the gateway virtual machine (VM) from your host computer.
#' 
#' After you delete a gateway, you cannot reactivate it. Completed snapshots of the gateway volumes are not deleted upon deleting the gateway, however, pending snapshots will not complete. After you delete a gateway, your next step is to remove it from your environment.
#' 
#' You no longer pay software charges after the gateway is deleted; however, your existing Amazon EBS snapshots persist and you will continue to be billed for these snapshots. You can choose to remove all remaining Amazon EBS snapshots by canceling your Amazon EC2 subscription.  If you prefer not to cancel your Amazon EC2 subscription, you can delete your snapshots using the Amazon EC2 console. For more information, see the [AWS Storage Gateway Detail Page](http://aws.amazon.com/storagegateway).
#'
#' @section Accepted Parameters:
#' ```
#' delete_gateway(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # This operation deletes the gateway, but not the gateway's VM from the
#' # host computer.
#' \donttest{delete_gateway(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
delete_gateway <- function (GatewayARN) 
{
    op <- new_operation(name = "DeleteGateway", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_gateway_input(GatewayARN = GatewayARN)
    output <- delete_gateway_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a snapshot of a volume
#'
#' Deletes a snapshot of a volume.
#' 
#' You can take snapshots of your gateway volumes on a scheduled or ad hoc basis. This API action enables you to delete a snapshot schedule for a volume. For more information, see [Working with Snapshots](http://docs.aws.amazon.com/storagegateway/latest/userguide/WorkingWithSnapshots.html). In the `DeleteSnapshotSchedule` request, you identify the volume by providing its Amazon Resource Name (ARN). This operation is only supported in stored and cached volume gateway types.
#' 
#' To list or delete a snapshot, you must use the Amazon EC2 API. in *Amazon Elastic Compute Cloud API Reference*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_snapshot_schedule(
#'   VolumeARN = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; 
#'
#' @examples
#' # This action enables you to delete a snapshot schedule for a volume.
#' \donttest{delete_snapshot_schedule(
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
delete_snapshot_schedule <- function (VolumeARN) 
{
    op <- new_operation(name = "DeleteSnapshotSchedule", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_snapshot_schedule_input(VolumeARN = VolumeARN)
    output <- delete_snapshot_schedule_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified virtual tape
#'
#' Deletes the specified virtual tape. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' delete_tape(
#'   GatewayARN = "string",
#'   TapeARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The unique Amazon Resource Name (ARN) of the gateway that the virtual tape to delete is associated with. Use the ListGateways operation to return a list of gateways for your account and region.
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape to delete.
#'
#' @examples
#' # This example deletes the specified virtual tape.
#' \donttest{delete_tape(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:204469490176:gateway/sgw-12A3456B",
#'   TapeARN = "arn:aws:storagegateway:us-east-1:204469490176:tape/TEST05A2A0"
#' )}
#'
#' @export
delete_tape <- function (GatewayARN, TapeARN) 
{
    op <- new_operation(name = "DeleteTape", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_tape_input(GatewayARN = GatewayARN, TapeARN = TapeARN)
    output <- delete_tape_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified virtual tape from the virtual tape shelf (VTS)
#'
#' Deletes the specified virtual tape from the virtual tape shelf (VTS). This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' delete_tape_archive(
#'   TapeARN = "string"
#' )
#' ```
#'
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape to delete from the virtual tape shelf (VTS).
#'
#' @examples
#' # Deletes the specified virtual tape from the virtual tape shelf (VTS).
#' \donttest{delete_tape_archive(
#'   TapeARN = "arn:aws:storagegateway:us-east-1:204469490176:tape/TEST05A2A0"
#' )}
#'
#' @export
delete_tape_archive <- function (TapeARN) 
{
    op <- new_operation(name = "DeleteTapeArchive", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_tape_archive_input(TapeARN = TapeARN)
    output <- delete_tape_archive_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified storage volume that you previously created using the CreateCachediSCSIVolume or CreateStorediSCSIVolume API
#'
#' Deletes the specified storage volume that you previously created using the CreateCachediSCSIVolume or CreateStorediSCSIVolume API. This operation is only supported in the cached volume and stored volume types. For stored volume gateways, the local disk that was configured as the storage volume is not deleted. You can reuse the local disk to create another storage volume.
#' 
#' Before you delete a volume, make sure there are no iSCSI connections to the volume you are deleting. You should also make sure there is no snapshot in progress. You can use the Amazon Elastic Compute Cloud (Amazon EC2) API to query snapshots on the volume you are deleting and check the snapshot status. For more information, go to [DescribeSnapshots](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/ApiReference-query-DescribeSnapshots.html) in the *Amazon Elastic Compute Cloud API Reference*.
#' 
#' In the request, you must provide the Amazon Resource Name (ARN) of the storage volume you want to delete.
#'
#' @section Accepted Parameters:
#' ```
#' delete_volume(
#'   VolumeARN = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume. Use the ListVolumes operation to return a list of gateway volumes.
#'
#' @examples
#' # Deletes the specified gateway volume that you previously created using
#' # the CreateCachediSCSIVolume or CreateStorediSCSIVolume API.
#' \donttest{delete_volume(
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
delete_volume <- function (VolumeARN) 
{
    op <- new_operation(name = "DeleteVolume", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_volume_input(VolumeARN = VolumeARN)
    output <- delete_volume_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the bandwidth rate limits of a gateway
#'
#' Returns the bandwidth rate limits of a gateway. By default, these limits are not set, which means no bandwidth rate limiting is in effect.
#' 
#' This operation only returns a value for a bandwidth rate limit only if the limit is set. If no limits are set for the gateway, then this operation returns only the gateway ARN in the response body. To specify which gateway to describe, use the Amazon Resource Name (ARN) of the gateway in your request.
#'
#' @section Accepted Parameters:
#' ```
#' describe_bandwidth_rate_limit(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Returns a value for a bandwidth rate limit if set. If not set, then only
#' # the gateway ARN is returned.
#' \donttest{describe_bandwidth_rate_limit(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_bandwidth_rate_limit <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeBandwidthRateLimit", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_bandwidth_rate_limit_input(GatewayARN = GatewayARN)
    output <- describe_bandwidth_rate_limit_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the cache of a gateway
#'
#' Returns information about the cache of a gateway. This operation is only supported in the cached volume, tape and file gateway types.
#' 
#' The response includes disk IDs that are configured as cache, and it includes the amount of cache allocated and used.
#'
#' @section Accepted Parameters:
#' ```
#' describe_cache(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Returns information about the cache of a gateway.
#' \donttest{describe_cache(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_cache <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeCache", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_cache_input(GatewayARN = GatewayARN)
    output <- describe_cache_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a description of the gateway volumes specified in the request
#'
#' Returns a description of the gateway volumes specified in the request. This operation is only supported in the cached volume gateway types.
#' 
#' The list of gateway volumes in the request must be from one gateway. In the response Amazon Storage Gateway returns volume information sorted by volume Amazon Resource Name (ARN).
#'
#' @section Accepted Parameters:
#' ```
#' describe_cachedi_scsi_volumes(
#'   VolumeARNs = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param VolumeARNs &#91;required&#93; 
#'
#' @examples
#' # Returns a description of the gateway cached iSCSI volumes specified in
#' # the request.
#' \donttest{describe_cachedi_scsi_volumes(
#'   VolumeARNs = list(
#'     "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#'   )
#' )}
#'
#' @export
describe_cachedi_scsi_volumes <- function (VolumeARNs) 
{
    op <- new_operation(name = "DescribeCachediSCSIVolumes", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_cachedi_scsi_volumes_input(VolumeARNs = VolumeARNs)
    output <- describe_cachedi_scsi_volumes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns an array of Challenge-Handshake Authentication Protocol (CHAP) credentials information for a specified iSCSI target, one for each target-initiator pair
#'
#' Returns an array of Challenge-Handshake Authentication Protocol (CHAP) credentials information for a specified iSCSI target, one for each target-initiator pair.
#'
#' @section Accepted Parameters:
#' ```
#' describe_chap_credentials(
#'   TargetARN = "string"
#' )
#' ```
#'
#' @param TargetARN &#91;required&#93; The Amazon Resource Name (ARN) of the iSCSI volume target. Use the DescribeStorediSCSIVolumes operation to return to retrieve the TargetARN for specified VolumeARN.
#'
#' @examples
#' # Returns an array of Challenge-Handshake Authentication Protocol (CHAP)
#' # credentials information for a specified iSCSI target, one for each
#' # target-initiator pair.
#' \donttest{describe_chap_credentials(
#'   TargetARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume"
#' )}
#'
#' @export
describe_chap_credentials <- function (TargetARN) 
{
    op <- new_operation(name = "DescribeChapCredentials", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_chap_credentials_input(TargetARN = TargetARN)
    output <- describe_chap_credentials_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns metadata about a gateway such as its name, network interfaces, configured time zone, and the state (whether the gateway is running or not)
#'
#' Returns metadata about a gateway such as its name, network interfaces, configured time zone, and the state (whether the gateway is running or not). To specify which gateway to describe, use the Amazon Resource Name (ARN) of the gateway in your request.
#'
#' @section Accepted Parameters:
#' ```
#' describe_gateway_information(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Returns metadata about a gateway such as its name, network interfaces,
#' # configured time zone, and the state (whether the gateway is running or
#' # not).
#' \donttest{describe_gateway_information(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_gateway_information <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeGatewayInformation", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_gateway_information_input(GatewayARN = GatewayARN)
    output <- describe_gateway_information_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns your gateway's weekly maintenance start time including the day and time of the week
#'
#' Returns your gateway\'s weekly maintenance start time including the day and time of the week. Note that values are in terms of the gateway\'s time zone.
#'
#' @section Accepted Parameters:
#' ```
#' describe_maintenance_start_time(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Returns your gateway's weekly maintenance start time including the day
#' # and time of the week.
#' \donttest{describe_maintenance_start_time(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_maintenance_start_time <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeMaintenanceStartTime", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_maintenance_start_time_input(GatewayARN = GatewayARN)
    output <- describe_maintenance_start_time_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a description for one or more Network File System (NFS) file shares from a file gateway
#'
#' Gets a description for one or more Network File System (NFS) file shares from a file gateway. This operation is only supported for file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' describe_nfs_file_shares(
#'   FileShareARNList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param FileShareARNList &#91;required&#93; An array containing the Amazon Resource Name (ARN) of each file share to be described.
#'
#' @export
describe_nfs_file_shares <- function (FileShareARNList) 
{
    op <- new_operation(name = "DescribeNFSFileShares", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_nfs_file_shares_input(FileShareARNList = FileShareARNList)
    output <- describe_nfs_file_shares_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a description for one or more Server Message Block (SMB) file shares from a file gateway
#'
#' Gets a description for one or more Server Message Block (SMB) file shares from a file gateway. This operation is only supported for file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' describe_smb_file_shares(
#'   FileShareARNList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param FileShareARNList &#91;required&#93; An array containing the Amazon Resource Name (ARN) of each file share to be described.
#'
#' @export
describe_smb_file_shares <- function (FileShareARNList) 
{
    op <- new_operation(name = "DescribeSMBFileShares", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_smb_file_shares_input(FileShareARNList = FileShareARNList)
    output <- describe_smb_file_shares_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a description of a Server Message Block (SMB) file share settings from a file gateway
#'
#' Gets a description of a Server Message Block (SMB) file share settings from a file gateway. This operation is only supported for file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' describe_smb_settings(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @export
describe_smb_settings <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeSMBSettings", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_smb_settings_input(GatewayARN = GatewayARN)
    output <- describe_smb_settings_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Describes the snapshot schedule for the specified gateway volume
#'
#' Describes the snapshot schedule for the specified gateway volume. The snapshot schedule information includes intervals at which snapshots are automatically initiated on the volume. This operation is only supported in the cached volume and stored volume types.
#'
#' @section Accepted Parameters:
#' ```
#' describe_snapshot_schedule(
#'   VolumeARN = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume. Use the ListVolumes operation to return a list of gateway volumes.
#'
#' @examples
#' # Describes the snapshot schedule for the specified gateway volume
#' # including intervals at which snapshots are automatically initiated.
#' \donttest{describe_snapshot_schedule(
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
describe_snapshot_schedule <- function (VolumeARN) 
{
    op <- new_operation(name = "DescribeSnapshotSchedule", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_snapshot_schedule_input(VolumeARN = VolumeARN)
    output <- describe_snapshot_schedule_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the description of the gateway volumes specified in the request
#'
#' Returns the description of the gateway volumes specified in the request. The list of gateway volumes in the request must be from one gateway. In the response Amazon Storage Gateway returns volume information sorted by volume ARNs. This operation is only supported in stored volume gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' describe_storedi_scsi_volumes(
#'   VolumeARNs = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param VolumeARNs &#91;required&#93; An array of strings where each string represents the Amazon Resource Name (ARN) of a stored volume. All of the specified stored volumes must from the same gateway. Use ListVolumes to get volume ARNs for a gateway.
#'
#' @examples
#' # Returns the description of the gateway volumes specified in the request
#' # belonging to the same gateway.
#' \donttest{describe_storedi_scsi_volumes(
#'   VolumeARNs = list(
#'     "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#'   )
#' )}
#'
#' @export
describe_storedi_scsi_volumes <- function (VolumeARNs) 
{
    op <- new_operation(name = "DescribeStorediSCSIVolumes", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_storedi_scsi_volumes_input(VolumeARNs = VolumeARNs)
    output <- describe_storedi_scsi_volumes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a description of specified virtual tapes in the virtual tape shelf (VTS)
#'
#' Returns a description of specified virtual tapes in the virtual tape shelf (VTS). This operation is only supported in the tape gateway type.
#' 
#' If a specific `TapeARN` is not specified, AWS Storage Gateway returns a description of all virtual tapes found in the VTS associated with your account.
#'
#' @section Accepted Parameters:
#' ```
#' describe_tape_archives(
#'   TapeARNs = list(
#'     "string"
#'   ),
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param TapeARNs Specifies one or more unique Amazon Resource Names (ARNs) that represent the virtual tapes you want to describe.
#' @param Marker An opaque string that indicates the position at which to begin describing virtual tapes.
#' @param Limit Specifies that the number of virtual tapes descried be limited to the specified number.
#'
#' @examples
#' # Returns a description of specified virtual tapes in the virtual tape
#' # shelf (VTS).
#' \donttest{describe_tape_archives(
#'   Limit = 123L,
#'   Marker = "1",
#'   TapeARNs = list(
#'     "arn:aws:storagegateway:us-east-1:999999999999:tape/AM08A1AD",
#'     "arn:aws:storagegateway:us-east-1:999999999999:tape/AMZN01A2A4"
#'   )
#' )}
#'
#' @export
describe_tape_archives <- function (TapeARNs = NULL, Marker = NULL, 
    Limit = NULL) 
{
    op <- new_operation(name = "DescribeTapeArchives", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_tape_archives_input(TapeARNs = TapeARNs, 
        Marker = Marker, Limit = Limit)
    output <- describe_tape_archives_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of virtual tape recovery points that are available for the specified tape gateway
#'
#' Returns a list of virtual tape recovery points that are available for the specified tape gateway.
#' 
#' A recovery point is a point-in-time view of a virtual tape at which all the data on the virtual tape is consistent. If your gateway crashes, virtual tapes that have recovery points can be recovered to a new gateway. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' describe_tape_recovery_points(
#'   GatewayARN = "string",
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param Marker An opaque string that indicates the position at which to begin describing the virtual tape recovery points.
#' @param Limit Specifies that the number of virtual tape recovery points that are described be limited to the specified number.
#'
#' @examples
#' # Returns a list of virtual tape recovery points that are available for
#' # the specified gateway-VTL.
#' \donttest{describe_tape_recovery_points(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   Limit = 1L,
#'   Marker = "1"
#' )}
#'
#' @export
describe_tape_recovery_points <- function (GatewayARN, Marker = NULL, 
    Limit = NULL) 
{
    op <- new_operation(name = "DescribeTapeRecoveryPoints", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- describe_tape_recovery_points_input(GatewayARN = GatewayARN, 
        Marker = Marker, Limit = Limit)
    output <- describe_tape_recovery_points_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a description of the specified Amazon Resource Name (ARN) of virtual tapes
#'
#' Returns a description of the specified Amazon Resource Name (ARN) of virtual tapes. If a `TapeARN` is not specified, returns a description of all virtual tapes associated with the specified gateway. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' describe_tapes(
#'   GatewayARN = "string",
#'   TapeARNs = list(
#'     "string"
#'   ),
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param TapeARNs Specifies one or more unique Amazon Resource Names (ARNs) that represent the virtual tapes you want to describe. If this parameter is not specified, Tape gateway returns a description of all virtual tapes associated with the specified gateway.
#' @param Marker A marker value, obtained in a previous call to `DescribeTapes`. This marker indicates which page of results to retrieve.
#' 
#' If not specified, the first page of results is retrieved.
#' @param Limit Specifies that the number of virtual tapes described be limited to the specified number.
#' 
#' Amazon Web Services may impose its own limit, if this field is not set.
#'
#' @examples
#' # Returns a description of the specified Amazon Resource Name (ARN) of
#' # virtual tapes. If a TapeARN is not specified, returns a description of
#' # all virtual tapes.
#' \donttest{describe_tapes(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   Limit = 2L,
#'   Marker = "1",
#'   TapeARNs = list(
#'     "arn:aws:storagegateway:us-east-1:999999999999:tape/TEST04A2A1",
#'     "arn:aws:storagegateway:us-east-1:999999999999:tape/TEST05A2A0"
#'   )
#' )}
#'
#' @export
describe_tapes <- function (GatewayARN, TapeARNs = NULL, Marker = NULL, 
    Limit = NULL) 
{
    op <- new_operation(name = "DescribeTapes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_tapes_input(GatewayARN = GatewayARN, TapeARNs = TapeARNs, 
        Marker = Marker, Limit = Limit)
    output <- describe_tapes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the upload buffer of a gateway
#'
#' Returns information about the upload buffer of a gateway. This operation is supported for the stored volume, cached volume and tape gateway types.
#' 
#' The response includes disk IDs that are configured as upload buffer space, and it includes the amount of upload buffer space allocated and used.
#'
#' @section Accepted Parameters:
#' ```
#' describe_upload_buffer(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Returns information about the upload buffer of a gateway including disk
#' # IDs and the amount of upload buffer space allocated/used.
#' \donttest{describe_upload_buffer(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#' 
#' # Returns information about the upload buffer of a gateway including disk
#' # IDs and the amount of upload buffer space allocated and used.
#' \donttest{describe_upload_buffer(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_upload_buffer <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeUploadBuffer", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_upload_buffer_input(GatewayARN = GatewayARN)
    output <- describe_upload_buffer_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a description of virtual tape library (VTL) devices for the specified tape gateway
#'
#' Returns a description of virtual tape library (VTL) devices for the specified tape gateway. In the response, AWS Storage Gateway returns VTL device information.
#' 
#' This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' describe_vtl_devices(
#'   GatewayARN = "string",
#'   VTLDeviceARNs = list(
#'     "string"
#'   ),
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param VTLDeviceARNs An array of strings, where each string represents the Amazon Resource Name (ARN) of a VTL device.
#' 
#' All of the specified VTL devices must be from the same gateway. If no VTL devices are specified, the result will contain all devices on the specified gateway.
#' @param Marker An opaque string that indicates the position at which to begin describing the VTL devices.
#' @param Limit Specifies that the number of VTL devices described be limited to the specified number.
#'
#' @examples
#' # Returns a description of virtual tape library (VTL) devices for the
#' # specified gateway.
#' \donttest{describe_vtl_devices(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   Limit = 123L,
#'   Marker = "1",
#'   VTLDeviceARNs = list()
#' )}
#'
#' @export
describe_vtl_devices <- function (GatewayARN, VTLDeviceARNs = NULL, 
    Marker = NULL, Limit = NULL) 
{
    op <- new_operation(name = "DescribeVTLDevices", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_vtl_devices_input(GatewayARN = GatewayARN, 
        VTLDeviceARNs = VTLDeviceARNs, Marker = Marker, Limit = Limit)
    output <- describe_vtl_devices_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the working storage of a gateway
#'
#' Returns information about the working storage of a gateway. This operation is only supported in the stored volumes gateway type. This operation is deprecated in cached volumes API version (20120630). Use DescribeUploadBuffer instead.
#' 
#' Working storage is also referred to as upload buffer. You can also use the DescribeUploadBuffer operation to add upload buffer to a stored volume gateway.
#' 
#' The response includes disk IDs that are configured as working storage, and it includes the amount of working storage allocated and used.
#'
#' @section Accepted Parameters:
#' ```
#' describe_working_storage(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # This operation is supported only for the gateway-stored volume
#' # architecture. This operation is deprecated in cached-volumes API version
#' # (20120630). Use DescribeUploadBuffer instead.
#' \donttest{describe_working_storage(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
describe_working_storage <- function (GatewayARN) 
{
    op <- new_operation(name = "DescribeWorkingStorage", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- describe_working_storage_input(GatewayARN = GatewayARN)
    output <- describe_working_storage_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Disconnects a volume from an iSCSI connection and then detaches the volume from the specified gateway
#'
#' Disconnects a volume from an iSCSI connection and then detaches the volume from the specified gateway. Detaching and attaching a volume enables you to recover your data from one gateway to a different gateway without creating a snapshot. It also makes it easier to move your volumes from an on-premises gateway to a gateway hosted on an Amazon EC2 instance.
#'
#' @section Accepted Parameters:
#' ```
#' detach_volume(
#'   VolumeARN = "string",
#'   ForceDetach = TRUE|FALSE
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume to detach from the gateway.
#' @param ForceDetach Set to `true` to forcibly remove the iSCSI connection of the target volume and detach the volume. The default is `false`. If this value is set to `false`, you must manually disconnect the iSCSI connection from the target volume.
#'
#' @export
detach_volume <- function (VolumeARN, ForceDetach = NULL) 
{
    op <- new_operation(name = "DetachVolume", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_volume_input(VolumeARN = VolumeARN, ForceDetach = ForceDetach)
    output <- detach_volume_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Disables a tape gateway when the gateway is no longer functioning
#'
#' Disables a tape gateway when the gateway is no longer functioning. For example, if your gateway VM is damaged, you can disable the gateway so you can recover virtual tapes.
#' 
#' Use this operation for a tape gateway that is not reachable or not functioning. This operation is only supported in the tape gateway type.
#' 
#' Once a gateway is disabled it cannot be enabled.
#'
#' @section Accepted Parameters:
#' ```
#' disable_gateway(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Disables a gateway when the gateway is no longer functioning. Use this
#' # operation for a gateway-VTL that is not reachable or not functioning.
#' \donttest{disable_gateway(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
disable_gateway <- function (GatewayARN) 
{
    op <- new_operation(name = "DisableGateway", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- disable_gateway_input(GatewayARN = GatewayARN)
    output <- disable_gateway_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds a file gateway to an Active Directory domain
#'
#' Adds a file gateway to an Active Directory domain. This operation is only supported for file gateways that support the SMB file protocol.
#'
#' @section Accepted Parameters:
#' ```
#' join_domain(
#'   GatewayARN = "string",
#'   DomainName = "string",
#'   OrganizationalUnit = "string",
#'   DomainControllers = list(
#'     "string"
#'   ),
#'   UserName = "string",
#'   Password = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the gateway. Use the `ListGateways` operation to return a list of gateways for your account and region.
#' @param DomainName &#91;required&#93; The name of the domain that you want the gateway to join.
#' @param OrganizationalUnit The organizational unit (OU) is a container with an Active Directory that can hold users, groups, computers, and other OUs and this parameter specifies the OU that the gateway will join within the AD domain.
#' @param DomainControllers List of IPv4 addresses, NetBIOS names, or host names of your domain server. If you need to specify the port number include it after the colon (":"). For example, `mydc.mydomain.com:389`.
#' @param UserName &#91;required&#93; Sets the user name of user who has permission to add the gateway to the Active Directory domain.
#' @param Password &#91;required&#93; Sets the password of the user who has permission to add the gateway to the Active Directory domain.
#'
#' @export
join_domain <- function (GatewayARN, DomainName, OrganizationalUnit = NULL, 
    DomainControllers = NULL, UserName, Password) 
{
    op <- new_operation(name = "JoinDomain", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- join_domain_input(GatewayARN = GatewayARN, DomainName = DomainName, 
        OrganizationalUnit = OrganizationalUnit, DomainControllers = DomainControllers, 
        UserName = UserName, Password = Password)
    output <- join_domain_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a list of the file shares for a specific file gateway, or the list of file shares that belong to the calling user account
#'
#' Gets a list of the file shares for a specific file gateway, or the list of file shares that belong to the calling user account. This operation is only supported for file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' list_file_shares(
#'   GatewayARN = "string",
#'   Limit = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param GatewayARN The Amazon resource Name (ARN) of the gateway whose file shares you want to list. If this field is not present, all file shares under your account are listed.
#' @param Limit The maximum number of file shares to return in the response. The value must be an integer with a value greater than zero. Optional.
#' @param Marker Opaque pagination token returned from a previous ListFileShares operation. If present, `Marker` specifies where to continue the list from after a previous call to ListFileShares. Optional.
#'
#' @export
list_file_shares <- function (GatewayARN = NULL, Limit = NULL, 
    Marker = NULL) 
{
    op <- new_operation(name = "ListFileShares", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_file_shares_input(GatewayARN = GatewayARN, 
        Limit = Limit, Marker = Marker)
    output <- list_file_shares_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists gateways owned by an AWS account in a region specified in the request
#'
#' Lists gateways owned by an AWS account in a region specified in the request. The returned list is ordered by gateway Amazon Resource Name (ARN).
#' 
#' By default, the operation returns a maximum of 100 gateways. This operation supports pagination that allows you to optionally reduce the number of gateways returned in a response.
#' 
#' If you have more gateways than are returned in a response (that is, the response returns only a truncated list of your gateways), the response contains a marker that you can specify in your next request to fetch the next page of gateways.
#'
#' @section Accepted Parameters:
#' ```
#' list_gateways(
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param Marker An opaque string that indicates the position at which to begin the returned list of gateways.
#' @param Limit Specifies that the list of gateways returned be limited to the specified number of items.
#'
#' @examples
#' # Lists gateways owned by an AWS account in a specified region as
#' # requested. Results are sorted by gateway ARN up to a maximum of 100
#' # gateways.
#' \donttest{list_gateways(
#'   Limit = 2L,
#'   Marker = "1"
#' )}
#'
#' @export
list_gateways <- function (Marker = NULL, Limit = NULL) 
{
    op <- new_operation(name = "ListGateways", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_gateways_input(Marker = Marker, Limit = Limit)
    output <- list_gateways_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of the gateway's local disks
#'
#' Returns a list of the gateway\'s local disks. To specify which gateway to describe, you use the Amazon Resource Name (ARN) of the gateway in the body of the request.
#' 
#' The request returns a list of all disks, specifying which are configured as working storage, cache storage, or stored volume or not configured at all. The response includes a `DiskStatus` field. This field can have a value of present (the disk is available to use), missing (the disk is no longer connected to the gateway), or mismatch (the disk node is occupied by a disk that has incorrect metadata or the disk content is corrupted).
#'
#' @section Accepted Parameters:
#' ```
#' list_local_disks(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # The request returns a list of all disks, specifying which are configured
#' # as working storage, cache storage, or stored volume or not configured at
#' # all.
#' \donttest{list_local_disks(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
list_local_disks <- function (GatewayARN) 
{
    op <- new_operation(name = "ListLocalDisks", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_local_disks_input(GatewayARN = GatewayARN)
    output <- list_local_disks_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the tags that have been added to the specified resource
#'
#' Lists the tags that have been added to the specified resource. This operation is only supported in the cached volume, stored volume and tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' list_tags_for_resource(
#'   ResourceARN = "string",
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param ResourceARN &#91;required&#93; The Amazon Resource Name (ARN) of the resource for which you want to list tags.
#' @param Marker An opaque string that indicates the position at which to begin returning the list of tags.
#' @param Limit Specifies that the list of tags returned be limited to the specified number of items.
#'
#' @examples
#' # Lists the tags that have been added to the specified resource.
#' \donttest{list_tags_for_resource(
#'   Limit = 1L,
#'   Marker = "1",
#'   ResourceARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-11A2222B"
#' )}
#'
#' @export
list_tags_for_resource <- function (ResourceARN, Marker = NULL, 
    Limit = NULL) 
{
    op <- new_operation(name = "ListTagsForResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_tags_for_resource_input(ResourceARN = ResourceARN, 
        Marker = Marker, Limit = Limit)
    output <- list_tags_for_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists virtual tapes in your virtual tape library (VTL) and your virtual tape shelf (VTS)
#'
#' Lists virtual tapes in your virtual tape library (VTL) and your virtual tape shelf (VTS). You specify the tapes to list by specifying one or more tape Amazon Resource Names (ARNs). If you don\'t specify a tape ARN, the operation lists all virtual tapes in both your VTL and VTS.
#' 
#' This operation supports pagination. By default, the operation returns a maximum of up to 100 tapes. You can optionally specify the `Limit` parameter in the body to limit the number of tapes in the response. If the number of tapes returned in the response is truncated, the response includes a `Marker` element that you can use in your subsequent request to retrieve the next set of tapes. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' list_tapes(
#'   TapeARNs = list(
#'     "string"
#'   ),
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param TapeARNs 
#' @param Marker A string that indicates the position at which to begin the returned list of tapes.
#' @param Limit An optional number limit for the tapes in the list returned by this call.
#'
#' @export
list_tapes <- function (TapeARNs = NULL, Marker = NULL, Limit = NULL) 
{
    op <- new_operation(name = "ListTapes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_tapes_input(TapeARNs = TapeARNs, Marker = Marker, 
        Limit = Limit)
    output <- list_tapes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists iSCSI initiators that are connected to a volume
#'
#' Lists iSCSI initiators that are connected to a volume. You can use this operation to determine whether a volume is being used or not. This operation is only supported in the cached volume and stored volume gateway types.
#'
#' @section Accepted Parameters:
#' ```
#' list_volume_initiators(
#'   VolumeARN = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume. Use the ListVolumes operation to return a list of gateway volumes for the gateway.
#'
#' @export
list_volume_initiators <- function (VolumeARN) 
{
    op <- new_operation(name = "ListVolumeInitiators", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_volume_initiators_input(VolumeARN = VolumeARN)
    output <- list_volume_initiators_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the recovery points for a specified gateway
#'
#' Lists the recovery points for a specified gateway. This operation is only supported in the cached volume gateway type.
#' 
#' Each cache volume has one recovery point. A volume recovery point is a point in time at which all data of the volume is consistent and from which you can create a snapshot or clone a new cached volume from a source volume. To create a snapshot from a volume recovery point use the CreateSnapshotFromVolumeRecoveryPoint operation.
#'
#' @section Accepted Parameters:
#' ```
#' list_volume_recovery_points(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Lists the recovery points for a specified gateway in which all data of
#' # the volume is consistent and can be used to create a snapshot.
#' \donttest{list_volume_recovery_points(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
list_volume_recovery_points <- function (GatewayARN) 
{
    op <- new_operation(name = "ListVolumeRecoveryPoints", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_volume_recovery_points_input(GatewayARN = GatewayARN)
    output <- list_volume_recovery_points_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the iSCSI stored volumes of a gateway
#'
#' Lists the iSCSI stored volumes of a gateway. Results are sorted by volume ARN. The response includes only the volume ARNs. If you want additional volume information, use the DescribeStorediSCSIVolumes or the DescribeCachediSCSIVolumes API.
#' 
#' The operation supports pagination. By default, the operation returns a maximum of up to 100 volumes. You can optionally specify the `Limit` field in the body to limit the number of volumes in the response. If the number of volumes returned in the response is truncated, the response includes a Marker field. You can use this Marker value in your subsequent request to retrieve the next set of volumes. This operation is only supported in the cached volume and stored volume gateway types.
#'
#' @section Accepted Parameters:
#' ```
#' list_volumes(
#'   GatewayARN = "string",
#'   Marker = "string",
#'   Limit = 123
#' )
#' ```
#'
#' @param GatewayARN 
#' @param Marker A string that indicates the position at which to begin the returned list of volumes. Obtain the marker from the response of a previous List iSCSI Volumes request.
#' @param Limit Specifies that the list of volumes returned be limited to the specified number of items.
#'
#' @examples
#' # Lists the iSCSI stored volumes of a gateway. Results are sorted by
#' # volume ARN up to a maximum of 100 volumes.
#' \donttest{list_volumes(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   Limit = 2L,
#'   Marker = "1"
#' )}
#'
#' @export
list_volumes <- function (GatewayARN = NULL, Marker = NULL, Limit = NULL) 
{
    op <- new_operation(name = "ListVolumes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_volumes_input(GatewayARN = GatewayARN, Marker = Marker, 
        Limit = Limit)
    output <- list_volumes_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sends you notification through CloudWatch Events when all files written to your NFS file share have been uploaded to Amazon S3
#'
#' Sends you notification through CloudWatch Events when all files written to your NFS file share have been uploaded to Amazon S3.
#' 
#' AWS Storage Gateway can send a notification through Amazon CloudWatch Events when all files written to your file share up to that point in time have been uploaded to Amazon S3. These files include files written to the NFS file share up to the time that you make a request for notification. When the upload is done, Storage Gateway sends you notification through an Amazon CloudWatch Event. You can configure CloudWatch Events to send the notification through event targets such as Amazon SNS or AWS Lambda function. This operation is only supported for file gateways.
#' 
#' For more information, see Getting File Upload Notification in the Storage Gateway User Guide (https://docs.aws.amazon.com/storagegateway/latest/userguide/monitoring-file-gateway.html\#get-upload-notification).
#'
#' @section Accepted Parameters:
#' ```
#' notify_when_uploaded(
#'   FileShareARN = "string"
#' )
#' ```
#'
#' @param FileShareARN &#91;required&#93; 
#'
#' @export
notify_when_uploaded <- function (FileShareARN) 
{
    op <- new_operation(name = "NotifyWhenUploaded", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- notify_when_uploaded_input(FileShareARN = FileShareARN)
    output <- notify_when_uploaded_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Refreshes the cache for the specified file share
#'
#' Refreshes the cache for the specified file share. This operation finds objects in the Amazon S3 bucket that were added, removed or replaced since the gateway last listed the bucket\'s contents and cached the results. This operation is only supported in the file gateway type. You can subscribe to be notified through an Amazon CloudWatch event when your RefreshCache operation completes. For more information, see [Getting Notified About File Operations](https://docs.aws.amazon.com/storagegateway/latest/userguide/monitoring-file-gateway.html#get-notification).
#'
#' @section Accepted Parameters:
#' ```
#' refresh_cache(
#'   FileShareARN = "string",
#'   FolderList = list(
#'     "string"
#'   ),
#'   Recursive = TRUE|FALSE
#' )
#' ```
#'
#' @param FileShareARN &#91;required&#93; The Amazon Resource Name (ARN) of the file share you want to refresh.
#' @param FolderList A comma-separated list of the paths of folders to refresh in the cache. The default is &#91;`"/"`&#93;. The default refreshes objects and folders at the root of the Amazon S3 bucket. If `Recursive` is set to \"true\", the entire S3 bucket that the file share has access to is refreshed.
#' @param Recursive A value that specifies whether to recursively refresh folders in the cache. The refresh includes folders that were in the cache the last time the gateway listed the folder\'s contents. If this value set to \"true\", each folder that is listed in `FolderList` is recursively updated. Otherwise, subfolders listed in `FolderList` are not refreshed. Only objects that are in folders listed directly under `FolderList` are found and used for the update. The default is \"true\".
#'
#' @export
refresh_cache <- function (FileShareARN, FolderList = NULL, Recursive = NULL) 
{
    op <- new_operation(name = "RefreshCache", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- refresh_cache_input(FileShareARN = FileShareARN, 
        FolderList = FolderList, Recursive = Recursive)
    output <- refresh_cache_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes one or more tags from the specified resource
#'
#' Removes one or more tags from the specified resource. This operation is only supported in the cached volume, stored volume and tape gateway types.
#'
#' @section Accepted Parameters:
#' ```
#' remove_tags_from_resource(
#'   ResourceARN = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param ResourceARN &#91;required&#93; The Amazon Resource Name (ARN) of the resource you want to remove the tags from.
#' @param TagKeys &#91;required&#93; The keys of the tags you want to remove from the specified resource. A tag is composed of a key/value pair.
#'
#' @examples
#' # Lists the iSCSI stored volumes of a gateway. Removes one or more tags
#' # from the specified resource.
#' \donttest{remove_tags_from_resource(
#'   ResourceARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-11A2222B",
#'   TagKeys = list(
#'     "Dev Gatgeway Region",
#'     "East Coast"
#'   )
#' )}
#'
#' @export
remove_tags_from_resource <- function (ResourceARN, TagKeys) 
{
    op <- new_operation(name = "RemoveTagsFromResource", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- remove_tags_from_resource_input(ResourceARN = ResourceARN, 
        TagKeys = TagKeys)
    output <- remove_tags_from_resource_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Resets all cache disks that have encountered a error and makes the disks available for reconfiguration as cache storage
#'
#' Resets all cache disks that have encountered a error and makes the disks available for reconfiguration as cache storage. If your cache disk encounters a error, the gateway prevents read and write operations on virtual tapes in the gateway. For example, an error can occur when a disk is corrupted or removed from the gateway. When a cache is reset, the gateway loses its cache storage. At this point you can reconfigure the disks as cache disks. This operation is only supported in the cached volume and tape types.
#' 
#' If the cache disk you are resetting contains data that has not been uploaded to Amazon S3 yet, that data can be lost. After you reset cache disks, there will be no configured cache disks left in the gateway, so you must configure at least one new cache disk for your gateway to function properly.
#'
#' @section Accepted Parameters:
#' ```
#' reset_cache(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Resets all cache disks that have encountered a error and makes the disks
#' # available for reconfiguration as cache storage.
#' \donttest{reset_cache(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-13B4567C"
#' )}
#'
#' @export
reset_cache <- function (GatewayARN) 
{
    op <- new_operation(name = "ResetCache", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- reset_cache_input(GatewayARN = GatewayARN)
    output <- reset_cache_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves an archived virtual tape from the virtual tape shelf (VTS) to a tape gateway
#'
#' Retrieves an archived virtual tape from the virtual tape shelf (VTS) to a tape gateway. Virtual tapes archived in the VTS are not associated with any gateway. However after a tape is retrieved, it is associated with a gateway, even though it is also listed in the VTS, that is, archive. This operation is only supported in the tape gateway type.
#' 
#' Once a tape is successfully retrieved to a gateway, it cannot be retrieved again to another gateway. You must archive the tape again before you can retrieve it to another gateway. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' retrieve_tape_archive(
#'   TapeARN = "string",
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape you want to retrieve from the virtual tape shelf (VTS).
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the gateway you want to retrieve the virtual tape to. Use the ListGateways operation to return a list of gateways for your account and region.
#' 
#' You retrieve archived virtual tapes to only one gateway and the gateway must be a tape gateway.
#'
#' @examples
#' # Retrieves an archived virtual tape from the virtual tape shelf (VTS) to
#' # a gateway-VTL. Virtual tapes archived in the VTS are not associated with
#' # any gateway.
#' \donttest{retrieve_tape_archive(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   TapeARN = "arn:aws:storagegateway:us-east-1:999999999999:tape/TEST0AA2AF"
#' )}
#'
#' @export
retrieve_tape_archive <- function (TapeARN, GatewayARN) 
{
    op <- new_operation(name = "RetrieveTapeArchive", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- retrieve_tape_archive_input(TapeARN = TapeARN, GatewayARN = GatewayARN)
    output <- retrieve_tape_archive_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the recovery point for the specified virtual tape
#'
#' Retrieves the recovery point for the specified virtual tape. This operation is only supported in the tape gateway type.
#' 
#' A recovery point is a point in time view of a virtual tape at which all the data on the tape is consistent. If your gateway crashes, virtual tapes that have recovery points can be recovered to a new gateway.
#' 
#' The virtual tape can be retrieved to only one gateway. The retrieved tape is read-only. The virtual tape can be retrieved to only a tape gateway. There is no charge for retrieving recovery points.
#'
#' @section Accepted Parameters:
#' ```
#' retrieve_tape_recovery_point(
#'   TapeARN = "string",
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param TapeARN &#91;required&#93; The Amazon Resource Name (ARN) of the virtual tape for which you want to retrieve the recovery point.
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Retrieves the recovery point for the specified virtual tape.
#' \donttest{retrieve_tape_recovery_point(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   TapeARN = "arn:aws:storagegateway:us-east-1:999999999999:tape/TEST0AA2AF"
#' )}
#'
#' @export
retrieve_tape_recovery_point <- function (TapeARN, GatewayARN) 
{
    op <- new_operation(name = "RetrieveTapeRecoveryPoint", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- retrieve_tape_recovery_point_input(TapeARN = TapeARN, 
        GatewayARN = GatewayARN)
    output <- retrieve_tape_recovery_point_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the password for your VM local console
#'
#' Sets the password for your VM local console. When you log in to the local console for the first time, you log in to the VM with the default credentials. We recommend that you set a new password. You don\'t need to know the default password to set a new password.
#'
#' @section Accepted Parameters:
#' ```
#' set_local_console_password(
#'   GatewayARN = "string",
#'   LocalConsolePassword = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param LocalConsolePassword &#91;required&#93; The password you want to set for your VM local console.
#'
#' @examples
#' # Sets the password for your VM local console.
#' \donttest{set_local_console_password(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B",
#'   LocalConsolePassword = "PassWordMustBeAtLeast6Chars."
#' )}
#'
#' @export
set_local_console_password <- function (GatewayARN, LocalConsolePassword) 
{
    op <- new_operation(name = "SetLocalConsolePassword", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- set_local_console_password_input(GatewayARN = GatewayARN, 
        LocalConsolePassword = LocalConsolePassword)
    output <- set_local_console_password_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the password for the guest user smbguest
#'
#' Sets the password for the guest user `smbguest`. The `smbguest` user is the user when the authentication method for the file share is set to `GuestAccess`.
#'
#' @section Accepted Parameters:
#' ```
#' set_smb_guest_password(
#'   GatewayARN = "string",
#'   Password = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; The Amazon Resource Name (ARN) of the file gateway the SMB file share is associated with.
#' @param Password &#91;required&#93; The password that you want to set for your SMB Server.
#'
#' @export
set_smb_guest_password <- function (GatewayARN, Password) 
{
    op <- new_operation(name = "SetSMBGuestPassword", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- set_smb_guest_password_input(GatewayARN = GatewayARN, 
        Password = Password)
    output <- set_smb_guest_password_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Shuts down a gateway
#'
#' Shuts down a gateway. To specify which gateway to shut down, use the Amazon Resource Name (ARN) of the gateway in the body of your request.
#' 
#' The operation shuts down the gateway service component running in the gateway\'s virtual machine (VM) and not the host VM.
#' 
#' If you want to shut down the VM, it is recommended that you first shut down the gateway component in the VM to avoid unpredictable conditions.
#' 
#' After the gateway is shutdown, you cannot call any other API except StartGateway, DescribeGatewayInformation, and ListGateways. For more information, see ActivateGateway. Your applications cannot read from or write to the gateway\'s storage volumes, and there are no snapshots taken.
#' 
#' When you make a shutdown request, you will get a `200 OK` success response immediately. However, it might take some time for the gateway to shut down. You can call the DescribeGatewayInformation API to check the status. For more information, see ActivateGateway.
#' 
#' If do not intend to use the gateway again, you must delete the gateway (using DeleteGateway) to no longer pay software charges associated with the gateway.
#'
#' @section Accepted Parameters:
#' ```
#' shutdown_gateway(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # This operation shuts down the gateway service component running in the
#' # storage gateway's virtual machine (VM) and not the VM.
#' \donttest{shutdown_gateway(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B"
#' )}
#'
#' @export
shutdown_gateway <- function (GatewayARN) 
{
    op <- new_operation(name = "ShutdownGateway", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- shutdown_gateway_input(GatewayARN = GatewayARN)
    output <- shutdown_gateway_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Starts a gateway that you previously shut down (see ShutdownGateway)
#'
#' Starts a gateway that you previously shut down (see ShutdownGateway). After the gateway starts, you can then make other API calls, your applications can read from or write to the gateway\'s storage volumes and you will be able to take snapshot backups.
#' 
#' When you make a request, you will get a 200 OK success response immediately. However, it might take some time for the gateway to be ready. You should call DescribeGatewayInformation and check the status before making any additional API calls. For more information, see ActivateGateway.
#' 
#' To specify which gateway to start, use the Amazon Resource Name (ARN) of the gateway in your request.
#'
#' @section Accepted Parameters:
#' ```
#' start_gateway(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Starts a gateway service that was previously shut down.
#' \donttest{start_gateway(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B"
#' )}
#'
#' @export
start_gateway <- function (GatewayARN) 
{
    op <- new_operation(name = "StartGateway", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- start_gateway_input(GatewayARN = GatewayARN)
    output <- start_gateway_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the bandwidth rate limits of a gateway
#'
#' Updates the bandwidth rate limits of a gateway. You can update both the upload and download bandwidth rate limit or specify only one of the two. If you don\'t set a bandwidth rate limit, the existing rate limit remains.
#' 
#' By default, a gateway\'s bandwidth rate limits are not set. If you don\'t set any limit, the gateway does not have any limitations on its bandwidth usage and could potentially use the maximum available bandwidth.
#' 
#' To specify which gateway to update, use the Amazon Resource Name (ARN) of the gateway in your request.
#'
#' @section Accepted Parameters:
#' ```
#' update_bandwidth_rate_limit(
#'   GatewayARN = "string",
#'   AverageUploadRateLimitInBitsPerSec = 123,
#'   AverageDownloadRateLimitInBitsPerSec = 123
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param AverageUploadRateLimitInBitsPerSec The average upload bandwidth rate limit in bits per second.
#' @param AverageDownloadRateLimitInBitsPerSec The average download bandwidth rate limit in bits per second.
#'
#' @examples
#' # Updates the bandwidth rate limits of a gateway. Both the upload and
#' # download bandwidth rate limit can be set, or either one of the two. If a
#' # new limit is not set, the existing rate limit remains.
#' \donttest{update_bandwidth_rate_limit(
#'   AverageDownloadRateLimitInBitsPerSec = 102400L,
#'   AverageUploadRateLimitInBitsPerSec = 51200L,
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
update_bandwidth_rate_limit <- function (GatewayARN, AverageUploadRateLimitInBitsPerSec = NULL, 
    AverageDownloadRateLimitInBitsPerSec = NULL) 
{
    op <- new_operation(name = "UpdateBandwidthRateLimit", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_bandwidth_rate_limit_input(GatewayARN = GatewayARN, 
        AverageUploadRateLimitInBitsPerSec = AverageUploadRateLimitInBitsPerSec, 
        AverageDownloadRateLimitInBitsPerSec = AverageDownloadRateLimitInBitsPerSec)
    output <- update_bandwidth_rate_limit_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the Challenge-Handshake Authentication Protocol (CHAP) credentials for a specified iSCSI target
#'
#' Updates the Challenge-Handshake Authentication Protocol (CHAP) credentials for a specified iSCSI target. By default, a gateway does not have CHAP enabled; however, for added security, you might use it.
#' 
#' When you update CHAP credentials, all existing connections on the target are closed and initiators must reconnect with the new credentials.
#'
#' @section Accepted Parameters:
#' ```
#' update_chap_credentials(
#'   TargetARN = "string",
#'   SecretToAuthenticateInitiator = "string",
#'   InitiatorName = "string",
#'   SecretToAuthenticateTarget = "string"
#' )
#' ```
#'
#' @param TargetARN &#91;required&#93; The Amazon Resource Name (ARN) of the iSCSI volume target. Use the DescribeStorediSCSIVolumes operation to return the TargetARN for specified VolumeARN.
#' @param SecretToAuthenticateInitiator &#91;required&#93; The secret key that the initiator (for example, the Windows client) must provide to participate in mutual CHAP with the target.
#' 
#' The secret key must be between 12 and 16 bytes when encoded in UTF-8.
#' @param InitiatorName &#91;required&#93; The iSCSI initiator that connects to the target.
#' @param SecretToAuthenticateTarget The secret key that the target must provide to participate in mutual CHAP with the initiator (e.g. Windows client).
#' 
#' Byte constraints: Minimum bytes of 12. Maximum bytes of 16.
#' 
#' The secret key must be between 12 and 16 bytes when encoded in UTF-8.
#'
#' @examples
#' # Updates the Challenge-Handshake Authentication Protocol (CHAP)
#' # credentials for a specified iSCSI target.
#' \donttest{update_chap_credentials(
#'   InitiatorName = "iqn.1991-05.com.microsoft:computername.domain.example.com",
#'   SecretToAuthenticateInitiator = "111111111111",
#'   SecretToAuthenticateTarget = "222222222222",
#'   TargetARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/target/iqn.1997-05.com.amazon:myvolume"
#' )}
#'
#' @export
update_chap_credentials <- function (TargetARN, SecretToAuthenticateInitiator, 
    InitiatorName, SecretToAuthenticateTarget = NULL) 
{
    op <- new_operation(name = "UpdateChapCredentials", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_chap_credentials_input(TargetARN = TargetARN, 
        SecretToAuthenticateInitiator = SecretToAuthenticateInitiator, 
        InitiatorName = InitiatorName, SecretToAuthenticateTarget = SecretToAuthenticateTarget)
    output <- update_chap_credentials_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a gateway's metadata, which includes the gateway's name and time zone
#'
#' Updates a gateway\'s metadata, which includes the gateway\'s name and time zone. To specify which gateway to update, use the Amazon Resource Name (ARN) of the gateway in your request.
#' 
#' For Gateways activated after September 2, 2015, the gateway\'s ARN contains the gateway ID rather than the gateway name. However, changing the name of the gateway has no effect on the gateway\'s ARN.
#'
#' @section Accepted Parameters:
#' ```
#' update_gateway_information(
#'   GatewayARN = "string",
#'   GatewayName = "string",
#'   GatewayTimezone = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param GatewayName 
#' @param GatewayTimezone 
#'
#' @examples
#' # Updates a gateway's metadata, which includes the gateway's name and time
#' # zone.
#' \donttest{update_gateway_information(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   GatewayName = "MyGateway2",
#'   GatewayTimezone = "GMT-12:00"
#' )}
#'
#' @export
update_gateway_information <- function (GatewayARN, GatewayName = NULL, 
    GatewayTimezone = NULL) 
{
    op <- new_operation(name = "UpdateGatewayInformation", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_gateway_information_input(GatewayARN = GatewayARN, 
        GatewayName = GatewayName, GatewayTimezone = GatewayTimezone)
    output <- update_gateway_information_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the gateway virtual machine (VM) software
#'
#' Updates the gateway virtual machine (VM) software. The request immediately triggers the software update.
#' 
#' When you make this request, you get a `200 OK` success response immediately. However, it might take some time for the update to complete. You can call DescribeGatewayInformation to verify the gateway is in the `STATE_RUNNING` state.
#' 
#' A software update forces a system restart of your gateway. You can minimize the chance of any disruption to your applications by increasing your iSCSI Initiators\' timeouts. For more information about increasing iSCSI Initiator timeouts for Windows and Linux, see [Customizing Your Windows iSCSI Settings](http://docs.aws.amazon.com/storagegateway/latest/userguide/ConfiguringiSCSIClientInitiatorWindowsClient.html#CustomizeWindowsiSCSISettings) and [Customizing Your Linux iSCSI Settings](http://docs.aws.amazon.com/storagegateway/latest/userguide/ConfiguringiSCSIClientInitiatorRedHatClient.html#CustomizeLinuxiSCSISettings), respectively.
#'
#' @section Accepted Parameters:
#' ```
#' update_gateway_software_now(
#'   GatewayARN = "string"
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#'
#' @examples
#' # Updates the gateway virtual machine (VM) software. The request
#' # immediately triggers the software update.
#' \donttest{update_gateway_software_now(
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B"
#' )}
#'
#' @export
update_gateway_software_now <- function (GatewayARN) 
{
    op <- new_operation(name = "UpdateGatewaySoftwareNow", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_gateway_software_now_input(GatewayARN = GatewayARN)
    output <- update_gateway_software_now_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a gateway's weekly maintenance start time information, including day and time of the week
#'
#' Updates a gateway\'s weekly maintenance start time information, including day and time of the week. The maintenance time is the time in your gateway\'s time zone.
#'
#' @section Accepted Parameters:
#' ```
#' update_maintenance_start_time(
#'   GatewayARN = "string",
#'   HourOfDay = 123,
#'   MinuteOfHour = 123,
#'   DayOfWeek = 123
#' )
#' ```
#'
#' @param GatewayARN &#91;required&#93; 
#' @param HourOfDay &#91;required&#93; The hour component of the maintenance start time represented as *hh*, where *hh* is the hour (00 to 23). The hour of the day is in the time zone of the gateway.
#' @param MinuteOfHour &#91;required&#93; The minute component of the maintenance start time represented as *mm*, where *mm* is the minute (00 to 59). The minute of the hour is in the time zone of the gateway.
#' @param DayOfWeek &#91;required&#93; The maintenance start time day of the week represented as an ordinal number from 0 to 6, where 0 represents Sunday and 6 Saturday.
#'
#' @examples
#' # Updates a gateway's weekly maintenance start time information, including
#' # day and time of the week. The maintenance time is in your gateway's time
#' # zone.
#' \donttest{update_maintenance_start_time(
#'   DayOfWeek = 2L,
#'   GatewayARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B",
#'   HourOfDay = 0L,
#'   MinuteOfHour = 30L
#' )}
#'
#' @export
update_maintenance_start_time <- function (GatewayARN, HourOfDay, 
    MinuteOfHour, DayOfWeek) 
{
    op <- new_operation(name = "UpdateMaintenanceStartTime", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_maintenance_start_time_input(GatewayARN = GatewayARN, 
        HourOfDay = HourOfDay, MinuteOfHour = MinuteOfHour, DayOfWeek = DayOfWeek)
    output <- update_maintenance_start_time_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a Network File System (NFS) file share
#'
#' Updates a Network File System (NFS) file share. This operation is only supported in the file gateway type.
#' 
#' To leave a file share field unchanged, set the corresponding input field to null.
#' 
#' Updates the following file share setting:
#' 
#' -   Default storage class for your S3 bucket
#' 
#' -   Metadata defaults for your S3 bucket
#' 
#' -   Allowed NFS clients for your file share
#' 
#' -   Squash settings
#' 
#' -   Write status of your file share
#' 
#' To leave a file share field unchanged, set the corresponding input field to null. This operation is only supported in file gateways.
#'
#' @section Accepted Parameters:
#' ```
#' update_nfs_file_share(
#'   FileShareARN = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string",
#'   NFSFileShareDefaults = list(
#'     FileMode = "string",
#'     DirectoryMode = "string",
#'     GroupId = 123,
#'     OwnerId = 123
#'   ),
#'   DefaultStorageClass = "string",
#'   ObjectACL = "private"|"public-read"|"public-read-write"|"authenticated-read"|"bucket-owner-read"|"bucket-owner-full-control"|"aws-exec-read",
#'   ClientList = list(
#'     "string"
#'   ),
#'   Squash = "string",
#'   ReadOnly = TRUE|FALSE,
#'   GuessMIMETypeEnabled = TRUE|FALSE,
#'   RequesterPays = TRUE|FALSE
#' )
#' ```
#'
#' @param FileShareARN &#91;required&#93; The Amazon Resource Name (ARN) of the file share to be updated.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#' @param NFSFileShareDefaults The default values for the file share. Optional.
#' @param DefaultStorageClass The default storage class for objects put into an Amazon S3 bucket by the file gateway. Possible values are `S3_STANDARD`, `S3_STANDARD_IA`, or `S3_ONEZONE_IA`. If this field is not populated, the default value `S3_STANDARD` is used. Optional.
#' @param ObjectACL A value that sets the access control list permission for objects in the S3 bucket that a file gateway puts objects into. The default value is \"private\".
#' @param ClientList The list of clients that are allowed to access the file gateway. The list must contain either valid IP addresses or valid CIDR blocks.
#' @param Squash The user mapped to anonymous user. Valid options are the following:
#' 
#' -   `RootSquash` - Only root is mapped to anonymous user.
#' 
#' -   `NoSquash` - No one is mapped to anonymous user
#' 
#' -   `AllSquash` - Everyone is mapped to anonymous user.
#' @param ReadOnly A value that sets the write status of a file share. This value is true if the write status is read-only, and otherwise false.
#' @param GuessMIMETypeEnabled A value that enables guessing of the MIME type for uploaded objects based on file extensions. Set this value to true to enable MIME type guessing, and otherwise to false. The default value is true.
#' @param RequesterPays A value that sets the access control list permission for objects in the Amazon S3 bucket that a file gateway puts objects into. The default value is `private`.
#'
#' @export
update_nfs_file_share <- function (FileShareARN, KMSEncrypted = NULL, 
    KMSKey = NULL, NFSFileShareDefaults = NULL, DefaultStorageClass = NULL, 
    ObjectACL = NULL, ClientList = NULL, Squash = NULL, ReadOnly = NULL, 
    GuessMIMETypeEnabled = NULL, RequesterPays = NULL) 
{
    op <- new_operation(name = "UpdateNFSFileShare", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_nfs_file_share_input(FileShareARN = FileShareARN, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey, NFSFileShareDefaults = NFSFileShareDefaults, 
        DefaultStorageClass = DefaultStorageClass, ObjectACL = ObjectACL, 
        ClientList = ClientList, Squash = Squash, ReadOnly = ReadOnly, 
        GuessMIMETypeEnabled = GuessMIMETypeEnabled, RequesterPays = RequesterPays)
    output <- update_nfs_file_share_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a Server Message Block (SMB) file share
#'
#' Updates a Server Message Block (SMB) file share.
#' 
#' To leave a file share field unchanged, set the corresponding input field to null. This operation is only supported for file gateways.
#' 
#' File gateways require AWS Security Token Service (AWS STS) to be activated to enable you to create a file share. Make sure that AWS STS is activated in the AWS Region you are creating your file gateway in. If AWS STS is not activated in this AWS Region, activate it. For information about how to activate AWS STS, see [Activating and Deactivating AWS STS in an AWS Region](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_temp_enable-regions.html) in the *AWS Identity and Access Management User Guide.*
#' 
#' File gateways don\'t support creating hard or symbolic links on a file share.
#'
#' @section Accepted Parameters:
#' ```
#' update_smb_file_share(
#'   FileShareARN = "string",
#'   KMSEncrypted = TRUE|FALSE,
#'   KMSKey = "string",
#'   DefaultStorageClass = "string",
#'   ObjectACL = "private"|"public-read"|"public-read-write"|"authenticated-read"|"bucket-owner-read"|"bucket-owner-full-control"|"aws-exec-read",
#'   ReadOnly = TRUE|FALSE,
#'   GuessMIMETypeEnabled = TRUE|FALSE,
#'   RequesterPays = TRUE|FALSE,
#'   ValidUserList = list(
#'     "string"
#'   ),
#'   InvalidUserList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param FileShareARN &#91;required&#93; The Amazon Resource Name (ARN) of the SMB file share that you want to update.
#' @param KMSEncrypted True to use Amazon S3 server side encryption with your own AWS KMS key, or false to use a key managed by Amazon S3. Optional.
#' @param KMSKey The Amazon Resource Name (ARN) of the AWS KMS key used for Amazon S3 server side encryption. This value can only be set when KMSEncrypted is true. Optional.
#' @param DefaultStorageClass The default storage class for objects put into an Amazon S3 bucket by the file gateway. Possible values are `S3_STANDARD`, `S3_STANDARD_IA`, or `S3_ONEZONE_IA`. If this field is not populated, the default value `S3_STANDARD` is used. Optional.
#' @param ObjectACL A value that sets the access control list permission for objects in the S3 bucket that a file gateway puts objects into. The default value is \"private\".
#' @param ReadOnly A value that sets the write status of a file share. This value is true if the write status is read-only, and otherwise false.
#' @param GuessMIMETypeEnabled A value that enables guessing of the MIME type for uploaded objects based on file extensions. Set this value to true to enable MIME type guessing, and otherwise to false. The default value is true.
#' @param RequesterPays A value that sets the access control list permission for objects in the Amazon S3 bucket that a file gateway puts objects into. The default value is `private`.
#' @param ValidUserList A list of users or groups in the Active Directory that are allowed to access the file share. A group must be prefixed with the @ character. For example `@group1`. Can only be set if Authentication is set to `ActiveDirectory`.
#' @param InvalidUserList A list of users or groups in the Active Directory that are not allowed to access the file share. A group must be prefixed with the @ character. For example `@group1`. Can only be set if Authentication is set to `ActiveDirectory`.
#'
#' @export
update_smb_file_share <- function (FileShareARN, KMSEncrypted = NULL, 
    KMSKey = NULL, DefaultStorageClass = NULL, ObjectACL = NULL, 
    ReadOnly = NULL, GuessMIMETypeEnabled = NULL, RequesterPays = NULL, 
    ValidUserList = NULL, InvalidUserList = NULL) 
{
    op <- new_operation(name = "UpdateSMBFileShare", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_smb_file_share_input(FileShareARN = FileShareARN, 
        KMSEncrypted = KMSEncrypted, KMSKey = KMSKey, DefaultStorageClass = DefaultStorageClass, 
        ObjectACL = ObjectACL, ReadOnly = ReadOnly, GuessMIMETypeEnabled = GuessMIMETypeEnabled, 
        RequesterPays = RequesterPays, ValidUserList = ValidUserList, 
        InvalidUserList = InvalidUserList)
    output <- update_smb_file_share_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a snapshot schedule configured for a gateway volume
#'
#' Updates a snapshot schedule configured for a gateway volume. This operation is only supported in the cached volume and stored volume gateway types.
#' 
#' The default snapshot schedule for volume is once every 24 hours, starting at the creation time of the volume. You can use this API to change the snapshot schedule configured for the volume.
#' 
#' In the request you must identify the gateway volume whose snapshot schedule you want to update, and the schedule information, including when you want the snapshot to begin on a day and the frequency (in hours) of snapshots.
#'
#' @section Accepted Parameters:
#' ```
#' update_snapshot_schedule(
#'   VolumeARN = "string",
#'   StartAt = 123,
#'   RecurrenceInHours = 123,
#'   Description = "string"
#' )
#' ```
#'
#' @param VolumeARN &#91;required&#93; The Amazon Resource Name (ARN) of the volume. Use the ListVolumes operation to return a list of gateway volumes.
#' @param StartAt &#91;required&#93; The hour of the day at which the snapshot schedule begins represented as *hh*, where *hh* is the hour (0 to 23). The hour of the day is in the time zone of the gateway.
#' @param RecurrenceInHours &#91;required&#93; Frequency of snapshots. Specify the number of hours between snapshots.
#' @param Description Optional description of the snapshot that overwrites the existing description.
#'
#' @examples
#' # Updates a snapshot schedule configured for a gateway volume.
#' \donttest{update_snapshot_schedule(
#'   Description = "Hourly snapshot",
#'   RecurrenceInHours = 1L,
#'   StartAt = 0L,
#'   VolumeARN = "arn:aws:storagegateway:us-east-1:111122223333:gateway/sgw-12A3456B/volume/vol-1122AABB"
#' )}
#'
#' @export
update_snapshot_schedule <- function (VolumeARN, StartAt, RecurrenceInHours, 
    Description = NULL) 
{
    op <- new_operation(name = "UpdateSnapshotSchedule", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_snapshot_schedule_input(VolumeARN = VolumeARN, 
        StartAt = StartAt, RecurrenceInHours = RecurrenceInHours, 
        Description = Description)
    output <- update_snapshot_schedule_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the type of medium changer in a tape gateway
#'
#' Updates the type of medium changer in a tape gateway. When you activate a tape gateway, you select a medium changer type for the tape gateway. This operation enables you to select a different type of medium changer after a tape gateway is activated. This operation is only supported in the tape gateway type.
#'
#' @section Accepted Parameters:
#' ```
#' update_vtl_device_type(
#'   VTLDeviceARN = "string",
#'   DeviceType = "string"
#' )
#' ```
#'
#' @param VTLDeviceARN &#91;required&#93; The Amazon Resource Name (ARN) of the medium changer you want to select.
#' @param DeviceType &#91;required&#93; The type of medium changer you want to select.
#' 
#' Valid Values: \"STK-L700\", \"AWS-Gateway-VTL\"
#'
#' @examples
#' # Updates the type of medium changer in a gateway-VTL after a gateway-VTL
#' # is activated.
#' \donttest{update_vtl_device_type(
#'   DeviceType = "Medium Changer",
#'   VTLDeviceARN = "arn:aws:storagegateway:us-east-1:999999999999:gateway/sgw-12A3456B/device/AMZN_SGW-1FAD4876_MEDIACHANGER_00001"
#' )}
#'
#' @export
update_vtl_device_type <- function (VTLDeviceARN, DeviceType) 
{
    op <- new_operation(name = "UpdateVTLDeviceType", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_vtl_device_type_input(VTLDeviceARN = VTLDeviceARN, 
        DeviceType = DeviceType)
    output <- update_vtl_device_type_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
