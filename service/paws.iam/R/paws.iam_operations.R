# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Adds a new client ID (also known as audience) to the list of client IDs already registered for the specified IAM OpenID Connect (OIDC) provider resource
#'
#' Adds a new client ID (also known as audience) to the list of client IDs already registered for the specified IAM OpenID Connect (OIDC) provider resource.
#' 
#' This operation is idempotent; it does not fail or return an error if you add an existing client ID to the provider.
#'
#' @section Accepted Parameters:
#' ```
#' add_client_id_to_open_id_connect_provider(
#'   OpenIDConnectProviderArn = "string",
#'   ClientID = "string"
#' )
#' ```
#'
#' @param OpenIDConnectProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM OpenID Connect (OIDC) provider resource to add the client ID to. You can get a list of OIDC provider ARNs by using the ListOpenIDConnectProviders operation.
#' @param ClientID &#91;required&#93; The client ID (also known as audience) to add to the IAM OpenID Connect provider resource.
#'
#' @examples
#' # The following add-client-id-to-open-id-connect-provider command adds the
#' # client ID my-application-ID to the OIDC provider named
#' # server.example.com:
#' \donttest{add_client_id_to_open_id_connect_provider(
#'   ClientID = "my-application-ID",
#'   OpenIDConnectProviderArn = "arn:aws:iam::123456789012:oidc-provider/server.example.com"
#' )}
#'
#' @export
add_client_id_to_open_id_connect_provider <- function (OpenIDConnectProviderArn, 
    ClientID) 
{
    op <- new_operation(name = "AddClientIDToOpenIDConnectProvider", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- add_client_id_to_open_id_connect_provider_input(OpenIDConnectProviderArn = OpenIDConnectProviderArn, 
        ClientID = ClientID)
    output <- add_client_id_to_open_id_connect_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds the specified IAM role to the specified instance profile
#'
#' Adds the specified IAM role to the specified instance profile. An instance profile can contain only one role, and this limit cannot be increased. You can remove the existing role and then add a different role to an instance profile. You must then wait for the change to appear across all of AWS because of [eventual consistency](https://en.wikipedia.org/wiki/Eventual_consistency). To force the change, you must [disassociate the instance profile](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_DisassociateIamInstanceProfile.html) and then [associate the instance profile](http://docs.aws.amazon.com/AWSEC2/latest/APIReference/API_AssociateIamInstanceProfile.html), or you can stop your instance and then restart it.
#' 
#' The caller of this API must be granted the `PassRole` permission on the IAM role by a permissions policy.
#' 
#' For more information about roles, go to [Working with Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html). For more information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#'
#' @section Accepted Parameters:
#' ```
#' add_role_to_instance_profile(
#'   InstanceProfileName = "string",
#'   RoleName = "string"
#' )
#' ```
#'
#' @param InstanceProfileName &#91;required&#93; The name of the instance profile to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param RoleName &#91;required&#93; The name of the role to add.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command adds the role named S3Access to the instance
#' # profile named Webserver:
#' \donttest{add_role_to_instance_profile(
#'   InstanceProfileName = "Webserver",
#'   RoleName = "S3Access"
#' )}
#'
#' @export
add_role_to_instance_profile <- function (InstanceProfileName, 
    RoleName) 
{
    op <- new_operation(name = "AddRoleToInstanceProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_role_to_instance_profile_input(InstanceProfileName = InstanceProfileName, 
        RoleName = RoleName)
    output <- add_role_to_instance_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds the specified user to the specified group
#'
#' Adds the specified user to the specified group.
#'
#' @section Accepted Parameters:
#' ```
#' add_user_to_group(
#'   GroupName = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param UserName &#91;required&#93; The name of the user to add.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command adds an IAM user named Bob to the IAM group named
#' # Admins:
#' \donttest{add_user_to_group(
#'   GroupName = "Admins",
#'   UserName = "Bob"
#' )}
#'
#' @export
add_user_to_group <- function (GroupName, UserName) 
{
    op <- new_operation(name = "AddUserToGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- add_user_to_group_input(GroupName = GroupName, UserName = UserName)
    output <- add_user_to_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches the specified managed policy to the specified IAM group
#'
#' Attaches the specified managed policy to the specified IAM group.
#' 
#' You use this API to attach a managed policy to a group. To embed an inline policy in a group, use PutGroupPolicy.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' attach_group_policy(
#'   GroupName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name (friendly name, not ARN) of the group to attach the policy to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to attach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @examples
#' # The following command attaches the AWS managed policy named
#' # ReadOnlyAccess to the IAM group named Finance.
#' \donttest{attach_group_policy(
#'   GroupName = "Finance",
#'   PolicyArn = "arn:aws:iam::aws:policy/ReadOnlyAccess"
#' )}
#'
#' @export
attach_group_policy <- function (GroupName, PolicyArn) 
{
    op <- new_operation(name = "AttachGroupPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_group_policy_input(GroupName = GroupName, 
        PolicyArn = PolicyArn)
    output <- attach_group_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches the specified managed policy to the specified IAM role
#'
#' Attaches the specified managed policy to the specified IAM role. When you attach a managed policy to a role, the managed policy becomes part of the role\'s permission (access) policy.
#' 
#' You cannot use a managed policy as the role\'s trust policy. The role\'s trust policy is created at the same time as the role, using CreateRole. You can update a role\'s trust policy using UpdateAssumeRolePolicy.
#' 
#' Use this API to attach a *managed* policy to a role. To embed an inline policy in a role, use PutRolePolicy. For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' attach_role_policy(
#'   RoleName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) of the role to attach the policy to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to attach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @examples
#' # The following command attaches the AWS managed policy named
#' # ReadOnlyAccess to the IAM role named ReadOnlyRole.
#' \donttest{attach_role_policy(
#'   PolicyArn = "arn:aws:iam::aws:policy/ReadOnlyAccess",
#'   RoleName = "ReadOnlyRole"
#' )}
#'
#' @export
attach_role_policy <- function (RoleName, PolicyArn) 
{
    op <- new_operation(name = "AttachRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_role_policy_input(RoleName = RoleName, PolicyArn = PolicyArn)
    output <- attach_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Attaches the specified managed policy to the specified user
#'
#' Attaches the specified managed policy to the specified user.
#' 
#' You use this API to attach a *managed* policy to a user. To embed an inline policy in a user, use PutUserPolicy.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' attach_user_policy(
#'   UserName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) of the IAM user to attach the policy to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to attach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @examples
#' # The following command attaches the AWS managed policy named
#' # AdministratorAccess to the IAM user named Alice.
#' \donttest{attach_user_policy(
#'   PolicyArn = "arn:aws:iam::aws:policy/AdministratorAccess",
#'   UserName = "Alice"
#' )}
#'
#' @export
attach_user_policy <- function (UserName, PolicyArn) 
{
    op <- new_operation(name = "AttachUserPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- attach_user_policy_input(UserName = UserName, PolicyArn = PolicyArn)
    output <- attach_user_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the password of the IAM user who is calling this operation
#'
#' Changes the password of the IAM user who is calling this operation. The AWS account root user password is not affected by this operation.
#' 
#' To change the password for a different user, see UpdateLoginProfile. For more information about modifying passwords, see [Managing Passwords](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_ManagingLogins.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' change_password(
#'   OldPassword = "string",
#'   NewPassword = "string"
#' )
#' ```
#'
#' @param OldPassword &#91;required&#93; The IAM user\'s current password.
#' @param NewPassword &#91;required&#93; The new password. The new password must conform to the AWS account\'s password policy, if one exists.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) that is used to validate this parameter is a string of characters. That string can include almost any printable ASCII character from the space (\\u0020) through the end of the ASCII character range (\\u00FF). You can also include the tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D) characters. Any of these characters are valid in a password. However, many tools, such as the AWS Management Console, might restrict the ability to type certain characters because they have special meaning within that tool.
#'
#' @examples
#' # The following command changes the password for the current IAM user.
#' \donttest{change_password(
#'   NewPassword = "]35d/{pB9Fo9wJ",
#'   OldPassword = "3s0K_;xh4~8XXI"
#' )}
#'
#' @export
change_password <- function (OldPassword, NewPassword) 
{
    op <- new_operation(name = "ChangePassword", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- change_password_input(OldPassword = OldPassword, 
        NewPassword = NewPassword)
    output <- change_password_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new AWS secret access key and corresponding AWS access key ID for the specified user
#'
#' Creates a new AWS secret access key and corresponding AWS access key ID for the specified user. The default status for new keys is `Active`.
#' 
#' If you do not specify a user name, IAM determines the user name implicitly based on the AWS access key ID signing the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials. This is true even if the AWS account has no associated users.
#' 
#' For information about limits on the number of keys you can create, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#' 
#' To ensure the security of your AWS account, the secret access key is accessible only during key and user creation. You must save the key (for example, in a text file) if you want to be able to access it again. If a secret key is lost, you can delete the access keys for the associated user and then create new keys.
#'
#' @section Accepted Parameters:
#' ```
#' create_access_key(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName The name of the IAM user that the new key will belong to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command creates an access key (access key ID and secret
#' # access key) for the IAM user named Bob.
#' \donttest{create_access_key(
#'   UserName = "Bob"
#' )}
#'
#' @export
create_access_key <- function (UserName = NULL) 
{
    op <- new_operation(name = "CreateAccessKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_access_key_input(UserName = UserName)
    output <- create_access_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an alias for your AWS account
#'
#' Creates an alias for your AWS account. For information about using an AWS account alias, see [Using an Alias for Your AWS Account ID](http://docs.aws.amazon.com/IAM/latest/UserGuide/AccountAlias.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_account_alias(
#'   AccountAlias = "string"
#' )
#' ```
#'
#' @param AccountAlias &#91;required&#93; The account alias to create.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of lowercase letters, digits, and dashes. You cannot start or finish with a dash, nor can you have two dashes in a row.
#'
#' @examples
#' # The following command associates the alias examplecorp to your AWS
#' # account.
#' \donttest{create_account_alias(
#'   AccountAlias = "examplecorp"
#' )}
#'
#' @export
create_account_alias <- function (AccountAlias) 
{
    op <- new_operation(name = "CreateAccountAlias", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_account_alias_input(AccountAlias = AccountAlias)
    output <- create_account_alias_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new group
#'
#' Creates a new group.
#' 
#' For information about the number of groups you can create, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_group(
#'   Path = "string",
#'   GroupName = "string"
#' )
#' ```
#'
#' @param Path The path to the group. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param GroupName &#91;required&#93; The name of the group to create. Do not include the path in this value.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-. The group name must be unique within the account. Group names are not distinguished by case. For example, you cannot create groups named both \"ADMINS\" and \"admins\".
#'
#' @examples
#' # The following command creates an IAM group named Admins.
#' \donttest{create_group(
#'   GroupName = "Admins"
#' )}
#'
#' @export
create_group <- function (Path = NULL, GroupName) 
{
    op <- new_operation(name = "CreateGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_group_input(Path = Path, GroupName = GroupName)
    output <- create_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new instance profile
#'
#' Creates a new instance profile. For information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#' 
#' For information about the number of instance profiles you can create, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_instance_profile(
#'   InstanceProfileName = "string",
#'   Path = "string"
#' )
#' ```
#'
#' @param InstanceProfileName &#91;required&#93; The name of the instance profile to create.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Path The path to the instance profile. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#'
#' @examples
#' # The following command creates an instance profile named Webserver that
#' # is ready to have a role attached and then be associated with an EC2
#' # instance.
#' \donttest{create_instance_profile(
#'   InstanceProfileName = "Webserver"
#' )}
#'
#' @export
create_instance_profile <- function (InstanceProfileName, Path = NULL) 
{
    op <- new_operation(name = "CreateInstanceProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_instance_profile_input(InstanceProfileName = InstanceProfileName, 
        Path = Path)
    output <- create_instance_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a password for the specified user, giving the user the ability to access AWS services through the AWS Management Console
#'
#' Creates a password for the specified user, giving the user the ability to access AWS services through the AWS Management Console. For more information about managing passwords, see [Managing Passwords](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_ManagingLogins.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_login_profile(
#'   UserName = "string",
#'   Password = "string",
#'   PasswordResetRequired = TRUE|FALSE
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user to create a password for. The user must already exist.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Password &#91;required&#93; The new password for the user.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) that is used to validate this parameter is a string of characters. That string can include almost any printable ASCII character from the space (\\u0020) through the end of the ASCII character range (\\u00FF). You can also include the tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D) characters. Any of these characters are valid in a password. However, many tools, such as the AWS Management Console, might restrict the ability to type certain characters because they have special meaning within that tool.
#' @param PasswordResetRequired Specifies whether the user is required to set a new password on next sign-in.
#'
#' @examples
#' # The following command changes IAM user Bob's password and sets the flag
#' # that required Bob to change the password the next time he signs in.
#' \donttest{create_login_profile(
#'   Password = "h]6EszR}vJ*m",
#'   PasswordResetRequired = TRUE,
#'   UserName = "Bob"
#' )}
#'
#' @export
create_login_profile <- function (UserName, Password, PasswordResetRequired = NULL) 
{
    op <- new_operation(name = "CreateLoginProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_login_profile_input(UserName = UserName, 
        Password = Password, PasswordResetRequired = PasswordResetRequired)
    output <- create_login_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an IAM entity to describe an identity provider (IdP) that supports OpenID Connect (OIDC)
#'
#' Creates an IAM entity to describe an identity provider (IdP) that supports [OpenID Connect (OIDC)](http://openid.net/connect/).
#' 
#' The OIDC provider that you create with this operation can be used as a principal in a role\'s trust policy. Such a policy establishes a trust relationship between AWS and the OIDC provider.
#' 
#' When you create the IAM OIDC provider, you specify the following:
#' 
#' -   The URL of the OIDC identity provider (IdP) to trust
#' 
#' -   A list of client IDs (also known as audiences) that identify the application or applications that are allowed to authenticate using the OIDC provider
#' 
#' -   A list of thumbprints of the server certificate(s) that the IdP uses.
#' 
#' You get all of this information from the OIDC IdP that you want to use to access AWS.
#' 
#' Because trust for the OIDC provider is derived from the IAM provider that this operation creates, it is best to limit access to the CreateOpenIDConnectProvider operation to highly privileged users.
#'
#' @section Accepted Parameters:
#' ```
#' create_open_id_connect_provider(
#'   Url = "string",
#'   ClientIDList = list(
#'     "string"
#'   ),
#'   ThumbprintList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param Url &#91;required&#93; The URL of the identity provider. The URL must begin with `https://` and should correspond to the `iss` claim in the provider\'s OpenID Connect ID tokens. Per the OIDC standard, path components are allowed but query parameters are not. Typically the URL consists of only a hostname, like `https://server.example.org` or `https://example.com`.
#' 
#' You cannot register the same provider multiple times in a single AWS account. If you try to submit a URL that has already been used for an OpenID Connect provider in the AWS account, you will get an error.
#' @param ClientIDList A list of client IDs (also known as audiences). When a mobile or web app registers with an OpenID Connect provider, they establish a value that identifies the application. (This is the value that\'s sent as the `client_id` parameter on OAuth requests.)
#' 
#' You can register multiple client IDs with the same provider. For example, you might have multiple applications that use the same OIDC provider. You cannot register more than 100 client IDs with a single IAM OIDC provider.
#' 
#' There is no defined format for a client ID. The `CreateOpenIDConnectProviderRequest` operation accepts client IDs up to 255 characters long.
#' @param ThumbprintList &#91;required&#93; A list of server certificate thumbprints for the OpenID Connect (OIDC) identity provider\'s server certificates. Typically this list includes only one entry. However, IAM lets you have up to five thumbprints for an OIDC provider. This lets you maintain multiple thumbprints if the identity provider is rotating certificates.
#' 
#' The server certificate thumbprint is the hex-encoded SHA-1 hash value of the X.509 certificate used by the domain where the OpenID Connect provider makes its keys available. It is always a 40-character string.
#' 
#' You must provide at least one thumbprint when creating an IAM OIDC provider. For example, assume that the OIDC provider is `server.example.com` and the provider stores its keys at https://keys.server.example.com/openid-connect. In that case, the thumbprint string would be the hex-encoded SHA-1 hash value of the certificate used by https://keys.server.example.com.
#' 
#' For more information about obtaining the OIDC provider\'s thumbprint, see [Obtaining the Thumbprint for an OpenID Connect Provider](http://docs.aws.amazon.com/IAM/latest/UserGuide/identity-providers-oidc-obtain-thumbprint.html) in the *IAM User Guide*.
#'
#' @examples
#' # The following example defines a new OIDC provider in IAM with a client
#' # ID of my-application-id and pointing at the server with a URL of
#' # https://server.example.com.
#' \donttest{create_open_id_connect_provider(
#'   ClientIDList = list(
#'     "my-application-id"
#'   ),
#'   ThumbprintList = list(
#'     "3768084dfb3d2b68b7897bf5f565da8efEXAMPLE"
#'   ),
#'   Url = "https://server.example.com"
#' )}
#'
#' @export
create_open_id_connect_provider <- function (Url, ClientIDList = NULL, 
    ThumbprintList) 
{
    op <- new_operation(name = "CreateOpenIDConnectProvider", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_open_id_connect_provider_input(Url = Url, 
        ClientIDList = ClientIDList, ThumbprintList = ThumbprintList)
    output <- create_open_id_connect_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new managed policy for your AWS account
#'
#' Creates a new managed policy for your AWS account.
#' 
#' This operation creates a policy version with a version identifier of `v1` and sets v1 as the policy\'s default version. For more information about policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#' 
#' For more information about managed policies in general, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_policy(
#'   PolicyName = "string",
#'   Path = "string",
#'   PolicyDocument = "string",
#'   Description = "string"
#' )
#' ```
#'
#' @param PolicyName &#91;required&#93; The friendly name of the policy.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Path The path for the policy.
#' 
#' For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param PolicyDocument &#91;required&#93; The JSON policy document that you want to use as the content for the new policy.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param Description A friendly description of the policy.
#' 
#' Typically used to store information about the permissions defined in the policy. For example, \"Grants access to production DynamoDB tables.\"
#' 
#' The policy description is immutable. After a value is assigned, it cannot be changed.
#'
#' @export
create_policy <- function (PolicyName, Path = NULL, PolicyDocument, 
    Description = NULL) 
{
    op <- new_operation(name = "CreatePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_policy_input(PolicyName = PolicyName, Path = Path, 
        PolicyDocument = PolicyDocument, Description = Description)
    output <- create_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new version of the specified managed policy
#'
#' Creates a new version of the specified managed policy. To update a managed policy, you create a new policy version. A managed policy can have up to five versions. If the policy has five versions, you must delete an existing version using DeletePolicyVersion before you create a new version.
#' 
#' Optionally, you can set the new version as the policy\'s default version. The default version is the version that is in effect for the IAM users, groups, and roles to which the policy is attached.
#' 
#' For more information about managed policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_policy_version(
#'   PolicyArn = "string",
#'   PolicyDocument = "string",
#'   SetAsDefault = TRUE|FALSE
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy to which you want to add a new version.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param PolicyDocument &#91;required&#93; The JSON policy document that you want to use as the content for this new version of the policy.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param SetAsDefault Specifies whether to set this version as the policy\'s default version.
#' 
#' When this parameter is `true`, the new policy version becomes the operative version. That is, it becomes the version that is in effect for the IAM users, groups, and roles that the policy is attached to.
#' 
#' For more information about managed policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @export
create_policy_version <- function (PolicyArn, PolicyDocument, 
    SetAsDefault = NULL) 
{
    op <- new_operation(name = "CreatePolicyVersion", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_policy_version_input(PolicyArn = PolicyArn, 
        PolicyDocument = PolicyDocument, SetAsDefault = SetAsDefault)
    output <- create_policy_version_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new role for your AWS account
#'
#' Creates a new role for your AWS account. For more information about roles, go to [IAM Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html). For information about limitations on role names and the number of roles you can create, go to [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_role(
#'   Path = "string",
#'   RoleName = "string",
#'   AssumeRolePolicyDocument = "string",
#'   Description = "string",
#'   MaxSessionDuration = 123,
#'   PermissionsBoundary = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param Path The path to the role. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param RoleName &#91;required&#93; The name of the role to create.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' 
#' Role names are not distinguished by case. For example, you cannot create roles named both \"PRODROLE\" and \"prodrole\".
#' @param AssumeRolePolicyDocument &#91;required&#93; The trust relationship policy document that grants an entity permission to assume the role.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param Description A description of the role.
#' @param MaxSessionDuration The maximum session duration (in seconds) that you want to set for the specified role. If you do not specify a value for this setting, the default maximum of one hour is applied. This setting can have a value from 1 hour to 12 hours.
#' 
#' Anyone who assumes the role from the AWS CLI or API can use the `DurationSeconds` API parameter or the `duration-seconds` CLI parameter to request a longer session. The `MaxSessionDuration` setting determines the maximum duration that can be requested using the `DurationSeconds` parameter. If users don\'t specify a value for the `DurationSeconds` parameter, their security credentials are valid for one hour by default. This applies when you use the `AssumeRole*` API operations or the `assume-role*` CLI operations but does not apply when you use those operations to create a console URL. For more information, see [Using IAM Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use.html) in the *IAM User Guide*.
#' @param PermissionsBoundary The ARN of the policy that is used to set the permissions boundary for the role.
#' @param Tags A list of tags that you want to attach to the newly created role. Each tag consists of a key name and an associated value. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#' 
#' If any one of the tags is invalid or if you exceed the allowed number of tags per role, then the entire request fails and the role is not created.
#'
#' @examples
#' # The following command creates a role named Test-Role and attaches a
#' # trust policy to it that is provided as a URL-encoded JSON string.
#' \donttest{create_role(
#'   AssumeRolePolicyDocument = "<URL-encoded-JSON>",
#'   Path = "/",
#'   RoleName = "Test-Role"
#' )}
#'
#' @export
create_role <- function (Path = NULL, RoleName, AssumeRolePolicyDocument, 
    Description = NULL, MaxSessionDuration = NULL, PermissionsBoundary = NULL, 
    Tags = NULL) 
{
    op <- new_operation(name = "CreateRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_role_input(Path = Path, RoleName = RoleName, 
        AssumeRolePolicyDocument = AssumeRolePolicyDocument, 
        Description = Description, MaxSessionDuration = MaxSessionDuration, 
        PermissionsBoundary = PermissionsBoundary, Tags = Tags)
    output <- create_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an IAM resource that describes an identity provider (IdP) that supports SAML 2
#'
#' Creates an IAM resource that describes an identity provider (IdP) that supports SAML 2.0.
#' 
#' The SAML provider resource that you create with this operation can be used as a principal in an IAM role\'s trust policy. Such a policy can enable federated users who sign-in using the SAML IdP to assume the role. You can create an IAM role that supports Web-based single sign-on (SSO) to the AWS Management Console or one that supports API access to AWS.
#' 
#' When you create the SAML provider resource, you upload a SAML metadata document that you get from your IdP. That document includes the issuer\'s name, expiration information, and keys that can be used to validate the SAML authentication response (assertions) that the IdP sends. You must generate the metadata document using the identity management software that is used as your organization\'s IdP.
#' 
#' This operation requires [Signature Version 4](http://docs.aws.amazon.com/general/latest/gr/signature-version-4.html).
#' 
#' For more information, see [Enabling SAML 2.0 Federated Users to Access the AWS Management Console](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_enable-console-saml.html) and [About SAML 2.0-based Federation](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_saml.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_saml_provider(
#'   SAMLMetadataDocument = "string",
#'   Name = "string"
#' )
#' ```
#'
#' @param SAMLMetadataDocument &#91;required&#93; An XML document generated by an identity provider (IdP) that supports SAML 2.0. The document includes the issuer\'s name, expiration information, and keys that can be used to validate the SAML authentication response (assertions) that are received from the IdP. You must generate the metadata document using the identity management software that is used as your organization\'s IdP.
#' 
#' For more information, see [About SAML 2.0-based Federation](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_providers_saml.html) in the *IAM User Guide*
#' @param Name &#91;required&#93; The name of the provider to create.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
create_saml_provider <- function (SAMLMetadataDocument, Name) 
{
    op <- new_operation(name = "CreateSAMLProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_saml_provider_input(SAMLMetadataDocument = SAMLMetadataDocument, 
        Name = Name)
    output <- create_saml_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates an IAM role that is linked to a specific AWS service
#'
#' Creates an IAM role that is linked to a specific AWS service. The service controls the attached policies and when the role can be deleted. This helps ensure that the service is not broken by an unexpectedly changed or deleted role, which could put your AWS resources into an unknown state. Allowing the service to control the role helps improve service stability and proper cleanup when a service and its role are no longer needed. For more information, see [Using Service-Linked Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/using-service-linked-roles.html) in the *IAM User Guide*.
#' 
#' To attach a policy to this service-linked role, you must make the request using the AWS service that depends on this role.
#'
#' @section Accepted Parameters:
#' ```
#' create_service_linked_role(
#'   AWSServiceName = "string",
#'   Description = "string",
#'   CustomSuffix = "string"
#' )
#' ```
#'
#' @param AWSServiceName &#91;required&#93; The service principal for the AWS service to which this role is attached. You use a string similar to a URL but without the http:// in front. For example: `elasticbeanstalk.amazonaws.com`.
#' 
#' Service principals are unique and case-sensitive. To find the exact service principal for your service-linked role, see [AWS Services That Work with IAM](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_aws-services-that-work-with-iam.html) in the *IAM User Guide* and look for the services that have **Yes** in the **Service-Linked Role** column. Choose the **Yes** link to view the service-linked role documentation for that service.
#' @param Description The description of the role.
#' @param CustomSuffix A string that you provide, which is combined with the service-provided prefix to form the complete role name. If you make multiple requests for the same service, then you must supply a different `CustomSuffix` for each request. Otherwise the request fails with a duplicate role name error. For example, you could add `-1` or `-debug` to the suffix.
#' 
#' Some services do not support the `CustomSuffix` parameter. If you provide an optional suffix and the operation fails, try the operation again without the suffix.
#'
#' @export
create_service_linked_role <- function (AWSServiceName, Description = NULL, 
    CustomSuffix = NULL) 
{
    op <- new_operation(name = "CreateServiceLinkedRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_service_linked_role_input(AWSServiceName = AWSServiceName, 
        Description = Description, CustomSuffix = CustomSuffix)
    output <- create_service_linked_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Generates a set of credentials consisting of a user name and password that can be used to access the service specified in the request
#'
#' Generates a set of credentials consisting of a user name and password that can be used to access the service specified in the request. These credentials are generated by IAM, and can be used only for the specified service.
#' 
#' You can have a maximum of two sets of service-specific credentials for each supported service per user.
#' 
#' The only supported service at this time is AWS CodeCommit.
#' 
#' You can reset the password to a new service-generated value by calling ResetServiceSpecificCredential.
#' 
#' For more information about service-specific credentials, see [Using IAM with AWS CodeCommit: Git Credentials, SSH Keys, and AWS Access Keys](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_ssh-keys.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_service_specific_credential(
#'   UserName = "string",
#'   ServiceName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user that is to be associated with the credentials. The new service-specific credentials have the same permissions as the associated user except that they can be used only to access the specified service.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param ServiceName &#91;required&#93; The name of the AWS service that is to be associated with the credentials. The service you specify here is the only service that can be accessed using these credentials.
#'
#' @export
create_service_specific_credential <- function (UserName, ServiceName) 
{
    op <- new_operation(name = "CreateServiceSpecificCredential", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- create_service_specific_credential_input(UserName = UserName, 
        ServiceName = ServiceName)
    output <- create_service_specific_credential_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new IAM user for your AWS account
#'
#' Creates a new IAM user for your AWS account.
#' 
#' For information about limitations on the number of IAM users you can create, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' create_user(
#'   Path = "string",
#'   UserName = "string",
#'   PermissionsBoundary = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param Path The path for the user name. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param UserName &#91;required&#93; The name of the user to create.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-. User names are not distinguished by case. For example, you cannot create users named both \"TESTUSER\" and \"testuser\".
#' @param PermissionsBoundary The ARN of the policy that is used to set the permissions boundary for the user.
#' @param Tags A list of tags that you want to attach to the newly created user. Each tag consists of a key name and an associated value. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#' 
#' If any one of the tags is invalid or if you exceed the allowed number of tags per user, then the entire request fails and the user is not created.
#'
#' @examples
#' # The following create-user command creates an IAM user named Bob in the
#' # current account.
#' \donttest{create_user(
#'   UserName = "Bob"
#' )}
#'
#' @export
create_user <- function (Path = NULL, UserName, PermissionsBoundary = NULL, 
    Tags = NULL) 
{
    op <- new_operation(name = "CreateUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_user_input(Path = Path, UserName = UserName, 
        PermissionsBoundary = PermissionsBoundary, Tags = Tags)
    output <- create_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new virtual MFA device for the AWS account
#'
#' Creates a new virtual MFA device for the AWS account. After creating the virtual MFA, use EnableMFADevice to attach the MFA device to an IAM user. For more information about creating and working with virtual MFA devices, go to [Using a Virtual MFA Device](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_VirtualMFA.html) in the *IAM User Guide*.
#' 
#' For information about limits on the number of MFA devices you can create, see [Limitations on Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#' 
#' The seed information contained in the QR code and the Base32 string should be treated like any other secret access information, such as your AWS access keys or your passwords. After you provision your virtual device, you should ensure that the information is destroyed following secure procedures.
#'
#' @section Accepted Parameters:
#' ```
#' create_virtual_mfa_device(
#'   Path = "string",
#'   VirtualMFADeviceName = "string"
#' )
#' ```
#'
#' @param Path The path for the virtual MFA device. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/).
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param VirtualMFADeviceName &#91;required&#93; The name of the virtual MFA device. Use with path to uniquely identify a virtual MFA device.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
create_virtual_mfa_device <- function (Path = NULL, VirtualMFADeviceName) 
{
    op <- new_operation(name = "CreateVirtualMFADevice", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_virtual_mfa_device_input(Path = Path, VirtualMFADeviceName = VirtualMFADeviceName)
    output <- create_virtual_mfa_device_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deactivates the specified MFA device and removes it from association with the user name for which it was originally enabled
#'
#' Deactivates the specified MFA device and removes it from association with the user name for which it was originally enabled.
#' 
#' For more information about creating and working with virtual MFA devices, go to [Enabling a Virtual Multi-factor Authentication (MFA) Device](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_VirtualMFA.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' deactivate_mfa_device(
#'   UserName = "string",
#'   SerialNumber = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user whose MFA device you want to deactivate.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SerialNumber &#91;required&#93; The serial number that uniquely identifies the MFA device. For virtual MFA devices, the serial number is the device ARN.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@:/-
#'
#' @export
deactivate_mfa_device <- function (UserName, SerialNumber) 
{
    op <- new_operation(name = "DeactivateMFADevice", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- deactivate_mfa_device_input(UserName = UserName, 
        SerialNumber = SerialNumber)
    output <- deactivate_mfa_device_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the access key pair associated with the specified IAM user
#'
#' Deletes the access key pair associated with the specified IAM user.
#' 
#' If you do not specify a user name, IAM determines the user name implicitly based on the AWS access key ID signing the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#'
#' @section Accepted Parameters:
#' ```
#' delete_access_key(
#'   UserName = "string",
#'   AccessKeyId = "string"
#' )
#' ```
#'
#' @param UserName The name of the user whose access key pair you want to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param AccessKeyId &#91;required&#93; The access key ID for the access key ID and secret access key you want to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#'
#' @examples
#' # The following command deletes one access key (access key ID and secret
#' # access key) assigned to the IAM user named Bob.
#' \donttest{delete_access_key(
#'   AccessKeyId = "AKIDPMS9RO4H3FEXAMPLE",
#'   UserName = "Bob"
#' )}
#'
#' @export
delete_access_key <- function (UserName = NULL, AccessKeyId) 
{
    op <- new_operation(name = "DeleteAccessKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_access_key_input(UserName = UserName, AccessKeyId = AccessKeyId)
    output <- delete_access_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified AWS account alias
#'
#' Deletes the specified AWS account alias. For information about using an AWS account alias, see [Using an Alias for Your AWS Account ID](http://docs.aws.amazon.com/IAM/latest/UserGuide/AccountAlias.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_account_alias(
#'   AccountAlias = "string"
#' )
#' ```
#'
#' @param AccountAlias &#91;required&#93; The name of the account alias to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of lowercase letters, digits, and dashes. You cannot start or finish with a dash, nor can you have two dashes in a row.
#'
#' @examples
#' # The following command removes the alias mycompany from the current AWS
#' # account:
#' \donttest{delete_account_alias(
#'   AccountAlias = "mycompany"
#' )}
#'
#' @export
delete_account_alias <- function (AccountAlias) 
{
    op <- new_operation(name = "DeleteAccountAlias", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_account_alias_input(AccountAlias = AccountAlias)
    output <- delete_account_alias_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the password policy for the AWS account
#'
#' Deletes the password policy for the AWS account. There are no parameters.
#'

#'

#'
#' @examples
#' # The following command removes the password policy from the current AWS
#' # account:
#' \donttest{delete_account_password_policy()}
#'
#' @export
delete_account_password_policy <- function () 
{
    op <- new_operation(name = "DeleteAccountPasswordPolicy", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_account_password_policy_input()
    output <- delete_account_password_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified IAM group
#'
#' Deletes the specified IAM group. The group must not contain any users or have any attached policies.
#'
#' @section Accepted Parameters:
#' ```
#' delete_group(
#'   GroupName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the IAM group to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
delete_group <- function (GroupName) 
{
    op <- new_operation(name = "DeleteGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_group_input(GroupName = GroupName)
    output <- delete_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified inline policy that is embedded in the specified IAM group
#'
#' Deletes the specified inline policy that is embedded in the specified IAM group.
#' 
#' A group can also have managed policies attached to it. To detach a managed policy from a group, use DetachGroupPolicy. For more information about policies, refer to [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_group_policy(
#'   GroupName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name (friendly name, not ARN) identifying the group that the policy is embedded in.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name identifying the policy document to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command deletes the policy named ExamplePolicy from the
#' # group named Admins:
#' \donttest{delete_group_policy(
#'   GroupName = "Admins",
#'   PolicyName = "ExamplePolicy"
#' )}
#'
#' @export
delete_group_policy <- function (GroupName, PolicyName) 
{
    op <- new_operation(name = "DeleteGroupPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_group_policy_input(GroupName = GroupName, 
        PolicyName = PolicyName)
    output <- delete_group_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified instance profile
#'
#' Deletes the specified instance profile. The instance profile must not have an associated role.
#' 
#' Make sure that you do not have any Amazon EC2 instances running with the instance profile you are about to delete. Deleting a role or instance profile that is associated with a running instance will break any applications running on the instance.
#' 
#' For more information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#'
#' @section Accepted Parameters:
#' ```
#' delete_instance_profile(
#'   InstanceProfileName = "string"
#' )
#' ```
#'
#' @param InstanceProfileName &#91;required&#93; The name of the instance profile to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command deletes the instance profile named
#' # ExampleInstanceProfile
#' \donttest{delete_instance_profile(
#'   InstanceProfileName = "ExampleInstanceProfile"
#' )}
#'
#' @export
delete_instance_profile <- function (InstanceProfileName) 
{
    op <- new_operation(name = "DeleteInstanceProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_instance_profile_input(InstanceProfileName = InstanceProfileName)
    output <- delete_instance_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the password for the specified IAM user, which terminates the user's ability to access AWS services through the AWS Management Console
#'
#' Deletes the password for the specified IAM user, which terminates the user\'s ability to access AWS services through the AWS Management Console.
#' 
#' Deleting a user\'s password does not prevent a user from accessing AWS through the command line interface or the API. To prevent all user access, you must also either make any access keys inactive or delete them. For more information about making keys inactive or deleting them, see UpdateAccessKey and DeleteAccessKey.
#'
#' @section Accepted Parameters:
#' ```
#' delete_login_profile(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user whose password you want to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command deletes the password for the IAM user named Bob.
#' \donttest{delete_login_profile(
#'   UserName = "Bob"
#' )}
#'
#' @export
delete_login_profile <- function (UserName) 
{
    op <- new_operation(name = "DeleteLoginProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_login_profile_input(UserName = UserName)
    output <- delete_login_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes an OpenID Connect identity provider (IdP) resource object in IAM
#'
#' Deletes an OpenID Connect identity provider (IdP) resource object in IAM.
#' 
#' Deleting an IAM OIDC provider resource does not update any roles that reference the provider as a principal in their trust policies. Any attempt to assume a role that references a deleted provider fails.
#' 
#' This operation is idempotent; it does not fail or return an error if you call the operation for a provider that does not exist.
#'
#' @section Accepted Parameters:
#' ```
#' delete_open_id_connect_provider(
#'   OpenIDConnectProviderArn = "string"
#' )
#' ```
#'
#' @param OpenIDConnectProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM OpenID Connect provider resource object to delete. You can get a list of OpenID Connect provider resource ARNs by using the ListOpenIDConnectProviders operation.
#'
#' @export
delete_open_id_connect_provider <- function (OpenIDConnectProviderArn) 
{
    op <- new_operation(name = "DeleteOpenIDConnectProvider", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_open_id_connect_provider_input(OpenIDConnectProviderArn = OpenIDConnectProviderArn)
    output <- delete_open_id_connect_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified managed policy
#'
#' Deletes the specified managed policy.
#' 
#' Before you can delete a managed policy, you must first detach the policy from all users, groups, and roles that it is attached to. In addition, you must delete all the policy\'s versions. The following steps describe the process for deleting a managed policy:
#' 
#' -   Detach the policy from all users, groups, and roles that the policy is attached to, using the DetachUserPolicy, DetachGroupPolicy, or DetachRolePolicy API operations. To list all the users, groups, and roles that a policy is attached to, use ListEntitiesForPolicy.
#' 
#' -   Delete all versions of the policy using DeletePolicyVersion. To list the policy\'s versions, use ListPolicyVersions. You cannot use DeletePolicyVersion to delete the version that is marked as the default version. You delete the policy\'s default version in the next step of the process.
#' 
#' -   Delete the policy (this automatically deletes the policy\'s default version) using this API.
#' 
#' For information about managed policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_policy(
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to delete.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
delete_policy <- function (PolicyArn) 
{
    op <- new_operation(name = "DeletePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_policy_input(PolicyArn = PolicyArn)
    output <- delete_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified version from the specified managed policy
#'
#' Deletes the specified version from the specified managed policy.
#' 
#' You cannot delete the default version from a policy using this API. To delete the default version from a policy, use DeletePolicy. To find out which version of a policy is marked as the default version, use ListPolicyVersions.
#' 
#' For information about versions for managed policies, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_policy_version(
#'   PolicyArn = "string",
#'   VersionId = "string"
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy from which you want to delete a version.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param VersionId &#91;required&#93; The policy version to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consists of the lowercase letter \'v\' followed by one or two digits, and optionally followed by a period \'.\' and a string of letters and digits.
#' 
#' For more information about managed policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @export
delete_policy_version <- function (PolicyArn, VersionId) 
{
    op <- new_operation(name = "DeletePolicyVersion", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_policy_version_input(PolicyArn = PolicyArn, 
        VersionId = VersionId)
    output <- delete_policy_version_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified role
#'
#' Deletes the specified role. The role must not have any policies attached. For more information about roles, go to [Working with Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html).
#' 
#' Make sure that you do not have any Amazon EC2 instances running with the role you are about to delete. Deleting a role or instance profile that is associated with a running instance will break any applications running on the instance.
#'
#' @section Accepted Parameters:
#' ```
#' delete_role(
#'   RoleName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command removes the role named Test-Role.
#' \donttest{delete_role(
#'   RoleName = "Test-Role"
#' )}
#'
#' @export
delete_role <- function (RoleName) 
{
    op <- new_operation(name = "DeleteRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_role_input(RoleName = RoleName)
    output <- delete_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the permissions boundary for the specified IAM role
#'
#' Deletes the permissions boundary for the specified IAM role.
#' 
#' Deleting the permissions boundary for a role might increase its permissions by allowing anyone who assumes the role to perform all the actions granted in its permissions policies.
#'
#' @section Accepted Parameters:
#' ```
#' delete_role_permissions_boundary(
#'   RoleName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) of the IAM role from which you want to remove the permissions boundary.
#'
#' @export
delete_role_permissions_boundary <- function (RoleName) 
{
    op <- new_operation(name = "DeleteRolePermissionsBoundary", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_role_permissions_boundary_input(RoleName = RoleName)
    output <- delete_role_permissions_boundary_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified inline policy that is embedded in the specified IAM role
#'
#' Deletes the specified inline policy that is embedded in the specified IAM role.
#' 
#' A role can also have managed policies attached to it. To detach a managed policy from a role, use DetachRolePolicy. For more information about policies, refer to [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_role_policy(
#'   RoleName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) identifying the role that the policy is embedded in.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the inline policy to delete from the specified IAM role.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command removes the policy named ExamplePolicy from the
#' # role named Test-Role.
#' \donttest{delete_role_policy(
#'   PolicyName = "ExamplePolicy",
#'   RoleName = "Test-Role"
#' )}
#'
#' @export
delete_role_policy <- function (RoleName, PolicyName) 
{
    op <- new_operation(name = "DeleteRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_role_policy_input(RoleName = RoleName, PolicyName = PolicyName)
    output <- delete_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a SAML provider resource in IAM
#'
#' Deletes a SAML provider resource in IAM.
#' 
#' Deleting the provider resource from IAM does not update any roles that reference the SAML provider resource\'s ARN as a principal in their trust policies. Any attempt to assume a role that references a non-existent provider resource ARN fails.
#' 
#' This operation requires [Signature Version 4](http://docs.aws.amazon.com/general/latest/gr/signature-version-4.html).
#'
#' @section Accepted Parameters:
#' ```
#' delete_saml_provider(
#'   SAMLProviderArn = "string"
#' )
#' ```
#'
#' @param SAMLProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the SAML provider to delete.
#'
#' @export
delete_saml_provider <- function (SAMLProviderArn) 
{
    op <- new_operation(name = "DeleteSAMLProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_saml_provider_input(SAMLProviderArn = SAMLProviderArn)
    output <- delete_saml_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified SSH public key
#'
#' Deletes the specified SSH public key.
#' 
#' The SSH public key deleted by this operation is used only for authenticating the associated IAM user to an AWS CodeCommit repository. For more information about using SSH keys to authenticate to an AWS CodeCommit repository, see [Set up AWS CodeCommit for SSH Connections](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-credentials-ssh.html) in the *AWS CodeCommit User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_ssh_public_key(
#'   UserName = "string",
#'   SSHPublicKeyId = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user associated with the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SSHPublicKeyId &#91;required&#93; The unique identifier for the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#'
#' @export
delete_ssh_public_key <- function (UserName, SSHPublicKeyId) 
{
    op <- new_operation(name = "DeleteSSHPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_ssh_public_key_input(UserName = UserName, 
        SSHPublicKeyId = SSHPublicKeyId)
    output <- delete_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified server certificate
#'
#' Deletes the specified server certificate.
#' 
#' For more information about working with server certificates, see [Working with Server Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs.html) in the *IAM User Guide*. This topic also includes a list of AWS services that can use the server certificates that you manage with IAM.
#' 
#' If you are using a server certificate with Elastic Load Balancing, deleting the certificate could have implications for your application. If Elastic Load Balancing doesn\'t detect the deletion of bound certificates, it may continue to use the certificates. This could cause Elastic Load Balancing to stop accepting traffic. We recommend that you remove the reference to the certificate from Elastic Load Balancing before using this command to delete the certificate. For more information, go to [DeleteLoadBalancerListeners](http://docs.aws.amazon.com/ElasticLoadBalancing/latest/APIReference/API_DeleteLoadBalancerListeners.html) in the *Elastic Load Balancing API Reference*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_server_certificate(
#'   ServerCertificateName = "string"
#' )
#' ```
#'
#' @param ServerCertificateName &#91;required&#93; The name of the server certificate you want to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
delete_server_certificate <- function (ServerCertificateName) 
{
    op <- new_operation(name = "DeleteServerCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_server_certificate_input(ServerCertificateName = ServerCertificateName)
    output <- delete_server_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Submits a service-linked role deletion request and returns a DeletionTaskId, which you can use to check the status of the deletion
#'
#' Submits a service-linked role deletion request and returns a `DeletionTaskId`, which you can use to check the status of the deletion. Before you call this operation, confirm that the role has no active sessions and that any resources used by the role in the linked service are deleted. If you call this operation more than once for the same service-linked role and an earlier deletion task is not complete, then the `DeletionTaskId` of the earlier request is returned.
#' 
#' If you submit a deletion request for a service-linked role whose linked service is still accessing a resource, then the deletion task fails. If it fails, the GetServiceLinkedRoleDeletionStatus API operation returns the reason for the failure, usually including the resources that must be deleted. To delete the service-linked role, you must first remove those resources from the linked service and then submit the deletion request again. Resources are specific to the service that is linked to the role. For more information about removing resources from a service, see the [AWS documentation](http://docs.aws.amazon.com/) for your service.
#' 
#' For more information about service-linked roles, see [Roles Terms and Concepts: AWS Service-Linked Role](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_terms-and-concepts.html#iam-term-service-linked-role) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_service_linked_role(
#'   RoleName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the service-linked role to be deleted.
#'
#' @export
delete_service_linked_role <- function (RoleName) 
{
    op <- new_operation(name = "DeleteServiceLinkedRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_service_linked_role_input(RoleName = RoleName)
    output <- delete_service_linked_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified service-specific credential
#'
#' Deletes the specified service-specific credential.
#'
#' @section Accepted Parameters:
#' ```
#' delete_service_specific_credential(
#'   UserName = "string",
#'   ServiceSpecificCredentialId = "string"
#' )
#' ```
#'
#' @param UserName The name of the IAM user associated with the service-specific credential. If this value is not specified, then the operation assumes the user whose credentials are used to call the operation.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param ServiceSpecificCredentialId &#91;required&#93; The unique identifier of the service-specific credential. You can get this value by calling ListServiceSpecificCredentials.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#'
#' @export
delete_service_specific_credential <- function (UserName = NULL, 
    ServiceSpecificCredentialId) 
{
    op <- new_operation(name = "DeleteServiceSpecificCredential", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_service_specific_credential_input(UserName = UserName, 
        ServiceSpecificCredentialId = ServiceSpecificCredentialId)
    output <- delete_service_specific_credential_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a signing certificate associated with the specified IAM user
#'
#' Deletes a signing certificate associated with the specified IAM user.
#' 
#' If you do not specify a user name, IAM determines the user name implicitly based on the AWS access key ID signing the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated IAM users.
#'
#' @section Accepted Parameters:
#' ```
#' delete_signing_certificate(
#'   UserName = "string",
#'   CertificateId = "string"
#' )
#' ```
#'
#' @param UserName The name of the user the signing certificate belongs to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param CertificateId &#91;required&#93; The ID of the signing certificate to delete.
#' 
#' The format of this parameter, as described by its [regex](http://wikipedia.org/wiki/regex) pattern, is a string of characters that can be upper- or lower-cased letters or digits.
#'
#' @examples
#' # The following command deletes the specified signing certificate for the
#' # IAM user named Anika.
#' \donttest{delete_signing_certificate(
#'   CertificateId = "TA7SMP42TDN5Z26OBPJE7EXAMPLE",
#'   UserName = "Anika"
#' )}
#'
#' @export
delete_signing_certificate <- function (UserName = NULL, CertificateId) 
{
    op <- new_operation(name = "DeleteSigningCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_signing_certificate_input(UserName = UserName, 
        CertificateId = CertificateId)
    output <- delete_signing_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified IAM user
#'
#' Deletes the specified IAM user. The user must not belong to any groups or have any access keys, signing certificates, MFA devices enabled for AWS, or attached policies.
#'
#' @section Accepted Parameters:
#' ```
#' delete_user(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command removes the IAM user named Bob from the current
#' # account.
#' \donttest{delete_user(
#'   UserName = "Bob"
#' )}
#'
#' @export
delete_user <- function (UserName) 
{
    op <- new_operation(name = "DeleteUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_user_input(UserName = UserName)
    output <- delete_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the permissions boundary for the specified IAM user
#'
#' Deletes the permissions boundary for the specified IAM user.
#' 
#' Deleting the permissions boundary for a user might increase its permissions by allowing the user to perform all the actions granted in its permissions policies.
#'
#' @section Accepted Parameters:
#' ```
#' delete_user_permissions_boundary(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) of the IAM user from which you want to remove the permissions boundary.
#'
#' @export
delete_user_permissions_boundary <- function (UserName) 
{
    op <- new_operation(name = "DeleteUserPermissionsBoundary", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- delete_user_permissions_boundary_input(UserName = UserName)
    output <- delete_user_permissions_boundary_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified inline policy that is embedded in the specified IAM user
#'
#' Deletes the specified inline policy that is embedded in the specified IAM user.
#' 
#' A user can also have managed policies attached to it. To detach a managed policy from a user, use DetachUserPolicy. For more information about policies, refer to [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' delete_user_policy(
#'   UserName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) identifying the user that the policy is embedded in.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name identifying the policy document to delete.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following delete-user-policy command removes the specified policy
#' # from the IAM user named Juan:
#' \donttest{delete_user_policy(
#'   PolicyName = "ExamplePolicy",
#'   UserName = "Juan"
#' )}
#'
#' @export
delete_user_policy <- function (UserName, PolicyName) 
{
    op <- new_operation(name = "DeleteUserPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_user_policy_input(UserName = UserName, PolicyName = PolicyName)
    output <- delete_user_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a virtual MFA device
#'
#' Deletes a virtual MFA device.
#' 
#' You must deactivate a user\'s virtual MFA device before you can delete it. For information about deactivating MFA devices, see DeactivateMFADevice.
#'
#' @section Accepted Parameters:
#' ```
#' delete_virtual_mfa_device(
#'   SerialNumber = "string"
#' )
#' ```
#'
#' @param SerialNumber &#91;required&#93; The serial number that uniquely identifies the MFA device. For virtual MFA devices, the serial number is the same as the ARN.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@:/-
#'
#' @examples
#' # The following delete-virtual-mfa-device command removes the specified
#' # MFA device from the current AWS account.
#' \donttest{delete_virtual_mfa_device(
#'   SerialNumber = "arn:aws:iam::123456789012:mfa/ExampleName"
#' )}
#'
#' @export
delete_virtual_mfa_device <- function (SerialNumber) 
{
    op <- new_operation(name = "DeleteVirtualMFADevice", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_virtual_mfa_device_input(SerialNumber = SerialNumber)
    output <- delete_virtual_mfa_device_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified managed policy from the specified IAM group
#'
#' Removes the specified managed policy from the specified IAM group.
#' 
#' A group can also have inline policies embedded with it. To delete an inline policy, use the DeleteGroupPolicy API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' detach_group_policy(
#'   GroupName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name (friendly name, not ARN) of the IAM group to detach the policy from.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to detach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
detach_group_policy <- function (GroupName, PolicyArn) 
{
    op <- new_operation(name = "DetachGroupPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_group_policy_input(GroupName = GroupName, 
        PolicyArn = PolicyArn)
    output <- detach_group_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified managed policy from the specified role
#'
#' Removes the specified managed policy from the specified role.
#' 
#' A role can also have inline policies embedded with it. To delete an inline policy, use the DeleteRolePolicy API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' detach_role_policy(
#'   RoleName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) of the IAM role to detach the policy from.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to detach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
detach_role_policy <- function (RoleName, PolicyArn) 
{
    op <- new_operation(name = "DetachRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_role_policy_input(RoleName = RoleName, PolicyArn = PolicyArn)
    output <- detach_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified managed policy from the specified user
#'
#' Removes the specified managed policy from the specified user.
#' 
#' A user can also have inline policies embedded with it. To delete an inline policy, use the DeleteUserPolicy API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' detach_user_policy(
#'   UserName = "string",
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) of the IAM user to detach the policy from.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy you want to detach.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
detach_user_policy <- function (UserName, PolicyArn) 
{
    op <- new_operation(name = "DetachUserPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- detach_user_policy_input(UserName = UserName, PolicyArn = PolicyArn)
    output <- detach_user_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Enables the specified MFA device and associates it with the specified IAM user
#'
#' Enables the specified MFA device and associates it with the specified IAM user. When enabled, the MFA device is required for every subsequent login by the IAM user associated with the device.
#'
#' @section Accepted Parameters:
#' ```
#' enable_mfa_device(
#'   UserName = "string",
#'   SerialNumber = "string",
#'   AuthenticationCode1 = "string",
#'   AuthenticationCode2 = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user for whom you want to enable the MFA device.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SerialNumber &#91;required&#93; The serial number that uniquely identifies the MFA device. For virtual MFA devices, the serial number is the device ARN.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@:/-
#' @param AuthenticationCode1 &#91;required&#93; An authentication code emitted by the device.
#' 
#' The format for this parameter is a string of six digits.
#' 
#' Submit your request immediately after generating the authentication codes. If you generate the codes and then wait too long to submit the request, the MFA device successfully associates with the user but the MFA device becomes out of sync. This happens because time-based one-time passwords (TOTP) expire after a short period of time. If this happens, you can [resync the device](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_mfa_sync.html).
#' @param AuthenticationCode2 &#91;required&#93; A subsequent authentication code emitted by the device.
#' 
#' The format for this parameter is a string of six digits.
#' 
#' Submit your request immediately after generating the authentication codes. If you generate the codes and then wait too long to submit the request, the MFA device successfully associates with the user but the MFA device becomes out of sync. This happens because time-based one-time passwords (TOTP) expire after a short period of time. If this happens, you can [resync the device](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_mfa_sync.html).
#'
#' @export
enable_mfa_device <- function (UserName, SerialNumber, AuthenticationCode1, 
    AuthenticationCode2) 
{
    op <- new_operation(name = "EnableMFADevice", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- enable_mfa_device_input(UserName = UserName, SerialNumber = SerialNumber, 
        AuthenticationCode1 = AuthenticationCode1, AuthenticationCode2 = AuthenticationCode2)
    output <- enable_mfa_device_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Generates a credential report for the AWS account
#'
#' Generates a credential report for the AWS account. For more information about the credential report, see [Getting Credential Reports](http://docs.aws.amazon.com/IAM/latest/UserGuide/credential-reports.html) in the *IAM User Guide*.
#'

#'

#'
#' @export
generate_credential_report <- function () 
{
    op <- new_operation(name = "GenerateCredentialReport", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- generate_credential_report_input()
    output <- generate_credential_report_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Generates a request for a report that includes details about when an IAM resource (user, group, role, or policy) was last used in an attempt to access AWS services
#'
#' Generates a request for a report that includes details about when an IAM resource (user, group, role, or policy) was last used in an attempt to access AWS services. Recent activity usually appears within four hours. IAM reports activity for the last 365 days, or less if your region began supporting this feature within the last year. For more information, see [Regions Where Data Is Tracked](http://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_access-advisor.html#access-advisor_tracking-period).
#' 
#' The service last accessed data includes all attempts to access an AWS API, not just the successful ones. This includes all attempts that were made using the AWS Management Console, the AWS API through any of the SDKs, or any of the command line tools. An unexpected entry in the service last accessed data does not mean that your account has been compromised, because the request might have been denied. Refer to your CloudTrail logs as the authoritative source for information about all API calls and whether they were successful or denied access. For more information, see [Logging IAM Events with CloudTrail](http://docs.aws.amazon.com/IAM/latest/UserGuide/cloudtrail-integration.html) in the *IAM User Guide*.
#' 
#' The `GenerateServiceLastAccessedDetails` operation returns a `JobId`. Use this parameter in the following operations to retrieve the following details from your report:
#' 
#' -   GetServiceLastAccessedDetails -- Use this operation for users, groups, roles, or policies to list every AWS service that the resource could access using permissions policies. For each service, the response includes information about the most recent access attempt.
#' 
#' -   GetServiceLastAccessedDetailsWithEntities -- Use this operation for groups and policies to list information about the associated entities (users or roles) that attempted to access a specific AWS service.
#' 
#' To check the status of the `GenerateServiceLastAccessedDetails` request, use the `JobId` parameter in the same operations and test the `JobStatus` response parameter.
#' 
#' For additional information about the permissions policies that allow an identity (user, group, or role) to access specific services, use the ListPoliciesGrantingServiceAccess operation.
#' 
#' Service last accessed data does not use other policy types when determining whether a resource could access a service. These other policy types include resource-based policies, access control lists, AWS Organizations policies, IAM permissions boundaries, and AWS STS assume role policies. It only applies permissions policy logic. For more about the evaluation of policy types, see [Evaluating Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html#policy-eval-basics) in the *IAM User Guide*.
#' 
#' For more information about service last accessed data, see [Reducing Policy Scope by Viewing User Activity](http://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_access-advisor.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' generate_service_last_accessed_details(
#'   Arn = "string"
#' )
#' ```
#'
#' @param Arn &#91;required&#93; The ARN of the IAM resource (user, group, role, or managed policy) used to generate information about when the resource was last used in an attempt to access an AWS service.
#'
#' @examples
#' # The following operation generates a report for the policy:
#' # ExamplePolicy1
#' \donttest{generate_service_last_accessed_details(
#'   Arn = "arn:aws:iam::123456789012:policy/ExamplePolicy1"
#' )}
#'
#' @export
generate_service_last_accessed_details <- function (Arn) 
{
    op <- new_operation(name = "GenerateServiceLastAccessedDetails", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- generate_service_last_accessed_details_input(Arn = Arn)
    output <- generate_service_last_accessed_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about when the specified access key was last used
#'
#' Retrieves information about when the specified access key was last used. The information includes the date and time of last use, along with the AWS service and region that were specified in the last request made with that key.
#'
#' @section Accepted Parameters:
#' ```
#' get_access_key_last_used(
#'   AccessKeyId = "string"
#' )
#' ```
#'
#' @param AccessKeyId &#91;required&#93; The identifier of an access key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#'
#' @export
get_access_key_last_used <- function (AccessKeyId) 
{
    op <- new_operation(name = "GetAccessKeyLastUsed", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_access_key_last_used_input(AccessKeyId = AccessKeyId)
    output <- get_access_key_last_used_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about all IAM users, groups, roles, and policies in your AWS account, including their relationships to one another
#'
#' Retrieves information about all IAM users, groups, roles, and policies in your AWS account, including their relationships to one another. Use this API to obtain a snapshot of the configuration of IAM permissions (users, groups, roles, and policies) in your account.
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#' 
#' You can optionally filter the results using the `Filter` parameter. You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' get_account_authorization_details(
#'   Filter = list(
#'     "User"|"Role"|"Group"|"LocalManagedPolicy"|"AWSManagedPolicy"
#'   ),
#'   MaxItems = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param Filter A list of entity types used to filter the results. Only the entities that match the types you specify are included in the output. Use the value `LocalManagedPolicy` to include customer managed policies.
#' 
#' The format for this parameter is a comma-separated (if more than one) list of strings. Each string value in the list must be one of the valid values listed below.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#'
#' @export
get_account_authorization_details <- function (Filter = NULL, 
    MaxItems = NULL, Marker = NULL) 
{
    op <- new_operation(name = "GetAccountAuthorizationDetails", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_account_authorization_details_input(Filter = Filter, 
        MaxItems = MaxItems, Marker = Marker)
    output <- get_account_authorization_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the password policy for the AWS account
#'
#' Retrieves the password policy for the AWS account. For more information about using a password policy, go to [Managing an IAM Password Policy](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_ManagingPasswordPolicies.html).
#'

#'

#'
#' @examples
#' # The following command displays details about the password policy for the
#' # current AWS account.
#' \donttest{get_account_password_policy()}
#'
#' @export
get_account_password_policy <- function () 
{
    op <- new_operation(name = "GetAccountPasswordPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_account_password_policy_input()
    output <- get_account_password_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about IAM entity usage and IAM quotas in the AWS account
#'
#' Retrieves information about IAM entity usage and IAM quotas in the AWS account.
#' 
#' For information about limitations on IAM entities, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#'

#'

#'
#' @examples
#' # The following command returns information about the IAM entity quotas
#' # and usage in the current AWS account.
#' \donttest{get_account_summary()}
#'
#' @export
get_account_summary <- function () 
{
    op <- new_operation(name = "GetAccountSummary", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_account_summary_input()
    output <- get_account_summary_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a list of all of the context keys referenced in the input policies
#'
#' Gets a list of all of the context keys referenced in the input policies. The policies are supplied as a list of one or more strings. To get the context keys from policies associated with an IAM user, group, or role, use GetContextKeysForPrincipalPolicy.
#' 
#' Context keys are variables maintained by AWS and its services that provide details about the context of an API query request. Context keys can be evaluated by testing against a value specified in an IAM policy. Use `GetContextKeysForCustomPolicy` to understand what key names and values you must supply when you call SimulateCustomPolicy. Note that all parameters are shown in unencoded form here for clarity but must be URL encoded to be included as a part of a real HTML request.
#'
#' @section Accepted Parameters:
#' ```
#' get_context_keys_for_custom_policy(
#'   PolicyInputList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param PolicyInputList &#91;required&#93; A list of policies for which you want the list of context keys referenced in those policies. Each document is specified as a string containing the complete, valid JSON text of an IAM policy.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @export
get_context_keys_for_custom_policy <- function (PolicyInputList) 
{
    op <- new_operation(name = "GetContextKeysForCustomPolicy", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_context_keys_for_custom_policy_input(PolicyInputList = PolicyInputList)
    output <- get_context_keys_for_custom_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a list of all of the context keys referenced in all the IAM policies that are attached to the specified IAM entity
#'
#' Gets a list of all of the context keys referenced in all the IAM policies that are attached to the specified IAM entity. The entity can be an IAM user, group, or role. If you specify a user, then the request also includes all of the policies attached to groups that the user is a member of.
#' 
#' You can optionally include a list of one or more additional policies, specified as strings. If you want to include *only* a list of policies by string, use GetContextKeysForCustomPolicy instead.
#' 
#' **Note:** This API discloses information about the permissions granted to other users. If you do not want users to see other user\'s permissions, then consider allowing them to use GetContextKeysForCustomPolicy instead.
#' 
#' Context keys are variables maintained by AWS and its services that provide details about the context of an API query request. Context keys can be evaluated by testing against a value in an IAM policy. Use GetContextKeysForPrincipalPolicy to understand what key names and values you must supply when you call SimulatePrincipalPolicy.
#'
#' @section Accepted Parameters:
#' ```
#' get_context_keys_for_principal_policy(
#'   PolicySourceArn = "string",
#'   PolicyInputList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param PolicySourceArn &#91;required&#93; The ARN of a user, group, or role whose policies contain the context keys that you want listed. If you specify a user, the list includes context keys that are found in all policies that are attached to the user. The list also includes all groups that the user is a member of. If you pick a group or a role, then it includes only those context keys that are found in policies attached to that entity. Note that all parameters are shown in unencoded form here for clarity, but must be URL encoded to be included as a part of a real HTML request.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param PolicyInputList An optional list of additional policies for which you want the list of context keys that are referenced.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @export
get_context_keys_for_principal_policy <- function (PolicySourceArn, 
    PolicyInputList = NULL) 
{
    op <- new_operation(name = "GetContextKeysForPrincipalPolicy", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_context_keys_for_principal_policy_input(PolicySourceArn = PolicySourceArn, 
        PolicyInputList = PolicyInputList)
    output <- get_context_keys_for_principal_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves a credential report for the AWS account
#'
#' Retrieves a credential report for the AWS account. For more information about the credential report, see [Getting Credential Reports](http://docs.aws.amazon.com/IAM/latest/UserGuide/credential-reports.html) in the *IAM User Guide*.
#'

#'

#'
#' @export
get_credential_report <- function () 
{
    op <- new_operation(name = "GetCredentialReport", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_credential_report_input()
    output <- get_credential_report_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns a list of IAM users that are in the specified IAM group
#'
#' Returns a list of IAM users that are in the specified IAM group. You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' get_group(
#'   GroupName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
get_group <- function (GroupName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "GetGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_group_input(GroupName = GroupName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- get_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the specified inline policy document that is embedded in the specified IAM group
#'
#' Retrieves the specified inline policy document that is embedded in the specified IAM group.
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#' 
#' An IAM group can also have managed policies attached to it. To retrieve a managed policy document that is attached to a group, use GetPolicy to determine the policy\'s default version, then use GetPolicyVersion to retrieve the policy document.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_group_policy(
#'   GroupName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group the policy is associated with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document to get.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
get_group_policy <- function (GroupName, PolicyName) 
{
    op <- new_operation(name = "GetGroupPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_group_policy_input(GroupName = GroupName, PolicyName = PolicyName)
    output <- get_group_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified instance profile, including the instance profile's path, GUID, ARN, and role
#'
#' Retrieves information about the specified instance profile, including the instance profile\'s path, GUID, ARN, and role. For more information about instance profiles, see [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_instance_profile(
#'   InstanceProfileName = "string"
#' )
#' ```
#'
#' @param InstanceProfileName &#91;required&#93; The name of the instance profile to get information about.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command gets information about the instance profile named
#' # ExampleInstanceProfile.
#' \donttest{get_instance_profile(
#'   InstanceProfileName = "ExampleInstanceProfile"
#' )}
#'
#' @export
get_instance_profile <- function (InstanceProfileName) 
{
    op <- new_operation(name = "GetInstanceProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_instance_profile_input(InstanceProfileName = InstanceProfileName)
    output <- get_instance_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the user name and password-creation date for the specified IAM user
#'
#' Retrieves the user name and password-creation date for the specified IAM user. If the user has not been assigned a password, the operation returns a 404 (`NoSuchEntity`) error.
#'
#' @section Accepted Parameters:
#' ```
#' get_login_profile(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user whose login profile you want to retrieve.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command gets information about the password for the IAM
#' # user named Anika.
#' \donttest{get_login_profile(
#'   UserName = "Anika"
#' )}
#'
#' @export
get_login_profile <- function (UserName) 
{
    op <- new_operation(name = "GetLoginProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_login_profile_input(UserName = UserName)
    output <- get_login_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the specified OpenID Connect (OIDC) provider resource object in IAM
#'
#' Returns information about the specified OpenID Connect (OIDC) provider resource object in IAM.
#'
#' @section Accepted Parameters:
#' ```
#' get_open_id_connect_provider(
#'   OpenIDConnectProviderArn = "string"
#' )
#' ```
#'
#' @param OpenIDConnectProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the OIDC provider resource object in IAM to get information for. You can get a list of OIDC provider resource ARNs by using the ListOpenIDConnectProviders operation.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
get_open_id_connect_provider <- function (OpenIDConnectProviderArn) 
{
    op <- new_operation(name = "GetOpenIDConnectProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_open_id_connect_provider_input(OpenIDConnectProviderArn = OpenIDConnectProviderArn)
    output <- get_open_id_connect_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified managed policy, including the policy's default version and the total number of IAM users, groups, and roles to which the policy is attached
#'
#' Retrieves information about the specified managed policy, including the policy\'s default version and the total number of IAM users, groups, and roles to which the policy is attached. To retrieve the list of the specific users, groups, and roles that the policy is attached to, use the ListEntitiesForPolicy API. This API returns metadata about the policy. To retrieve the actual policy document for a specific version of the policy, use GetPolicyVersion.
#' 
#' This API retrieves information about managed policies. To retrieve information about an inline policy that is embedded with an IAM user, group, or role, use the GetUserPolicy, GetGroupPolicy, or GetRolePolicy API.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_policy(
#'   PolicyArn = "string"
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the managed policy that you want information about.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
get_policy <- function (PolicyArn) 
{
    op <- new_operation(name = "GetPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_policy_input(PolicyArn = PolicyArn)
    output <- get_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified version of the specified managed policy, including the policy document
#'
#' Retrieves information about the specified version of the specified managed policy, including the policy document.
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#' 
#' To list the available versions for a policy, use ListPolicyVersions.
#' 
#' This API retrieves information about managed policies. To retrieve information about an inline policy that is embedded in a user, group, or role, use the GetUserPolicy, GetGroupPolicy, or GetRolePolicy API.
#' 
#' For more information about the types of policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' For more information about managed policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_policy_version(
#'   PolicyArn = "string",
#'   VersionId = "string"
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the managed policy that you want information about.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param VersionId &#91;required&#93; Identifies the policy version to retrieve.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consists of the lowercase letter \'v\' followed by one or two digits, and optionally followed by a period \'.\' and a string of letters and digits.
#'
#' @export
get_policy_version <- function (PolicyArn, VersionId) 
{
    op <- new_operation(name = "GetPolicyVersion", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_policy_version_input(PolicyArn = PolicyArn, 
        VersionId = VersionId)
    output <- get_policy_version_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified role, including the role's path, GUID, ARN, and the role's trust policy that grants permission to assume the role
#'
#' Retrieves information about the specified role, including the role\'s path, GUID, ARN, and the role\'s trust policy that grants permission to assume the role. For more information about roles, see [Working with Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html).
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#'
#' @section Accepted Parameters:
#' ```
#' get_role(
#'   RoleName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the IAM role to get information about.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command gets information about the role named Test-Role.
#' \donttest{get_role(
#'   RoleName = "Test-Role"
#' )}
#'
#' @export
get_role <- function (RoleName) 
{
    op <- new_operation(name = "GetRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_role_input(RoleName = RoleName)
    output <- get_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the specified inline policy document that is embedded with the specified IAM role
#'
#' Retrieves the specified inline policy document that is embedded with the specified IAM role.
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#' 
#' An IAM role can also have managed policies attached to it. To retrieve a managed policy document that is attached to a role, use GetPolicy to determine the policy\'s default version, then use GetPolicyVersion to retrieve the policy document.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' For more information about roles, see [Using Roles to Delegate Permissions and Federate Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/roles-toplevel.html).
#'
#' @section Accepted Parameters:
#' ```
#' get_role_policy(
#'   RoleName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role associated with the policy.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document to get.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
get_role_policy <- function (RoleName, PolicyName) 
{
    op <- new_operation(name = "GetRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_role_policy_input(RoleName = RoleName, PolicyName = PolicyName)
    output <- get_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the SAML provider metadocument that was uploaded when the IAM SAML provider resource object was created or updated
#'
#' Returns the SAML provider metadocument that was uploaded when the IAM SAML provider resource object was created or updated.
#' 
#' This operation requires [Signature Version 4](http://docs.aws.amazon.com/general/latest/gr/signature-version-4.html).
#'
#' @section Accepted Parameters:
#' ```
#' get_saml_provider(
#'   SAMLProviderArn = "string"
#' )
#' ```
#'
#' @param SAMLProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the SAML provider resource object in IAM to get information about.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
get_saml_provider <- function (SAMLProviderArn) 
{
    op <- new_operation(name = "GetSAMLProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_saml_provider_input(SAMLProviderArn = SAMLProviderArn)
    output <- get_saml_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the specified SSH public key, including metadata about the key
#'
#' Retrieves the specified SSH public key, including metadata about the key.
#' 
#' The SSH public key retrieved by this operation is used only for authenticating the associated IAM user to an AWS CodeCommit repository. For more information about using SSH keys to authenticate to an AWS CodeCommit repository, see [Set up AWS CodeCommit for SSH Connections](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-credentials-ssh.html) in the *AWS CodeCommit User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_ssh_public_key(
#'   UserName = "string",
#'   SSHPublicKeyId = "string",
#'   Encoding = "SSH"|"PEM"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user associated with the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SSHPublicKeyId &#91;required&#93; The unique identifier for the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#' @param Encoding &#91;required&#93; Specifies the public key encoding format to use in the response. To retrieve the public key in ssh-rsa format, use `SSH`. To retrieve the public key in PEM format, use `PEM`.
#'
#' @export
get_ssh_public_key <- function (UserName, SSHPublicKeyId, Encoding) 
{
    op <- new_operation(name = "GetSSHPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_ssh_public_key_input(UserName = UserName, SSHPublicKeyId = SSHPublicKeyId, 
        Encoding = Encoding)
    output <- get_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified server certificate stored in IAM
#'
#' Retrieves information about the specified server certificate stored in IAM.
#' 
#' For more information about working with server certificates, see [Working with Server Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs.html) in the *IAM User Guide*. This topic includes a list of AWS services that can use the server certificates that you manage with IAM.
#'
#' @section Accepted Parameters:
#' ```
#' get_server_certificate(
#'   ServerCertificateName = "string"
#' )
#' ```
#'
#' @param ServerCertificateName &#91;required&#93; The name of the server certificate you want to retrieve information about.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
get_server_certificate <- function (ServerCertificateName) 
{
    op <- new_operation(name = "GetServerCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_server_certificate_input(ServerCertificateName = ServerCertificateName)
    output <- get_server_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' After you generate a user, group, role, or policy report using the GenerateServiceLastAccessedDetails operation, you can use the JobId parameter in GetServiceLastAccessedDetails
#'
#' After you generate a user, group, role, or policy report using the `GenerateServiceLastAccessedDetails` operation, you can use the `JobId` parameter in `GetServiceLastAccessedDetails`. This operation retrieves the status of your report job and a list of AWS services that the resource (user, group, role, or managed policy) can access.
#' 
#' Service last accessed data does not use other policy types when determining whether a resource could access a service. These other policy types include resource-based policies, access control lists, AWS Organizations policies, IAM permissions boundaries, and AWS STS assume role policies. It only applies permissions policy logic. For more about the evaluation of policy types, see [Evaluating Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html#policy-eval-basics) in the *IAM User Guide*.
#' 
#' For each service that the resource could access using permissions policies, the operation returns details about the most recent access attempt. If there was no attempt, the service is listed without details about the most recent attempt to access the service. If the operation fails, the `GetServiceLastAccessedDetails` operation returns the reason that it failed.
#' 
#' The `GetServiceLastAccessedDetails` operation returns a list of services that includes the number of entities that have attempted to access the service and the date and time of the last attempt. It also returns the ARN of the following entity, depending on the resource ARN that you used to generate the report:
#' 
#' -   **User** -- Returns the user ARN that you used to generate the report
#' 
#' -   **Group** -- Returns the ARN of the group member (user) that last attempted to access the service
#' 
#' -   **Role** -- Returns the role ARN that you used to generate the report
#' 
#' -   **Policy** -- Returns the ARN of the user or role that last used the policy to attempt to access the service
#' 
#' By default, the list is sorted by service namespace.
#'
#' @section Accepted Parameters:
#' ```
#' get_service_last_accessed_details(
#'   JobId = "string",
#'   MaxItems = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param JobId &#91;required&#93; The ID of the request generated by the GenerateServiceLastAccessedDetails operation.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#'
#' @examples
#' # The following operation gets details about the report with the job ID:
#' # examplef-1305-c245-eba4-71fe298bcda7
#' \donttest{get_service_last_accessed_details(
#'   JobId = "examplef-1305-c245-eba4-71fe298bcda7"
#' )}
#'
#' @export
get_service_last_accessed_details <- function (JobId, MaxItems = NULL, 
    Marker = NULL) 
{
    op <- new_operation(name = "GetServiceLastAccessedDetails", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_service_last_accessed_details_input(JobId = JobId, 
        MaxItems = MaxItems, Marker = Marker)
    output <- get_service_last_accessed_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' After you generate a group or policy report using the GenerateServiceLastAccessedDetails operation, you can use the JobId parameter in GetServiceLastAccessedDetailsWithEntities
#'
#' After you generate a group or policy report using the `GenerateServiceLastAccessedDetails` operation, you can use the `JobId` parameter in `GetServiceLastAccessedDetailsWithEntities`. This operation retrieves the status of your report job and a list of entities that could have used group or policy permissions to access the specified service.
#' 
#' -   **Group** -- For a group report, this operation returns a list of users in the group that could have used the group's policies in an attempt to access the service.
#' 
#' -   **Policy** -- For a policy report, this operation returns a list of entities (users or roles) that could have used the policy in an attempt to access the service.
#' 
#' You can also use this operation for user or role reports to retrieve details about those entities.
#' 
#' If the operation fails, the `GetServiceLastAccessedDetailsWithEntities` operation returns the reason that it failed.
#' 
#' By default, the list of associated entities is sorted by date, with the most recent access listed first.
#'
#' @section Accepted Parameters:
#' ```
#' get_service_last_accessed_details_with_entities(
#'   JobId = "string",
#'   ServiceNamespace = "string",
#'   MaxItems = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param JobId &#91;required&#93; The ID of the request generated by the `GenerateServiceLastAccessedDetails` operation.
#' @param ServiceNamespace &#91;required&#93; The service namespace for an AWS service. Provide the service namespace to learn when the IAM entity last attempted to access the specified service.
#' 
#' To learn the service namespace for a service, go to [Actions, Resources, and Condition Keys for AWS Services](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_actions-resources-contextkeys.html) in the *IAM User Guide* and choose the name of the service to view details for that service. In the first paragraph, find the service prefix. For example, `(service prefix: a4b)`. For more information about service namespaces, see [AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html#genref-aws-service-namespaces) in the *AWS General Reference*.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#'
#' @examples
#' # The following operation returns details about the entities that
#' # attempted to access the IAM service.
#' \donttest{get_service_last_accessed_details_with_entities(
#'   JobId = "examplef-1305-c245-eba4-71fe298bcda7",
#'   ServiceNamespace = "iam"
#' )}
#'
#' @export
get_service_last_accessed_details_with_entities <- function (JobId, 
    ServiceNamespace, MaxItems = NULL, Marker = NULL) 
{
    op <- new_operation(name = "GetServiceLastAccessedDetailsWithEntities", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_service_last_accessed_details_with_entities_input(JobId = JobId, 
        ServiceNamespace = ServiceNamespace, MaxItems = MaxItems, 
        Marker = Marker)
    output <- get_service_last_accessed_details_with_entities_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the status of your service-linked role deletion
#'
#' Retrieves the status of your service-linked role deletion. After you use the DeleteServiceLinkedRole API operation to submit a service-linked role for deletion, you can use the `DeletionTaskId` parameter in `GetServiceLinkedRoleDeletionStatus` to check the status of the deletion. If the deletion fails, this operation returns the reason that it failed, if that information is returned by the service.
#'
#' @section Accepted Parameters:
#' ```
#' get_service_linked_role_deletion_status(
#'   DeletionTaskId = "string"
#' )
#' ```
#'
#' @param DeletionTaskId &#91;required&#93; The deletion task identifier. This identifier is returned by the DeleteServiceLinkedRole operation in the format `task/aws-service-role/<service-principal-name>/<role-name>/<task-uuid>`.
#'
#' @export
get_service_linked_role_deletion_status <- function (DeletionTaskId) 
{
    op <- new_operation(name = "GetServiceLinkedRoleDeletionStatus", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- get_service_linked_role_deletion_status_input(DeletionTaskId = DeletionTaskId)
    output <- get_service_linked_role_deletion_status_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves information about the specified IAM user, including the user's creation date, path, unique ID, and ARN
#'
#' Retrieves information about the specified IAM user, including the user\'s creation date, path, unique ID, and ARN.
#' 
#' If you do not specify a user name, IAM determines the user name implicitly based on the AWS access key ID used to sign the request to this API.
#'
#' @section Accepted Parameters:
#' ```
#' get_user(
#'   UserName = "string"
#' )
#' ```
#'
#' @param UserName The name of the user to get information about.
#' 
#' This parameter is optional. If it is not included, it defaults to the user making the request. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command gets information about the IAM user named Bob.
#' \donttest{get_user(
#'   UserName = "Bob"
#' )}
#'
#' @export
get_user <- function (UserName = NULL) 
{
    op <- new_operation(name = "GetUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_user_input(UserName = UserName)
    output <- get_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves the specified inline policy document that is embedded in the specified IAM user
#'
#' Retrieves the specified inline policy document that is embedded in the specified IAM user.
#' 
#' Policies returned by this API are URL-encoded compliant with [RFC 3986](https://tools.ietf.org/html/rfc3986). You can use a URL decoding method to convert the policy back to plain JSON text. For example, if you use Java, you can use the `decode` method of the `java.net.URLDecoder` utility class in the Java SDK. Other languages and SDKs provide similar functionality.
#' 
#' An IAM user can also have managed policies attached to it. To retrieve a managed policy document that is attached to a user, use GetPolicy to determine the policy\'s default version, then use GetPolicyVersion to retrieve the policy document.
#' 
#' For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' get_user_policy(
#'   UserName = "string",
#'   PolicyName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user who the policy is associated with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document to get.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
get_user_policy <- function (UserName, PolicyName) 
{
    op <- new_operation(name = "GetUserPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_user_policy_input(UserName = UserName, PolicyName = PolicyName)
    output <- get_user_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the access key IDs associated with the specified IAM user
#'
#' Returns information about the access key IDs associated with the specified IAM user. If there is none, the operation returns an empty list.
#' 
#' Although each user is limited to a small number of keys, you can still paginate the results using the `MaxItems` and `Marker` parameters.
#' 
#' If the `UserName` field is not specified, the user name is determined implicitly based on the AWS access key ID used to sign the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#' 
#' To ensure the security of your AWS account, the secret access key is accessible only during key and user creation.
#'
#' @section Accepted Parameters:
#' ```
#' list_access_keys(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName The name of the user.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the access keys IDs for the IAM user named
#' # Alice.
#' \donttest{list_access_keys(
#'   UserName = "Alice"
#' )}
#'
#' @export
list_access_keys <- function (UserName = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListAccessKeys", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_access_keys_input(UserName = UserName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_access_keys_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the account alias associated with the AWS account (Note: you can have only one)
#'
#' Lists the account alias associated with the AWS account (Note: you can have only one). For information about using an AWS account alias, see [Using an Alias for Your AWS Account ID](http://docs.aws.amazon.com/IAM/latest/UserGuide/AccountAlias.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' list_account_aliases(
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the aliases for the current account.
#' \donttest{list_account_aliases()}
#'
#' @export
list_account_aliases <- function (Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListAccountAliases", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_account_aliases_input(Marker = Marker, MaxItems = MaxItems)
    output <- list_account_aliases_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all managed policies that are attached to the specified IAM group
#'
#' Lists all managed policies that are attached to the specified IAM group.
#' 
#' An IAM group can also have inline policies embedded with it. To list the inline policies for a group, use the ListGroupPolicies API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. You can use the `PathPrefix` parameter to limit the list of policies to only those matching the specified path prefix. If there are no policies attached to the specified group (or none that match the specified path prefix), the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_attached_group_policies(
#'   GroupName = "string",
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name (friendly name, not ARN) of the group to list attached policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PathPrefix The path prefix for filtering the results. This parameter is optional. If it is not included, it defaults to a slash (/), listing all policies.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_attached_group_policies <- function (GroupName, PathPrefix = NULL, 
    Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListAttachedGroupPolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_attached_group_policies_input(GroupName = GroupName, 
        PathPrefix = PathPrefix, Marker = Marker, MaxItems = MaxItems)
    output <- list_attached_group_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all managed policies that are attached to the specified IAM role
#'
#' Lists all managed policies that are attached to the specified IAM role.
#' 
#' An IAM role can also have inline policies embedded with it. To list the inline policies for a role, use the ListRolePolicies API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. You can use the `PathPrefix` parameter to limit the list of policies to only those matching the specified path prefix. If there are no policies attached to the specified role (or none that match the specified path prefix), the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_attached_role_policies(
#'   RoleName = "string",
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) of the role to list attached policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PathPrefix The path prefix for filtering the results. This parameter is optional. If it is not included, it defaults to a slash (/), listing all policies.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_attached_role_policies <- function (RoleName, PathPrefix = NULL, 
    Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListAttachedRolePolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_attached_role_policies_input(RoleName = RoleName, 
        PathPrefix = PathPrefix, Marker = Marker, MaxItems = MaxItems)
    output <- list_attached_role_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all managed policies that are attached to the specified IAM user
#'
#' Lists all managed policies that are attached to the specified IAM user.
#' 
#' An IAM user can also have inline policies embedded with it. To list the inline policies for a user, use the ListUserPolicies API. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. You can use the `PathPrefix` parameter to limit the list of policies to only those matching the specified path prefix. If there are no policies attached to the specified group (or none that match the specified path prefix), the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_attached_user_policies(
#'   UserName = "string",
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) of the user to list attached policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PathPrefix The path prefix for filtering the results. This parameter is optional. If it is not included, it defaults to a slash (/), listing all policies.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_attached_user_policies <- function (UserName, PathPrefix = NULL, 
    Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListAttachedUserPolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_attached_user_policies_input(UserName = UserName, 
        PathPrefix = PathPrefix, Marker = Marker, MaxItems = MaxItems)
    output <- list_attached_user_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all IAM users, groups, and roles that the specified managed policy is attached to
#'
#' Lists all IAM users, groups, and roles that the specified managed policy is attached to.
#' 
#' You can use the optional `EntityFilter` parameter to limit the results to a particular type of entity (users, groups, or roles). For example, to list only the roles that are attached to the specified policy, set `EntityFilter` to `Role`.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_entities_for_policy(
#'   PolicyArn = "string",
#'   EntityFilter = "User"|"Role"|"Group"|"LocalManagedPolicy"|"AWSManagedPolicy",
#'   PathPrefix = "string",
#'   PolicyUsageFilter = "PermissionsPolicy"|"PermissionsBoundary",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy for which you want the versions.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param EntityFilter The entity type to use for filtering the results.
#' 
#' For example, when `EntityFilter` is `Role`, only the roles that are attached to the specified policy are returned. This parameter is optional. If it is not included, all attached entities (users, groups, and roles) are returned. The argument for this parameter must be one of the valid values listed below.
#' @param PathPrefix The path prefix for filtering the results. This parameter is optional. If it is not included, it defaults to a slash (/), listing all entities.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param PolicyUsageFilter The policy usage method to use for filtering the results.
#' 
#' To list only permissions policies, set `PolicyUsageFilter` to `PermissionsPolicy`. To list only the policies used to set permissions boundaries, set the value to `PermissionsBoundary`.
#' 
#' This parameter is optional. If it is not included, all policies are returned.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_entities_for_policy <- function (PolicyArn, EntityFilter = NULL, 
    PathPrefix = NULL, PolicyUsageFilter = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListEntitiesForPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_entities_for_policy_input(PolicyArn = PolicyArn, 
        EntityFilter = EntityFilter, PathPrefix = PathPrefix, 
        PolicyUsageFilter = PolicyUsageFilter, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_entities_for_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the names of the inline policies that are embedded in the specified IAM group
#'
#' Lists the names of the inline policies that are embedded in the specified IAM group.
#' 
#' An IAM group can also have managed policies attached to it. To list the managed policies that are attached to a group, use ListAttachedGroupPolicies. For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. If there are no inline policies embedded with the specified group, the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_group_policies(
#'   GroupName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group to list policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the names of in-line policies that are
#' # embedded in the IAM group named Admins.
#' \donttest{list_group_policies(
#'   GroupName = "Admins"
#' )}
#'
#' @export
list_group_policies <- function (GroupName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListGroupPolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_group_policies_input(GroupName = GroupName, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_group_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the IAM groups that have the specified path prefix
#'
#' Lists the IAM groups that have the specified path prefix.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_groups(
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PathPrefix The path prefix for filtering the results. For example, the prefix `/division_abc/subdivision_xyz/` gets all groups whose path starts with `/division_abc/subdivision_xyz/`.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/), listing all groups. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the IAM groups in the current account:
#' \donttest{list_groups()}
#'
#' @export
list_groups <- function (PathPrefix = NULL, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListGroups", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_groups_input(PathPrefix = PathPrefix, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_groups_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the IAM groups that the specified IAM user belongs to
#'
#' Lists the IAM groups that the specified IAM user belongs to.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_groups_for_user(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user to list groups for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command displays the groups that the IAM user named Bob
#' # belongs to.
#' \donttest{list_groups_for_user(
#'   UserName = "Bob"
#' )}
#'
#' @export
list_groups_for_user <- function (UserName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListGroupsForUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_groups_for_user_input(UserName = UserName, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_groups_for_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the instance profiles that have the specified path prefix
#'
#' Lists the instance profiles that have the specified path prefix. If there are none, the operation returns an empty list. For more information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_instance_profiles(
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PathPrefix The path prefix for filtering the results. For example, the prefix `/application_abc/component_xyz/` gets all instance profiles whose path starts with `/application_abc/component_xyz/`.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/), listing all instance profiles. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_instance_profiles <- function (PathPrefix = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListInstanceProfiles", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_instance_profiles_input(PathPrefix = PathPrefix, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_instance_profiles_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the instance profiles that have the specified associated IAM role
#'
#' Lists the instance profiles that have the specified associated IAM role. If there are none, the operation returns an empty list. For more information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_instance_profiles_for_role(
#'   RoleName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role to list instance profiles for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_instance_profiles_for_role <- function (RoleName, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListInstanceProfilesForRole", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_instance_profiles_for_role_input(RoleName = RoleName, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_instance_profiles_for_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the MFA devices for an IAM user
#'
#' Lists the MFA devices for an IAM user. If the request includes a IAM user name, then this operation lists all the MFA devices associated with the specified user. If you do not specify a user name, IAM determines the user name implicitly based on the AWS access key ID signing the request for this API.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_mfa_devices(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName The name of the user whose MFA devices you want to list.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_mfa_devices <- function (UserName = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListMFADevices", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_mfa_devices_input(UserName = UserName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_mfa_devices_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists information about the IAM OpenID Connect (OIDC) provider resource objects defined in the AWS account
#'
#' Lists information about the IAM OpenID Connect (OIDC) provider resource objects defined in the AWS account.
#'
#' @section Accepted Parameters:
#' ```
#' list_open_id_connect_providers()
#' ```
#'
#' @export
list_open_id_connect_providers <- function () 
{
    op <- new_operation(name = "ListOpenIDConnectProviders", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_open_id_connect_providers_input()
    output <- list_open_id_connect_providers_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists all the managed policies that are available in your AWS account, including your own customer-defined managed policies and all AWS managed policies
#'
#' Lists all the managed policies that are available in your AWS account, including your own customer-defined managed policies and all AWS managed policies.
#' 
#' You can filter the list of policies that is returned using the optional `OnlyAttached`, `Scope`, and `PathPrefix` parameters. For example, to list only the customer managed policies in your AWS account, set `Scope` to `Local`. To list only AWS managed policies, set `Scope` to `AWS`.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#' 
#' For more information about managed policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' list_policies(
#'   Scope = "All"|"AWS"|"Local",
#'   OnlyAttached = TRUE|FALSE,
#'   PathPrefix = "string",
#'   PolicyUsageFilter = "PermissionsPolicy"|"PermissionsBoundary",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param Scope The scope to use for filtering the results.
#' 
#' To list only AWS managed policies, set `Scope` to `AWS`. To list only the customer managed policies in your AWS account, set `Scope` to `Local`.
#' 
#' This parameter is optional. If it is not included, or if it is set to `All`, all policies are returned.
#' @param OnlyAttached A flag to filter the results to only the attached policies.
#' 
#' When `OnlyAttached` is `true`, the returned list contains only the policies that are attached to an IAM user, group, or role. When `OnlyAttached` is `false`, or when the parameter is not included, all policies are returned.
#' @param PathPrefix The path prefix for filtering the results. This parameter is optional. If it is not included, it defaults to a slash (/), listing all policies. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param PolicyUsageFilter The policy usage method to use for filtering the results.
#' 
#' To list only permissions policies, set `PolicyUsageFilter` to `PermissionsPolicy`. To list only the policies used to set permissions boundaries, set the value to `PermissionsBoundary`.
#' 
#' This parameter is optional. If it is not included, all policies are returned.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_policies <- function (Scope = NULL, OnlyAttached = NULL, 
    PathPrefix = NULL, PolicyUsageFilter = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListPolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_policies_input(Scope = Scope, OnlyAttached = OnlyAttached, 
        PathPrefix = PathPrefix, PolicyUsageFilter = PolicyUsageFilter, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Retrieves a list of policies that the IAM identity (user, group, or role) can use to access each specified service
#'
#' Retrieves a list of policies that the IAM identity (user, group, or role) can use to access each specified service.
#' 
#' This operation does not use other policy types when determining whether a resource could access a service. These other policy types include resource-based policies, access control lists, AWS Organizations policies, IAM permissions boundaries, and AWS STS assume role policies. It only applies permissions policy logic. For more about the evaluation of policy types, see [Evaluating Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html#policy-eval-basics) in the *IAM User Guide*.
#' 
#' The list of policies returned by the operation depends on the ARN of the identity that you provide.
#' 
#' -   **User** -- The list of policies includes the managed and inline policies that are attached to the user directly. The list also includes any additional managed and inline policies that are attached to the group to which the user belongs.
#' 
#' -   **Group** -- The list of policies includes only the managed and inline policies that are attached to the group directly. Policies that are attached to the group's user are not included.
#' 
#' -   **Role** -- The list of policies includes only the managed and inline policies that are attached to the role.
#' 
#' For each managed policy, this operation returns the ARN and policy name. For each inline policy, it returns the policy name and the entity to which it is attached. Inline policies do not have an ARN. For more information about these policy types, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/access_policies_managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' Policies that are attached to users and roles as permissions boundaries are not returned. To view which managed policy is currently used to set the permissions boundary for a user or role, use the GetUser or GetRole operations.
#'
#' @section Accepted Parameters:
#' ```
#' list_policies_granting_service_access(
#'   Marker = "string",
#'   Arn = "string",
#'   ServiceNamespaces = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param Arn &#91;required&#93; The ARN of the IAM identity (user, group, or role) whose policies you want to list.
#' @param ServiceNamespaces &#91;required&#93; The service namespace for the AWS services whose policies you want to list.
#' 
#' To learn the service namespace for a service, go to [Actions, Resources, and Condition Keys for AWS Services](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_actions-resources-contextkeys.html) in the *IAM User Guide*. Choose the name of the service to view details for that service. In the first paragraph, find the service prefix. For example, `(service prefix: a4b)`. For more information about service namespaces, see [AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html#genref-aws-service-namespaces) in the *AWS General Reference*.
#'
#' @examples
#' # The following operation lists policies that allow ExampleUser01 to
#' # access IAM or EC2.
#' \donttest{list_policies_granting_service_access(
#'   Arn = "arn:aws:iam::123456789012:user/ExampleUser01",
#'   ServiceNamespaces = list(
#'     "iam",
#'     "ec2"
#'   )
#' )}
#'
#' @export
list_policies_granting_service_access <- function (Marker = NULL, 
    Arn, ServiceNamespaces) 
{
    op <- new_operation(name = "ListPoliciesGrantingServiceAccess", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_policies_granting_service_access_input(Marker = Marker, 
        Arn = Arn, ServiceNamespaces = ServiceNamespaces)
    output <- list_policies_granting_service_access_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists information about the versions of the specified managed policy, including the version that is currently set as the policy's default version
#'
#' Lists information about the versions of the specified managed policy, including the version that is currently set as the policy\'s default version.
#' 
#' For more information about managed policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' list_policy_versions(
#'   PolicyArn = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy for which you want the versions.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_policy_versions <- function (PolicyArn, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListPolicyVersions", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_policy_versions_input(PolicyArn = PolicyArn, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_policy_versions_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the names of the inline policies that are embedded in the specified IAM role
#'
#' Lists the names of the inline policies that are embedded in the specified IAM role.
#' 
#' An IAM role can also have managed policies attached to it. To list the managed policies that are attached to a role, use ListAttachedRolePolicies. For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. If there are no inline policies embedded with the specified role, the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_role_policies(
#'   RoleName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role to list policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_role_policies <- function (RoleName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListRolePolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_role_policies_input(RoleName = RoleName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_role_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the tags that are attached to the specified role
#'
#' Lists the tags that are attached to the specified role. The returned list of tags is sorted by tag key. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' list_role_tags(
#'   RoleName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the IAM role for which you want to see the list of tags.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response to indicate where the next call should start.
#' @param MaxItems (Optional) Use this only when paginating results to indicate the maximum number of items that you want in the response. If additional items exist beyond the maximum that you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, it defaults to 100. Note that IAM might return fewer results, even when more results are available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following example shows how to list the tags attached to a role.
#' \donttest{list_role_tags(
#'   RoleName = "taggedrole1"
#' )}
#'
#' @export
list_role_tags <- function (RoleName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListRoleTags", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_role_tags_input(RoleName = RoleName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_role_tags_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the IAM roles that have the specified path prefix
#'
#' Lists the IAM roles that have the specified path prefix. If there are none, the operation returns an empty list. For more information about roles, go to [Working with Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html).
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_roles(
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PathPrefix The path prefix for filtering the results. For example, the prefix `/application_abc/component_xyz/` gets all roles whose path starts with `/application_abc/component_xyz/`.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/), listing all roles. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_roles <- function (PathPrefix = NULL, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListRoles", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_roles_input(PathPrefix = PathPrefix, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_roles_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the SAML provider resource objects defined in IAM in the account
#'
#' Lists the SAML provider resource objects defined in IAM in the account.
#' 
#' This operation requires [Signature Version 4](http://docs.aws.amazon.com/general/latest/gr/signature-version-4.html).
#'
#' @section Accepted Parameters:
#' ```
#' list_saml_providers()
#' ```
#'
#' @export
list_saml_providers <- function () 
{
    op <- new_operation(name = "ListSAMLProviders", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_saml_providers_input()
    output <- list_saml_providers_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the SSH public keys associated with the specified IAM user
#'
#' Returns information about the SSH public keys associated with the specified IAM user. If there none exists, the operation returns an empty list.
#' 
#' The SSH public keys returned by this operation are used only for authenticating the IAM user to an AWS CodeCommit repository. For more information about using SSH keys to authenticate to an AWS CodeCommit repository, see [Set up AWS CodeCommit for SSH Connections](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-credentials-ssh.html) in the *AWS CodeCommit User Guide*.
#' 
#' Although each user is limited to a small number of keys, you can still paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_ssh_public_keys(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName The name of the IAM user to list SSH public keys for. If none is specified, the `UserName` field is determined implicitly based on the AWS access key used to sign the request.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_ssh_public_keys <- function (UserName = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListSSHPublicKeys", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_ssh_public_keys_input(UserName = UserName, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_ssh_public_keys_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the server certificates stored in IAM that have the specified path prefix
#'
#' Lists the server certificates stored in IAM that have the specified path prefix. If none exist, the operation returns an empty list.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#' 
#' For more information about working with server certificates, see [Working with Server Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs.html) in the *IAM User Guide*. This topic also includes a list of AWS services that can use the server certificates that you manage with IAM.
#'
#' @section Accepted Parameters:
#' ```
#' list_server_certificates(
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PathPrefix The path prefix for filtering the results. For example: `/company/servercerts` would get all server certificates for which the path starts with `/company/servercerts`.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/), listing all server certificates. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_server_certificates <- function (PathPrefix = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListServerCertificates", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_server_certificates_input(PathPrefix = PathPrefix, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_server_certificates_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the service-specific credentials associated with the specified IAM user
#'
#' Returns information about the service-specific credentials associated with the specified IAM user. If none exists, the operation returns an empty list. The service-specific credentials returned by this operation are used only for authenticating the IAM user to a specific service. For more information about using service-specific credentials to authenticate to an AWS service, see [Set Up service-specific credentials](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-gc.html) in the AWS CodeCommit User Guide.
#'
#' @section Accepted Parameters:
#' ```
#' list_service_specific_credentials(
#'   UserName = "string",
#'   ServiceName = "string"
#' )
#' ```
#'
#' @param UserName The name of the user whose service-specific credentials you want information about. If this value is not specified, then the operation assumes the user whose credentials are used to call the operation.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param ServiceName Filters the returned results to only those for the specified AWS service. If not specified, then AWS returns service-specific credentials for all services.
#'
#' @export
list_service_specific_credentials <- function (UserName = NULL, 
    ServiceName = NULL) 
{
    op <- new_operation(name = "ListServiceSpecificCredentials", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- list_service_specific_credentials_input(UserName = UserName, 
        ServiceName = ServiceName)
    output <- list_service_specific_credentials_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the signing certificates associated with the specified IAM user
#'
#' Returns information about the signing certificates associated with the specified IAM user. If there none exists, the operation returns an empty list.
#' 
#' Although each user is limited to a small number of signing certificates, you can still paginate the results using the `MaxItems` and `Marker` parameters.
#' 
#' If the `UserName` field is not specified, the user name is determined implicitly based on the AWS access key ID used to sign the request for this API. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#'
#' @section Accepted Parameters:
#' ```
#' list_signing_certificates(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName The name of the IAM user whose signing certificates you want to examine.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the signing certificates for the IAM user
#' # named Bob.
#' \donttest{list_signing_certificates(
#'   UserName = "Bob"
#' )}
#'
#' @export
list_signing_certificates <- function (UserName = NULL, Marker = NULL, 
    MaxItems = NULL) 
{
    op <- new_operation(name = "ListSigningCertificates", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_signing_certificates_input(UserName = UserName, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_signing_certificates_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the names of the inline policies embedded in the specified IAM user
#'
#' Lists the names of the inline policies embedded in the specified IAM user.
#' 
#' An IAM user can also have managed policies attached to it. To list the managed policies that are attached to a user, use ListAttachedUserPolicies. For more information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters. If there are no inline policies embedded with the specified user, the operation returns an empty list.
#'
#' @section Accepted Parameters:
#' ```
#' list_user_policies(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user to list policies for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @export
list_user_policies <- function (UserName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListUserPolicies", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_user_policies_input(UserName = UserName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_user_policies_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the tags that are attached to the specified user
#'
#' Lists the tags that are attached to the specified user. The returned list of tags is sorted by tag key. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' list_user_tags(
#'   UserName = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user whose tags you want to see.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@-
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response to indicate where the next call should start.
#' @param MaxItems (Optional) Use this only when paginating results to indicate the maximum number of items that you want in the response. If additional items exist beyond the maximum that you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, it defaults to 100. Note that IAM might return fewer results, even when more results are available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following example shows how to list the tags attached to a user.
#' \donttest{list_user_tags(
#'   UserName = "anika"
#' )}
#'
#' @export
list_user_tags <- function (UserName, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListUserTags", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_user_tags_input(UserName = UserName, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_user_tags_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the IAM users that have the specified path prefix
#'
#' Lists the IAM users that have the specified path prefix. If no path prefix is specified, the operation returns all users in the AWS account. If there are none, the operation returns an empty list.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_users(
#'   PathPrefix = "string",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param PathPrefix The path prefix for filtering the results. For example: `/division_abc/subdivision_xyz/`, which would get all user names whose path starts with `/division_abc/subdivision_xyz/`.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/), listing all user names. This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the IAM users in the current account.
#' \donttest{list_users()}
#'
#' @export
list_users <- function (PathPrefix = NULL, Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListUsers", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_users_input(PathPrefix = PathPrefix, Marker = Marker, 
        MaxItems = MaxItems)
    output <- list_users_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Lists the virtual MFA devices defined in the AWS account by assignment status
#'
#' Lists the virtual MFA devices defined in the AWS account by assignment status. If you do not specify an assignment status, the operation returns a list of all virtual MFA devices. Assignment status can be `Assigned`, `Unassigned`, or `Any`.
#' 
#' You can paginate the results using the `MaxItems` and `Marker` parameters.
#'
#' @section Accepted Parameters:
#' ```
#' list_virtual_mfa_devices(
#'   AssignmentStatus = "Assigned"|"Unassigned"|"Any",
#'   Marker = "string",
#'   MaxItems = 123
#' )
#' ```
#'
#' @param AssignmentStatus The status (`Unassigned` or `Assigned`) of the devices to list. If you do not specify an `AssignmentStatus`, the operation defaults to `Any`, which lists both assigned and unassigned virtual MFA devices.,
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#'
#' @examples
#' # The following command lists the virtual MFA devices that have been
#' # configured for the current account.
#' \donttest{list_virtual_mfa_devices()}
#'
#' @export
list_virtual_mfa_devices <- function (AssignmentStatus = NULL, 
    Marker = NULL, MaxItems = NULL) 
{
    op <- new_operation(name = "ListVirtualMFADevices", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_virtual_mfa_devices_input(AssignmentStatus = AssignmentStatus, 
        Marker = Marker, MaxItems = MaxItems)
    output <- list_virtual_mfa_devices_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or updates an inline policy document that is embedded in the specified IAM group
#'
#' Adds or updates an inline policy document that is embedded in the specified IAM group.
#' 
#' A user can also have managed policies attached to it. To attach a managed policy to a group, use AttachGroupPolicy. To create a new managed policy, use CreatePolicy. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' For information about limits on the number of inline policies that you can embed in a group, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#' 
#' Because policy documents can be large, you should use POST rather than GET when calling `PutGroupPolicy`. For general information about using the Query API with IAM, go to [Making Query Requests](http://docs.aws.amazon.com/IAM/latest/UserGuide/IAM_UsingQueryAPI.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' put_group_policy(
#'   GroupName = "string",
#'   PolicyName = "string",
#'   PolicyDocument = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group to associate the policy with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyDocument &#91;required&#93; The policy document.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following command adds a policy named AllPerms to the IAM group
#' # named Admins.
#' \donttest{put_group_policy(
#'   GroupName = "Admins",
#'   PolicyDocument = "{\"Version\":\"2012-10-17\",\"Statement\":{\"Effect\":\"Allow\",\"Action\":\"*\",\"Resource\":\"*\"}}",
#'   PolicyName = "AllPerms"
#' )}
#'
#' @export
put_group_policy <- function (GroupName, PolicyName, PolicyDocument) 
{
    op <- new_operation(name = "PutGroupPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_group_policy_input(GroupName = GroupName, PolicyName = PolicyName, 
        PolicyDocument = PolicyDocument)
    output <- put_group_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or updates the policy that is specified as the IAM role's permissions boundary
#'
#' Adds or updates the policy that is specified as the IAM role\'s permissions boundary. You can use an AWS managed policy or a customer managed policy to set the boundary for a role. Use the boundary to control the maximum permissions that the role can have. Setting a permissions boundary is an advanced feature that can affect the permissions for the role.
#' 
#' You cannot set the boundary for a service-linked role.
#' 
#' Policies used as permissions boundaries do not provide permissions. You must also attach a permissions policy to the role. To learn how the effective permissions for a role are evaluated, see [IAM JSON Policy Evaluation Logic](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html) in the IAM User Guide.
#'
#' @section Accepted Parameters:
#' ```
#' put_role_permissions_boundary(
#'   RoleName = "string",
#'   PermissionsBoundary = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name (friendly name, not ARN) of the IAM role for which you want to set the permissions boundary.
#' @param PermissionsBoundary &#91;required&#93; The ARN of the policy that is used to set the permissions boundary for the role.
#'
#' @export
put_role_permissions_boundary <- function (RoleName, PermissionsBoundary) 
{
    op <- new_operation(name = "PutRolePermissionsBoundary", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- put_role_permissions_boundary_input(RoleName = RoleName, 
        PermissionsBoundary = PermissionsBoundary)
    output <- put_role_permissions_boundary_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or updates an inline policy document that is embedded in the specified IAM role
#'
#' Adds or updates an inline policy document that is embedded in the specified IAM role.
#' 
#' When you embed an inline policy in a role, the inline policy is used as part of the role\'s access (permissions) policy. The role\'s trust policy is created at the same time as the role, using CreateRole. You can update a role\'s trust policy using UpdateAssumeRolePolicy. For more information about IAM roles, go to [Using Roles to Delegate Permissions and Federate Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/roles-toplevel.html).
#' 
#' A role can also have a managed policy attached to it. To attach a managed policy to a role, use AttachRolePolicy. To create a new managed policy, use CreatePolicy. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' For information about limits on the number of inline policies that you can embed with a role, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#' 
#' Because policy documents can be large, you should use POST rather than GET when calling `PutRolePolicy`. For general information about using the Query API with IAM, go to [Making Query Requests](http://docs.aws.amazon.com/IAM/latest/UserGuide/IAM_UsingQueryAPI.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' put_role_policy(
#'   RoleName = "string",
#'   PolicyName = "string",
#'   PolicyDocument = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role to associate the policy with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyDocument &#91;required&#93; The policy document.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following command adds a permissions policy to the role named
#' # Test-Role.
#' \donttest{put_role_policy(
#'   PolicyDocument = "{\"Version\":\"2012-10-17\",\"Statement\":{\"Effect\":\"Allow\",\"Action\":\"s3:*\",\"Resource\":\"*\"}}",
#'   PolicyName = "S3AccessPolicy",
#'   RoleName = "S3Access"
#' )}
#'
#' @export
put_role_policy <- function (RoleName, PolicyName, PolicyDocument) 
{
    op <- new_operation(name = "PutRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_role_policy_input(RoleName = RoleName, PolicyName = PolicyName, 
        PolicyDocument = PolicyDocument)
    output <- put_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or updates the policy that is specified as the IAM user's permissions boundary
#'
#' Adds or updates the policy that is specified as the IAM user\'s permissions boundary. You can use an AWS managed policy or a customer managed policy to set the boundary for a user. Use the boundary to control the maximum permissions that the user can have. Setting a permissions boundary is an advanced feature that can affect the permissions for the user.
#' 
#' Policies that are used as permissions boundaries do not provide permissions. You must also attach a permissions policy to the user. To learn how the effective permissions for a user are evaluated, see [IAM JSON Policy Evaluation Logic](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html) in the IAM User Guide.
#'
#' @section Accepted Parameters:
#' ```
#' put_user_permissions_boundary(
#'   UserName = "string",
#'   PermissionsBoundary = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name (friendly name, not ARN) of the IAM user for which you want to set the permissions boundary.
#' @param PermissionsBoundary &#91;required&#93; The ARN of the policy that is used to set the permissions boundary for the user.
#'
#' @export
put_user_permissions_boundary <- function (UserName, PermissionsBoundary) 
{
    op <- new_operation(name = "PutUserPermissionsBoundary", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- put_user_permissions_boundary_input(UserName = UserName, 
        PermissionsBoundary = PermissionsBoundary)
    output <- put_user_permissions_boundary_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds or updates an inline policy document that is embedded in the specified IAM user
#'
#' Adds or updates an inline policy document that is embedded in the specified IAM user.
#' 
#' An IAM user can also have a managed policy attached to it. To attach a managed policy to a user, use AttachUserPolicy. To create a new managed policy, use CreatePolicy. For information about policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#' 
#' For information about limits on the number of inline policies that you can embed in a user, see [Limitations on IAM Entities](http://docs.aws.amazon.com/IAM/latest/UserGuide/LimitationsOnEntities.html) in the *IAM User Guide*.
#' 
#' Because policy documents can be large, you should use POST rather than GET when calling `PutUserPolicy`. For general information about using the Query API with IAM, go to [Making Query Requests](http://docs.aws.amazon.com/IAM/latest/UserGuide/IAM_UsingQueryAPI.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' put_user_policy(
#'   UserName = "string",
#'   PolicyName = "string",
#'   PolicyDocument = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user to associate the policy with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyName &#91;required&#93; The name of the policy document.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyDocument &#91;required&#93; The policy document.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following command attaches a policy to the IAM user named Bob.
#' \donttest{put_user_policy(
#'   PolicyDocument = "{\"Version\":\"2012-10-17\",\"Statement\":{\"Effect\":\"Allow\",\"Action\":\"*\",\"Resource\":\"*\"}}",
#'   PolicyName = "AllAccessPolicy",
#'   UserName = "Bob"
#' )}
#'
#' @export
put_user_policy <- function (UserName, PolicyName, PolicyDocument) 
{
    op <- new_operation(name = "PutUserPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_user_policy_input(UserName = UserName, PolicyName = PolicyName, 
        PolicyDocument = PolicyDocument)
    output <- put_user_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified client ID (also known as audience) from the list of client IDs registered for the specified IAM OpenID Connect (OIDC) provider resource object
#'
#' Removes the specified client ID (also known as audience) from the list of client IDs registered for the specified IAM OpenID Connect (OIDC) provider resource object.
#' 
#' This operation is idempotent; it does not fail or return an error if you try to remove a client ID that does not exist.
#'
#' @section Accepted Parameters:
#' ```
#' remove_client_id_from_open_id_connect_provider(
#'   OpenIDConnectProviderArn = "string",
#'   ClientID = "string"
#' )
#' ```
#'
#' @param OpenIDConnectProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM OIDC provider resource to remove the client ID from. You can get a list of OIDC provider ARNs by using the ListOpenIDConnectProviders operation.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param ClientID &#91;required&#93; The client ID (also known as audience) to remove from the IAM OIDC provider resource. For more information about client IDs, see CreateOpenIDConnectProvider.
#'
#' @export
remove_client_id_from_open_id_connect_provider <- function (OpenIDConnectProviderArn, 
    ClientID) 
{
    op <- new_operation(name = "RemoveClientIDFromOpenIDConnectProvider", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- remove_client_id_from_open_id_connect_provider_input(OpenIDConnectProviderArn = OpenIDConnectProviderArn, 
        ClientID = ClientID)
    output <- remove_client_id_from_open_id_connect_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified IAM role from the specified EC2 instance profile
#'
#' Removes the specified IAM role from the specified EC2 instance profile.
#' 
#' Make sure that you do not have any Amazon EC2 instances running with the role you are about to remove from the instance profile. Removing a role from an instance profile that is associated with a running instance might break any applications running on the instance.
#' 
#' For more information about IAM roles, go to [Working with Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/WorkingWithRoles.html). For more information about instance profiles, go to [About Instance Profiles](http://docs.aws.amazon.com/IAM/latest/UserGuide/AboutInstanceProfiles.html).
#'
#' @section Accepted Parameters:
#' ```
#' remove_role_from_instance_profile(
#'   InstanceProfileName = "string",
#'   RoleName = "string"
#' )
#' ```
#'
#' @param InstanceProfileName &#91;required&#93; The name of the instance profile to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param RoleName &#91;required&#93; The name of the role to remove.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command removes the role named Test-Role from the instance
#' # profile named ExampleInstanceProfile.
#' \donttest{remove_role_from_instance_profile(
#'   InstanceProfileName = "ExampleInstanceProfile",
#'   RoleName = "Test-Role"
#' )}
#'
#' @export
remove_role_from_instance_profile <- function (InstanceProfileName, 
    RoleName) 
{
    op <- new_operation(name = "RemoveRoleFromInstanceProfile", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- remove_role_from_instance_profile_input(InstanceProfileName = InstanceProfileName, 
        RoleName = RoleName)
    output <- remove_role_from_instance_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified user from the specified group
#'
#' Removes the specified user from the specified group.
#'
#' @section Accepted Parameters:
#' ```
#' remove_user_from_group(
#'   GroupName = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; The name of the group to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param UserName &#91;required&#93; The name of the user to remove.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command removes the user named Bob from the IAM group
#' # named Admins.
#' \donttest{remove_user_from_group(
#'   GroupName = "Admins",
#'   UserName = "Bob"
#' )}
#'
#' @export
remove_user_from_group <- function (GroupName, UserName) 
{
    op <- new_operation(name = "RemoveUserFromGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- remove_user_from_group_input(GroupName = GroupName, 
        UserName = UserName)
    output <- remove_user_from_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Resets the password for a service-specific credential
#'
#' Resets the password for a service-specific credential. The new password is AWS generated and cryptographically strong. It cannot be configured by the user. Resetting the password immediately invalidates the previous password associated with this user.
#'
#' @section Accepted Parameters:
#' ```
#' reset_service_specific_credential(
#'   UserName = "string",
#'   ServiceSpecificCredentialId = "string"
#' )
#' ```
#'
#' @param UserName The name of the IAM user associated with the service-specific credential. If this value is not specified, then the operation assumes the user whose credentials are used to call the operation.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param ServiceSpecificCredentialId &#91;required&#93; The unique identifier of the service-specific credential.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#'
#' @export
reset_service_specific_credential <- function (UserName = NULL, 
    ServiceSpecificCredentialId) 
{
    op <- new_operation(name = "ResetServiceSpecificCredential", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- reset_service_specific_credential_input(UserName = UserName, 
        ServiceSpecificCredentialId = ServiceSpecificCredentialId)
    output <- reset_service_specific_credential_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Synchronizes the specified MFA device with its IAM resource object on the AWS servers
#'
#' Synchronizes the specified MFA device with its IAM resource object on the AWS servers.
#' 
#' For more information about creating and working with virtual MFA devices, go to [Using a Virtual MFA Device](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_VirtualMFA.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' resync_mfa_device(
#'   UserName = "string",
#'   SerialNumber = "string",
#'   AuthenticationCode1 = "string",
#'   AuthenticationCode2 = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user whose MFA device you want to resynchronize.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SerialNumber &#91;required&#93; Serial number that uniquely identifies the MFA device.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param AuthenticationCode1 &#91;required&#93; An authentication code emitted by the device.
#' 
#' The format for this parameter is a sequence of six digits.
#' @param AuthenticationCode2 &#91;required&#93; A subsequent authentication code emitted by the device.
#' 
#' The format for this parameter is a sequence of six digits.
#'
#' @export
resync_mfa_device <- function (UserName, SerialNumber, AuthenticationCode1, 
    AuthenticationCode2) 
{
    op <- new_operation(name = "ResyncMFADevice", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- resync_mfa_device_input(UserName = UserName, SerialNumber = SerialNumber, 
        AuthenticationCode1 = AuthenticationCode1, AuthenticationCode2 = AuthenticationCode2)
    output <- resync_mfa_device_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the specified version of the specified policy as the policy's default (operative) version
#'
#' Sets the specified version of the specified policy as the policy\'s default (operative) version.
#' 
#' This operation affects all users, groups, and roles that the policy is attached to. To list the users, groups, and roles that the policy is attached to, use the ListEntitiesForPolicy API.
#' 
#' For information about managed policies, see [Managed Policies and Inline Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-vs-inline.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' set_default_policy_version(
#'   PolicyArn = "string",
#'   VersionId = "string"
#' )
#' ```
#'
#' @param PolicyArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM policy whose default version you want to set.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param VersionId &#91;required&#93; The version of the policy to set as the default (operative) version.
#' 
#' For more information about managed policy versions, see [Versioning for Managed Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/policies-managed-versions.html) in the *IAM User Guide*.
#'
#' @export
set_default_policy_version <- function (PolicyArn, VersionId) 
{
    op <- new_operation(name = "SetDefaultPolicyVersion", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- set_default_policy_version_input(PolicyArn = PolicyArn, 
        VersionId = VersionId)
    output <- set_default_policy_version_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Simulate how a set of IAM policies and optionally a resource-based policy works with a list of API operations and AWS resources to determine the policies' effective permissions
#'
#' Simulate how a set of IAM policies and optionally a resource-based policy works with a list of API operations and AWS resources to determine the policies\' effective permissions. The policies are provided as strings.
#' 
#' The simulation does not perform the API operations; it only checks the authorization to determine if the simulated policies allow or deny the operations.
#' 
#' If you want to simulate existing policies attached to an IAM user, group, or role, use SimulatePrincipalPolicy instead.
#' 
#' Context keys are variables maintained by AWS and its services that provide details about the context of an API query request. You can use the `Condition` element of an IAM policy to evaluate context keys. To get the list of context keys that the policies require for correct simulation, use GetContextKeysForCustomPolicy.
#' 
#' If the output is long, you can use `MaxItems` and `Marker` parameters to paginate the results.
#'
#' @section Accepted Parameters:
#' ```
#' simulate_custom_policy(
#'   PolicyInputList = list(
#'     "string"
#'   ),
#'   ActionNames = list(
#'     "string"
#'   ),
#'   ResourceArns = list(
#'     "string"
#'   ),
#'   ResourcePolicy = "string",
#'   ResourceOwner = "string",
#'   CallerArn = "string",
#'   ContextEntries = list(
#'     list(
#'       ContextKeyName = "string",
#'       ContextKeyValues = list(
#'         "string"
#'       ),
#'       ContextKeyType = "string"|"stringList"|"numeric"|"numericList"|"boolean"|"booleanList"|"ip"|"ipList"|"binary"|"binaryList"|"date"|"dateList"
#'     )
#'   ),
#'   ResourceHandlingOption = "string",
#'   MaxItems = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param PolicyInputList &#91;required&#93; A list of policy documents to include in the simulation. Each document is specified as a string containing the complete, valid JSON text of an IAM policy. Do not include any resource-based policies in this parameter. Any resource-based policy must be submitted with the `ResourcePolicy` parameter. The policies cannot be \"scope-down\" policies, such as you could include in a call to [GetFederationToken](http://docs.aws.amazon.com/IAM/latest/APIReference/API_GetFederationToken.html) or one of the [AssumeRole](http://docs.aws.amazon.com/IAM/latest/APIReference/API_AssumeRole.html) API operations. In other words, do not use policies designed to restrict what a user can do while using the temporary credentials.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param ActionNames &#91;required&#93; A list of names of API operations to evaluate in the simulation. Each operation is evaluated against each resource. Each operation must include the service identifier, such as `iam:CreateUser`.
#' @param ResourceArns A list of ARNs of AWS resources to include in the simulation. If this parameter is not provided, then the value defaults to `*` (all resources). Each API in the `ActionNames` parameter is evaluated for each resource in this list. The simulation determines the access result (allowed or denied) of each combination and reports it in the response.
#' 
#' The simulation does not automatically retrieve policies for the specified resources. If you want to include a resource policy in the simulation, then you must include the policy as a string in the `ResourcePolicy` parameter.
#' 
#' If you include a `ResourcePolicy`, then it must be applicable to all of the resources included in the simulation or you receive an invalid input error.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param ResourcePolicy A resource-based policy to include in the simulation provided as a string. Each resource in the simulation is treated as if it had this policy attached. You can include only one resource-based policy in a simulation.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param ResourceOwner An ARN representing the AWS account ID that specifies the owner of any simulated resource that does not identify its owner in the resource ARN, such as an S3 bucket or object. If `ResourceOwner` is specified, it is also used as the account owner of any `ResourcePolicy` included in the simulation. If the `ResourceOwner` parameter is not specified, then the owner of the resources and the resource policy defaults to the account of the identity provided in `CallerArn`. This parameter is required only if you specify a resource-based policy and account that owns the resource is different from the account that owns the simulated calling user `CallerArn`.
#' 
#' The ARN for an account uses the following syntax: `arn:aws:iam::AWS-account-ID:rootAWS-account-ID:root`. For example, to represent the account with the 112233445566 ID, use the following ARN: `arn:aws:iam::112233445566-ID:root`.
#' @param CallerArn The ARN of the IAM user that you want to use as the simulated caller of the API operations. `CallerArn` is required if you include a `ResourcePolicy` so that the policy\'s `Principal` element has a value to use in evaluating the policy.
#' 
#' You can specify only the ARN of an IAM user. You cannot specify the ARN of an assumed role, federated user, or a service principal.
#' @param ContextEntries A list of context keys and corresponding values for the simulation to use. Whenever a context key is evaluated in one of the simulated IAM permission policies, the corresponding value is supplied.
#' @param ResourceHandlingOption Specifies the type of simulation to run. Different API operations that support resource-based policies require different combinations of resources. By specifying the type of simulation to run, you enable the policy simulator to enforce the presence of the required resources to ensure reliable simulation results. If your simulation does not match one of the following scenarios, then you can omit this parameter. The following list shows each of the supported scenario values and the resources that you must define to run the simulation.
#' 
#' Each of the EC2 scenarios requires that you specify instance, image, and security-group resources. If your scenario includes an EBS volume, then you must specify that volume as a resource. If the EC2 scenario includes VPC, then you must supply the network-interface resource. If it includes an IP subnet, then you must specify the subnet resource. For more information on the EC2 scenario options, see [Supported Platforms](http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-supported-platforms.html) in the *Amazon EC2 User Guide*.
#' 
#' -   **EC2-Classic-InstanceStore**
#' 
#'     instance, image, security-group
#' 
#' -   **EC2-Classic-EBS**
#' 
#'     instance, image, security-group, volume
#' 
#' -   **EC2-VPC-InstanceStore**
#' 
#'     instance, image, security-group, network-interface
#' 
#' -   **EC2-VPC-InstanceStore-Subnet**
#' 
#'     instance, image, security-group, network-interface, subnet
#' 
#' -   **EC2-VPC-EBS**
#' 
#'     instance, image, security-group, network-interface, volume
#' 
#' -   **EC2-VPC-EBS-Subnet**
#' 
#'     instance, image, security-group, network-interface, subnet, volume
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#'
#' @export
simulate_custom_policy <- function (PolicyInputList, ActionNames, 
    ResourceArns = NULL, ResourcePolicy = NULL, ResourceOwner = NULL, 
    CallerArn = NULL, ContextEntries = NULL, ResourceHandlingOption = NULL, 
    MaxItems = NULL, Marker = NULL) 
{
    op <- new_operation(name = "SimulateCustomPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- simulate_custom_policy_input(PolicyInputList = PolicyInputList, 
        ActionNames = ActionNames, ResourceArns = ResourceArns, 
        ResourcePolicy = ResourcePolicy, ResourceOwner = ResourceOwner, 
        CallerArn = CallerArn, ContextEntries = ContextEntries, 
        ResourceHandlingOption = ResourceHandlingOption, MaxItems = MaxItems, 
        Marker = Marker)
    output <- simulate_custom_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Simulate how a set of IAM policies attached to an IAM entity works with a list of API operations and AWS resources to determine the policies' effective permissions
#'
#' Simulate how a set of IAM policies attached to an IAM entity works with a list of API operations and AWS resources to determine the policies\' effective permissions. The entity can be an IAM user, group, or role. If you specify a user, then the simulation also includes all of the policies that are attached to groups that the user belongs to.
#' 
#' You can optionally include a list of one or more additional policies specified as strings to include in the simulation. If you want to simulate only policies specified as strings, use SimulateCustomPolicy instead.
#' 
#' You can also optionally include one resource-based policy to be evaluated with each of the resources included in the simulation.
#' 
#' The simulation does not perform the API operations, it only checks the authorization to determine if the simulated policies allow or deny the operations.
#' 
#' **Note:** This API discloses information about the permissions granted to other users. If you do not want users to see other user\'s permissions, then consider allowing them to use SimulateCustomPolicy instead.
#' 
#' Context keys are variables maintained by AWS and its services that provide details about the context of an API query request. You can use the `Condition` element of an IAM policy to evaluate context keys. To get the list of context keys that the policies require for correct simulation, use GetContextKeysForPrincipalPolicy.
#' 
#' If the output is long, you can use the `MaxItems` and `Marker` parameters to paginate the results.
#'
#' @section Accepted Parameters:
#' ```
#' simulate_principal_policy(
#'   PolicySourceArn = "string",
#'   PolicyInputList = list(
#'     "string"
#'   ),
#'   ActionNames = list(
#'     "string"
#'   ),
#'   ResourceArns = list(
#'     "string"
#'   ),
#'   ResourcePolicy = "string",
#'   ResourceOwner = "string",
#'   CallerArn = "string",
#'   ContextEntries = list(
#'     list(
#'       ContextKeyName = "string",
#'       ContextKeyValues = list(
#'         "string"
#'       ),
#'       ContextKeyType = "string"|"stringList"|"numeric"|"numericList"|"boolean"|"booleanList"|"ip"|"ipList"|"binary"|"binaryList"|"date"|"dateList"
#'     )
#'   ),
#'   ResourceHandlingOption = "string",
#'   MaxItems = 123,
#'   Marker = "string"
#' )
#' ```
#'
#' @param PolicySourceArn &#91;required&#93; The Amazon Resource Name (ARN) of a user, group, or role whose policies you want to include in the simulation. If you specify a user, group, or role, the simulation includes all policies that are associated with that entity. If you specify a user, the simulation also includes all policies that are attached to any groups the user belongs to.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param PolicyInputList An optional list of additional policy documents to include in the simulation. Each document is specified as a string containing the complete, valid JSON text of an IAM policy.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param ActionNames &#91;required&#93; A list of names of API operations to evaluate in the simulation. Each operation is evaluated for each resource. Each operation must include the service identifier, such as `iam:CreateUser`.
#' @param ResourceArns A list of ARNs of AWS resources to include in the simulation. If this parameter is not provided, then the value defaults to `*` (all resources). Each API in the `ActionNames` parameter is evaluated for each resource in this list. The simulation determines the access result (allowed or denied) of each combination and reports it in the response.
#' 
#' The simulation does not automatically retrieve policies for the specified resources. If you want to include a resource policy in the simulation, then you must include the policy as a string in the `ResourcePolicy` parameter.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param ResourcePolicy A resource-based policy to include in the simulation provided as a string. Each resource in the simulation is treated as if it had this policy attached. You can include only one resource-based policy in a simulation.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param ResourceOwner An AWS account ID that specifies the owner of any simulated resource that does not identify its owner in the resource ARN, such as an S3 bucket or object. If `ResourceOwner` is specified, it is also used as the account owner of any `ResourcePolicy` included in the simulation. If the `ResourceOwner` parameter is not specified, then the owner of the resources and the resource policy defaults to the account of the identity provided in `CallerArn`. This parameter is required only if you specify a resource-based policy and account that owns the resource is different from the account that owns the simulated calling user `CallerArn`.
#' @param CallerArn The ARN of the IAM user that you want to specify as the simulated caller of the API operations. If you do not specify a `CallerArn`, it defaults to the ARN of the user that you specify in `PolicySourceArn`, if you specified a user. If you include both a `PolicySourceArn` (for example, `arn:aws:iam::123456789012:user/David`) and a `CallerArn` (for example, `arn:aws:iam::123456789012:user/Bob`), the result is that you simulate calling the API operations as Bob, as if Bob had David\'s policies.
#' 
#' You can specify only the ARN of an IAM user. You cannot specify the ARN of an assumed role, federated user, or a service principal.
#' 
#' `CallerArn` is required if you include a `ResourcePolicy` and the `PolicySourceArn` is not the ARN for an IAM user. This is required so that the resource-based policy\'s `Principal` element has a value to use in evaluating the policy.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param ContextEntries A list of context keys and corresponding values for the simulation to use. Whenever a context key is evaluated in one of the simulated IAM permission policies, the corresponding value is supplied.
#' @param ResourceHandlingOption Specifies the type of simulation to run. Different API operations that support resource-based policies require different combinations of resources. By specifying the type of simulation to run, you enable the policy simulator to enforce the presence of the required resources to ensure reliable simulation results. If your simulation does not match one of the following scenarios, then you can omit this parameter. The following list shows each of the supported scenario values and the resources that you must define to run the simulation.
#' 
#' Each of the EC2 scenarios requires that you specify instance, image, and security group resources. If your scenario includes an EBS volume, then you must specify that volume as a resource. If the EC2 scenario includes VPC, then you must supply the network interface resource. If it includes an IP subnet, then you must specify the subnet resource. For more information on the EC2 scenario options, see [Supported Platforms](http://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ec2-supported-platforms.html) in the *Amazon EC2 User Guide*.
#' 
#' -   **EC2-Classic-InstanceStore**
#' 
#'     instance, image, security group
#' 
#' -   **EC2-Classic-EBS**
#' 
#'     instance, image, security group, volume
#' 
#' -   **EC2-VPC-InstanceStore**
#' 
#'     instance, image, security group, network interface
#' 
#' -   **EC2-VPC-InstanceStore-Subnet**
#' 
#'     instance, image, security group, network interface, subnet
#' 
#' -   **EC2-VPC-EBS**
#' 
#'     instance, image, security group, network interface, volume
#' 
#' -   **EC2-VPC-EBS-Subnet**
#' 
#'     instance, image, security group, network interface, subnet, volume
#' @param MaxItems Use this only when paginating results to indicate the maximum number of items you want in the response. If additional items exist beyond the maximum you specify, the `IsTruncated` response element is `true`.
#' 
#' If you do not include this parameter, the number of items defaults to 100. Note that IAM might return fewer results, even when there are more results available. In that case, the `IsTruncated` response element returns `true`, and `Marker` contains a value to include in the subsequent call that tells the service where to continue from.
#' @param Marker Use this parameter only when paginating results and only after you receive a response indicating that the results are truncated. Set it to the value of the `Marker` element in the response that you received to indicate where the next call should start.
#'
#' @export
simulate_principal_policy <- function (PolicySourceArn, PolicyInputList = NULL, 
    ActionNames, ResourceArns = NULL, ResourcePolicy = NULL, 
    ResourceOwner = NULL, CallerArn = NULL, ContextEntries = NULL, 
    ResourceHandlingOption = NULL, MaxItems = NULL, Marker = NULL) 
{
    op <- new_operation(name = "SimulatePrincipalPolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- simulate_principal_policy_input(PolicySourceArn = PolicySourceArn, 
        PolicyInputList = PolicyInputList, ActionNames = ActionNames, 
        ResourceArns = ResourceArns, ResourcePolicy = ResourcePolicy, 
        ResourceOwner = ResourceOwner, CallerArn = CallerArn, 
        ContextEntries = ContextEntries, ResourceHandlingOption = ResourceHandlingOption, 
        MaxItems = MaxItems, Marker = Marker)
    output <- simulate_principal_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds one or more tags to an IAM role
#'
#' Adds one or more tags to an IAM role. The role can be a regular role or a service-linked role. If a tag with the same key name already exists, then that tag is overwritten with the new value.
#' 
#' A tag consists of a key name and an associated value. By assigning tags to your resources, you can do the following:
#' 
#' -   **Administrative grouping and discovery** - Attach tags to resources to aid in organization and search. For example, you could search for all resources with the key name *Project* and the value *MyImportantProject*. Or search for all resources with the key name *Cost Center* and the value *41200*.
#' 
#' -   **Access control** - Reference tags in IAM user-based and resource-based policies. You can use tags to restrict access to only an IAM user or role that has a specified tag attached. You can also restrict access to only those resources that have a certain tag attached. For examples of policies that show how to use tags to control access, see [Control Access Using IAM Tags](http://docs.aws.amazon.com/IAM/latest/UserGuide/access_tags.html) in the *IAM User Guide*.
#' 
#' -   **Cost allocation** - Use tags to help track which individuals and teams are using which AWS resources.
#' 
#' 
#' -   Make sure that you have no invalid tags and that you do not exceed the allowed number of tags per role. In either case, the entire request fails and *no* tags are added to the role.
#' 
#' -   AWS always interprets the tag `Value` as a single string. If you need to store an array, you can store comma-separated values in the string. However, you must interpret the value in your code.
#' 
#' For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' tag_role(
#'   RoleName = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role that you want to add tags to.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Tags &#91;required&#93; The list of tags that you want to attach to the role. Each tag consists of a key name and an associated value. You can specify this with a JSON string.
#'
#' @examples
#' # The following example shows how to add tags to an existing role.
#' \donttest{tag_role(
#'   RoleName = "taggedrole",
#'   Tags = list(
#'     list(
#'       Key = "Dept",
#'       Value = "Accounting"
#'     ),
#'     list(
#'       Key = "CostCenter",
#'       Value = "12345"
#'     )
#'   )
#' )}
#'
#' @export
tag_role <- function (RoleName, Tags) 
{
    op <- new_operation(name = "TagRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- tag_role_input(RoleName = RoleName, Tags = Tags)
    output <- tag_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Adds one or more tags to an IAM user
#'
#' Adds one or more tags to an IAM user. If a tag with the same key name already exists, then that tag is overwritten with the new value.
#' 
#' A tag consists of a key name and an associated value. By assigning tags to your resources, you can do the following:
#' 
#' -   **Administrative grouping and discovery** - Attach tags to resources to aid in organization and search. For example, you could search for all resources with the key name *Project* and the value *MyImportantProject*. Or search for all resources with the key name *Cost Center* and the value *41200*.
#' 
#' -   **Access control** - Reference tags in IAM user-based and resource-based policies. You can use tags to restrict access to only an IAM requesting user or to a role that has a specified tag attached. You can also restrict access to only those resources that have a certain tag attached. For examples of policies that show how to use tags to control access, see [Control Access Using IAM Tags](http://docs.aws.amazon.com/IAM/latest/UserGuide/access_tags.html) in the *IAM User Guide*.
#' 
#' -   **Cost allocation** - Use tags to help track which individuals and teams are using which AWS resources.
#' 
#' 
#' -   Make sure that you have no invalid tags and that you do not exceed the allowed number of tags per role. In either case, the entire request fails and *no* tags are added to the role.
#' 
#' -   AWS always interprets the tag `Value` as a single string. If you need to store an array, you can store comma-separated values in the string. However, you must interpret the value in your code.
#' 
#' For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' tag_user(
#'   UserName = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user that you want to add tags to.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@-
#' @param Tags &#91;required&#93; The list of tags that you want to attach to the user. Each tag consists of a key name and an associated value.
#'
#' @examples
#' # The following example shows how to add tags to an existing user.
#' \donttest{tag_user(
#'   Tags = list(
#'     list(
#'       Key = "Dept",
#'       Value = "Accounting"
#'     ),
#'     list(
#'       Key = "CostCenter",
#'       Value = "12345"
#'     )
#'   ),
#'   UserName = "anika"
#' )}
#'
#' @export
tag_user <- function (UserName, Tags) 
{
    op <- new_operation(name = "TagUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- tag_user_input(UserName = UserName, Tags = Tags)
    output <- tag_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified tags from the role
#'
#' Removes the specified tags from the role. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' untag_role(
#'   RoleName = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the IAM role from which you want to remove tags.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param TagKeys &#91;required&#93; A list of key names as a simple array of strings. The tags with matching keys are removed from the specified role.
#'
#' @examples
#' # The following example shows how to remove a tag with the key 'Dept' from
#' # a role named 'taggedrole'.
#' \donttest{untag_role(
#'   RoleName = "taggedrole",
#'   TagKeys = list(
#'     "Dept"
#'   )
#' )}
#'
#' @export
untag_role <- function (RoleName, TagKeys) 
{
    op <- new_operation(name = "UntagRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- untag_role_input(RoleName = RoleName, TagKeys = TagKeys)
    output <- untag_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the specified tags from the user
#'
#' Removes the specified tags from the user. For more information about tagging, see [Tagging IAM Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_tags.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' untag_user(
#'   UserName = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user from which you want to remove tags.
#' 
#' This parameter accepts (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that consist of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: =,.@-
#' @param TagKeys &#91;required&#93; A list of key names as a simple array of strings. The tags with matching keys are removed from the specified user.
#'
#' @examples
#' # The following example shows how to remove tags that are attached to a
#' # user named 'anika'.
#' \donttest{untag_user(
#'   TagKeys = list(
#'     "Dept"
#'   ),
#'   UserName = "anika"
#' )}
#'
#' @export
untag_user <- function (UserName, TagKeys) 
{
    op <- new_operation(name = "UntagUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- untag_user_input(UserName = UserName, TagKeys = TagKeys)
    output <- untag_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the status of the specified access key from Active to Inactive, or vice versa
#'
#' Changes the status of the specified access key from Active to Inactive, or vice versa. This operation can be used to disable a user\'s key as part of a key rotation workflow.
#' 
#' If the `UserName` field is not specified, the user name is determined implicitly based on the AWS access key ID used to sign the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#' 
#' For information about rotating keys, see [Managing Keys and Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/ManagingCredentials.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_access_key(
#'   UserName = "string",
#'   AccessKeyId = "string",
#'   Status = "Active"|"Inactive"
#' )
#' ```
#'
#' @param UserName The name of the user whose key you want to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param AccessKeyId &#91;required&#93; The access key ID of the secret access key you want to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#' @param Status &#91;required&#93; The status you want to assign to the secret access key. `Active` means that the key can be used for API calls to AWS, while `Inactive` means that the key cannot be used.
#'
#' @examples
#' # The following command deactivates the specified access key (access key
#' # ID and secret access key) for the IAM user named Bob.
#' \donttest{update_access_key(
#'   AccessKeyId = "AKIAIOSFODNN7EXAMPLE",
#'   Status = "Inactive",
#'   UserName = "Bob"
#' )}
#'
#' @export
update_access_key <- function (UserName = NULL, AccessKeyId, 
    Status) 
{
    op <- new_operation(name = "UpdateAccessKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_access_key_input(UserName = UserName, AccessKeyId = AccessKeyId, 
        Status = Status)
    output <- update_access_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the password policy settings for the AWS account
#'
#' Updates the password policy settings for the AWS account.
#' 
#' -   This operation does not support partial updates. No parameters are required, but if you do not specify a parameter, that parameter\'s value reverts to its default value. See the **Request Parameters** section for each parameter\'s default value. Also note that some parameters do not allow the default parameter to be explicitly set. Instead, to invoke the default value, do not include that parameter when you invoke the operation.
#' 
#' For more information about using a password policy, see [Managing an IAM Password Policy](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_ManagingPasswordPolicies.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_account_password_policy(
#'   MinimumPasswordLength = 123,
#'   RequireSymbols = TRUE|FALSE,
#'   RequireNumbers = TRUE|FALSE,
#'   RequireUppercaseCharacters = TRUE|FALSE,
#'   RequireLowercaseCharacters = TRUE|FALSE,
#'   AllowUsersToChangePassword = TRUE|FALSE,
#'   MaxPasswordAge = 123,
#'   PasswordReusePrevention = 123,
#'   HardExpiry = TRUE|FALSE
#' )
#' ```
#'
#' @param MinimumPasswordLength The minimum number of characters allowed in an IAM user password.
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `6`.
#' @param RequireSymbols Specifies whether IAM user passwords must contain at least one of the following non-alphanumeric characters:
#' 
#' ! @ \# \$ % \^ & \* ( ) \_ + - = &#91; &#93; { } \| \'
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that passwords do not require at least one symbol character.
#' @param RequireNumbers Specifies whether IAM user passwords must contain at least one numeric character (0 to 9).
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that passwords do not require at least one numeric character.
#' @param RequireUppercaseCharacters Specifies whether IAM user passwords must contain at least one uppercase character from the ISO basic Latin alphabet (A to Z).
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that passwords do not require at least one uppercase character.
#' @param RequireLowercaseCharacters Specifies whether IAM user passwords must contain at least one lowercase character from the ISO basic Latin alphabet (a to z).
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that passwords do not require at least one lowercase character.
#' @param AllowUsersToChangePassword Allows all IAM users in your account to use the AWS Management Console to change their own passwords. For more information, see [Letting IAM Users Change Their Own Passwords](http://docs.aws.amazon.com/IAM/latest/UserGuide/HowToPwdIAMUser.html) in the *IAM User Guide*.
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that IAM users in the account do not automatically have permissions to change their own password.
#' @param MaxPasswordAge The number of days that an IAM user password is valid.
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `0`. The result is that IAM user passwords never expire.
#' @param PasswordReusePrevention Specifies the number of previous passwords that IAM users are prevented from reusing.
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `0`. The result is that IAM users are not prevented from reusing previous passwords.
#' @param HardExpiry Prevents IAM users from setting a new password after their password has expired. The IAM user cannot be accessed until an administrator resets the password.
#' 
#' If you do not specify a value for this parameter, then the operation uses the default value of `false`. The result is that IAM users can change their passwords after they expire and continue to sign in as the user.
#'
#' @examples
#' # The following command sets the password policy to require a minimum
#' # length of eight characters and to require one or more numbers in the
#' # password:
#' \donttest{update_account_password_policy(
#'   MinimumPasswordLength = 8L,
#'   RequireNumbers = TRUE
#' )}
#'
#' @export
update_account_password_policy <- function (MinimumPasswordLength = NULL, 
    RequireSymbols = NULL, RequireNumbers = NULL, RequireUppercaseCharacters = NULL, 
    RequireLowercaseCharacters = NULL, AllowUsersToChangePassword = NULL, 
    MaxPasswordAge = NULL, PasswordReusePrevention = NULL, HardExpiry = NULL) 
{
    op <- new_operation(name = "UpdateAccountPasswordPolicy", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_account_password_policy_input(MinimumPasswordLength = MinimumPasswordLength, 
        RequireSymbols = RequireSymbols, RequireNumbers = RequireNumbers, 
        RequireUppercaseCharacters = RequireUppercaseCharacters, 
        RequireLowercaseCharacters = RequireLowercaseCharacters, 
        AllowUsersToChangePassword = AllowUsersToChangePassword, 
        MaxPasswordAge = MaxPasswordAge, PasswordReusePrevention = PasswordReusePrevention, 
        HardExpiry = HardExpiry)
    output <- update_account_password_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the policy that grants an IAM entity permission to assume a role
#'
#' Updates the policy that grants an IAM entity permission to assume a role. This is typically referred to as the \"role trust policy\". For more information about roles, go to [Using Roles to Delegate Permissions and Federate Identities](http://docs.aws.amazon.com/IAM/latest/UserGuide/roles-toplevel.html).
#'
#' @section Accepted Parameters:
#' ```
#' update_assume_role_policy(
#'   RoleName = "string",
#'   PolicyDocument = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role to update with the new policy.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param PolicyDocument &#91;required&#93; The policy that grants an entity permission to assume the role.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following command updates the role trust policy for the role named
#' # Test-Role:
#' \donttest{update_assume_role_policy(
#'   PolicyDocument = "{\"Version\":\"2012-10-17\",\"Statement\":[{\"Effect\":\"Allow\",\"Principal\":{\"Service\":[\"ec2.amazonaws.com\"]},\"Action\":[\"sts:AssumeRole\"]}]}",
#'   RoleName = "S3AccessForEC2Instances"
#' )}
#'
#' @export
update_assume_role_policy <- function (RoleName, PolicyDocument) 
{
    op <- new_operation(name = "UpdateAssumeRolePolicy", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_assume_role_policy_input(RoleName = RoleName, 
        PolicyDocument = PolicyDocument)
    output <- update_assume_role_policy_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the name and/or the path of the specified IAM group
#'
#' Updates the name and/or the path of the specified IAM group.
#' 
#' You should understand the implications of changing a group\'s path or name. For more information, see [Renaming Users and Groups](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_WorkingWithGroupsAndUsers.html) in the *IAM User Guide*.
#' 
#' The person making the request (the principal), must have permission to change the role group with the old name and the new name. For example, to change the group named `Managers` to `MGRs`, the principal must have a policy that allows them to update both groups. If the principal has permission to update the `Managers` group, but not the `MGRs` group, then the update fails. For more information about permissions, see [Access Management](http://docs.aws.amazon.com/IAM/latest/UserGuide/access.html).
#'
#' @section Accepted Parameters:
#' ```
#' update_group(
#'   GroupName = "string",
#'   NewPath = "string",
#'   NewGroupName = "string"
#' )
#' ```
#'
#' @param GroupName &#91;required&#93; Name of the IAM group to update. If you\'re changing the name of the group, this is the original name.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param NewPath New path for the IAM group. Only include this if changing the group\'s path.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param NewGroupName New name for the IAM group. Only include this if changing the group\'s name.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command changes the name of the IAM group Test to Test-1.
#' \donttest{update_group(
#'   GroupName = "Test",
#'   NewGroupName = "Test-1"
#' )}
#'
#' @export
update_group <- function (GroupName, NewPath = NULL, NewGroupName = NULL) 
{
    op <- new_operation(name = "UpdateGroup", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_group_input(GroupName = GroupName, NewPath = NewPath, 
        NewGroupName = NewGroupName)
    output <- update_group_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the password for the specified IAM user
#'
#' Changes the password for the specified IAM user.
#' 
#' IAM users can change their own passwords by calling ChangePassword. For more information about modifying passwords, see [Managing Passwords](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_ManagingLogins.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_login_profile(
#'   UserName = "string",
#'   Password = "string",
#'   PasswordResetRequired = TRUE|FALSE
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the user whose password you want to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param Password The new password for the specified IAM user.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' 
#' However, the format can be further restricted by the account administrator by setting a password policy on the AWS account. For more information, see UpdateAccountPasswordPolicy.
#' @param PasswordResetRequired Allows this new password to be used only once by requiring the specified IAM user to set a new password on next sign-in.
#'
#' @examples
#' # The following command creates or changes the password for the IAM user
#' # named Bob.
#' \donttest{update_login_profile(
#'   Password = "SomeKindOfPassword123!@#",
#'   UserName = "Bob"
#' )}
#'
#' @export
update_login_profile <- function (UserName, Password = NULL, 
    PasswordResetRequired = NULL) 
{
    op <- new_operation(name = "UpdateLoginProfile", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_login_profile_input(UserName = UserName, 
        Password = Password, PasswordResetRequired = PasswordResetRequired)
    output <- update_login_profile_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC) provider resource object with a new list of thumbprints
#'
#' Replaces the existing list of server certificate thumbprints associated with an OpenID Connect (OIDC) provider resource object with a new list of thumbprints.
#' 
#' The list that you pass with this operation completely replaces the existing list of thumbprints. (The lists are not merged.)
#' 
#' Typically, you need to update a thumbprint only when the identity provider\'s certificate changes, which occurs rarely. However, if the provider\'s certificate *does* change, any attempt to assume an IAM role that specifies the OIDC provider as a principal fails until the certificate thumbprint is updated.
#' 
#' Trust for the OIDC provider is derived from the provider\'s certificate and is validated by the thumbprint. Therefore, it is best to limit access to the `UpdateOpenIDConnectProviderThumbprint` operation to highly privileged users.
#'
#' @section Accepted Parameters:
#' ```
#' update_open_id_connect_provider_thumbprint(
#'   OpenIDConnectProviderArn = "string",
#'   ThumbprintList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param OpenIDConnectProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the IAM OIDC provider resource object for which you want to update the thumbprint. You can get a list of OIDC provider ARNs by using the ListOpenIDConnectProviders operation.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#' @param ThumbprintList &#91;required&#93; A list of certificate thumbprints that are associated with the specified IAM OpenID Connect provider. For more information, see CreateOpenIDConnectProvider.
#'
#' @export
update_open_id_connect_provider_thumbprint <- function (OpenIDConnectProviderArn, 
    ThumbprintList) 
{
    op <- new_operation(name = "UpdateOpenIDConnectProviderThumbprint", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_open_id_connect_provider_thumbprint_input(OpenIDConnectProviderArn = OpenIDConnectProviderArn, 
        ThumbprintList = ThumbprintList)
    output <- update_open_id_connect_provider_thumbprint_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the description or maximum session duration setting of a role
#'
#' Updates the description or maximum session duration setting of a role.
#'
#' @section Accepted Parameters:
#' ```
#' update_role(
#'   RoleName = "string",
#'   Description = "string",
#'   MaxSessionDuration = 123
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role that you want to modify.
#' @param Description The new description that you want to apply to the specified role.
#' @param MaxSessionDuration The maximum session duration (in seconds) that you want to set for the specified role. If you do not specify a value for this setting, the default maximum of one hour is applied. This setting can have a value from 1 hour to 12 hours.
#' 
#' Anyone who assumes the role from the AWS CLI or API can use the `DurationSeconds` API parameter or the `duration-seconds` CLI parameter to request a longer session. The `MaxSessionDuration` setting determines the maximum duration that can be requested using the `DurationSeconds` parameter. If users don\'t specify a value for the `DurationSeconds` parameter, their security credentials are valid for one hour by default. This applies when you use the `AssumeRole*` API operations or the `assume-role*` CLI operations but does not apply when you use those operations to create a console URL. For more information, see [Using IAM Roles](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_roles_use.html) in the *IAM User Guide*.
#'
#' @export
update_role <- function (RoleName, Description = NULL, MaxSessionDuration = NULL) 
{
    op <- new_operation(name = "UpdateRole", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_role_input(RoleName = RoleName, Description = Description, 
        MaxSessionDuration = MaxSessionDuration)
    output <- update_role_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Use UpdateRole instead
#'
#' Use UpdateRole instead.
#' 
#' Modifies only the description of a role. This operation performs the same function as the `Description` parameter in the `UpdateRole` operation.
#'
#' @section Accepted Parameters:
#' ```
#' update_role_description(
#'   RoleName = "string",
#'   Description = "string"
#' )
#' ```
#'
#' @param RoleName &#91;required&#93; The name of the role that you want to modify.
#' @param Description &#91;required&#93; The new description that you want to apply to the specified role.
#'
#' @export
update_role_description <- function (RoleName, Description) 
{
    op <- new_operation(name = "UpdateRoleDescription", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_role_description_input(RoleName = RoleName, 
        Description = Description)
    output <- update_role_description_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the metadata document for an existing SAML provider resource object
#'
#' Updates the metadata document for an existing SAML provider resource object.
#' 
#' This operation requires [Signature Version 4](http://docs.aws.amazon.com/general/latest/gr/signature-version-4.html).
#'
#' @section Accepted Parameters:
#' ```
#' update_saml_provider(
#'   SAMLMetadataDocument = "string",
#'   SAMLProviderArn = "string"
#' )
#' ```
#'
#' @param SAMLMetadataDocument &#91;required&#93; An XML document generated by an identity provider (IdP) that supports SAML 2.0. The document includes the issuer\'s name, expiration information, and keys that can be used to validate the SAML authentication response (assertions) that are received from the IdP. You must generate the metadata document using the identity management software that is used as your organization\'s IdP.
#' @param SAMLProviderArn &#91;required&#93; The Amazon Resource Name (ARN) of the SAML provider to update.
#' 
#' For more information about ARNs, see [Amazon Resource Names (ARNs) and AWS Service Namespaces](http://docs.aws.amazon.com/general/latest/gr/aws-arns-and-namespaces.html) in the *AWS General Reference*.
#'
#' @export
update_saml_provider <- function (SAMLMetadataDocument, SAMLProviderArn) 
{
    op <- new_operation(name = "UpdateSAMLProvider", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_saml_provider_input(SAMLMetadataDocument = SAMLMetadataDocument, 
        SAMLProviderArn = SAMLProviderArn)
    output <- update_saml_provider_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the status of an IAM user's SSH public key to active or inactive
#'
#' Sets the status of an IAM user\'s SSH public key to active or inactive. SSH public keys that are inactive cannot be used for authentication. This operation can be used to disable a user\'s SSH public key as part of a key rotation work flow.
#' 
#' The SSH public key affected by this operation is used only for authenticating the associated IAM user to an AWS CodeCommit repository. For more information about using SSH keys to authenticate to an AWS CodeCommit repository, see [Set up AWS CodeCommit for SSH Connections](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-credentials-ssh.html) in the *AWS CodeCommit User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_ssh_public_key(
#'   UserName = "string",
#'   SSHPublicKeyId = "string",
#'   Status = "Active"|"Inactive"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user associated with the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SSHPublicKeyId &#91;required&#93; The unique identifier for the SSH public key.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#' @param Status &#91;required&#93; The status to assign to the SSH public key. `Active` means that the key can be used for authentication with an AWS CodeCommit repository. `Inactive` means that the key cannot be used.
#'
#' @export
update_ssh_public_key <- function (UserName, SSHPublicKeyId, 
    Status) 
{
    op <- new_operation(name = "UpdateSSHPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_ssh_public_key_input(UserName = UserName, 
        SSHPublicKeyId = SSHPublicKeyId, Status = Status)
    output <- update_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the name and/or the path of the specified server certificate stored in IAM
#'
#' Updates the name and/or the path of the specified server certificate stored in IAM.
#' 
#' For more information about working with server certificates, see [Working with Server Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs.html) in the *IAM User Guide*. This topic also includes a list of AWS services that can use the server certificates that you manage with IAM.
#' 
#' You should understand the implications of changing a server certificate\'s path or name. For more information, see [Renaming a Server Certificate](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs_manage.html#RenamingServerCerts) in the *IAM User Guide*.
#' 
#' The person making the request (the principal), must have permission to change the server certificate with the old name and the new name. For example, to change the certificate named `ProductionCert` to `ProdCert`, the principal must have a policy that allows them to update both certificates. If the principal has permission to update the `ProductionCert` group, but not the `ProdCert` certificate, then the update fails. For more information about permissions, see [Access Management](http://docs.aws.amazon.com/IAM/latest/UserGuide/access.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' update_server_certificate(
#'   ServerCertificateName = "string",
#'   NewPath = "string",
#'   NewServerCertificateName = "string"
#' )
#' ```
#'
#' @param ServerCertificateName &#91;required&#93; The name of the server certificate that you want to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param NewPath The new path for the server certificate. Include this only if you are updating the server certificate\'s path.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param NewServerCertificateName The new name for the server certificate. Include this only if you are updating the server certificate\'s name. The name of the certificate cannot contain any spaces.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @export
update_server_certificate <- function (ServerCertificateName, 
    NewPath = NULL, NewServerCertificateName = NULL) 
{
    op <- new_operation(name = "UpdateServerCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_server_certificate_input(ServerCertificateName = ServerCertificateName, 
        NewPath = NewPath, NewServerCertificateName = NewServerCertificateName)
    output <- update_server_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Sets the status of a service-specific credential to Active or Inactive
#'
#' Sets the status of a service-specific credential to `Active` or `Inactive`. Service-specific credentials that are inactive cannot be used for authentication to the service. This operation can be used to disable a user\'s service-specific credential as part of a credential rotation work flow.
#'
#' @section Accepted Parameters:
#' ```
#' update_service_specific_credential(
#'   UserName = "string",
#'   ServiceSpecificCredentialId = "string",
#'   Status = "Active"|"Inactive"
#' )
#' ```
#'
#' @param UserName The name of the IAM user associated with the service-specific credential. If you do not specify this value, then the operation assumes the user whose credentials are used to call the operation.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param ServiceSpecificCredentialId &#91;required&#93; The unique identifier of the service-specific credential.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#' @param Status &#91;required&#93; The status to be assigned to the service-specific credential.
#'
#' @export
update_service_specific_credential <- function (UserName = NULL, 
    ServiceSpecificCredentialId, Status) 
{
    op <- new_operation(name = "UpdateServiceSpecificCredential", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- update_service_specific_credential_input(UserName = UserName, 
        ServiceSpecificCredentialId = ServiceSpecificCredentialId, 
        Status = Status)
    output <- update_service_specific_credential_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Changes the status of the specified user signing certificate from active to disabled, or vice versa
#'
#' Changes the status of the specified user signing certificate from active to disabled, or vice versa. This operation can be used to disable an IAM user\'s signing certificate as part of a certificate rotation work flow.
#' 
#' If the `UserName` field is not specified, the user name is determined implicitly based on the AWS access key ID used to sign the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#'
#' @section Accepted Parameters:
#' ```
#' update_signing_certificate(
#'   UserName = "string",
#'   CertificateId = "string",
#'   Status = "Active"|"Inactive"
#' )
#' ```
#'
#' @param UserName The name of the IAM user the signing certificate belongs to.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param CertificateId &#91;required&#93; The ID of the signing certificate you want to update.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters that can consist of any upper or lowercased letter or digit.
#' @param Status &#91;required&#93; The status you want to assign to the certificate. `Active` means that the certificate can be used for API calls to AWS `Inactive` means that the certificate cannot be used.
#'
#' @examples
#' # The following command changes the status of a signing certificate for a
#' # user named Bob to Inactive.
#' \donttest{update_signing_certificate(
#'   CertificateId = "TA7SMP42TDN5Z26OBPJE7EXAMPLE",
#'   Status = "Inactive",
#'   UserName = "Bob"
#' )}
#'
#' @export
update_signing_certificate <- function (UserName = NULL, CertificateId, 
    Status) 
{
    op <- new_operation(name = "UpdateSigningCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_signing_certificate_input(UserName = UserName, 
        CertificateId = CertificateId, Status = Status)
    output <- update_signing_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates the name and/or the path of the specified IAM user
#'
#' Updates the name and/or the path of the specified IAM user.
#' 
#' You should understand the implications of changing an IAM user\'s path or name. For more information, see [Renaming an IAM User](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_users_manage.html#id_users_renaming) and [Renaming an IAM Group](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_groups_manage_rename.html) in the *IAM User Guide*.
#' 
#' To change a user name, the requester must have appropriate permissions on both the source object and the target object. For example, to change Bob to Robert, the entity making the request must have permission on Bob and Robert, or must have permission on all (\*). For more information about permissions, see [Permissions and Policies](http://docs.aws.amazon.com/IAM/latest/UserGuide/PermissionsAndPolicies.html).
#'
#' @section Accepted Parameters:
#' ```
#' update_user(
#'   UserName = "string",
#'   NewPath = "string",
#'   NewUserName = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; Name of the user to update. If you\'re changing the name of the user, this is the original user name.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param NewPath New path for the IAM user. Include this parameter only if you\'re changing the user\'s path.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' @param NewUserName New name for the user. Include this parameter only if you\'re changing the user\'s name.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#'
#' @examples
#' # The following command changes the name of the IAM user Bob to Robert. It
#' # does not change the user's path.
#' \donttest{update_user(
#'   NewUserName = "Robert",
#'   UserName = "Bob"
#' )}
#'
#' @export
update_user <- function (UserName, NewPath = NULL, NewUserName = NULL) 
{
    op <- new_operation(name = "UpdateUser", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_user_input(UserName = UserName, NewPath = NewPath, 
        NewUserName = NewUserName)
    output <- update_user_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Uploads an SSH public key and associates it with the specified IAM user
#'
#' Uploads an SSH public key and associates it with the specified IAM user.
#' 
#' The SSH public key uploaded by this operation can be used only for authenticating the associated IAM user to an AWS CodeCommit repository. For more information about using SSH keys to authenticate to an AWS CodeCommit repository, see [Set up AWS CodeCommit for SSH Connections](http://docs.aws.amazon.com/codecommit/latest/userguide/setting-up-credentials-ssh.html) in the *AWS CodeCommit User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' upload_ssh_public_key(
#'   UserName = "string",
#'   SSHPublicKeyBody = "string"
#' )
#' ```
#'
#' @param UserName &#91;required&#93; The name of the IAM user to associate the SSH public key with.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param SSHPublicKeyBody &#91;required&#93; The SSH public key. The public key must be encoded in ssh-rsa format or PEM format. The minimum bit-length of the public key is 2048 bits. For example, you can generate a 2048-bit key, and the resulting PEM file is 1679 bytes long.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @export
upload_ssh_public_key <- function (UserName, SSHPublicKeyBody) 
{
    op <- new_operation(name = "UploadSSHPublicKey", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- upload_ssh_public_key_input(UserName = UserName, 
        SSHPublicKeyBody = SSHPublicKeyBody)
    output <- upload_ssh_public_key_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Uploads a server certificate entity for the AWS account
#'
#' Uploads a server certificate entity for the AWS account. The server certificate entity includes a public key certificate, a private key, and an optional certificate chain, which should all be PEM-encoded.
#' 
#' We recommend that you use [AWS Certificate Manager](http://docs.aws.amazon.com/certificate-manager/) to provision, manage, and deploy your server certificates. With ACM you can request a certificate, deploy it to AWS resources, and let ACM handle certificate renewals for you. Certificates provided by ACM are free. For more information about using ACM, see the [AWS Certificate Manager User Guide](http://docs.aws.amazon.com/acm/latest/userguide/).
#' 
#' For more information about working with server certificates, see [Working with Server Certificates](http://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_server-certs.html) in the *IAM User Guide*. This topic includes a list of AWS services that can use the server certificates that you manage with IAM.
#' 
#' For information about the number of server certificates you can upload, see [Limitations on IAM Entities and Objects](http://docs.aws.amazon.com/IAM/latest/UserGuide/reference_iam-limits.html) in the *IAM User Guide*.
#' 
#' Because the body of the public key certificate, private key, and the certificate chain can be large, you should use POST rather than GET when calling `UploadServerCertificate`. For information about setting up signatures and authorization through the API, go to [Signing AWS API Requests](http://docs.aws.amazon.com/general/latest/gr/signing_aws_api_requests.html) in the *AWS General Reference*. For general information about using the Query API with IAM, go to [Calling the API by Making HTTP Query Requests](http://docs.aws.amazon.com/IAM/latest/UserGuide/programming.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' upload_server_certificate(
#'   Path = "string",
#'   ServerCertificateName = "string",
#'   CertificateBody = "string",
#'   PrivateKey = "string",
#'   CertificateChain = "string"
#' )
#' ```
#'
#' @param Path The path for the server certificate. For more information about paths, see [IAM Identifiers](http://docs.aws.amazon.com/IAM/latest/UserGuide/Using_Identifiers.html) in the *IAM User Guide*.
#' 
#' This parameter is optional. If it is not included, it defaults to a slash (/). This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of either a forward slash (/) by itself or a string that must begin and end with forward slashes. In addition, it can contain any ASCII character from the ! (\\u0021) through the DEL character (\\u007F), including most punctuation characters, digits, and upper and lowercased letters.
#' 
#' If you are uploading a server certificate specifically for use with Amazon CloudFront distributions, you must specify a path using the `path` parameter. The path must begin with `/cloudfront` and must include a trailing slash (for example, `/cloudfront/test/`).
#' @param ServerCertificateName &#91;required&#93; The name for the server certificate. Do not include the path in this value. The name of the certificate cannot contain any spaces.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param CertificateBody &#91;required&#93; The contents of the public key certificate in PEM-encoded format.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param PrivateKey &#91;required&#93; The contents of the private key in PEM-encoded format.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#' @param CertificateChain The contents of the certificate chain. This is typically a concatenation of the PEM-encoded public key certificates of the chain.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following upload-server-certificate command uploads a server
#' # certificate to your AWS account:
#' \donttest{upload_server_certificate(
#'   CertificateBody = "-----BEGIN CERTIFICATE-----<a very long certificate text string>-----END CERTIFICATE-----",
#'   Path = "/company/servercerts/",
#'   PrivateKey = "-----BEGIN DSA PRIVATE KEY-----<a very long private key string>-----END DSA PRIVATE KEY-----",
#'   ServerCertificateName = "ProdServerCert"
#' )}
#'
#' @export
upload_server_certificate <- function (Path = NULL, ServerCertificateName, 
    CertificateBody, PrivateKey, CertificateChain = NULL) 
{
    op <- new_operation(name = "UploadServerCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- upload_server_certificate_input(Path = Path, ServerCertificateName = ServerCertificateName, 
        CertificateBody = CertificateBody, PrivateKey = PrivateKey, 
        CertificateChain = CertificateChain)
    output <- upload_server_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Uploads an X
#'
#' Uploads an X.509 signing certificate and associates it with the specified IAM user. Some AWS services use X.509 signing certificates to validate requests that are signed with a corresponding private key. When you upload the certificate, its default status is `Active`.
#' 
#' If the `UserName` field is not specified, the IAM user name is determined implicitly based on the AWS access key ID used to sign the request. This operation works for access keys under the AWS account. Consequently, you can use this operation to manage AWS account root user credentials even if the AWS account has no associated users.
#' 
#' Because the body of an X.509 certificate can be large, you should use POST rather than GET when calling `UploadSigningCertificate`. For information about setting up signatures and authorization through the API, go to [Signing AWS API Requests](http://docs.aws.amazon.com/general/latest/gr/signing_aws_api_requests.html) in the *AWS General Reference*. For general information about using the Query API with IAM, go to [Making Query Requests](http://docs.aws.amazon.com/IAM/latest/UserGuide/IAM_UsingQueryAPI.html) in the *IAM User Guide*.
#'
#' @section Accepted Parameters:
#' ```
#' upload_signing_certificate(
#'   UserName = "string",
#'   CertificateBody = "string"
#' )
#' ```
#'
#' @param UserName The name of the user the signing certificate is for.
#' 
#' This parameter allows (through its [regex pattern](http://wikipedia.org/wiki/regex)) a string of characters consisting of upper and lowercase alphanumeric characters with no spaces. You can also include any of the following characters: \_+=,.@-
#' @param CertificateBody &#91;required&#93; The contents of the signing certificate.
#' 
#' The [regex pattern](http://wikipedia.org/wiki/regex) used to validate this parameter is a string of characters consisting of the following:
#' 
#' -   Any printable ASCII character ranging from the space character (\\u0020) through the end of the ASCII character range
#' 
#' -   The printable characters in the Basic Latin and Latin-1 Supplement character set (through \\u00FF)
#' 
#' -   The special characters tab (\\u0009), line feed (\\u000A), and carriage return (\\u000D)
#'
#' @examples
#' # The following command uploads a signing certificate for the IAM user
#' # named Bob.
#' \donttest{upload_signing_certificate(
#'   CertificateBody = "-----BEGIN CERTIFICATE-----<certificate-body>-----END CERTIFICATE-----",
#'   UserName = "Bob"
#' )}
#'
#' @export
upload_signing_certificate <- function (UserName = NULL, CertificateBody) 
{
    op <- new_operation(name = "UploadSigningCertificate", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- upload_signing_certificate_input(UserName = UserName, 
        CertificateBody = CertificateBody)
    output <- upload_signing_certificate_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
