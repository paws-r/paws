# This file is generated by make.paws. Please do not edit here.

#' @importFrom paws.common new_operation new_request send_request
NULL

#' Returns information about a specified job and whether that job has been received by the job worker
#'
#' Returns information about a specified job and whether that job has been received by the job worker. Only used for custom actions.
#'
#' @section Accepted Parameters:
#' ```
#' acknowledge_job(
#'   jobId = "string",
#'   nonce = "string"
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID of the job for which you want to confirm receipt.
#' @param nonce &#91;required&#93; A system-generated random number that AWS CodePipeline uses to ensure that the job is being worked on by only one job worker. Get this number from the response of the PollForJobs request that returned this job.
#'
#' @export
acknowledge_job <- function (jobId, nonce) 
{
    op <- new_operation(name = "AcknowledgeJob", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- acknowledge_job_input(jobId = jobId, nonce = nonce)
    output <- acknowledge_job_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Confirms a job worker has received the specified job
#'
#' Confirms a job worker has received the specified job. Only used for partner actions.
#'
#' @section Accepted Parameters:
#' ```
#' acknowledge_third_party_job(
#'   jobId = "string",
#'   nonce = "string",
#'   clientToken = "string"
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID of the job.
#' @param nonce &#91;required&#93; A system-generated random number that AWS CodePipeline uses to ensure that the job is being worked on by only one job worker. Get this number from the response to a GetThirdPartyJobDetails request.
#' @param clientToken &#91;required&#93; The clientToken portion of the clientId and clientToken pair used to verify that the calling entity is allowed access to the job and its details.
#'
#' @export
acknowledge_third_party_job <- function (jobId, nonce, clientToken) 
{
    op <- new_operation(name = "AcknowledgeThirdPartyJob", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- acknowledge_third_party_job_input(jobId = jobId, 
        nonce = nonce, clientToken = clientToken)
    output <- acknowledge_third_party_job_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a new custom action that can be used in all pipelines associated with the AWS account
#'
#' Creates a new custom action that can be used in all pipelines associated with the AWS account. Only used for custom actions.
#'
#' @section Accepted Parameters:
#' ```
#' create_custom_action_type(
#'   category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'   provider = "string",
#'   version = "string",
#'   settings = list(
#'     thirdPartyConfigurationUrl = "string",
#'     entityUrlTemplate = "string",
#'     executionUrlTemplate = "string",
#'     revisionUrlTemplate = "string"
#'   ),
#'   configurationProperties = list(
#'     list(
#'       name = "string",
#'       required = TRUE|FALSE,
#'       key = TRUE|FALSE,
#'       secret = TRUE|FALSE,
#'       queryable = TRUE|FALSE,
#'       description = "string",
#'       type = "String"|"Number"|"Boolean"
#'     )
#'   ),
#'   inputArtifactDetails = list(
#'     minimumCount = 123,
#'     maximumCount = 123
#'   ),
#'   outputArtifactDetails = list(
#'     minimumCount = 123,
#'     maximumCount = 123
#'   )
#' )
#' ```
#'
#' @param category &#91;required&#93; The category of the custom action, such as a build action or a test action.
#' 
#' Although Source and Approval are listed as valid values, they are not currently functional. These values are reserved for future use.
#' @param provider &#91;required&#93; The provider of the service used in the custom action, such as AWS CodeDeploy.
#' @param version &#91;required&#93; The version identifier of the custom action.
#' @param settings Returns information about the settings for an action type.
#' @param configurationProperties The configuration properties for the custom action.
#' 
#' You can refer to a name in the configuration properties of the custom action within the URL templates by following the format of {Config:name}, as long as the configuration property is both required and not secret. For more information, see [Create a Custom Action for a Pipeline](http://docs.aws.amazon.com/codepipeline/latest/userguide/how-to-create-custom-action.html).
#' @param inputArtifactDetails &#91;required&#93; The details of the input artifact for the action, such as its commit ID.
#' @param outputArtifactDetails &#91;required&#93; The details of the output artifact of the action, such as its commit ID.
#'
#' @export
create_custom_action_type <- function (category, provider, version, 
    settings = NULL, configurationProperties = NULL, inputArtifactDetails, 
    outputArtifactDetails) 
{
    op <- new_operation(name = "CreateCustomActionType", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_custom_action_type_input(category = category, 
        provider = provider, version = version, settings = settings, 
        configurationProperties = configurationProperties, inputArtifactDetails = inputArtifactDetails, 
        outputArtifactDetails = outputArtifactDetails)
    output <- create_custom_action_type_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Creates a pipeline
#'
#' Creates a pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' create_pipeline(
#'   pipeline = list(
#'     name = "string",
#'     roleArn = "string",
#'     artifactStore = list(
#'       type = "S3",
#'       location = "string",
#'       encryptionKey = list(
#'         id = "string",
#'         type = "KMS"
#'       )
#'     ),
#'     artifactStores = list(
#'       list(
#'         type = "S3",
#'         location = "string",
#'         encryptionKey = list(
#'           id = "string",
#'           type = "KMS"
#'         )
#'       )
#'     ),
#'     stages = list(
#'       list(
#'         name = "string",
#'         blockers = list(
#'           list(
#'             name = "string",
#'             type = "Schedule"
#'           )
#'         ),
#'         actions = list(
#'           list(
#'             name = "string",
#'             actionTypeId = list(
#'               category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'               owner = "AWS"|"ThirdParty"|"Custom",
#'               provider = "string",
#'               version = "string"
#'             ),
#'             runOrder = 123,
#'             configuration = list(
#'               "string"
#'             ),
#'             outputArtifacts = list(
#'               list(
#'                 name = "string"
#'               )
#'             ),
#'             inputArtifacts = list(
#'               list(
#'                 name = "string"
#'               )
#'             ),
#'             roleArn = "string",
#'             region = "string"
#'           )
#'         )
#'       )
#'     ),
#'     version = 123
#'   )
#' )
#' ```
#'
#' @param pipeline &#91;required&#93; Represents the structure of actions and stages to be performed in the pipeline.
#'
#' @export
create_pipeline <- function (pipeline) 
{
    op <- new_operation(name = "CreatePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- create_pipeline_input(pipeline = pipeline)
    output <- create_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Marks a custom action as deleted
#'
#' Marks a custom action as deleted. PollForJobs for the custom action will fail after the action is marked for deletion. Only used for custom actions.
#' 
#' To re-create a custom action after it has been deleted you must use a string in the version field that has never been used before. This string can be an incremented version number, for example. To restore a deleted custom action, use a JSON file that is identical to the deleted action, including the original string in the version field.
#'
#' @section Accepted Parameters:
#' ```
#' delete_custom_action_type(
#'   category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'   provider = "string",
#'   version = "string"
#' )
#' ```
#'
#' @param category &#91;required&#93; The category of the custom action that you want to delete, such as source or deploy.
#' @param provider &#91;required&#93; The provider of the service used in the custom action, such as AWS CodeDeploy.
#' @param version &#91;required&#93; The version of the custom action to delete.
#'
#' @export
delete_custom_action_type <- function (category, provider, version) 
{
    op <- new_operation(name = "DeleteCustomActionType", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_custom_action_type_input(category = category, 
        provider = provider, version = version)
    output <- delete_custom_action_type_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes the specified pipeline
#'
#' Deletes the specified pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' delete_pipeline(
#'   name = "string"
#' )
#' ```
#'
#' @param name &#91;required&#93; The name of the pipeline to be deleted.
#'
#' @export
delete_pipeline <- function (name) 
{
    op <- new_operation(name = "DeletePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_pipeline_input(name = name)
    output <- delete_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Deletes a previously created webhook by name
#'
#' Deletes a previously created webhook by name. Deleting the webhook stops AWS CodePipeline from starting a pipeline every time an external event occurs. The API will return successfully when trying to delete a webhook that is already deleted. If a deleted webhook is re-created by calling PutWebhook with the same name, it will have a different URL.
#'
#' @section Accepted Parameters:
#' ```
#' delete_webhook(
#'   name = "string"
#' )
#' ```
#'
#' @param name &#91;required&#93; The name of the webhook you want to delete.
#'
#' @export
delete_webhook <- function (name) 
{
    op <- new_operation(name = "DeleteWebhook", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- delete_webhook_input(name = name)
    output <- delete_webhook_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Removes the connection between the webhook that was created by CodePipeline and the external tool with events to be detected
#'
#' Removes the connection between the webhook that was created by CodePipeline and the external tool with events to be detected. Currently only supported for webhooks that target an action type of GitHub.
#'
#' @section Accepted Parameters:
#' ```
#' deregister_webhook_with_third_party(
#'   webhookName = "string"
#' )
#' ```
#'
#' @param webhookName The name of the webhook you want to deregister.
#'
#' @export
deregister_webhook_with_third_party <- function (webhookName = NULL) 
{
    op <- new_operation(name = "DeregisterWebhookWithThirdParty", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- deregister_webhook_with_third_party_input(webhookName = webhookName)
    output <- deregister_webhook_with_third_party_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Prevents artifacts in a pipeline from transitioning to the next stage in the pipeline
#'
#' Prevents artifacts in a pipeline from transitioning to the next stage in the pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' disable_stage_transition(
#'   pipelineName = "string",
#'   stageName = "string",
#'   transitionType = "Inbound"|"Outbound",
#'   reason = "string"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline in which you want to disable the flow of artifacts from one stage to another.
#' @param stageName &#91;required&#93; The name of the stage where you want to disable the inbound or outbound transition of artifacts.
#' @param transitionType &#91;required&#93; Specifies whether artifacts will be prevented from transitioning into the stage and being processed by the actions in that stage (inbound), or prevented from transitioning from the stage after they have been processed by the actions in that stage (outbound).
#' @param reason &#91;required&#93; The reason given to the user why a stage is disabled, such as waiting for manual approval or manual tests. This message is displayed in the pipeline console UI.
#'
#' @export
disable_stage_transition <- function (pipelineName, stageName, 
    transitionType, reason) 
{
    op <- new_operation(name = "DisableStageTransition", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- disable_stage_transition_input(pipelineName = pipelineName, 
        stageName = stageName, transitionType = transitionType, 
        reason = reason)
    output <- disable_stage_transition_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Enables artifacts in a pipeline to transition to a stage in a pipeline
#'
#' Enables artifacts in a pipeline to transition to a stage in a pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' enable_stage_transition(
#'   pipelineName = "string",
#'   stageName = "string",
#'   transitionType = "Inbound"|"Outbound"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline in which you want to enable the flow of artifacts from one stage to another.
#' @param stageName &#91;required&#93; The name of the stage where you want to enable the transition of artifacts, either into the stage (inbound) or from that stage to the next stage (outbound).
#' @param transitionType &#91;required&#93; Specifies whether artifacts will be allowed to enter the stage and be processed by the actions in that stage (inbound) or whether already-processed artifacts will be allowed to transition to the next stage (outbound).
#'
#' @export
enable_stage_transition <- function (pipelineName, stageName, 
    transitionType) 
{
    op <- new_operation(name = "EnableStageTransition", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- enable_stage_transition_input(pipelineName = pipelineName, 
        stageName = stageName, transitionType = transitionType)
    output <- enable_stage_transition_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about a job
#'
#' Returns information about a job. Only used for custom actions.
#' 
#' When this API is called, AWS CodePipeline returns temporary credentials for the Amazon S3 bucket used to store artifacts for the pipeline, if the action requires access to that Amazon S3 bucket for input or output artifacts. Additionally, this API returns any secret values defined for the action.
#'
#' @section Accepted Parameters:
#' ```
#' get_job_details(
#'   jobId = "string"
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID for the job.
#'
#' @export
get_job_details <- function (jobId) 
{
    op <- new_operation(name = "GetJobDetails", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_job_details_input(jobId = jobId)
    output <- get_job_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns the metadata, structure, stages, and actions of a pipeline
#'
#' Returns the metadata, structure, stages, and actions of a pipeline. Can be used to return the entire structure of a pipeline in JSON format, which can then be modified and used to update the pipeline structure with UpdatePipeline.
#'
#' @section Accepted Parameters:
#' ```
#' get_pipeline(
#'   name = "string",
#'   version = 123
#' )
#' ```
#'
#' @param name &#91;required&#93; The name of the pipeline for which you want to get information. Pipeline names must be unique under an Amazon Web Services (AWS) user account.
#' @param version The version number of the pipeline. If you do not specify a version, defaults to the most current version.
#'
#' @export
get_pipeline <- function (name, version = NULL) 
{
    op <- new_operation(name = "GetPipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_pipeline_input(name = name, version = version)
    output <- get_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about an execution of a pipeline, including details about artifacts, the pipeline execution ID, and the name, version, and status of the pipeline
#'
#' Returns information about an execution of a pipeline, including details about artifacts, the pipeline execution ID, and the name, version, and status of the pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' get_pipeline_execution(
#'   pipelineName = "string",
#'   pipelineExecutionId = "string"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline about which you want to get execution details.
#' @param pipelineExecutionId &#91;required&#93; The ID of the pipeline execution about which you want to get execution details.
#'
#' @export
get_pipeline_execution <- function (pipelineName, pipelineExecutionId) 
{
    op <- new_operation(name = "GetPipelineExecution", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_pipeline_execution_input(pipelineName = pipelineName, 
        pipelineExecutionId = pipelineExecutionId)
    output <- get_pipeline_execution_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about the state of a pipeline, including the stages and actions
#'
#' Returns information about the state of a pipeline, including the stages and actions.
#'
#' @section Accepted Parameters:
#' ```
#' get_pipeline_state(
#'   name = "string"
#' )
#' ```
#'
#' @param name &#91;required&#93; The name of the pipeline about which you want to get information.
#'
#' @export
get_pipeline_state <- function (name) 
{
    op <- new_operation(name = "GetPipelineState", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_pipeline_state_input(name = name)
    output <- get_pipeline_state_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Requests the details of a job for a third party action
#'
#' Requests the details of a job for a third party action. Only used for partner actions.
#' 
#' When this API is called, AWS CodePipeline returns temporary credentials for the Amazon S3 bucket used to store artifacts for the pipeline, if the action requires access to that Amazon S3 bucket for input or output artifacts. Additionally, this API returns any secret values defined for the action.
#'
#' @section Accepted Parameters:
#' ```
#' get_third_party_job_details(
#'   jobId = "string",
#'   clientToken = "string"
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID used for identifying the job.
#' @param clientToken &#91;required&#93; The clientToken portion of the clientId and clientToken pair used to verify that the calling entity is allowed access to the job and its details.
#'
#' @export
get_third_party_job_details <- function (jobId, clientToken) 
{
    op <- new_operation(name = "GetThirdPartyJobDetails", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- get_third_party_job_details_input(jobId = jobId, 
        clientToken = clientToken)
    output <- get_third_party_job_details_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a summary of all AWS CodePipeline action types associated with your account
#'
#' Gets a summary of all AWS CodePipeline action types associated with your account.
#'
#' @section Accepted Parameters:
#' ```
#' list_action_types(
#'   actionOwnerFilter = "AWS"|"ThirdParty"|"Custom",
#'   nextToken = "string"
#' )
#' ```
#'
#' @param actionOwnerFilter Filters the list of action types to those created by a specified entity.
#' @param nextToken An identifier that was returned from the previous list action types call, which can be used to return the next set of action types in the list.
#'
#' @export
list_action_types <- function (actionOwnerFilter = NULL, nextToken = NULL) 
{
    op <- new_operation(name = "ListActionTypes", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_action_types_input(actionOwnerFilter = actionOwnerFilter, 
        nextToken = nextToken)
    output <- list_action_types_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a summary of the most recent executions for a pipeline
#'
#' Gets a summary of the most recent executions for a pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' list_pipeline_executions(
#'   pipelineName = "string",
#'   maxResults = 123,
#'   nextToken = "string"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline for which you want to get execution summary information.
#' @param maxResults The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned nextToken value. The available pipeline execution history is limited to the most recent 12 months, based on pipeline execution start times. Default value is 100.
#' @param nextToken The token that was returned from the previous ListPipelineExecutions call, which can be used to return the next set of pipeline executions in the list.
#'
#' @export
list_pipeline_executions <- function (pipelineName, maxResults = NULL, 
    nextToken = NULL) 
{
    op <- new_operation(name = "ListPipelineExecutions", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_pipeline_executions_input(pipelineName = pipelineName, 
        maxResults = maxResults, nextToken = nextToken)
    output <- list_pipeline_executions_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a summary of all of the pipelines associated with your account
#'
#' Gets a summary of all of the pipelines associated with your account.
#'
#' @section Accepted Parameters:
#' ```
#' list_pipelines(
#'   nextToken = "string"
#' )
#' ```
#'
#' @param nextToken An identifier that was returned from the previous list pipelines call, which can be used to return the next set of pipelines in the list.
#'
#' @export
list_pipelines <- function (nextToken = NULL) 
{
    op <- new_operation(name = "ListPipelines", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_pipelines_input(nextToken = nextToken)
    output <- list_pipelines_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Gets a listing of all the webhooks in this region for this account
#'
#' Gets a listing of all the webhooks in this region for this account. The output lists all webhooks and includes the webhook URL and ARN, as well the configuration for each webhook.
#'
#' @section Accepted Parameters:
#' ```
#' list_webhooks(
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @param NextToken The token that was returned from the previous ListWebhooks call, which can be used to return the next set of webhooks in the list.
#' @param MaxResults The maximum number of results to return in a single call. To retrieve the remaining results, make another call with the returned nextToken value.
#'
#' @export
list_webhooks <- function (NextToken = NULL, MaxResults = NULL) 
{
    op <- new_operation(name = "ListWebhooks", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- list_webhooks_input(NextToken = NextToken, MaxResults = MaxResults)
    output <- list_webhooks_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Returns information about any jobs for AWS CodePipeline to act upon
#'
#' Returns information about any jobs for AWS CodePipeline to act upon. PollForJobs is only valid for action types with \"Custom\" in the owner field. If the action type contains \"AWS\" or \"ThirdParty\" in the owner field, the PollForJobs action returns an error.
#' 
#' When this API is called, AWS CodePipeline returns temporary credentials for the Amazon S3 bucket used to store artifacts for the pipeline, if the action requires access to that Amazon S3 bucket for input or output artifacts. Additionally, this API returns any secret values defined for the action.
#'
#' @section Accepted Parameters:
#' ```
#' poll_for_jobs(
#'   actionTypeId = list(
#'     category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'     owner = "AWS"|"ThirdParty"|"Custom",
#'     provider = "string",
#'     version = "string"
#'   ),
#'   maxBatchSize = 123,
#'   queryParam = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @param actionTypeId &#91;required&#93; Represents information about an action type.
#' @param maxBatchSize The maximum number of jobs to return in a poll for jobs call.
#' @param queryParam A map of property names and values. For an action type with no queryable properties, this value must be null or an empty map. For an action type with a queryable property, you must supply that property as a key in the map. Only jobs whose action configuration matches the mapped value will be returned.
#'
#' @export
poll_for_jobs <- function (actionTypeId, maxBatchSize = NULL, 
    queryParam = NULL) 
{
    op <- new_operation(name = "PollForJobs", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- poll_for_jobs_input(actionTypeId = actionTypeId, 
        maxBatchSize = maxBatchSize, queryParam = queryParam)
    output <- poll_for_jobs_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Determines whether there are any third party jobs for a job worker to act on
#'
#' Determines whether there are any third party jobs for a job worker to act on. Only used for partner actions.
#' 
#' When this API is called, AWS CodePipeline returns temporary credentials for the Amazon S3 bucket used to store artifacts for the pipeline, if the action requires access to that Amazon S3 bucket for input or output artifacts.
#'
#' @section Accepted Parameters:
#' ```
#' poll_for_third_party_jobs(
#'   actionTypeId = list(
#'     category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'     owner = "AWS"|"ThirdParty"|"Custom",
#'     provider = "string",
#'     version = "string"
#'   ),
#'   maxBatchSize = 123
#' )
#' ```
#'
#' @param actionTypeId &#91;required&#93; Represents information about an action type.
#' @param maxBatchSize The maximum number of jobs to return in a poll for jobs call.
#'
#' @export
poll_for_third_party_jobs <- function (actionTypeId, maxBatchSize = NULL) 
{
    op <- new_operation(name = "PollForThirdPartyJobs", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- poll_for_third_party_jobs_input(actionTypeId = actionTypeId, 
        maxBatchSize = maxBatchSize)
    output <- poll_for_third_party_jobs_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Provides information to AWS CodePipeline about new revisions to a source
#'
#' Provides information to AWS CodePipeline about new revisions to a source.
#'
#' @section Accepted Parameters:
#' ```
#' put_action_revision(
#'   pipelineName = "string",
#'   stageName = "string",
#'   actionName = "string",
#'   actionRevision = list(
#'     revisionId = "string",
#'     revisionChangeId = "string",
#'     created = as.POSIXct("2015-01-01")
#'   )
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline that will start processing the revision to the source.
#' @param stageName &#91;required&#93; The name of the stage that contains the action that will act upon the revision.
#' @param actionName &#91;required&#93; The name of the action that will process the revision.
#' @param actionRevision &#91;required&#93; Represents information about the version (or revision) of an action.
#'
#' @export
put_action_revision <- function (pipelineName, stageName, actionName, 
    actionRevision) 
{
    op <- new_operation(name = "PutActionRevision", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_action_revision_input(pipelineName = pipelineName, 
        stageName = stageName, actionName = actionName, actionRevision = actionRevision)
    output <- put_action_revision_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Provides the response to a manual approval request to AWS CodePipeline
#'
#' Provides the response to a manual approval request to AWS CodePipeline. Valid responses include Approved and Rejected.
#'
#' @section Accepted Parameters:
#' ```
#' put_approval_result(
#'   pipelineName = "string",
#'   stageName = "string",
#'   actionName = "string",
#'   result = list(
#'     summary = "string",
#'     status = "Approved"|"Rejected"
#'   ),
#'   token = "string"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline that contains the action.
#' @param stageName &#91;required&#93; The name of the stage that contains the action.
#' @param actionName &#91;required&#93; The name of the action for which approval is requested.
#' @param result &#91;required&#93; Represents information about the result of the approval request.
#' @param token &#91;required&#93; The system-generated token used to identify a unique approval request. The token for each open approval request can be obtained using the GetPipelineState action and is used to validate that the approval request corresponding to this token is still valid.
#'
#' @export
put_approval_result <- function (pipelineName, stageName, actionName, 
    result, token) 
{
    op <- new_operation(name = "PutApprovalResult", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_approval_result_input(pipelineName = pipelineName, 
        stageName = stageName, actionName = actionName, result = result, 
        token = token)
    output <- put_approval_result_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Represents the failure of a job as returned to the pipeline by a job worker
#'
#' Represents the failure of a job as returned to the pipeline by a job worker. Only used for custom actions.
#'
#' @section Accepted Parameters:
#' ```
#' put_job_failure_result(
#'   jobId = "string",
#'   failureDetails = list(
#'     type = "JobFailed"|"ConfigurationError"|"PermissionError"|"RevisionOutOfSync"|"RevisionUnavailable"|"SystemUnavailable",
#'     message = "string",
#'     externalExecutionId = "string"
#'   )
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID of the job that failed. This is the same ID returned from PollForJobs.
#' @param failureDetails &#91;required&#93; The details about the failure of a job.
#'
#' @export
put_job_failure_result <- function (jobId, failureDetails) 
{
    op <- new_operation(name = "PutJobFailureResult", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_job_failure_result_input(jobId = jobId, failureDetails = failureDetails)
    output <- put_job_failure_result_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Represents the success of a job as returned to the pipeline by a job worker
#'
#' Represents the success of a job as returned to the pipeline by a job worker. Only used for custom actions.
#'
#' @section Accepted Parameters:
#' ```
#' put_job_success_result(
#'   jobId = "string",
#'   currentRevision = list(
#'     revision = "string",
#'     changeIdentifier = "string",
#'     created = as.POSIXct("2015-01-01"),
#'     revisionSummary = "string"
#'   ),
#'   continuationToken = "string",
#'   executionDetails = list(
#'     summary = "string",
#'     externalExecutionId = "string",
#'     percentComplete = 123
#'   )
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The unique system-generated ID of the job that succeeded. This is the same ID returned from PollForJobs.
#' @param currentRevision The ID of the current revision of the artifact successfully worked upon by the job.
#' @param continuationToken A token generated by a job worker, such as an AWS CodeDeploy deployment ID, that a successful job provides to identify a custom action in progress. Future jobs will use this token in order to identify the running instance of the action. It can be reused to return additional information about the progress of the custom action. When the action is complete, no continuation token should be supplied.
#' @param executionDetails The execution details of the successful job, such as the actions taken by the job worker.
#'
#' @export
put_job_success_result <- function (jobId, currentRevision = NULL, 
    continuationToken = NULL, executionDetails = NULL) 
{
    op <- new_operation(name = "PutJobSuccessResult", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_job_success_result_input(jobId = jobId, currentRevision = currentRevision, 
        continuationToken = continuationToken, executionDetails = executionDetails)
    output <- put_job_success_result_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Represents the failure of a third party job as returned to the pipeline by a job worker
#'
#' Represents the failure of a third party job as returned to the pipeline by a job worker. Only used for partner actions.
#'
#' @section Accepted Parameters:
#' ```
#' put_third_party_job_failure_result(
#'   jobId = "string",
#'   clientToken = "string",
#'   failureDetails = list(
#'     type = "JobFailed"|"ConfigurationError"|"PermissionError"|"RevisionOutOfSync"|"RevisionUnavailable"|"SystemUnavailable",
#'     message = "string",
#'     externalExecutionId = "string"
#'   )
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The ID of the job that failed. This is the same ID returned from PollForThirdPartyJobs.
#' @param clientToken &#91;required&#93; The clientToken portion of the clientId and clientToken pair used to verify that the calling entity is allowed access to the job and its details.
#' @param failureDetails &#91;required&#93; Represents information about failure details.
#'
#' @export
put_third_party_job_failure_result <- function (jobId, clientToken, 
    failureDetails) 
{
    op <- new_operation(name = "PutThirdPartyJobFailureResult", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- put_third_party_job_failure_result_input(jobId = jobId, 
        clientToken = clientToken, failureDetails = failureDetails)
    output <- put_third_party_job_failure_result_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Represents the success of a third party job as returned to the pipeline by a job worker
#'
#' Represents the success of a third party job as returned to the pipeline by a job worker. Only used for partner actions.
#'
#' @section Accepted Parameters:
#' ```
#' put_third_party_job_success_result(
#'   jobId = "string",
#'   clientToken = "string",
#'   currentRevision = list(
#'     revision = "string",
#'     changeIdentifier = "string",
#'     created = as.POSIXct("2015-01-01"),
#'     revisionSummary = "string"
#'   ),
#'   continuationToken = "string",
#'   executionDetails = list(
#'     summary = "string",
#'     externalExecutionId = "string",
#'     percentComplete = 123
#'   )
#' )
#' ```
#'
#' @param jobId &#91;required&#93; The ID of the job that successfully completed. This is the same ID returned from PollForThirdPartyJobs.
#' @param clientToken &#91;required&#93; The clientToken portion of the clientId and clientToken pair used to verify that the calling entity is allowed access to the job and its details.
#' @param currentRevision Represents information about a current revision.
#' @param continuationToken A token generated by a job worker, such as an AWS CodeDeploy deployment ID, that a successful job provides to identify a partner action in progress. Future jobs will use this token in order to identify the running instance of the action. It can be reused to return additional information about the progress of the partner action. When the action is complete, no continuation token should be supplied.
#' @param executionDetails The details of the actions taken and results produced on an artifact as it passes through stages in the pipeline.
#'
#' @export
put_third_party_job_success_result <- function (jobId, clientToken, 
    currentRevision = NULL, continuationToken = NULL, executionDetails = NULL) 
{
    op <- new_operation(name = "PutThirdPartyJobSuccessResult", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- put_third_party_job_success_result_input(jobId = jobId, 
        clientToken = clientToken, currentRevision = currentRevision, 
        continuationToken = continuationToken, executionDetails = executionDetails)
    output <- put_third_party_job_success_result_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Defines a webhook and returns a unique webhook URL generated by CodePipeline
#'
#' Defines a webhook and returns a unique webhook URL generated by CodePipeline. This URL can be supplied to third party source hosting providers to call every time there\'s a code change. When CodePipeline receives a POST request on this URL, the pipeline defined in the webhook is started as long as the POST request satisfied the authentication and filtering requirements supplied when defining the webhook. RegisterWebhookWithThirdParty and DeregisterWebhookWithThirdParty APIs can be used to automatically configure supported third parties to call the generated webhook URL.
#'
#' @section Accepted Parameters:
#' ```
#' put_webhook(
#'   webhook = list(
#'     name = "string",
#'     targetPipeline = "string",
#'     targetAction = "string",
#'     filters = list(
#'       list(
#'         jsonPath = "string",
#'         matchEquals = "string"
#'       )
#'     ),
#'     authentication = "GITHUB_HMAC"|"IP"|"UNAUTHENTICATED",
#'     authenticationConfiguration = list(
#'       AllowedIPRange = "string",
#'       SecretToken = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @param webhook &#91;required&#93; The detail provided in an input file to create the webhook, such as the webhook name, the pipeline name, and the action name. Give the webhook a unique name which identifies the webhook being defined. You may choose to name the webhook after the pipeline and action it targets so that you can easily recognize what it\'s used for later.
#'
#' @export
put_webhook <- function (webhook) 
{
    op <- new_operation(name = "PutWebhook", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- put_webhook_input(webhook = webhook)
    output <- put_webhook_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Configures a connection between the webhook that was created and the external tool with events to be detected
#'
#' Configures a connection between the webhook that was created and the external tool with events to be detected.
#'
#' @section Accepted Parameters:
#' ```
#' register_webhook_with_third_party(
#'   webhookName = "string"
#' )
#' ```
#'
#' @param webhookName The name of an existing webhook created with PutWebhook to register with a supported third party.
#'
#' @export
register_webhook_with_third_party <- function (webhookName = NULL) 
{
    op <- new_operation(name = "RegisterWebhookWithThirdParty", 
        http_method = "POST", http_path = "/", paginator = list())
    input <- register_webhook_with_third_party_input(webhookName = webhookName)
    output <- register_webhook_with_third_party_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Resumes the pipeline execution by retrying the last failed actions in a stage
#'
#' Resumes the pipeline execution by retrying the last failed actions in a stage.
#'
#' @section Accepted Parameters:
#' ```
#' retry_stage_execution(
#'   pipelineName = "string",
#'   stageName = "string",
#'   pipelineExecutionId = "string",
#'   retryMode = "FAILED_ACTIONS"
#' )
#' ```
#'
#' @param pipelineName &#91;required&#93; The name of the pipeline that contains the failed stage.
#' @param stageName &#91;required&#93; The name of the failed stage to be retried.
#' @param pipelineExecutionId &#91;required&#93; The ID of the pipeline execution in the failed stage to be retried. Use the GetPipelineState action to retrieve the current pipelineExecutionId of the failed stage
#' @param retryMode &#91;required&#93; The scope of the retry attempt. Currently, the only supported value is FAILED\_ACTIONS.
#'
#' @export
retry_stage_execution <- function (pipelineName, stageName, pipelineExecutionId, 
    retryMode) 
{
    op <- new_operation(name = "RetryStageExecution", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- retry_stage_execution_input(pipelineName = pipelineName, 
        stageName = stageName, pipelineExecutionId = pipelineExecutionId, 
        retryMode = retryMode)
    output <- retry_stage_execution_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Starts the specified pipeline
#'
#' Starts the specified pipeline. Specifically, it begins processing the latest commit to the source location specified as part of the pipeline.
#'
#' @section Accepted Parameters:
#' ```
#' start_pipeline_execution(
#'   name = "string",
#'   clientRequestToken = "string"
#' )
#' ```
#'
#' @param name &#91;required&#93; The name of the pipeline to start.
#' @param clientRequestToken The system-generated unique ID used to identify a unique execution request.
#'
#' @export
start_pipeline_execution <- function (name, clientRequestToken = NULL) 
{
    op <- new_operation(name = "StartPipelineExecution", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- start_pipeline_execution_input(name = name, clientRequestToken = clientRequestToken)
    output <- start_pipeline_execution_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}

#' Updates a specified pipeline with edits or changes to its structure
#'
#' Updates a specified pipeline with edits or changes to its structure. Use a JSON file with the pipeline structure in conjunction with UpdatePipeline to provide the full structure of the pipeline. Updating the pipeline increases the version number of the pipeline by 1.
#'
#' @section Accepted Parameters:
#' ```
#' update_pipeline(
#'   pipeline = list(
#'     name = "string",
#'     roleArn = "string",
#'     artifactStore = list(
#'       type = "S3",
#'       location = "string",
#'       encryptionKey = list(
#'         id = "string",
#'         type = "KMS"
#'       )
#'     ),
#'     artifactStores = list(
#'       list(
#'         type = "S3",
#'         location = "string",
#'         encryptionKey = list(
#'           id = "string",
#'           type = "KMS"
#'         )
#'       )
#'     ),
#'     stages = list(
#'       list(
#'         name = "string",
#'         blockers = list(
#'           list(
#'             name = "string",
#'             type = "Schedule"
#'           )
#'         ),
#'         actions = list(
#'           list(
#'             name = "string",
#'             actionTypeId = list(
#'               category = "Source"|"Build"|"Deploy"|"Test"|"Invoke"|"Approval",
#'               owner = "AWS"|"ThirdParty"|"Custom",
#'               provider = "string",
#'               version = "string"
#'             ),
#'             runOrder = 123,
#'             configuration = list(
#'               "string"
#'             ),
#'             outputArtifacts = list(
#'               list(
#'                 name = "string"
#'               )
#'             ),
#'             inputArtifacts = list(
#'               list(
#'                 name = "string"
#'               )
#'             ),
#'             roleArn = "string",
#'             region = "string"
#'           )
#'         )
#'       )
#'     ),
#'     version = 123
#'   )
#' )
#' ```
#'
#' @param pipeline &#91;required&#93; The name of the pipeline to be updated.
#'
#' @export
update_pipeline <- function (pipeline) 
{
    op <- new_operation(name = "UpdatePipeline", http_method = "POST", 
        http_path = "/", paginator = list())
    input <- update_pipeline_input(pipeline = pipeline)
    output <- update_pipeline_output()
    svc <- service()
    request <- new_request(svc, op, input, output)
    response <- send_request(request)
    return(response)
}
