# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common new_handlers new_service
NULL

#' AWS MediaTailor
#'
#' @description
#' Use the AWS Elemental MediaTailor SDK to configure scalable ad insertion
#' for your live and VOD content. With AWS Elemental MediaTailor, you can
#' serve targeted ads to viewers while maintaining broadcast quality in
#' over-the-top (OTT) video applications. For information about using the
#' service, including detailed information about the settings covered in
#' this guide, see the AWS Elemental MediaTailor User Guide.
#' 
#' Through the SDK, you manage AWS Elemental MediaTailor configurations the
#' same as you do through the console. For example, you specify ad
#' insertion behavior and mapping information for the origin server and the
#' ad decision server (ADS).
#'
#' @section Example:
#' ```
#' mediatailor <- paws::mediatailor()
#' mediatailor$operation()
#' ```
#'
#' @section Operations:
#' \tabular{ll}{
#'  \link[=mediatailor_delete_playback_configuration]{delete_playback_configuration} \tab Deletes the configuration for the specified name \cr
#'  \link[=mediatailor_get_playback_configuration]{get_playback_configuration} \tab Returns the configuration for the specified name \cr
#'  \link[=mediatailor_list_playback_configurations]{list_playback_configurations} \tab Returns a list of the configurations defined in AWS Elemental MediaTailor\cr
#'  \link[=mediatailor_put_playback_configuration]{put_playback_configuration} \tab Adds a new configuration to AWS Elemental MediaTailor 
#' }
#'
#' @rdname mediatailor
#' @export
mediatailor <- function() {
  .mediatailor$operations
}

# Private API objects: metadata, handlers, interfaces, etc.
.mediatailor <- list()

.mediatailor$operations <- list()

.mediatailor$metadata <- list(
  service_name = "api.mediatailor",
  endpoints = list("*" = "api.mediatailor.{region}.amazonaws.com", "cn-*" = "api.mediatailor.{region}.amazonaws.com.cn"),
  service_id = "MediaTailor",
  api_version = "2018-04-23",
  signing_name = "mediatailor",
  json_version = "1.1",
  target_prefix = ""
)

.mediatailor$handlers <- new_handlers("restjson", "v4")

.mediatailor$service <- function() {
  new_service(.mediatailor$metadata, .mediatailor$handlers)
}
