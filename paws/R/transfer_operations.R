# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common new_operation new_request send_request
#' @include transfer_service.R
NULL

#' Instantiates an autoscaling virtual server based on Secure File Transfer
#' Protocol (SFTP) in AWS
#'
#' Instantiates an autoscaling virtual server based on Secure File Transfer
#' Protocol (SFTP) in AWS. The call returns the `ServerId` property
#' assigned by the service to the newly created server. Reference this
#' `ServerId` property when you make updates to your server, or work with
#' users.
#' 
#' The response returns the `ServerId` value for the newly created server.
#'
#' @usage
#' transfer_create_server(EndpointDetails, EndpointType,
#'   IdentityProviderDetails, IdentityProviderType, LoggingRole, Tags)
#'
#' @param EndpointDetails 
#' @param EndpointType 
#' @param IdentityProviderDetails An array containing all of the information required to call a
#' customer-supplied authentication API. This parameter is not required
#' when the `IdentityProviderType` value of server that is created uses the
#' `SERVICE_MANAGED` authentication method.
#' @param IdentityProviderType The mode of authentication enabled for this service. The default value
#' is `SERVICE_MANAGED`, which allows you to store and access SFTP user
#' credentials within the service. An `IdentityProviderType` value of
#' `API_GATEWAY` indicates that user authentication requires a call to an
#' API Gateway endpoint URL provided by you to integrate an identity
#' provider of your choice.
#' @param LoggingRole A value that allows the service to write your SFTP users\' activity to
#' your Amazon CloudWatch logs for monitoring and auditing purposes.
#' @param Tags Key-value pairs that can be used to group and search for servers.
#'
#' @section Request syntax:
#' ```
#' transfer$create_server(
#'   EndpointDetails = list(
#'     VpcEndpointId = "string"
#'   ),
#'   EndpointType = "PUBLIC"|"VPC_ENDPOINT",
#'   IdentityProviderDetails = list(
#'     Url = "string",
#'     InvocationRole = "string"
#'   ),
#'   IdentityProviderType = "SERVICE_MANAGED"|"API_GATEWAY",
#'   LoggingRole = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_create_server
transfer_create_server <- function(EndpointDetails = NULL, EndpointType = NULL, IdentityProviderDetails = NULL, IdentityProviderType = NULL, LoggingRole = NULL, Tags = NULL) {
  op <- new_operation(
    name = "CreateServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$create_server_input(EndpointDetails = EndpointDetails, EndpointType = EndpointType, IdentityProviderDetails = IdentityProviderDetails, IdentityProviderType = IdentityProviderType, LoggingRole = LoggingRole, Tags = Tags)
  output <- .transfer$create_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$create_server <- transfer_create_server

#' Adds a user and associate them with an existing Secure File Transfer
#' Protocol (SFTP) server
#'
#' Adds a user and associate them with an existing Secure File Transfer
#' Protocol (SFTP) server. Using parameters for `CreateUser`, you can
#' specify the user name, set the home directory, store the user\'s public
#' key, and assign the user\'s AWS Identity and Access Management (IAM)
#' role. You can also optionally add a scope-down policy, and assign
#' metadata with tags that can be used to group and search for users.
#' 
#' The response returns the `UserName` and `ServerId` values of the new
#' user for that server.
#'
#' @usage
#' transfer_create_user(HomeDirectory, Policy, Role, ServerId,
#'   SshPublicKeyBody, Tags, UserName)
#'
#' @param HomeDirectory The landing directory (folder) for a user when they log in to the server
#' using their SFTP client. An example is `/home/<i>username</i> `.
#' @param Policy A scope-down policy for your user so you can use the same IAM role
#' across multiple users. This policy scopes down user access to portions
#' of their Amazon S3 bucket. Variables you can use inside this policy
#' include `$\{Transfer:UserName\}`, `$\{Transfer:HomeDirectory\}`, and
#' `$\{Transfer:HomeBucket\}`.
#' @param Role &#91;required&#93; The IAM role that controls your user\'s access to your Amazon S3 bucket.
#' The policies attached to this role will determine the level of access
#' you want to provide your users when transferring files into and out of
#' your Amazon S3 bucket or buckets. The IAM role should also contain a
#' trust relationship that allows the SFTP server to access your resources
#' when servicing your SFTP user\'s transfer requests.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance. This is
#' the specific SFTP server that you added your user to.
#' @param SshPublicKeyBody The public portion of the Secure Shall (SSH) key used to authenticate
#' the user to the SFTP server.
#' @param Tags Key-value pairs that can be used to group and search for users. Tags are
#' metadata attached to users for any purpose.
#' @param UserName &#91;required&#93; A unique string that identifies a user and is associated with a server
#' as specified by the `ServerId`.
#'
#' @section Request syntax:
#' ```
#' transfer$create_user(
#'   HomeDirectory = "string",
#'   Policy = "string",
#'   Role = "string",
#'   ServerId = "string",
#'   SshPublicKeyBody = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   ),
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_create_user
transfer_create_user <- function(HomeDirectory = NULL, Policy = NULL, Role, ServerId, SshPublicKeyBody = NULL, Tags = NULL, UserName) {
  op <- new_operation(
    name = "CreateUser",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$create_user_input(HomeDirectory = HomeDirectory, Policy = Policy, Role = Role, ServerId = ServerId, SshPublicKeyBody = SshPublicKeyBody, Tags = Tags, UserName = UserName)
  output <- .transfer$create_user_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$create_user <- transfer_create_user

#' Deletes the Secure File Transfer Protocol (SFTP) server that you specify
#'
#' Deletes the Secure File Transfer Protocol (SFTP) server that you
#' specify. If you used `SERVICE_MANAGED` as your `IdentityProviderType`,
#' you need to delete all users associated with this server before deleting
#' the server itself
#' 
#' No response returns from this call.
#'
#' @usage
#' transfer_delete_server(ServerId)
#'
#' @param ServerId &#91;required&#93; A unique system-assigned identifier for an SFTP server instance.
#'
#' @section Request syntax:
#' ```
#' transfer$delete_server(
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_delete_server
transfer_delete_server <- function(ServerId) {
  op <- new_operation(
    name = "DeleteServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$delete_server_input(ServerId = ServerId)
  output <- .transfer$delete_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$delete_server <- transfer_delete_server

#' Deletes a user's Secure Shell (SSH) public key
#'
#' Deletes a user\'s Secure Shell (SSH) public key.
#' 
#' No response is returned from this call.
#'
#' @usage
#' transfer_delete_ssh_public_key(ServerId, SshPublicKeyId, UserName)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for a Secure File Transfer Protocol
#' (SFTP) server instance that has the user assigned to it.
#' @param SshPublicKeyId &#91;required&#93; A unique identifier used to reference your user\'s specific SSH key.
#' @param UserName &#91;required&#93; A unique string that identifies a user whose public key is being
#' deleted.
#'
#' @section Request syntax:
#' ```
#' transfer$delete_ssh_public_key(
#'   ServerId = "string",
#'   SshPublicKeyId = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_delete_ssh_public_key
transfer_delete_ssh_public_key <- function(ServerId, SshPublicKeyId, UserName) {
  op <- new_operation(
    name = "DeleteSshPublicKey",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$delete_ssh_public_key_input(ServerId = ServerId, SshPublicKeyId = SshPublicKeyId, UserName = UserName)
  output <- .transfer$delete_ssh_public_key_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$delete_ssh_public_key <- transfer_delete_ssh_public_key

#' Deletes the user belonging to the server you specify
#'
#' Deletes the user belonging to the server you specify.
#' 
#' No response returns from this call.
#' 
#' When you delete a user from a server, the user\'s information is lost.
#'
#' @usage
#' transfer_delete_user(ServerId, UserName)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that has
#' the user assigned to it.
#' @param UserName &#91;required&#93; A unique string that identifies a user that is being deleted from the
#' server.
#'
#' @section Request syntax:
#' ```
#' transfer$delete_user(
#'   ServerId = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_delete_user
transfer_delete_user <- function(ServerId, UserName) {
  op <- new_operation(
    name = "DeleteUser",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$delete_user_input(ServerId = ServerId, UserName = UserName)
  output <- .transfer$delete_user_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$delete_user <- transfer_delete_user

#' Describes the server that you specify by passing the ServerId parameter
#'
#' Describes the server that you specify by passing the `ServerId`
#' parameter.
#' 
#' The response contains a description of the server\'s properties.
#'
#' @usage
#' transfer_describe_server(ServerId)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server.
#'
#' @section Request syntax:
#' ```
#' transfer$describe_server(
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_describe_server
transfer_describe_server <- function(ServerId) {
  op <- new_operation(
    name = "DescribeServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$describe_server_input(ServerId = ServerId)
  output <- .transfer$describe_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$describe_server <- transfer_describe_server

#' Describes the user assigned to a specific server, as identified by its
#' ServerId property
#'
#' Describes the user assigned to a specific server, as identified by its
#' `ServerId` property.
#' 
#' The response from this call returns the properties of the user
#' associated with the `ServerId` value that was specified.
#'
#' @usage
#' transfer_describe_user(ServerId, UserName)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that has this
#' user assigned.
#' @param UserName &#91;required&#93; The name of the user assigned to one or more servers. User names are
#' part of the sign-in credentials to use the AWS Transfer service and
#' perform file transfer tasks.
#'
#' @section Request syntax:
#' ```
#' transfer$describe_user(
#'   ServerId = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_describe_user
transfer_describe_user <- function(ServerId, UserName) {
  op <- new_operation(
    name = "DescribeUser",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$describe_user_input(ServerId = ServerId, UserName = UserName)
  output <- .transfer$describe_user_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$describe_user <- transfer_describe_user

#' Adds a Secure Shell (SSH) public key to a user account identified by a
#' UserName value assigned to a specific server, identified by ServerId
#'
#' Adds a Secure Shell (SSH) public key to a user account identified by a
#' `UserName` value assigned to a specific server, identified by
#' `ServerId`.
#' 
#' The response returns the `UserName` value, the `ServerId` value, and the
#' name of the `SshPublicKeyId`.
#'
#' @usage
#' transfer_import_ssh_public_key(ServerId, SshPublicKeyBody, UserName)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server.
#' @param SshPublicKeyBody &#91;required&#93; The public key portion of an SSH key pair.
#' @param UserName &#91;required&#93; The name of the user account that is assigned to one or more servers.
#'
#' @section Request syntax:
#' ```
#' transfer$import_ssh_public_key(
#'   ServerId = "string",
#'   SshPublicKeyBody = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_import_ssh_public_key
transfer_import_ssh_public_key <- function(ServerId, SshPublicKeyBody, UserName) {
  op <- new_operation(
    name = "ImportSshPublicKey",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$import_ssh_public_key_input(ServerId = ServerId, SshPublicKeyBody = SshPublicKeyBody, UserName = UserName)
  output <- .transfer$import_ssh_public_key_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$import_ssh_public_key <- transfer_import_ssh_public_key

#' Lists the Secure File Transfer Protocol (SFTP) servers that are
#' associated with your AWS account
#'
#' Lists the Secure File Transfer Protocol (SFTP) servers that are
#' associated with your AWS account.
#'
#' @usage
#' transfer_list_servers(MaxResults, NextToken)
#'
#' @param MaxResults Specifies the number of servers to return as a response to the
#' `ListServers` query.
#' @param NextToken When additional results are obtained from the ListServers command, a
#' `NextToken` parameter is returned in the output. You can then pass the
#' `NextToken` parameter in a subsequent command to continue listing
#' additional servers.
#'
#' @section Request syntax:
#' ```
#' transfer$list_servers(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_list_servers
transfer_list_servers <- function(MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListServers",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$list_servers_input(MaxResults = MaxResults, NextToken = NextToken)
  output <- .transfer$list_servers_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$list_servers <- transfer_list_servers

#' Lists all of the tags associated with the Amazon Resource Number (ARN)
#' you specify
#'
#' Lists all of the tags associated with the Amazon Resource Number (ARN)
#' you specify. The resource can be a user, server, or role.
#'
#' @usage
#' transfer_list_tags_for_resource(Arn, MaxResults, NextToken)
#'
#' @param Arn &#91;required&#93; Requests the tags associated with a particular Amazon Resource Name
#' (ARN). An ARN is an identifier for a specific AWS resource, such as a
#' server, user, or role.
#' @param MaxResults Specifies the number of tags to return as a response to the
#' `ListTagsForResource` request.
#' @param NextToken When you request additional results from the `ListTagsForResource` call,
#' a `NextToken` parameter is returned in the input. You can then pass in a
#' subsequent command the `NextToken` parameter to continue listing
#' additional tags.
#'
#' @section Request syntax:
#' ```
#' transfer$list_tags_for_resource(
#'   Arn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_list_tags_for_resource
transfer_list_tags_for_resource <- function(Arn, MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListTagsForResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$list_tags_for_resource_input(Arn = Arn, MaxResults = MaxResults, NextToken = NextToken)
  output <- .transfer$list_tags_for_resource_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$list_tags_for_resource <- transfer_list_tags_for_resource

#' Lists the users for the server that you specify by passing the ServerId
#' parameter
#'
#' Lists the users for the server that you specify by passing the
#' `ServerId` parameter.
#'
#' @usage
#' transfer_list_users(MaxResults, NextToken, ServerId)
#'
#' @param MaxResults Specifies the number of users to return as a response to the `ListUsers`
#' request.
#' @param NextToken When you can get additional results from the `ListUsers` call, a
#' `NextToken` parameter is returned in the output. You can then pass in a
#' subsequent command the `NextToken` parameter to continue listing
#' additional users.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for a Secure File Transfer Protocol
#' (SFTP) server that has users are assigned to it.
#'
#' @section Request syntax:
#' ```
#' transfer$list_users(
#'   MaxResults = 123,
#'   NextToken = "string",
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_list_users
transfer_list_users <- function(MaxResults = NULL, NextToken = NULL, ServerId) {
  op <- new_operation(
    name = "ListUsers",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$list_users_input(MaxResults = MaxResults, NextToken = NextToken, ServerId = ServerId)
  output <- .transfer$list_users_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$list_users <- transfer_list_users

#' Changes the state of a Secure File Transfer Protocol (SFTP) server from
#' OFFLINE to ONLINE
#'
#' Changes the state of a Secure File Transfer Protocol (SFTP) server from
#' `OFFLINE` to `ONLINE`. It has no impact on an SFTP server that is
#' already `ONLINE`. An `ONLINE` server can accept and process file
#' transfer jobs.
#' 
#' The state of `STARTING` indicates that the server is in an intermediate
#' state, either not fully able to respond, or not fully online. The values
#' of `START_FAILED` can indicate an error condition.
#' 
#' No response is returned from this call.
#'
#' @usage
#' transfer_start_server(ServerId)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that you start.
#'
#' @section Request syntax:
#' ```
#' transfer$start_server(
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_start_server
transfer_start_server <- function(ServerId) {
  op <- new_operation(
    name = "StartServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$start_server_input(ServerId = ServerId)
  output <- .transfer$start_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$start_server <- transfer_start_server

#' Changes the state of an SFTP server from ONLINE to OFFLINE
#'
#' Changes the state of an SFTP server from `ONLINE` to `OFFLINE`. An
#' `OFFLINE` server cannot accept and process file transfer jobs.
#' Information tied to your server such as server and user properties are
#' not affected by stopping your server. Stopping a server will not reduce
#' or impact your Secure File Transfer Protocol (SFTP) endpoint billing.
#' 
#' The states of `STOPPING` indicates that the server is in an intermediate
#' state, either not fully able to respond, or not fully offline. The
#' values of `STOP_FAILED` can indicate an error condition.
#' 
#' No response is returned from this call.
#'
#' @usage
#' transfer_stop_server(ServerId)
#'
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server that you stopped.
#'
#' @section Request syntax:
#' ```
#' transfer$stop_server(
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_stop_server
transfer_stop_server <- function(ServerId) {
  op <- new_operation(
    name = "StopServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$stop_server_input(ServerId = ServerId)
  output <- .transfer$stop_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$stop_server <- transfer_stop_server

#' Attaches a key-value pair to a resource, as identified by its Amazon
#' Resource Name (ARN)
#'
#' Attaches a key-value pair to a resource, as identified by its Amazon
#' Resource Name (ARN). Resources are users, servers, roles, and other
#' entities.
#' 
#' There is no response returned from this call.
#'
#' @usage
#' transfer_tag_resource(Arn, Tags)
#'
#' @param Arn &#91;required&#93; An Amazon Resource Name (ARN) for a specific AWS resource, such as a
#' server, user, or role.
#' @param Tags &#91;required&#93; Key-value pairs assigned to ARNs that you can use to group and search
#' for resources by type. You can attach this metadata to user accounts for
#' any purpose.
#'
#' @section Request syntax:
#' ```
#' transfer$tag_resource(
#'   Arn = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_tag_resource
transfer_tag_resource <- function(Arn, Tags) {
  op <- new_operation(
    name = "TagResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$tag_resource_input(Arn = Arn, Tags = Tags)
  output <- .transfer$tag_resource_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$tag_resource <- transfer_tag_resource

#' If the IdentityProviderType of the server is API_Gateway, tests whether
#' your API Gateway is set up successfully
#'
#' If the `IdentityProviderType` of the server is `API_Gateway`, tests
#' whether your API Gateway is set up successfully. We highly recommend
#' that you call this method to test your authentication method as soon as
#' you create your server. By doing so, you can troubleshoot issues with
#' the API Gateway integration to ensure that your users can successfully
#' use the service.
#'
#' @usage
#' transfer_test_identity_provider(ServerId, UserName, UserPassword)
#'
#' @param ServerId &#91;required&#93; A system assigned identifier for a specific server. That server\'s user
#' authentication method is tested with a user name and password.
#' @param UserName &#91;required&#93; This request parameter is name of the user account to be tested.
#' @param UserPassword The password of the user account to be tested.
#'
#' @section Request syntax:
#' ```
#' transfer$test_identity_provider(
#'   ServerId = "string",
#'   UserName = "string",
#'   UserPassword = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_test_identity_provider
transfer_test_identity_provider <- function(ServerId, UserName, UserPassword = NULL) {
  op <- new_operation(
    name = "TestIdentityProvider",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$test_identity_provider_input(ServerId = ServerId, UserName = UserName, UserPassword = UserPassword)
  output <- .transfer$test_identity_provider_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$test_identity_provider <- transfer_test_identity_provider

#' Detaches a key-value pair from a resource, as identified by its Amazon
#' Resource Name (ARN)
#'
#' Detaches a key-value pair from a resource, as identified by its Amazon
#' Resource Name (ARN). Resources are users, servers, roles, and other
#' entities.
#' 
#' No response is returned from this call.
#'
#' @usage
#' transfer_untag_resource(Arn, TagKeys)
#'
#' @param Arn &#91;required&#93; This is the value of the resource that will have the tag removed. An
#' Amazon Resource Name (ARN) is an identifier for a specific AWS resource,
#' such as a server, user, or role.
#' @param TagKeys &#91;required&#93; TagKeys are key-value pairs assigned to ARNs that can be used to group
#' and search for resources by type. This metadata can be attached to
#' resources for any purpose.
#'
#' @section Request syntax:
#' ```
#' transfer$untag_resource(
#'   Arn = "string",
#'   TagKeys = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_untag_resource
transfer_untag_resource <- function(Arn, TagKeys) {
  op <- new_operation(
    name = "UntagResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$untag_resource_input(Arn = Arn, TagKeys = TagKeys)
  output <- .transfer$untag_resource_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$untag_resource <- transfer_untag_resource

#' Updates the server properties after that server has been created
#'
#' Updates the server properties after that server has been created.
#' 
#' The `UpdateServer` call returns the `ServerId` of the Secure File
#' Transfer Protocol (SFTP) server you updated.
#'
#' @usage
#' transfer_update_server(EndpointDetails, EndpointType,
#'   IdentityProviderDetails, LoggingRole, ServerId)
#'
#' @param EndpointDetails 
#' @param EndpointType 
#' @param IdentityProviderDetails This response parameter is an array containing all of the information
#' required to call a customer\'s authentication API method.
#' @param LoggingRole Changes the AWS Identity and Access Management (IAM) role that allows
#' Amazon S3 events to be logged in Amazon CloudWatch, turning logging on
#' or off.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that the
#' user account is assigned to.
#'
#' @section Request syntax:
#' ```
#' transfer$update_server(
#'   EndpointDetails = list(
#'     VpcEndpointId = "string"
#'   ),
#'   EndpointType = "PUBLIC"|"VPC_ENDPOINT",
#'   IdentityProviderDetails = list(
#'     Url = "string",
#'     InvocationRole = "string"
#'   ),
#'   LoggingRole = "string",
#'   ServerId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_update_server
transfer_update_server <- function(EndpointDetails = NULL, EndpointType = NULL, IdentityProviderDetails = NULL, LoggingRole = NULL, ServerId) {
  op <- new_operation(
    name = "UpdateServer",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$update_server_input(EndpointDetails = EndpointDetails, EndpointType = EndpointType, IdentityProviderDetails = IdentityProviderDetails, LoggingRole = LoggingRole, ServerId = ServerId)
  output <- .transfer$update_server_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$update_server <- transfer_update_server

#' Assigns new properties to a user
#'
#' Assigns new properties to a user. Parameters you pass modify any or all
#' of the following: the home directory, role, and policy for the
#' `UserName` and `ServerId` you specify.
#' 
#' The response returns the `ServerId` and the `UserName` for the updated
#' user.
#'
#' @usage
#' transfer_update_user(HomeDirectory, Policy, Role, ServerId, UserName)
#'
#' @param HomeDirectory The HomeDirectory parameter specifies the landing directory (folder) for
#' a user when they log in to the server using their client. An example
#' would be: `/home/<i>username</i> `.
#' @param Policy Allows you to supply a scope-down policy for your user so you can use
#' the same AWS Identity and Access Management (IAM) role across multiple
#' users. The policy scopes down users access to portions of your Amazon S3
#' bucket. Variables you can use inside this policy include
#' `$\{Transfer:UserName\}`, `$\{Transfer:HomeDirectory\}`, and
#' `$\{Transfer:HomeBucket\}`.
#' @param Role The IAM role that controls your user\'s access to your Amazon S3 bucket.
#' The policies attached to this role will determine the level of access
#' you want to provide your users when transferring files into and out of
#' your Amazon S3 bucket or buckets. The IAM role should also contain a
#' trust relationship that allows the Secure File Transfer Protocol (SFTP)
#' server to access your resources when servicing your SFTP user\'s
#' transfer requests.
#' @param ServerId &#91;required&#93; A system-assigned unique identifier for an SFTP server instance that the
#' user account is assigned to.
#' @param UserName &#91;required&#93; A unique string that identifies a user and is associated with a server
#' as specified by the ServerId. This is the string that will be used by
#' your user when they log in to your SFTP server.
#'
#' @section Request syntax:
#' ```
#' transfer$update_user(
#'   HomeDirectory = "string",
#'   Policy = "string",
#'   Role = "string",
#'   ServerId = "string",
#'   UserName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname transfer_update_user
transfer_update_user <- function(HomeDirectory = NULL, Policy = NULL, Role = NULL, ServerId, UserName) {
  op <- new_operation(
    name = "UpdateUser",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .transfer$update_user_input(HomeDirectory = HomeDirectory, Policy = Policy, Role = Role, ServerId = ServerId, UserName = UserName)
  output <- .transfer$update_user_output()
  svc <- .transfer$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.transfer$operations$update_user <- transfer_update_user
