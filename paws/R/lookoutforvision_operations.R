# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common get_config new_operation new_request send_request
#' @include lookoutforvision_service.R
NULL

#' Creates a new dataset in an Amazon Lookout for Vision project
#'
#' @description
#' Creates a new dataset in an Amazon Lookout for Vision project.
#' `CreateDataset` can create a training or a test dataset from a valid
#' dataset source (`DatasetSource`).
#' 
#' If you want a single dataset project, specify `train` for the value of
#' `DatasetType`.
#' 
#' To have a project with separate training and test datasets, call
#' `CreateDataset` twice. On the first call, specify `train` for the value
#' of `DatasetType`. On the second call, specify `test` for the value of
#' `DatasetType`. of dataset with
#'
#' @usage
#' lookoutforvision_create_dataset(ProjectName, DatasetType, DatasetSource,
#'   ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project in which you want to create a dataset.
#' @param DatasetType &#91;required&#93; The type of the dataset. Specify `train` for a training dataset. Specify
#' `test` for a test dataset.
#' @param DatasetSource The location of the manifest file that Amazon Lookout for Vision uses to
#' create the dataset.
#' 
#' If you don't specify `DatasetSource`, an empty dataset is created and
#' the operation synchronously returns. Later, you can add JSON Lines by
#' calling UpdateDatasetEntries.
#' 
#' If you specify a value for `DataSource`, the manifest at the S3 location
#' is validated and used to create the dataset. The call to `CreateDataset`
#' is asynchronous and might take a while to complete. To find out the
#' current status, Check the value of `Status` returned in a call to
#' DescribeDataset.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to
#' `CreateDataset` completes only once. You choose the value to pass. For
#' example, An issue, such as an network outage, might prevent you from
#' getting a response from `CreateDataset`. In this case, safely retry your
#' call to `CreateDataset` by using the same `ClientToken` parameter value.
#' An error occurs if the other input parameters are not the same as in the
#' first request. Using a different value for `ClientToken` is considered a
#' new call to `CreateDataset`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$create_dataset(
#'   ProjectName = "string",
#'   DatasetType = "string",
#'   DatasetSource = list(
#'     GroundTruthManifest = list(
#'       S3Object = list(
#'         Bucket = "string",
#'         Key = "string",
#'         VersionId = "string"
#'       )
#'     )
#'   ),
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_create_dataset
lookoutforvision_create_dataset <- function(ProjectName, DatasetType, DatasetSource = NULL, ClientToken = NULL) {
  op <- new_operation(
    name = "CreateDataset",
    http_method = "POST",
    http_path = "/2020-11-20/projects/{projectName}/datasets",
    paginator = list()
  )
  input <- .lookoutforvision$create_dataset_input(ProjectName = ProjectName, DatasetType = DatasetType, DatasetSource = DatasetSource, ClientToken = ClientToken)
  output <- .lookoutforvision$create_dataset_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$create_dataset <- lookoutforvision_create_dataset

#' Creates a new version of a model within an an Amazon Lookout for Vision
#' project
#'
#' @description
#' Creates a new version of a model within an an Amazon Lookout for Vision
#' project. `CreateModel` is an asynchronous operation in which Amazon
#' Lookout for Vision trains, tests, and evaluates a new version of a
#' model.
#' 
#' To get the current status, check the `Status` field returned in the
#' response from DescribeModel.
#' 
#' If the project has a single dataset, Amazon Lookout for Vision
#' internally splits the dataset to create a training and a test dataset.
#' If the project has a training and a test dataset, Lookout for Vision
#' uses the respective datasets to train and test the model.
#' 
#' After training completes, the evaluation metrics are stored at the
#' location specified in `OutputConfig`.
#'
#' @usage
#' lookoutforvision_create_model(ProjectName, Description, ClientToken,
#'   OutputConfig, KmsKeyId)
#'
#' @param ProjectName &#91;required&#93; The name of the project in which you want to create a model version.
#' @param Description A description for the version of the model.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to `CreateModel`
#' completes only once. You choose the value to pass. For example, An
#' issue, such as an network outage, might prevent you from getting a
#' response from `CreateModel`. In this case, safely retry your call to
#' `CreateModel` by using the same `ClientToken` parameter value. An error
#' occurs if the other input parameters are not the same as in the first
#' request. Using a different value for `ClientToken` is considered a new
#' call to `CreateModel`. An idempotency token is active for 8 hours.
#' @param OutputConfig &#91;required&#93; The location where Amazon Lookout for Vision saves the training results.
#' @param KmsKeyId The identifier of the AWS Key Management Service (AWS KMS) customer
#' master key (CMK) to use for encypting the model. If this parameter is
#' not specified, the model is encrypted by a key that AWS owns and
#' manages.
#'
#' @section Request syntax:
#' ```
#' svc$create_model(
#'   ProjectName = "string",
#'   Description = list(
#'     ModelVersion = "string",
#'     ModelArn = "string",
#'     CreationTimestamp = as.POSIXct(
#'       "2015-01-01"
#'     ),
#'     Description = "string",
#'     Status = "TRAINING"|"TRAINED"|"TRAINING_FAILED"|"STARTING_HOSTING"|"HOSTED"|"HOSTING_FAILED"|"STOPPING_HOSTING"|"SYSTEM_UPDATING"|"DELETING",
#'     StatusMessage = "string",
#'     Performance = list(
#'       F1Score = 123.0,
#'       Recall = 123.0,
#'       Precision = 123.0
#'     ),
#'     OutputConfig = list(
#'       S3Location = list(
#'         Bucket = "string",
#'         Prefix = "string"
#'       )
#'     ),
#'     EvaluationManifest = list(
#'       Bucket = "string",
#'       Key = "string"
#'     ),
#'     EvaluationResult = list(
#'       Bucket = "string",
#'       Key = "string"
#'     ),
#'     EvaluationEndTimestamp = as.POSIXct(
#'       "2015-01-01"
#'     ),
#'     KmsKeyId = "string"
#'   ),
#'   ClientToken = "string",
#'   OutputConfig = list(
#'     S3Location = list(
#'       Bucket = "string",
#'       Prefix = "string"
#'     )
#'   ),
#'   KmsKeyId = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_create_model
lookoutforvision_create_model <- function(ProjectName, Description = NULL, ClientToken = NULL, OutputConfig, KmsKeyId = NULL) {
  op <- new_operation(
    name = "CreateModel",
    http_method = "POST",
    http_path = "/2020-11-20/projects/{projectName}/models",
    paginator = list()
  )
  input <- .lookoutforvision$create_model_input(ProjectName = ProjectName, Description = Description, ClientToken = ClientToken, OutputConfig = OutputConfig, KmsKeyId = KmsKeyId)
  output <- .lookoutforvision$create_model_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$create_model <- lookoutforvision_create_model

#' Creates an empty Amazon Lookout for Vision project
#'
#' @description
#' Creates an empty Amazon Lookout for Vision project. After you create the
#' project, add a dataset by calling CreateDataset.
#'
#' @usage
#' lookoutforvision_create_project(ProjectName, ClientToken)
#'
#' @param ProjectName &#91;required&#93; S nsme for the project.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to
#' `CreateProject` completes only once. You choose the value to pass. For
#' example, An issue, such as an network outage, might prevent you from
#' getting a response from `CreateProject`. In this case, safely retry your
#' call to `CreateProject` by using the same `ClientToken` parameter value.
#' An error occurs if the other input parameters are not the same as in the
#' first request. Using a different value for `ClientToken` is considered a
#' new call to `CreateProject`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$create_project(
#'   ProjectName = "string",
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_create_project
lookoutforvision_create_project <- function(ProjectName, ClientToken = NULL) {
  op <- new_operation(
    name = "CreateProject",
    http_method = "POST",
    http_path = "/2020-11-20/projects",
    paginator = list()
  )
  input <- .lookoutforvision$create_project_input(ProjectName = ProjectName, ClientToken = ClientToken)
  output <- .lookoutforvision$create_project_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$create_project <- lookoutforvision_create_project

#' Deletes an existing Amazon Lookout for Vision dataset
#'
#' @description
#' Deletes an existing Amazon Lookout for Vision `dataset`.
#' 
#' If your the project has a single dataset, you must create a new dataset
#' before you can create a model.
#' 
#' If you project has a training dataset and a test dataset consider the
#' following.
#' 
#' -   If you delete the test dataset, your project reverts to a single
#'     dataset project. If you then train the model, Amazon Lookout for
#'     Vision internally splits the remaining dataset into a training and
#'     test dataset.
#' 
#' -   If you delete the training dataset, you must create a training
#'     dataset before you can create a model.
#' 
#' It might take a while to delete the dataset. To check the current
#' status, check the `Status` field in the response from a call to
#' DescribeDataset.
#'
#' @usage
#' lookoutforvision_delete_dataset(ProjectName, DatasetType, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the dataset that you want to
#' delete.
#' @param DatasetType &#91;required&#93; The type of the dataset to delete. Specify `train` to delete the
#' training dataset. Specify `test` to delete the test dataset. To delete
#' the dataset in a single dataset project, specify `train`.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to
#' `DeleteDataset` completes only once. You choose the value to pass. For
#' example, An issue, such as an network outage, might prevent you from
#' getting a response from `DeleteDataset`. In this case, safely retry your
#' call to `DeleteDataset` by using the same `ClientToken` parameter value.
#' An error occurs if the other input parameters are not the same as in the
#' first request. Using a different value for `ClientToken` is considered a
#' new call to `DeleteDataset`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$delete_dataset(
#'   ProjectName = "string",
#'   DatasetType = "string",
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_delete_dataset
lookoutforvision_delete_dataset <- function(ProjectName, DatasetType, ClientToken = NULL) {
  op <- new_operation(
    name = "DeleteDataset",
    http_method = "DELETE",
    http_path = "/2020-11-20/projects/{projectName}/datasets/{datasetType}",
    paginator = list()
  )
  input <- .lookoutforvision$delete_dataset_input(ProjectName = ProjectName, DatasetType = DatasetType, ClientToken = ClientToken)
  output <- .lookoutforvision$delete_dataset_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$delete_dataset <- lookoutforvision_delete_dataset

#' Deletes an Amazon Lookout for Vision model
#'
#' @description
#' Deletes an Amazon Lookout for Vision model. You can't delete a running
#' model. To stop a running model, use the StopModel operation.
#'
#' @usage
#' lookoutforvision_delete_model(ProjectName, ModelVersion, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the model that you want to delete.
#' @param ModelVersion &#91;required&#93; The version of the model that you want to delete.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to `DeleteModel`
#' completes only once. You choose the value to pass. For example, An
#' issue, such as an network outage, might prevent you from getting a
#' response from `DeleteModel`. In this case, safely retry your call to
#' `DeleteModel` by using the same `ClientToken` parameter value. An error
#' occurs if the other input parameters are not the same as in the first
#' request. Using a different value for `ClientToken` is considered a new
#' call to `DeleteModel`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$delete_model(
#'   ProjectName = "string",
#'   ModelVersion = "string",
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_delete_model
lookoutforvision_delete_model <- function(ProjectName, ModelVersion, ClientToken = NULL) {
  op <- new_operation(
    name = "DeleteModel",
    http_method = "DELETE",
    http_path = "/2020-11-20/projects/{projectName}/models/{modelVersion}",
    paginator = list()
  )
  input <- .lookoutforvision$delete_model_input(ProjectName = ProjectName, ModelVersion = ModelVersion, ClientToken = ClientToken)
  output <- .lookoutforvision$delete_model_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$delete_model <- lookoutforvision_delete_model

#' Deletes an Amazon Lookout for Vision project
#'
#' @description
#' Deletes an Amazon Lookout for Vision project.
#' 
#' To delete a project, you must first delete each version of the model
#' associated with the project. To delete a model use the DeleteModel
#' operation.
#' 
#' The training and test datasets are deleted automatically for you. The
#' images referenced by the training and test datasets aren't deleted.
#'
#' @usage
#' lookoutforvision_delete_project(ProjectName, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project to delete.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to
#' `DeleteProject` completes only once. You choose the value to pass. For
#' example, An issue, such as an network outage, might prevent you from
#' getting a response from `DeleteProject`. In this case, safely retry your
#' call to `DeleteProject` by using the same `ClientToken` parameter value.
#' An error occurs if the other input parameters are not the same as in the
#' first request. Using a different value for `ClientToken` is considered a
#' new call to `DeleteProject`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$delete_project(
#'   ProjectName = "string",
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_delete_project
lookoutforvision_delete_project <- function(ProjectName, ClientToken = NULL) {
  op <- new_operation(
    name = "DeleteProject",
    http_method = "DELETE",
    http_path = "/2020-11-20/projects/{projectName}",
    paginator = list()
  )
  input <- .lookoutforvision$delete_project_input(ProjectName = ProjectName, ClientToken = ClientToken)
  output <- .lookoutforvision$delete_project_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$delete_project <- lookoutforvision_delete_project

#' Describe an Amazon Lookout for Vision dataset
#'
#' @description
#' Describe an Amazon Lookout for Vision dataset.
#'
#' @usage
#' lookoutforvision_describe_dataset(ProjectName, DatasetType)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the dataset that you want to
#' describe.
#' @param DatasetType &#91;required&#93; The type of the dataset to describe. Specify `train` to describe the
#' training dataset. Specify `test` to describe the test dataset. If you
#' have a single dataset project, specify `train`
#'
#' @section Request syntax:
#' ```
#' svc$describe_dataset(
#'   ProjectName = "string",
#'   DatasetType = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_describe_dataset
lookoutforvision_describe_dataset <- function(ProjectName, DatasetType) {
  op <- new_operation(
    name = "DescribeDataset",
    http_method = "GET",
    http_path = "/2020-11-20/projects/{projectName}/datasets/{datasetType}",
    paginator = list()
  )
  input <- .lookoutforvision$describe_dataset_input(ProjectName = ProjectName, DatasetType = DatasetType)
  output <- .lookoutforvision$describe_dataset_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$describe_dataset <- lookoutforvision_describe_dataset

#' Describes a version of an Amazon Lookout for Vision model
#'
#' @description
#' Describes a version of an Amazon Lookout for Vision model.
#'
#' @usage
#' lookoutforvision_describe_model(ProjectName, ModelVersion)
#'
#' @param ProjectName &#91;required&#93; The project that contains the version of a model that you want to
#' describe.
#' @param ModelVersion &#91;required&#93; The version of the model that you want to describe.
#'
#' @section Request syntax:
#' ```
#' svc$describe_model(
#'   ProjectName = "string",
#'   ModelVersion = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_describe_model
lookoutforvision_describe_model <- function(ProjectName, ModelVersion) {
  op <- new_operation(
    name = "DescribeModel",
    http_method = "GET",
    http_path = "/2020-11-20/projects/{projectName}/models/{modelVersion}",
    paginator = list()
  )
  input <- .lookoutforvision$describe_model_input(ProjectName = ProjectName, ModelVersion = ModelVersion)
  output <- .lookoutforvision$describe_model_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$describe_model <- lookoutforvision_describe_model

#' Describes an Amazon Lookout for Vision project
#'
#' @description
#' Describes an Amazon Lookout for Vision project.
#'
#' @usage
#' lookoutforvision_describe_project(ProjectName)
#'
#' @param ProjectName &#91;required&#93; The name of the project that you want to describe.
#'
#' @section Request syntax:
#' ```
#' svc$describe_project(
#'   ProjectName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_describe_project
lookoutforvision_describe_project <- function(ProjectName) {
  op <- new_operation(
    name = "DescribeProject",
    http_method = "GET",
    http_path = "/2020-11-20/projects/{projectName}",
    paginator = list()
  )
  input <- .lookoutforvision$describe_project_input(ProjectName = ProjectName)
  output <- .lookoutforvision$describe_project_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$describe_project <- lookoutforvision_describe_project

#' Detects anomalies in an image that you supply
#'
#' @description
#' Detects anomalies in an image that you supply.
#' 
#' The response from `DetectAnomalies` includes a boolean prediction that
#' the image contains one or more anomalies and a confidence value for the
#' prediction.
#' 
#' Before calling `DetectAnomalies`, you must first start your model with
#' the StartModel operation. You are charged for the amount of time, in
#' minutes, that a model runs and for the number of anomaly detection units
#' that your model uses. If you are not using a model, use the StopModel
#' operation to stop your model.
#'
#' @usage
#' lookoutforvision_detect_anomalies(ProjectName, ModelVersion, Body,
#'   ContentType)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the model version that you want to
#' use.
#' @param ModelVersion &#91;required&#93; The version of the model that you want to use.
#' @param Body &#91;required&#93; The unencrypted image bytes that you want to analyze.
#' @param ContentType &#91;required&#93; The type of the image passed in `Body`. Valid values are `image/png`
#' (PNG format images) and `image/jpeg` (JPG format images).
#'
#' @section Request syntax:
#' ```
#' svc$detect_anomalies(
#'   ProjectName = "string",
#'   ModelVersion = "string",
#'   Body = raw,
#'   ContentType = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_detect_anomalies
lookoutforvision_detect_anomalies <- function(ProjectName, ModelVersion, Body, ContentType) {
  op <- new_operation(
    name = "DetectAnomalies",
    http_method = "POST",
    http_path = "/2020-11-20/projects/{projectName}/models/{modelVersion}/detect",
    paginator = list()
  )
  input <- .lookoutforvision$detect_anomalies_input(ProjectName = ProjectName, ModelVersion = ModelVersion, Body = Body, ContentType = ContentType)
  output <- .lookoutforvision$detect_anomalies_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$detect_anomalies <- lookoutforvision_detect_anomalies

#' Lists the JSON Lines within a dataset
#'
#' @description
#' Lists the JSON Lines within a dataset. An Amazon Lookout for Vision JSON
#' Line contains the anomaly information for a single image, including the
#' image location and the assigned label.
#'
#' @usage
#' lookoutforvision_list_dataset_entries(ProjectName, DatasetType, Labeled,
#'   AnomalyClass, BeforeCreationDate, AfterCreationDate, NextToken,
#'   MaxResults, SourceRefContains)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the dataset that you want to list.
#' @param DatasetType &#91;required&#93; The type of the dataset that you want to list. Specify `train` to list
#' the training dataset. Specify `test` to list the test dataset. If you
#' have a single dataset project, specify `train`.
#' @param Labeled Specify `true` to include labeled entries, otherwise specify `false`. If
#' you don't specify a value, Lookout for Vision returns all entries.
#' @param AnomalyClass Specify `normal` to include only normal images. Specify `anomaly` to
#' only include anomalous entries. If you don't specify a value, Amazon
#' Lookout for Vision returns normal and anomalous images.
#' @param BeforeCreationDate Only includes entries before the specified date in the response. For
#' example, `2020-06-23T00:00:00`.
#' @param AfterCreationDate Only includes entries after the specified date in the response. For
#' example, `2020-06-23T00:00:00`.
#' @param NextToken If the previous response was incomplete (because there is more data to
#' retrieve), Amazon Lookout for Vision returns a pagination token in the
#' response. You can use this pagination token to retrieve the next set of
#' dataset entries.
#' @param MaxResults The maximum number of results to return per paginated call. The largest
#' value you can specify is 100. If you specify a value greater than 100, a
#' ValidationException error occurs. The default value is 100.
#' @param SourceRefContains Perform a "contains" search on the values of the `source-ref` key within
#' the dataset. For example a value of "IMG\\_17" returns all JSON Lines
#' where the `source-ref` key value matches **IMG\\_17**.
#'
#' @section Request syntax:
#' ```
#' svc$list_dataset_entries(
#'   ProjectName = "string",
#'   DatasetType = "string",
#'   Labeled = TRUE|FALSE,
#'   AnomalyClass = "string",
#'   BeforeCreationDate = as.POSIXct(
#'     "2015-01-01"
#'   ),
#'   AfterCreationDate = as.POSIXct(
#'     "2015-01-01"
#'   ),
#'   NextToken = "string",
#'   MaxResults = 123,
#'   SourceRefContains = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_list_dataset_entries
lookoutforvision_list_dataset_entries <- function(ProjectName, DatasetType, Labeled = NULL, AnomalyClass = NULL, BeforeCreationDate = NULL, AfterCreationDate = NULL, NextToken = NULL, MaxResults = NULL, SourceRefContains = NULL) {
  op <- new_operation(
    name = "ListDatasetEntries",
    http_method = "GET",
    http_path = "/2020-11-20/projects/{projectName}/datasets/{datasetType}/entries",
    paginator = list()
  )
  input <- .lookoutforvision$list_dataset_entries_input(ProjectName = ProjectName, DatasetType = DatasetType, Labeled = Labeled, AnomalyClass = AnomalyClass, BeforeCreationDate = BeforeCreationDate, AfterCreationDate = AfterCreationDate, NextToken = NextToken, MaxResults = MaxResults, SourceRefContains = SourceRefContains)
  output <- .lookoutforvision$list_dataset_entries_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$list_dataset_entries <- lookoutforvision_list_dataset_entries

#' Lists the versions of a model in an Amazon Lookout for Vision project
#'
#' @description
#' Lists the versions of a model in an Amazon Lookout for Vision project.
#'
#' @usage
#' lookoutforvision_list_models(ProjectName, NextToken, MaxResults)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the model versions that you want
#' to list.
#' @param NextToken If the previous response was incomplete (because there is more data to
#' retrieve), Amazon Lookout for Vision returns a pagination token in the
#' response. You can use this pagination token to retrieve the next set of
#' models.
#' @param MaxResults The maximum number of results to return per paginated call. The largest
#' value you can specify is 100. If you specify a value greater than 100, a
#' ValidationException error occurs. The default value is 100.
#'
#' @section Request syntax:
#' ```
#' svc$list_models(
#'   ProjectName = "string",
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_list_models
lookoutforvision_list_models <- function(ProjectName, NextToken = NULL, MaxResults = NULL) {
  op <- new_operation(
    name = "ListModels",
    http_method = "GET",
    http_path = "/2020-11-20/projects/{projectName}/models",
    paginator = list()
  )
  input <- .lookoutforvision$list_models_input(ProjectName = ProjectName, NextToken = NextToken, MaxResults = MaxResults)
  output <- .lookoutforvision$list_models_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$list_models <- lookoutforvision_list_models

#' Lists the Amazon Lookout for Vision projects in your AWS account
#'
#' @description
#' Lists the Amazon Lookout for Vision projects in your AWS account.
#'
#' @usage
#' lookoutforvision_list_projects(NextToken, MaxResults)
#'
#' @param NextToken If the previous response was incomplete (because there is more data to
#' retrieve), Amazon Lookout for Vision returns a pagination token in the
#' response. You can use this pagination token to retrieve the next set of
#' projects.
#' @param MaxResults The maximum number of results to return per paginated call. The largest
#' value you can specify is 100. If you specify a value greater than 100, a
#' ValidationException error occurs. The default value is 100.
#'
#' @section Request syntax:
#' ```
#' svc$list_projects(
#'   NextToken = "string",
#'   MaxResults = 123
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_list_projects
lookoutforvision_list_projects <- function(NextToken = NULL, MaxResults = NULL) {
  op <- new_operation(
    name = "ListProjects",
    http_method = "GET",
    http_path = "/2020-11-20/projects",
    paginator = list()
  )
  input <- .lookoutforvision$list_projects_input(NextToken = NextToken, MaxResults = MaxResults)
  output <- .lookoutforvision$list_projects_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$list_projects <- lookoutforvision_list_projects

#' Starts the running of the version of an Amazon Lookout for Vision model
#'
#' @description
#' Starts the running of the version of an Amazon Lookout for Vision model.
#' Starting a model takes a while to complete. To check the current state
#' of the model, use DescribeModel.
#' 
#' Once the model is running, you can detect custom labels in new images by
#' calling DetectAnomalies.
#' 
#' You are charged for the amount of time that the model is running. To
#' stop a running model, call StopModel.
#'
#' @usage
#' lookoutforvision_start_model(ProjectName, ModelVersion,
#'   MinInferenceUnits, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the model that you want to start.
#' @param ModelVersion &#91;required&#93; The version of the model that you want to start.
#' @param MinInferenceUnits &#91;required&#93; The minimum number of inference units to use. A single inference unit
#' represents 1 hour of processing and can support up to 5 Transaction Pers
#' Second (TPS). Use a higher number to increase the TPS throughput of your
#' model. You are charged for the number of inference units that you use.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to `StartModel`
#' completes only once. You choose the value to pass. For example, An
#' issue, such as an network outage, might prevent you from getting a
#' response from `StartModel`. In this case, safely retry your call to
#' `StartModel` by using the same `ClientToken` parameter value. An error
#' occurs if the other input parameters are not the same as in the first
#' request. Using a different value for `ClientToken` is considered a new
#' call to `StartModel`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$start_model(
#'   ProjectName = "string",
#'   ModelVersion = "string",
#'   MinInferenceUnits = 123,
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_start_model
lookoutforvision_start_model <- function(ProjectName, ModelVersion, MinInferenceUnits, ClientToken = NULL) {
  op <- new_operation(
    name = "StartModel",
    http_method = "POST",
    http_path = "/2020-11-20/projects/{projectName}/models/{modelVersion}/start",
    paginator = list()
  )
  input <- .lookoutforvision$start_model_input(ProjectName = ProjectName, ModelVersion = ModelVersion, MinInferenceUnits = MinInferenceUnits, ClientToken = ClientToken)
  output <- .lookoutforvision$start_model_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$start_model <- lookoutforvision_start_model

#' Stops a running model
#'
#' @description
#' Stops a running model. The operation might take a while to complete. To
#' check the current status, call DescribeModel.
#'
#' @usage
#' lookoutforvision_stop_model(ProjectName, ModelVersion, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the model that you want to stop.
#' @param ModelVersion &#91;required&#93; The version of the model that you want to stop.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to `StopModel`
#' completes only once. You choose the value to pass. For example, An
#' issue, such as an network outage, might prevent you from getting a
#' response from `StopModel`. In this case, safely retry your call to
#' `StopModel` by using the same `ClientToken` parameter value. An error
#' occurs if the other input parameters are not the same as in the first
#' request. Using a different value for `ClientToken` is considered a new
#' call to `StopModel`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$stop_model(
#'   ProjectName = "string",
#'   ModelVersion = "string",
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_stop_model
lookoutforvision_stop_model <- function(ProjectName, ModelVersion, ClientToken = NULL) {
  op <- new_operation(
    name = "StopModel",
    http_method = "POST",
    http_path = "/2020-11-20/projects/{projectName}/models/{modelVersion}/stop",
    paginator = list()
  )
  input <- .lookoutforvision$stop_model_input(ProjectName = ProjectName, ModelVersion = ModelVersion, ClientToken = ClientToken)
  output <- .lookoutforvision$stop_model_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$stop_model <- lookoutforvision_stop_model

#' Adds one or more JSON Line entries to a dataset
#'
#' @description
#' Adds one or more JSON Line entries to a dataset. A JSON Line includes
#' information about an image used for training or testing an Amazon
#' Lookout for Vision model. The following is an example JSON Line.
#' 
#' Updating a dataset might take a while to complete. To check the current
#' status, call DescribeDataset and check the `Status` field in the
#' response.
#'
#' @usage
#' lookoutforvision_update_dataset_entries(ProjectName, DatasetType,
#'   Changes, ClientToken)
#'
#' @param ProjectName &#91;required&#93; The name of the project that contains the dataset that you want to
#' update.
#' @param DatasetType &#91;required&#93; The type of the dataset that you want to update. Specify `train` to
#' update the training dataset. Specify `test` to update the test dataset.
#' If you have a single dataset project, specify `train`.
#' @param Changes &#91;required&#93; The entries to add to the dataset.
#' @param ClientToken ClientToken is an idempotency token that ensures a call to
#' `UpdateDatasetEntries` completes only once. You choose the value to
#' pass. For example, An issue, such as an network outage, might prevent
#' you from getting a response from `UpdateDatasetEntries`. In this case,
#' safely retry your call to `UpdateDatasetEntries` by using the same
#' `ClientToken` parameter value. An error occurs if the other input
#' parameters are not the same as in the first request. Using a different
#' value for `ClientToken` is considered a new call to
#' `UpdateDatasetEntries`. An idempotency token is active for 8 hours.
#'
#' @section Request syntax:
#' ```
#' svc$update_dataset_entries(
#'   ProjectName = "string",
#'   DatasetType = "string",
#'   Changes = raw,
#'   ClientToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname lookoutforvision_update_dataset_entries
lookoutforvision_update_dataset_entries <- function(ProjectName, DatasetType, Changes, ClientToken = NULL) {
  op <- new_operation(
    name = "UpdateDatasetEntries",
    http_method = "PATCH",
    http_path = "/2020-11-20/projects/{projectName}/datasets/{datasetType}/entries",
    paginator = list()
  )
  input <- .lookoutforvision$update_dataset_entries_input(ProjectName = ProjectName, DatasetType = DatasetType, Changes = Changes, ClientToken = ClientToken)
  output <- .lookoutforvision$update_dataset_entries_output()
  config <- get_config()
  svc <- .lookoutforvision$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.lookoutforvision$operations$update_dataset_entries <- lookoutforvision_update_dataset_entries
