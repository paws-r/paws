# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common new_operation new_request send_request
#' @include datasync_service.R
NULL

#' Cancels execution of a task
#'
#' Cancels execution of a task.
#' 
#' When you cancel a task execution, the transfer of some files are
#' abruptly interrupted. The contents of files that are transferred to the
#' destination might be incomplete or inconsistent with the source files.
#' However, if you start a new task execution on the same task and you
#' allow the task execution to complete, file content on the destination is
#' complete and consistent. This applies to other unexpected failures that
#' interrupt a task execution. In all of these cases, AWS DataSync
#' successfully complete the transfer when you start the next task
#' execution.
#'
#' @usage
#' datasync_cancel_task_execution(TaskExecutionArn)
#'
#' @param TaskExecutionArn &#91;required&#93; The Amazon Resource Name (ARN) of the task execution to cancel.
#'
#' @section Request syntax:
#' ```
#' datasync$cancel_task_execution(
#'   TaskExecutionArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_cancel_task_execution
datasync_cancel_task_execution <- function(TaskExecutionArn) {
  op <- new_operation(
    name = "CancelTaskExecution",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$cancel_task_execution_input(TaskExecutionArn = TaskExecutionArn)
  output <- .datasync$cancel_task_execution_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$cancel_task_execution <- datasync_cancel_task_execution

#' Activates an AWS DataSync agent that you have deployed on your host
#'
#' Activates an AWS DataSync agent that you have deployed on your host. The
#' activation process associates your agent with your account. In the
#' activation process, you specify information such as the AWS Region that
#' you want to activate the agent in. You activate the agent in the AWS
#' Region where your target locations (in Amazon S3 or Amazon EFS) reside.
#' Your tasks are created in this AWS Region.
#' 
#' You can use an agent for more than one location. If a task uses multiple
#' agents, all of them need to have status AVAILABLE for the task to run.
#' If you use multiple agents for a source location, the status of all the
#' agents must be AVAILABLE for the task to run. For more information, see
#' [Activating a Sync
#' Agent](https://docs.aws.amazon.com/sync-service/latest/userguide/working-with-sync-agents.html#activating-sync-agent)
#' in the *AWS DataSync User Guide.*
#' 
#' Agents are automatically updated by AWS on a regular basis, using a
#' mechanism that ensures minimal interruption to your tasks.
#'
#' @usage
#' datasync_create_agent(ActivationKey, AgentName, Tags)
#'
#' @param ActivationKey &#91;required&#93; Your agent activation key. You can get the activation key either by
#' sending an HTTP GET request with redirects that enable you to get the
#' agent IP address (port 80). Alternatively, you can get it from the AWS
#' DataSync console.
#' 
#' The redirect URL returned in the response provides you the activation
#' key for your agent in the query string parameter `activationKey`. It
#' might also include other activation-related parameters; however, these
#' are merely defaults. The arguments you pass to this API call determine
#' the actual configuration of your agent. For more information, see
#' [Activating a Sync
#' Agent](https://docs.aws.amazon.com/sync-service/latest/userguide/working-with-sync-agents.html#activating-sync-agent)
#' in the *AWS DataSync User Guide.*
#' @param AgentName The name you configured for your agent. This value is a text reference
#' that is used to identify the agent in the console.
#' @param Tags The key-value pair that represents the tag you want to associate with
#' the agent. The value can be an empty string. This value helps you
#' manage, filter, and search for your agents.
#' 
#' Valid characters for key and value are letters, spaces, and numbers
#' representable in UTF-8 format, and the following special characters: + -
#' = . \\_ : / @.
#'
#' @section Request syntax:
#' ```
#' datasync$create_agent(
#'   ActivationKey = "string",
#'   AgentName = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_create_agent
datasync_create_agent <- function(ActivationKey, AgentName = NULL, Tags = NULL) {
  op <- new_operation(
    name = "CreateAgent",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$create_agent_input(ActivationKey = ActivationKey, AgentName = AgentName, Tags = Tags)
  output <- .datasync$create_agent_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$create_agent <- datasync_create_agent

#' Creates an endpoint for an Amazon EFS file system
#'
#' Creates an endpoint for an Amazon EFS file system.
#'
#' @usage
#' datasync_create_location_efs(Subdirectory, EfsFilesystemArn, Ec2Config,
#'   Tags)
#'
#' @param Subdirectory &#91;required&#93; A subdirectory in the location's path. This subdirectory in the EFS file
#' system is used to read data from the EFS source location or write data
#' to the EFS destination. By default, AWS DataSync uses the root
#' directory.
#' @param EfsFilesystemArn &#91;required&#93; The Amazon Resource Name (ARN) for the Amazon EFS file system.
#' @param Ec2Config &#91;required&#93; The subnet and security group that the Amazon EFS file system uses.
#' @param Tags The key-value pair that represents a tag that you want to add to the
#' resource. The value can be an empty string. This value helps you manage,
#' filter, and search for your resources. We recommend that you create a
#' name tag for your location.
#'
#' @section Request syntax:
#' ```
#' datasync$create_location_efs(
#'   Subdirectory = "string",
#'   EfsFilesystemArn = "string",
#'   Ec2Config = list(
#'     SubnetArn = "string",
#'     SecurityGroupArns = list(
#'       "string"
#'     )
#'   ),
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_create_location_efs
datasync_create_location_efs <- function(Subdirectory, EfsFilesystemArn, Ec2Config, Tags = NULL) {
  op <- new_operation(
    name = "CreateLocationEfs",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$create_location_efs_input(Subdirectory = Subdirectory, EfsFilesystemArn = EfsFilesystemArn, Ec2Config = Ec2Config, Tags = Tags)
  output <- .datasync$create_location_efs_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$create_location_efs <- datasync_create_location_efs

#' Creates an endpoint for a Network File System (NFS) file system
#'
#' Creates an endpoint for a Network File System (NFS) file system.
#'
#' @usage
#' datasync_create_location_nfs(Subdirectory, ServerHostname, OnPremConfig,
#'   Tags)
#'
#' @param Subdirectory &#91;required&#93; The subdirectory in the NFS file system that is used to read data from
#' the NFS source location or write data to the NFS destination. The NFS
#' path should be a path that\'s exported by the NFS server, or a
#' subdirectory of that path. The path should be such that it can be
#' mounted by other NFS clients in your network.
#' 
#' To see all the paths exported by your NFS server. run
#' \"`showmount -e nfs-server-name`\" from an NFS client that has access to
#' your server. You can specify any directory that appears in the results,
#' and any subdirectory of that directory. Ensure that the NFS export is
#' accessible without Kerberos authentication.
#' 
#' To transfer all the data in the folder you specified, DataSync needs to
#' have permissions to read all the data. To ensure this, either configure
#' the NFS export with `no_root_squash,` or ensure that the permissions for
#' all of the files that you want sync allow read access for all users.
#' Doing either enables the agent to read the files. For the agent to
#' access directories, you must additionally enable all execute access. For
#' information about NFS export configuration, see [18.7. The /etc/exports
#' Configuration
#' File](https://www.centos.org/docs/5/html/Deployment_Guide-en-US/s1-nfs-server-config-exports.html)
#' in the Centos documentation.
#' @param ServerHostname &#91;required&#93; The name of the NFS server. This value is the IP address or Domain Name
#' Service (DNS) name of the NFS server. An agent that is installed
#' on-premises uses this host name to mount the NFS server in a network.
#' 
#' This name must either be DNS-compliant or must be an IP version 4 (IPv4)
#' address.
#' @param OnPremConfig &#91;required&#93; Contains a list of Amazon Resource Names (ARNs) of agents that are used
#' to connect to an NFS server.
#' @param Tags The key-value pair that represents the tag that you want to add to the
#' location. The value can be an empty string. We recommend using tags to
#' name your resources.
#'
#' @section Request syntax:
#' ```
#' datasync$create_location_nfs(
#'   Subdirectory = "string",
#'   ServerHostname = "string",
#'   OnPremConfig = list(
#'     AgentArns = list(
#'       "string"
#'     )
#'   ),
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_create_location_nfs
datasync_create_location_nfs <- function(Subdirectory, ServerHostname, OnPremConfig, Tags = NULL) {
  op <- new_operation(
    name = "CreateLocationNfs",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$create_location_nfs_input(Subdirectory = Subdirectory, ServerHostname = ServerHostname, OnPremConfig = OnPremConfig, Tags = Tags)
  output <- .datasync$create_location_nfs_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$create_location_nfs <- datasync_create_location_nfs

#' Creates an endpoint for an Amazon S3 bucket
#'
#' Creates an endpoint for an Amazon S3 bucket.
#' 
#' For AWS DataSync to access a destination S3 bucket, it needs an AWS
#' Identity and Access Management (IAM) role that has the required
#' permissions. You can set up the required permissions by creating an IAM
#' policy that grants the required permissions and attaching the policy to
#' the role. An example of such a policy is shown in the examples section.
#' For more information, see [Configuring Amazon S3 Location
#' Settings](https://docs.aws.amazon.com/sync-service/latest/userguide/configuring-s3-locations.html)
#' in the *AWS DataSync User Guide*.
#'
#' @usage
#' datasync_create_location_s3(Subdirectory, S3BucketArn, S3Config, Tags)
#'
#' @param Subdirectory &#91;required&#93; A subdirectory in the Amazon S3 bucket. This subdirectory in Amazon S3
#' is used to read data from the S3 source location or write data to the S3
#' destination.
#' @param S3BucketArn &#91;required&#93; The Amazon Resource Name (ARN) of the Amazon S3 bucket.
#' @param S3Config &#91;required&#93; 
#' @param Tags The key-value pair that represents the tag that you want to add to the
#' location. The value can be an empty string. We recommend using tags to
#' name your resources.
#'
#' @section Request syntax:
#' ```
#' datasync$create_location_s3(
#'   Subdirectory = "string",
#'   S3BucketArn = "string",
#'   S3Config = list(
#'     BucketAccessRoleArn = "string"
#'   ),
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_create_location_s3
datasync_create_location_s3 <- function(Subdirectory, S3BucketArn, S3Config, Tags = NULL) {
  op <- new_operation(
    name = "CreateLocationS3",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$create_location_s3_input(Subdirectory = Subdirectory, S3BucketArn = S3BucketArn, S3Config = S3Config, Tags = Tags)
  output <- .datasync$create_location_s3_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$create_location_s3 <- datasync_create_location_s3

#' Creates a task
#'
#' Creates a task. A task is a set of two locations (source and
#' destination) and a set of default `OverrideOptions` that you use to
#' control the behavior of a task. If you don\'t specify default values for
#' `Options` when you create a task, AWS DataSync populates them with safe
#' service defaults.
#' 
#' When you initially create a task, it enters the INITIALIZING status and
#' then the CREATING status. In CREATING status, AWS DataSync attempts to
#' mount the source Network File System (NFS) location. The task
#' transitions to the AVAILABLE status without waiting for the destination
#' location to mount. Instead, AWS DataSync mounts a destination before
#' every task execution and then unmounts it after every task execution.
#' 
#' If an agent that is associated with a source (NFS) location goes
#' offline, the task transitions to the UNAVAILABLE status. If the status
#' of the task remains in the CREATING status for more than a few minutes,
#' it means that your agent might be having trouble mounting the source NFS
#' file system. Check the task\'s `ErrorCode` and `ErrorDetail`. Mount
#' issues are often caused by either a misconfigured firewall or a mistyped
#' NFS server host name.
#'
#' @usage
#' datasync_create_task(SourceLocationArn, DestinationLocationArn,
#'   CloudWatchLogGroupArn, Name, Options, Tags)
#'
#' @param SourceLocationArn &#91;required&#93; The Amazon Resource Name (ARN) of the source location for the task.
#' @param DestinationLocationArn &#91;required&#93; The Amazon Resource Name (ARN) of an AWS storage resource\'s location.
#' @param CloudWatchLogGroupArn The Amazon Resource Name (ARN) of the Amazon CloudWatch log group that
#' is used to monitor and log events in the task. For more information on
#' these groups, see [Working with Log Groups and Log
#' Streams](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/Working-with-log-groups-and-streams.html)
#' in the *Amazon CloudWatch User Guide.*
#' 
#' For more information about how to useCloudWatchLogs with DataSync, see
#' [Monitoring Your
#' Task](https://docs.aws.amazon.com/datasync/latest/userguide/monitor-datasync.html).
#' @param Name The name of a task. This value is a text reference that is used to
#' identify the task in the console.
#' @param Options The set of configuration options that control the behavior of a single
#' execution of the task that occurs when you call `StartTaskExecution`.
#' You can configure these options to preserve metadata such as user ID
#' (UID) and group ID (GID), file permissions, data integrity verification,
#' and so on.
#' 
#' For each individual task execution, you can override these options by
#' specifying the `OverrideOptions` before starting a the task execution.
#' For more information, see the operation.
#' @param Tags The key-value pair that represents the tag that you want to add to the
#' resource. The value can be an empty string.
#'
#' @section Request syntax:
#' ```
#' datasync$create_task(
#'   SourceLocationArn = "string",
#'   DestinationLocationArn = "string",
#'   CloudWatchLogGroupArn = "string",
#'   Name = "string",
#'   Options = list(
#'     VerifyMode = "POINT_IN_TIME_CONSISTENT"|"NONE",
#'     Atime = "NONE"|"BEST_EFFORT",
#'     Mtime = "NONE"|"PRESERVE",
#'     Uid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     Gid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     PreserveDeletedFiles = "PRESERVE"|"REMOVE",
#'     PreserveDevices = "NONE"|"PRESERVE",
#'     PosixPermissions = "NONE"|"BEST_EFFORT"|"PRESERVE",
#'     BytesPerSecond = 123
#'   ),
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_create_task
datasync_create_task <- function(SourceLocationArn, DestinationLocationArn, CloudWatchLogGroupArn = NULL, Name = NULL, Options = NULL, Tags = NULL) {
  op <- new_operation(
    name = "CreateTask",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$create_task_input(SourceLocationArn = SourceLocationArn, DestinationLocationArn = DestinationLocationArn, CloudWatchLogGroupArn = CloudWatchLogGroupArn, Name = Name, Options = Options, Tags = Tags)
  output <- .datasync$create_task_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$create_task <- datasync_create_task

#' Deletes an agent
#'
#' Deletes an agent. To specify which agent to delete, use the Amazon
#' Resource Name (ARN) of the agent in your request. The operation
#' disassociates the agent from your AWS account. However, it doesn\'t
#' delete the agent virtual machine (VM) from your on-premises environment.
#' 
#' After you delete an agent, you can\'t reactivate it and you longer pay
#' software charges for it.
#'
#' @usage
#' datasync_delete_agent(AgentArn)
#'
#' @param AgentArn &#91;required&#93; The Amazon Resource Name (ARN) of the agent to delete. Use the
#' `ListAgents` operation to return a list of agents for your account and
#' AWS Region.
#'
#' @section Request syntax:
#' ```
#' datasync$delete_agent(
#'   AgentArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_delete_agent
datasync_delete_agent <- function(AgentArn) {
  op <- new_operation(
    name = "DeleteAgent",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$delete_agent_input(AgentArn = AgentArn)
  output <- .datasync$delete_agent_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$delete_agent <- datasync_delete_agent

#' Deletes the configuration of a location used by AWS DataSync
#'
#' Deletes the configuration of a location used by AWS DataSync.
#'
#' @usage
#' datasync_delete_location(LocationArn)
#'
#' @param LocationArn &#91;required&#93; The Amazon Resource Name (ARN) of the location to delete.
#'
#' @section Request syntax:
#' ```
#' datasync$delete_location(
#'   LocationArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_delete_location
datasync_delete_location <- function(LocationArn) {
  op <- new_operation(
    name = "DeleteLocation",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$delete_location_input(LocationArn = LocationArn)
  output <- .datasync$delete_location_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$delete_location <- datasync_delete_location

#' Deletes a task
#'
#' Deletes a task.
#'
#' @usage
#' datasync_delete_task(TaskArn)
#'
#' @param TaskArn &#91;required&#93; The Amazon Resource Name (ARN) of the task to delete.
#'
#' @section Request syntax:
#' ```
#' datasync$delete_task(
#'   TaskArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_delete_task
datasync_delete_task <- function(TaskArn) {
  op <- new_operation(
    name = "DeleteTask",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$delete_task_input(TaskArn = TaskArn)
  output <- .datasync$delete_task_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$delete_task <- datasync_delete_task

#' Returns metadata such as the name, the network interfaces, and the
#' status (that is, whether the agent is running or not) for an agent
#'
#' Returns metadata such as the name, the network interfaces, and the
#' status (that is, whether the agent is running or not) for an agent. To
#' specify which agent to describe, use the Amazon Resource Name (ARN) of
#' the agent in your request.
#'
#' @usage
#' datasync_describe_agent(AgentArn)
#'
#' @param AgentArn &#91;required&#93; The Amazon Resource Name (ARN) of the agent to describe.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_agent(
#'   AgentArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_agent
datasync_describe_agent <- function(AgentArn) {
  op <- new_operation(
    name = "DescribeAgent",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_agent_input(AgentArn = AgentArn)
  output <- .datasync$describe_agent_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_agent <- datasync_describe_agent

#' Returns metadata, such as the path information about an Amazon EFS
#' location
#'
#' Returns metadata, such as the path information about an Amazon EFS
#' location.
#'
#' @usage
#' datasync_describe_location_efs(LocationArn)
#'
#' @param LocationArn &#91;required&#93; The Amazon Resource Name (ARN) of the EFS location to describe.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_location_efs(
#'   LocationArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_location_efs
datasync_describe_location_efs <- function(LocationArn) {
  op <- new_operation(
    name = "DescribeLocationEfs",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_location_efs_input(LocationArn = LocationArn)
  output <- .datasync$describe_location_efs_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_location_efs <- datasync_describe_location_efs

#' Returns metadata, such as the path information, about a NFS location
#'
#' Returns metadata, such as the path information, about a NFS location.
#'
#' @usage
#' datasync_describe_location_nfs(LocationArn)
#'
#' @param LocationArn &#91;required&#93; The Amazon resource Name (ARN) of the NFS location to describe.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_location_nfs(
#'   LocationArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_location_nfs
datasync_describe_location_nfs <- function(LocationArn) {
  op <- new_operation(
    name = "DescribeLocationNfs",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_location_nfs_input(LocationArn = LocationArn)
  output <- .datasync$describe_location_nfs_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_location_nfs <- datasync_describe_location_nfs

#' Returns metadata, such as bucket name, about an Amazon S3 bucket
#' location
#'
#' Returns metadata, such as bucket name, about an Amazon S3 bucket
#' location.
#'
#' @usage
#' datasync_describe_location_s3(LocationArn)
#'
#' @param LocationArn &#91;required&#93; The Amazon Resource Name (ARN) of the Amazon S3 bucket location to
#' describe.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_location_s3(
#'   LocationArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_location_s3
datasync_describe_location_s3 <- function(LocationArn) {
  op <- new_operation(
    name = "DescribeLocationS3",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_location_s3_input(LocationArn = LocationArn)
  output <- .datasync$describe_location_s3_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_location_s3 <- datasync_describe_location_s3

#' Returns metadata about a task
#'
#' Returns metadata about a task.
#'
#' @usage
#' datasync_describe_task(TaskArn)
#'
#' @param TaskArn &#91;required&#93; The Amazon Resource Name (ARN) of the task to describe.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_task(
#'   TaskArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_task
datasync_describe_task <- function(TaskArn) {
  op <- new_operation(
    name = "DescribeTask",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_task_input(TaskArn = TaskArn)
  output <- .datasync$describe_task_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_task <- datasync_describe_task

#' Returns detailed metadata about a task that is being executed
#'
#' Returns detailed metadata about a task that is being executed.
#'
#' @usage
#' datasync_describe_task_execution(TaskExecutionArn)
#'
#' @param TaskExecutionArn &#91;required&#93; The Amazon Resource Name (ARN) of the task that is being executed.
#'
#' @section Request syntax:
#' ```
#' datasync$describe_task_execution(
#'   TaskExecutionArn = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_describe_task_execution
datasync_describe_task_execution <- function(TaskExecutionArn) {
  op <- new_operation(
    name = "DescribeTaskExecution",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$describe_task_execution_input(TaskExecutionArn = TaskExecutionArn)
  output <- .datasync$describe_task_execution_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$describe_task_execution <- datasync_describe_task_execution

#' Returns a list of agents owned by an AWS account in the AWS Region
#' specified in the request
#'
#' Returns a list of agents owned by an AWS account in the AWS Region
#' specified in the request. The returned list is ordered by agent Amazon
#' Resource Name (ARN).
#' 
#' By default, this operation returns a maximum of 100 agents. This
#' operation supports pagination that enables you to optionally reduce the
#' number of agents returned in a response.
#' 
#' If you have more agents than are returned in a response (that is, the
#' response returns only a truncated list of your agents), the response
#' contains a marker that you can specify in your next request to fetch the
#' next page of agents.
#'
#' @usage
#' datasync_list_agents(MaxResults, NextToken)
#'
#' @param MaxResults The maximum number of agents to list.
#' @param NextToken An opaque string that indicates the position at which to begin the next
#' list of agents.
#'
#' @section Request syntax:
#' ```
#' datasync$list_agents(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_list_agents
datasync_list_agents <- function(MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListAgents",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$list_agents_input(MaxResults = MaxResults, NextToken = NextToken)
  output <- .datasync$list_agents_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$list_agents <- datasync_list_agents

#' Returns a lists of source and destination locations
#'
#' Returns a lists of source and destination locations.
#' 
#' If you have more locations than are returned in a response (that is, the
#' response returns only a truncated list of your agents), the response
#' contains a token that you can specify in your next request to fetch the
#' next page of locations.
#'
#' @usage
#' datasync_list_locations(MaxResults, NextToken)
#'
#' @param MaxResults The maximum number of locations to return.
#' @param NextToken An opaque string that indicates the position at which to begin the next
#' list of locations.
#'
#' @section Request syntax:
#' ```
#' datasync$list_locations(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_list_locations
datasync_list_locations <- function(MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListLocations",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$list_locations_input(MaxResults = MaxResults, NextToken = NextToken)
  output <- .datasync$list_locations_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$list_locations <- datasync_list_locations

#' Returns all the tags associated with a specified resources
#'
#' Returns all the tags associated with a specified resources.
#'
#' @usage
#' datasync_list_tags_for_resource(ResourceArn, MaxResults, NextToken)
#'
#' @param ResourceArn &#91;required&#93; The Amazon Resource Name (ARN) of the resource whose tags to list.
#' @param MaxResults The maximum number of locations to return.
#' @param NextToken An opaque string that indicates the position at which to begin the next
#' list of locations.
#'
#' @section Request syntax:
#' ```
#' datasync$list_tags_for_resource(
#'   ResourceArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_list_tags_for_resource
datasync_list_tags_for_resource <- function(ResourceArn, MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListTagsForResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$list_tags_for_resource_input(ResourceArn = ResourceArn, MaxResults = MaxResults, NextToken = NextToken)
  output <- .datasync$list_tags_for_resource_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$list_tags_for_resource <- datasync_list_tags_for_resource

#' Returns a list of executed tasks
#'
#' Returns a list of executed tasks.
#'
#' @usage
#' datasync_list_task_executions(TaskArn, MaxResults, NextToken)
#'
#' @param TaskArn The Amazon Resource Name (ARN) of the task whose tasks you want to list.
#' @param MaxResults The maximum number of executed tasks to list.
#' @param NextToken An opaque string that indicates the position at which to begin the next
#' list of the executed tasks.
#'
#' @section Request syntax:
#' ```
#' datasync$list_task_executions(
#'   TaskArn = "string",
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_list_task_executions
datasync_list_task_executions <- function(TaskArn = NULL, MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListTaskExecutions",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$list_task_executions_input(TaskArn = TaskArn, MaxResults = MaxResults, NextToken = NextToken)
  output <- .datasync$list_task_executions_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$list_task_executions <- datasync_list_task_executions

#' Returns a list of all the tasks
#'
#' Returns a list of all the tasks.
#'
#' @usage
#' datasync_list_tasks(MaxResults, NextToken)
#'
#' @param MaxResults The maximum number of tasks to return.
#' @param NextToken An opaque string that indicates the position at which to begin the next
#' list of tasks.
#'
#' @section Request syntax:
#' ```
#' datasync$list_tasks(
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_list_tasks
datasync_list_tasks <- function(MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "ListTasks",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$list_tasks_input(MaxResults = MaxResults, NextToken = NextToken)
  output <- .datasync$list_tasks_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$list_tasks <- datasync_list_tasks

#' Starts a specific invocation of a task
#'
#' Starts a specific invocation of a task. A `TaskExecution` value
#' represents an individual run of a task. Each task can have at most one
#' `TaskExecution` at a time.
#' 
#' `TaskExecution` has the following transition phases: INITIALIZING \\|
#' PREPARING \\| TRANSFERRING \\| VERIFYING \\| SUCCESS/FAILURE.
#' 
#' For detailed information, see *Task Execution* in [Components and
#' Terminology](https://docs.aws.amazon.com/sync-service/latest/userguide/how-awssync-works.html#terminology)
#' in the *AWS DataSync User Guide*.
#'
#' @usage
#' datasync_start_task_execution(TaskArn, OverrideOptions)
#'
#' @param TaskArn &#91;required&#93; The Amazon Resource Name (ARN) of the task to start.
#' @param OverrideOptions 
#'
#' @section Request syntax:
#' ```
#' datasync$start_task_execution(
#'   TaskArn = "string",
#'   OverrideOptions = list(
#'     VerifyMode = "POINT_IN_TIME_CONSISTENT"|"NONE",
#'     Atime = "NONE"|"BEST_EFFORT",
#'     Mtime = "NONE"|"PRESERVE",
#'     Uid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     Gid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     PreserveDeletedFiles = "PRESERVE"|"REMOVE",
#'     PreserveDevices = "NONE"|"PRESERVE",
#'     PosixPermissions = "NONE"|"BEST_EFFORT"|"PRESERVE",
#'     BytesPerSecond = 123
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_start_task_execution
datasync_start_task_execution <- function(TaskArn, OverrideOptions = NULL) {
  op <- new_operation(
    name = "StartTaskExecution",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$start_task_execution_input(TaskArn = TaskArn, OverrideOptions = OverrideOptions)
  output <- .datasync$start_task_execution_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$start_task_execution <- datasync_start_task_execution

#' Applies a key-value pair to an AWS resource
#'
#' Applies a key-value pair to an AWS resource.
#'
#' @usage
#' datasync_tag_resource(ResourceArn, Tags)
#'
#' @param ResourceArn &#91;required&#93; The Amazon Resource Name (ARN) of the resource to apply the tag to.
#' @param Tags &#91;required&#93; The tags to apply.
#'
#' @section Request syntax:
#' ```
#' datasync$tag_resource(
#'   ResourceArn = "string",
#'   Tags = list(
#'     list(
#'       Key = "string",
#'       Value = "string"
#'     )
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_tag_resource
datasync_tag_resource <- function(ResourceArn, Tags) {
  op <- new_operation(
    name = "TagResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$tag_resource_input(ResourceArn = ResourceArn, Tags = Tags)
  output <- .datasync$tag_resource_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$tag_resource <- datasync_tag_resource

#' Removes a tag from an AWS resource
#'
#' Removes a tag from an AWS resource.
#'
#' @usage
#' datasync_untag_resource(ResourceArn, Keys)
#'
#' @param ResourceArn &#91;required&#93; The Amazon Resource Name (ARN) of the resource to remove the tag from.
#' @param Keys &#91;required&#93; The keys in the key-value pair in the tag to remove.
#'
#' @section Request syntax:
#' ```
#' datasync$untag_resource(
#'   ResourceArn = "string",
#'   Keys = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_untag_resource
datasync_untag_resource <- function(ResourceArn, Keys) {
  op <- new_operation(
    name = "UntagResource",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$untag_resource_input(ResourceArn = ResourceArn, Keys = Keys)
  output <- .datasync$untag_resource_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$untag_resource <- datasync_untag_resource

#' Updates the name of an agent
#'
#' Updates the name of an agent.
#'
#' @usage
#' datasync_update_agent(AgentArn, Name)
#'
#' @param AgentArn &#91;required&#93; The Amazon Resource Name (ARN) of the agent to update.
#' @param Name The name that you want to use to configure the agent.
#'
#' @section Request syntax:
#' ```
#' datasync$update_agent(
#'   AgentArn = "string",
#'   Name = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_update_agent
datasync_update_agent <- function(AgentArn, Name = NULL) {
  op <- new_operation(
    name = "UpdateAgent",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$update_agent_input(AgentArn = AgentArn, Name = Name)
  output <- .datasync$update_agent_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$update_agent <- datasync_update_agent

#' Updates the metadata associated with a task
#'
#' Updates the metadata associated with a task.
#'
#' @usage
#' datasync_update_task(TaskArn, Options, Name)
#'
#' @param TaskArn &#91;required&#93; The Amazon Resource Name (ARN) of the resource name of the task to
#' update.
#' @param Options 
#' @param Name The name of the task to update.
#'
#' @section Request syntax:
#' ```
#' datasync$update_task(
#'   TaskArn = "string",
#'   Options = list(
#'     VerifyMode = "POINT_IN_TIME_CONSISTENT"|"NONE",
#'     Atime = "NONE"|"BEST_EFFORT",
#'     Mtime = "NONE"|"PRESERVE",
#'     Uid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     Gid = "NONE"|"INT_VALUE"|"NAME"|"BOTH",
#'     PreserveDeletedFiles = "PRESERVE"|"REMOVE",
#'     PreserveDevices = "NONE"|"PRESERVE",
#'     PosixPermissions = "NONE"|"BEST_EFFORT"|"PRESERVE",
#'     BytesPerSecond = 123
#'   ),
#'   Name = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname datasync_update_task
datasync_update_task <- function(TaskArn, Options = NULL, Name = NULL) {
  op <- new_operation(
    name = "UpdateTask",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .datasync$update_task_input(TaskArn = TaskArn, Options = Options, Name = Name)
  output <- .datasync$update_task_output()
  svc <- .datasync$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.datasync$operations$update_task <- datasync_update_task
