# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common new_handlers new_service set_config
NULL

#' Amazon Sagemaker Edge Manager
#'
#' @description
#' SageMaker Edge Manager dataplane service for communicating with active
#' agents.
#'
#' @param
#' config
#' Optional configuration of credentials, endpoint, and/or region.
#'
#' @section Service syntax:
#' ```
#' svc <- sagemakeredgemanager(
#'   config = list(
#'     credentials = list(
#'       creds = list(
#'         access_key_id = "string",
#'         secret_access_key = "string",
#'         session_token = "string"
#'       ),
#'       profile = "string"
#'     ),
#'     endpoint = "string",
#'     region = "string"
#'   )
#' )
#' ```
#'
#' @examples
#' \dontrun{
#' svc <- sagemakeredgemanager()
#' svc$get_device_registration(
#'   Foo = 123
#' )
#' }
#'
#' @section Operations:
#' \tabular{ll}{
#'  \link[=sagemakeredgemanager_get_device_registration]{get_device_registration} \tab Use to check if a device is registered with SageMaker Edge Manager\cr
#'  \link[=sagemakeredgemanager_send_heartbeat]{send_heartbeat} \tab Use to get the current status of devices registered on SageMaker Edge Manager
#' }
#'
#' @rdname sagemakeredgemanager
#' @export
sagemakeredgemanager <- function(config = list()) {
  svc <- .sagemakeredgemanager$operations
  svc <- set_config(svc, config)
  return(svc)
}

# Private API objects: metadata, handlers, interfaces, etc.
.sagemakeredgemanager <- list()

.sagemakeredgemanager$operations <- list()

.sagemakeredgemanager$metadata <- list(
  service_name = "sagemakeredgemanager",
  endpoints = list("*" = list(endpoint = "sagemakeredgemanager.{region}.amazonaws.com", global = FALSE), "cn-*" = list(endpoint = "sagemakeredgemanager.{region}.amazonaws.com.cn", global = FALSE), "us-iso-*" = list(endpoint = "sagemakeredgemanager.{region}.c2s.ic.gov", global = FALSE), "us-isob-*" = list(endpoint = "sagemakeredgemanager.{region}.sc2s.sgov.gov", global = FALSE)),
  service_id = "Sagemaker Edge",
  api_version = "2020-09-23",
  signing_name = "sagemaker",
  json_version = "1.1",
  target_prefix = ""
)

.sagemakeredgemanager$service <- function(config = list()) {
  handlers <- new_handlers("restjson", "v4")
  new_service(.sagemakeredgemanager$metadata, handlers, config)
}
