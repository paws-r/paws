# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common new_operation new_request send_request
#' @include kinesisvideo_service.R
NULL

#' Creates a new Kinesis video stream
#'
#' Creates a new Kinesis video stream.
#' 
#' When you create a new stream, Kinesis Video Streams assigns it a version
#' number. When you change the stream\'s metadata, Kinesis Video Streams
#' updates the version.
#' 
#' `CreateStream` is an asynchronous operation.
#' 
#' For information about how the service works, see [How it
#' Works](https://docs.aws.amazon.com/kinesisvideostreams/latest/dg/how-it-works.html).
#' 
#' You must have permissions for the `KinesisVideo:CreateStream` action.
#'
#' @usage
#' kinesisvideo_create_stream(DeviceName, StreamName, MediaType, KmsKeyId,
#'   DataRetentionInHours, Tags)
#'
#' @param DeviceName The name of the device that is writing to the stream.
#' 
#' In the current implementation, Kinesis Video Streams does not use this
#' name.
#' @param StreamName &#91;required&#93; A name for the stream that you are creating.
#' 
#' The stream name is an identifier for the stream, and must be unique for
#' each account and region.
#' @param MediaType The media type of the stream. Consumers of the stream can use this
#' information when processing the stream. For more information about media
#' types, see [Media
#' Types](http://www.iana.org/assignments/media-types/media-types.xhtml).
#' If you choose to specify the `MediaType`, see [Naming
#' Requirements](https://tools.ietf.org/html/rfc6838#section-4.2) for
#' guidelines.
#' 
#' This parameter is optional; the default value is `null` (or empty in
#' JSON).
#' @param KmsKeyId The ID of the AWS Key Management Service (AWS KMS) key that you want
#' Kinesis Video Streams to use to encrypt stream data.
#' 
#' If no key ID is specified, the default, Kinesis Video-managed key
#' (`aws/kinesisvideo`) is used.
#' 
#' For more information, see
#' [DescribeKey](https://docs.aws.amazon.com/kms/latest/APIReference/API_DescribeKey.html#API_DescribeKey_RequestParameters).
#' @param DataRetentionInHours The number of hours that you want to retain the data in the stream.
#' Kinesis Video Streams retains the data in a data store that is
#' associated with the stream.
#' 
#' The default value is 0, indicating that the stream does not persist
#' data.
#' 
#' When the `DataRetentionInHours` value is 0, consumers can still consume
#' the fragments that remain in the service host buffer, which has a
#' retention time limit of 5 minutes and a retention memory limit of 200
#' MB. Fragments are removed from the buffer when either limit is reached.
#' @param Tags A list of tags to associate with the specified stream. Each tag is a
#' key-value pair (the value is optional).
#'
#' @section Request syntax:
#' ```
#' svc$create_stream(
#'   DeviceName = "string",
#'   StreamName = "string",
#'   MediaType = "string",
#'   KmsKeyId = "string",
#'   DataRetentionInHours = 123,
#'   Tags = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_create_stream
kinesisvideo_create_stream <- function(DeviceName = NULL, StreamName, MediaType = NULL, KmsKeyId = NULL, DataRetentionInHours = NULL, Tags = NULL) {
  op <- new_operation(
    name = "CreateStream",
    http_method = "POST",
    http_path = "/createStream",
    paginator = list()
  )
  input <- .kinesisvideo$create_stream_input(DeviceName = DeviceName, StreamName = StreamName, MediaType = MediaType, KmsKeyId = KmsKeyId, DataRetentionInHours = DataRetentionInHours, Tags = Tags)
  output <- .kinesisvideo$create_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$create_stream <- kinesisvideo_create_stream

#' Deletes a Kinesis video stream and the data contained in the stream
#'
#' Deletes a Kinesis video stream and the data contained in the stream.
#' 
#' This method marks the stream for deletion, and makes the data in the
#' stream inaccessible immediately.
#' 
#' To ensure that you have the latest version of the stream before deleting
#' it, you can specify the stream version. Kinesis Video Streams assigns a
#' version to each stream. When you update a stream, Kinesis Video Streams
#' assigns a new version number. To get the latest stream version, use the
#' `DescribeStream` API.
#' 
#' This operation requires permission for the `KinesisVideo:DeleteStream`
#' action.
#'
#' @usage
#' kinesisvideo_delete_stream(StreamARN, CurrentVersion)
#'
#' @param StreamARN &#91;required&#93; The Amazon Resource Name (ARN) of the stream that you want to delete.
#' @param CurrentVersion Optional: The version of the stream that you want to delete.
#' 
#' Specify the version as a safeguard to ensure that your are deleting the
#' correct stream. To get the stream version, use the `DescribeStream` API.
#' 
#' If not specified, only the `CreationTime` is checked before deleting the
#' stream.
#'
#' @section Request syntax:
#' ```
#' svc$delete_stream(
#'   StreamARN = "string",
#'   CurrentVersion = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_delete_stream
kinesisvideo_delete_stream <- function(StreamARN, CurrentVersion = NULL) {
  op <- new_operation(
    name = "DeleteStream",
    http_method = "POST",
    http_path = "/deleteStream",
    paginator = list()
  )
  input <- .kinesisvideo$delete_stream_input(StreamARN = StreamARN, CurrentVersion = CurrentVersion)
  output <- .kinesisvideo$delete_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$delete_stream <- kinesisvideo_delete_stream

#' Returns the most current information about the specified stream
#'
#' Returns the most current information about the specified stream. You
#' must specify either the `StreamName` or the `StreamARN`.
#'
#' @usage
#' kinesisvideo_describe_stream(StreamName, StreamARN)
#'
#' @param StreamName The name of the stream.
#' @param StreamARN The Amazon Resource Name (ARN) of the stream.
#'
#' @section Request syntax:
#' ```
#' svc$describe_stream(
#'   StreamName = "string",
#'   StreamARN = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_describe_stream
kinesisvideo_describe_stream <- function(StreamName = NULL, StreamARN = NULL) {
  op <- new_operation(
    name = "DescribeStream",
    http_method = "POST",
    http_path = "/describeStream",
    paginator = list()
  )
  input <- .kinesisvideo$describe_stream_input(StreamName = StreamName, StreamARN = StreamARN)
  output <- .kinesisvideo$describe_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$describe_stream <- kinesisvideo_describe_stream

#' Gets an endpoint for a specified stream for either reading or writing
#'
#' Gets an endpoint for a specified stream for either reading or writing.
#' Use this endpoint in your application to read from the specified stream
#' (using the `GetMedia` or `GetMediaForFragmentList` operations) or write
#' to it (using the `PutMedia` operation).
#' 
#' The returned endpoint does not have the API name appended. The client
#' needs to add the API name to the returned endpoint.
#' 
#' In the request, specify the stream either by `StreamName` or
#' `StreamARN`.
#'
#' @usage
#' kinesisvideo_get_data_endpoint(StreamName, StreamARN, APIName)
#'
#' @param StreamName The name of the stream that you want to get the endpoint for. You must
#' specify either this parameter or a `StreamARN` in the request.
#' @param StreamARN The Amazon Resource Name (ARN) of the stream that you want to get the
#' endpoint for. You must specify either this parameter or a `StreamName`
#' in the request.
#' @param APIName &#91;required&#93; The name of the API action for which to get an endpoint.
#'
#' @section Request syntax:
#' ```
#' svc$get_data_endpoint(
#'   StreamName = "string",
#'   StreamARN = "string",
#'   APIName = "PUT_MEDIA"|"GET_MEDIA"|"LIST_FRAGMENTS"|"GET_MEDIA_FOR_FRAGMENT_LIST"|"GET_HLS_STREAMING_SESSION_URL"|"GET_DASH_STREAMING_SESSION_URL"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_get_data_endpoint
kinesisvideo_get_data_endpoint <- function(StreamName = NULL, StreamARN = NULL, APIName) {
  op <- new_operation(
    name = "GetDataEndpoint",
    http_method = "POST",
    http_path = "/getDataEndpoint",
    paginator = list()
  )
  input <- .kinesisvideo$get_data_endpoint_input(StreamName = StreamName, StreamARN = StreamARN, APIName = APIName)
  output <- .kinesisvideo$get_data_endpoint_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$get_data_endpoint <- kinesisvideo_get_data_endpoint

#' Returns an array of StreamInfo objects
#'
#' Returns an array of `StreamInfo` objects. Each object describes a
#' stream. To retrieve only streams that satisfy a specific condition, you
#' can specify a `StreamNameCondition`.
#'
#' @usage
#' kinesisvideo_list_streams(MaxResults, NextToken, StreamNameCondition)
#'
#' @param MaxResults The maximum number of streams to return in the response. The default is
#' 10,000.
#' @param NextToken If you specify this parameter, when the result of a `ListStreams`
#' operation is truncated, the call returns the `NextToken` in the
#' response. To get another batch of streams, provide this token in your
#' next request.
#' @param StreamNameCondition Optional: Returns only streams that satisfy a specific condition.
#' Currently, you can specify only the prefix of a stream name as a
#' condition.
#'
#' @section Request syntax:
#' ```
#' svc$list_streams(
#'   MaxResults = 123,
#'   NextToken = "string",
#'   StreamNameCondition = list(
#'     ComparisonOperator = "BEGINS_WITH",
#'     ComparisonValue = "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_list_streams
kinesisvideo_list_streams <- function(MaxResults = NULL, NextToken = NULL, StreamNameCondition = NULL) {
  op <- new_operation(
    name = "ListStreams",
    http_method = "POST",
    http_path = "/listStreams",
    paginator = list()
  )
  input <- .kinesisvideo$list_streams_input(MaxResults = MaxResults, NextToken = NextToken, StreamNameCondition = StreamNameCondition)
  output <- .kinesisvideo$list_streams_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$list_streams <- kinesisvideo_list_streams

#' Returns a list of tags associated with the specified stream
#'
#' Returns a list of tags associated with the specified stream.
#' 
#' In the request, you must specify either the `StreamName` or the
#' `StreamARN`.
#'
#' @usage
#' kinesisvideo_list_tags_for_stream(NextToken, StreamARN, StreamName)
#'
#' @param NextToken If you specify this parameter and the result of a `ListTagsForStream`
#' call is truncated, the response includes a token that you can use in the
#' next request to fetch the next batch of tags.
#' @param StreamARN The Amazon Resource Name (ARN) of the stream that you want to list tags
#' for.
#' @param StreamName The name of the stream that you want to list tags for.
#'
#' @section Request syntax:
#' ```
#' svc$list_tags_for_stream(
#'   NextToken = "string",
#'   StreamARN = "string",
#'   StreamName = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_list_tags_for_stream
kinesisvideo_list_tags_for_stream <- function(NextToken = NULL, StreamARN = NULL, StreamName = NULL) {
  op <- new_operation(
    name = "ListTagsForStream",
    http_method = "POST",
    http_path = "/listTagsForStream",
    paginator = list()
  )
  input <- .kinesisvideo$list_tags_for_stream_input(NextToken = NextToken, StreamARN = StreamARN, StreamName = StreamName)
  output <- .kinesisvideo$list_tags_for_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$list_tags_for_stream <- kinesisvideo_list_tags_for_stream

#' Adds one or more tags to a stream
#'
#' Adds one or more tags to a stream. A *tag* is a key-value pair (the
#' value is optional) that you can define and assign to AWS resources. If
#' you specify a tag that already exists, the tag value is replaced with
#' the value that you specify in the request. For more information, see
#' [Using Cost Allocation
#' Tags](https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html)
#' in the *AWS Billing and Cost Management User Guide*.
#' 
#' You must provide either the `StreamName` or the `StreamARN`.
#' 
#' This operation requires permission for the `KinesisVideo:TagStream`
#' action.
#' 
#' Kinesis video streams support up to 50 tags.
#'
#' @usage
#' kinesisvideo_tag_stream(StreamARN, StreamName, Tags)
#'
#' @param StreamARN The Amazon Resource Name (ARN) of the resource that you want to add the
#' tag or tags to.
#' @param StreamName The name of the stream that you want to add the tag or tags to.
#' @param Tags &#91;required&#93; A list of tags to associate with the specified stream. Each tag is a
#' key-value pair (the value is optional).
#'
#' @section Request syntax:
#' ```
#' svc$tag_stream(
#'   StreamARN = "string",
#'   StreamName = "string",
#'   Tags = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_tag_stream
kinesisvideo_tag_stream <- function(StreamARN = NULL, StreamName = NULL, Tags) {
  op <- new_operation(
    name = "TagStream",
    http_method = "POST",
    http_path = "/tagStream",
    paginator = list()
  )
  input <- .kinesisvideo$tag_stream_input(StreamARN = StreamARN, StreamName = StreamName, Tags = Tags)
  output <- .kinesisvideo$tag_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$tag_stream <- kinesisvideo_tag_stream

#' Removes one or more tags from a stream
#'
#' Removes one or more tags from a stream. In the request, specify only a
#' tag key or keys; don\'t specify the value. If you specify a tag key that
#' does not exist, it\'s ignored.
#' 
#' In the request, you must provide the `StreamName` or `StreamARN`.
#'
#' @usage
#' kinesisvideo_untag_stream(StreamARN, StreamName, TagKeyList)
#'
#' @param StreamARN The Amazon Resource Name (ARN) of the stream that you want to remove
#' tags from.
#' @param StreamName The name of the stream that you want to remove tags from.
#' @param TagKeyList &#91;required&#93; A list of the keys of the tags that you want to remove.
#'
#' @section Request syntax:
#' ```
#' svc$untag_stream(
#'   StreamARN = "string",
#'   StreamName = "string",
#'   TagKeyList = list(
#'     "string"
#'   )
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_untag_stream
kinesisvideo_untag_stream <- function(StreamARN = NULL, StreamName = NULL, TagKeyList) {
  op <- new_operation(
    name = "UntagStream",
    http_method = "POST",
    http_path = "/untagStream",
    paginator = list()
  )
  input <- .kinesisvideo$untag_stream_input(StreamARN = StreamARN, StreamName = StreamName, TagKeyList = TagKeyList)
  output <- .kinesisvideo$untag_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$untag_stream <- kinesisvideo_untag_stream

#' Increases or decreases the stream's data retention period by the value
#' that you specify
#'
#' Increases or decreases the stream\'s data retention period by the value
#' that you specify. To indicate whether you want to increase or decrease
#' the data retention period, specify the `Operation` parameter in the
#' request body. In the request, you must specify either the `StreamName`
#' or the `StreamARN`.
#' 
#' The retention period that you specify replaces the current value.
#' 
#' This operation requires permission for the
#' `KinesisVideo:UpdateDataRetention` action.
#' 
#' Changing the data retention period affects the data in the stream as
#' follows:
#' 
#' -   If the data retention period is increased, existing data is retained
#'     for the new retention period. For example, if the data retention
#'     period is increased from one hour to seven hours, all existing data
#'     is retained for seven hours.
#' 
#' -   If the data retention period is decreased, existing data is retained
#'     for the new retention period. For example, if the data retention
#'     period is decreased from seven hours to one hour, all existing data
#'     is retained for one hour, and any data older than one hour is
#'     deleted immediately.
#'
#' @usage
#' kinesisvideo_update_data_retention(StreamName, StreamARN,
#'   CurrentVersion, Operation, DataRetentionChangeInHours)
#'
#' @param StreamName The name of the stream whose retention period you want to change.
#' @param StreamARN The Amazon Resource Name (ARN) of the stream whose retention period you
#' want to change.
#' @param CurrentVersion &#91;required&#93; The version of the stream whose retention period you want to change. To
#' get the version, call either the `DescribeStream` or the `ListStreams`
#' API.
#' @param Operation &#91;required&#93; Indicates whether you want to increase or decrease the retention period.
#' @param DataRetentionChangeInHours &#91;required&#93; The retention period, in hours. The value you specify replaces the
#' current value. The maximum value for this parameter is 87600 (ten
#' years).
#'
#' @section Request syntax:
#' ```
#' svc$update_data_retention(
#'   StreamName = "string",
#'   StreamARN = "string",
#'   CurrentVersion = "string",
#'   Operation = "INCREASE_DATA_RETENTION"|"DECREASE_DATA_RETENTION",
#'   DataRetentionChangeInHours = 123
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_update_data_retention
kinesisvideo_update_data_retention <- function(StreamName = NULL, StreamARN = NULL, CurrentVersion, Operation, DataRetentionChangeInHours) {
  op <- new_operation(
    name = "UpdateDataRetention",
    http_method = "POST",
    http_path = "/updateDataRetention",
    paginator = list()
  )
  input <- .kinesisvideo$update_data_retention_input(StreamName = StreamName, StreamARN = StreamARN, CurrentVersion = CurrentVersion, Operation = Operation, DataRetentionChangeInHours = DataRetentionChangeInHours)
  output <- .kinesisvideo$update_data_retention_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$update_data_retention <- kinesisvideo_update_data_retention

#' Updates stream metadata, such as the device name and media type
#'
#' Updates stream metadata, such as the device name and media type.
#' 
#' You must provide the stream name or the Amazon Resource Name (ARN) of
#' the stream.
#' 
#' To make sure that you have the latest version of the stream before
#' updating it, you can specify the stream version. Kinesis Video Streams
#' assigns a version to each stream. When you update a stream, Kinesis
#' Video Streams assigns a new version number. To get the latest stream
#' version, use the `DescribeStream` API.
#' 
#' `UpdateStream` is an asynchronous operation, and takes time to complete.
#'
#' @usage
#' kinesisvideo_update_stream(StreamName, StreamARN, CurrentVersion,
#'   DeviceName, MediaType)
#'
#' @param StreamName The name of the stream whose metadata you want to update.
#' 
#' The stream name is an identifier for the stream, and must be unique for
#' each account and region.
#' @param StreamARN The ARN of the stream whose metadata you want to update.
#' @param CurrentVersion &#91;required&#93; The version of the stream whose metadata you want to update.
#' @param DeviceName The name of the device that is writing to the stream.
#' 
#' In the current implementation, Kinesis Video Streams does not use this
#' name.
#' @param MediaType The stream\'s media type. Use `MediaType` to specify the type of content
#' that the stream contains to the consumers of the stream. For more
#' information about media types, see [Media
#' Types](http://www.iana.org/assignments/media-types/media-types.xhtml).
#' If you choose to specify the `MediaType`, see [Naming
#' Requirements](https://tools.ietf.org/html/rfc6838#section-4.2).
#' 
#' To play video on the console, you must specify the correct video type.
#' For example, if the video in the stream is H.264, specify `video/h264`
#' as the `MediaType`.
#'
#' @section Request syntax:
#' ```
#' svc$update_stream(
#'   StreamName = "string",
#'   StreamARN = "string",
#'   CurrentVersion = "string",
#'   DeviceName = "string",
#'   MediaType = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname kinesisvideo_update_stream
kinesisvideo_update_stream <- function(StreamName = NULL, StreamARN = NULL, CurrentVersion, DeviceName = NULL, MediaType = NULL) {
  op <- new_operation(
    name = "UpdateStream",
    http_method = "POST",
    http_path = "/updateStream",
    paginator = list()
  )
  input <- .kinesisvideo$update_stream_input(StreamName = StreamName, StreamARN = StreamARN, CurrentVersion = CurrentVersion, DeviceName = DeviceName, MediaType = MediaType)
  output <- .kinesisvideo$update_stream_output()
  svc <- .kinesisvideo$service()
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.kinesisvideo$operations$update_stream <- kinesisvideo_update_stream
