# This file is generated by make.paws. Please do not edit here.
#' @importFrom paws.common get_config new_operation new_request send_request
#' @include migrationhubconfig_service.R
NULL

#' This API sets up the home region for the calling account only
#'
#' @description
#' This API sets up the home region for the calling account only.
#'
#' @usage
#' migrationhubconfig_create_home_region_control(HomeRegion, Target,
#'   DryRun)
#'
#' @param HomeRegion &#91;required&#93; The name of the home region of the calling account.
#' @param Target &#91;required&#93; The account for which this command sets up a home region control. The
#' `Target` is always of type `ACCOUNT`.
#' @param DryRun Optional Boolean flag to indicate whether any effect should take place.
#' It tests whether the caller has permission to make the call.
#'
#' @section Request syntax:
#' ```
#' svc$create_home_region_control(
#'   HomeRegion = "string",
#'   Target = list(
#'     Type = "ACCOUNT",
#'     Id = "string"
#'   ),
#'   DryRun = TRUE|FALSE
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname migrationhubconfig_create_home_region_control
migrationhubconfig_create_home_region_control <- function(HomeRegion, Target, DryRun = NULL) {
  op <- new_operation(
    name = "CreateHomeRegionControl",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .migrationhubconfig$create_home_region_control_input(HomeRegion = HomeRegion, Target = Target, DryRun = DryRun)
  output <- .migrationhubconfig$create_home_region_control_output()
  config <- get_config()
  svc <- .migrationhubconfig$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.migrationhubconfig$operations$create_home_region_control <- migrationhubconfig_create_home_region_control

#' This API permits filtering on the ControlId and HomeRegion fields
#'
#' @description
#' This API permits filtering on the `ControlId` and `HomeRegion` fields.
#'
#' @usage
#' migrationhubconfig_describe_home_region_controls(ControlId, HomeRegion,
#'   Target, MaxResults, NextToken)
#'
#' @param ControlId The `ControlID` is a unique identifier string of your
#' `HomeRegionControl` object.
#' @param HomeRegion The name of the home region you'd like to view.
#' @param Target The target parameter specifies the identifier to which the home region
#' is applied, which is always of type `ACCOUNT`. It applies the home
#' region to the current `ACCOUNT`.
#' @param MaxResults The maximum number of filtering results to display per page.
#' @param NextToken If a `NextToken` was returned by a previous call, more results are
#' available. To retrieve the next page of results, make the call again
#' using the returned token in `NextToken`.
#'
#' @section Request syntax:
#' ```
#' svc$describe_home_region_controls(
#'   ControlId = "string",
#'   HomeRegion = "string",
#'   Target = list(
#'     Type = "ACCOUNT",
#'     Id = "string"
#'   ),
#'   MaxResults = 123,
#'   NextToken = "string"
#' )
#' ```
#'
#' @keywords internal
#'
#' @rdname migrationhubconfig_describe_home_region_controls
migrationhubconfig_describe_home_region_controls <- function(ControlId = NULL, HomeRegion = NULL, Target = NULL, MaxResults = NULL, NextToken = NULL) {
  op <- new_operation(
    name = "DescribeHomeRegionControls",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .migrationhubconfig$describe_home_region_controls_input(ControlId = ControlId, HomeRegion = HomeRegion, Target = Target, MaxResults = MaxResults, NextToken = NextToken)
  output <- .migrationhubconfig$describe_home_region_controls_output()
  config <- get_config()
  svc <- .migrationhubconfig$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.migrationhubconfig$operations$describe_home_region_controls <- migrationhubconfig_describe_home_region_controls

#' Returns the calling account’s home region, if configured
#'
#' @description
#' Returns the calling account’s home region, if configured. This API is
#' used by other AWS services to determine the regional endpoint for
#' calling AWS Application Discovery Service and Migration Hub. You must
#' call `GetHomeRegion` at least once before you call any other AWS
#' Application Discovery Service and AWS Migration Hub APIs, to obtain the
#' account's Migration Hub home region.
#'
#' @usage
#' migrationhubconfig_get_home_region()
#'
#' @section Request syntax:
#' ```
#' svc$get_home_region()
#' ```
#'
#' @keywords internal
#'
#' @rdname migrationhubconfig_get_home_region
migrationhubconfig_get_home_region <- function() {
  op <- new_operation(
    name = "GetHomeRegion",
    http_method = "POST",
    http_path = "/",
    paginator = list()
  )
  input <- .migrationhubconfig$get_home_region_input()
  output <- .migrationhubconfig$get_home_region_output()
  config <- get_config()
  svc <- .migrationhubconfig$service(config)
  request <- new_request(svc, op, input, output)
  response <- send_request(request)
  return(response)
}
.migrationhubconfig$operations$get_home_region <- migrationhubconfig_get_home_region
